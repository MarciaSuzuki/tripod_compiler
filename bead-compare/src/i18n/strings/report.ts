import type { StringModule } from "../index";

/**
 * Report screen strings (#/report/:aId/:bId). Keys are `report.section.item`.
 *
 * Placeholder: the Report engineer replaces this file. Keep the named export
 * `report`, which `src/i18n/index.tsx` imports and merges; both languages
 * must define the same keys (tests/i18n.test.ts checks).
 */
export const report = {
  "pt-BR": {},
  en: {},
} satisfies StringModule;
