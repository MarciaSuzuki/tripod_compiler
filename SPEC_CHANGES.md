# SPEC_CHANGES — governed edit log for the Tripod STA spec

Every change to the **locked spec** (`validation-rules.json` and the controlled vocabulary it
defines) is recorded here, in order, before it ships. This log is the human-readable audit
trail behind the pinned spec version the compiler vendors. It exists for the same reason the
compiler does: schema drift is only safe when it is **deliberate, recorded, and versioned**
(Tripod training paper §12).

## Rules
- **No silent edits.** A change to `validation-rules.json` is not "done" until it has an entry
  here with status `APPROVED` and an implementation note.
- **One entry per decision**, newest at the top. Never rewrite a shipped entry; supersede it
  with a new one.
- **SC IDs are append-only and never reused.** Once allocated, a number is permanently bound to
  one decision. If a decision is abandoned, renamed, or its number was taken in another session,
  give it the **next free** number and mark the old allocation `VOID`/`SUPERSEDED(→SC-YYYY)` in the
  SC-ID allocation ledger below — never silently rebind a live number to a different decision.
- **Canonical home (one sequence, one home).** This log governs the spec, and the canonical spec
  lives in the wiki vault — so the canonical `SPEC_CHANGES.md` (and `VOCABULARY_LOG.md`) live in the
  wiki `_spec/` beside `validation-rules.json`, and the compiler **vendors + pins** them exactly as it
  vendors the schemas. `tripod check-drift --vault` then asserts the compiler's mirror == the wiki
  canonical, so a wiki-side spec edit cannot bypass the log. (Ruling 2026-05-29. The wiki is
  remote-less, so the compiler's pushed mirror is the durable copy — vendor+pin is the durability
  layer, not a convenience.) **Implementation pending** (next governance task, gates SC-0008): add
  both files to `_spec/pins.json` + the pin table and relocate them under `_spec/` so the pin path
  resolves; until then they remain compiler-repo docs and this rule states the target, not the state.
  - **Current state — honest (verified 2026-06-06, during SC-0025).** The above is the *target*, not the
    state, and the gap is wider than the log files: the vault `_spec/` **schema** files are a stale snapshot
    frozen ~SC-0014 — `validation-rules.json` **v0.7** (compiler `v0.12`), `approved-enumerations.json` **v0.1**
    (compiler `v0.10`), `compilation-log.schema.json` **v0.5** (compiler `v0.7`), and `quarantined-vocabulary.json`
    (SC-0023) is **absent entirely**. Every SC from ~SC-0015 onward edited the spec compiler-side and never wrote
    it back, and `check-drift --vault` (the guardrail that would catch this) was never built. **So the de-facto
    canonical spec is the compiler's pinned `_spec/`; the vault `_spec/` is a stale snapshot. Do NOT vendor or
    sync *from* the vault `_spec/`** — that would silently revert ~10 SCs. The bulk reconcile (catch the vault up
    + create the quarantine file) and `check-drift --vault` ship **together** under SC-0008; until they do,
    nothing should treat the vault `_spec/` as canonical. (Same class as the SC-0026 finding: an authoritative
    artifact left unguarded because its check is the unbuilt one.)
- **Bump the spec version** on every shipped change and record the new version + file hash so
  the compiler's vendored-copy drift-check can pin to it.
- **Reconcile the vault on every spec change, and prove it (SC-0008).** After editing `_spec/`, write the
  change back to the wiki vault and run `npm run check-drift:vault` (= `check-drift --vault <wiki>/_spec`) —
  it must report all `vault:ok`, exit 0, before the change is "done." A spec edit not written back leaves the
  vault stale (the drift SC-0008 had to repair). **New files must be explicitly staged/committed** — an
  untracked file can be silently skipped by auto-commit, so verify it actually landed on the vault *remote*,
  not just local disk (the guard reads local disk and can't catch a missing-on-remote file). The conditional
  `--vault` test (set `TRIPOD_VAULT_SPEC`) enforces clean-vault on any local `npm test`.
- **Migrate artifacts in the same entry.** If a change invalidates existing artifacts
  (e.g. a value moves lists), list the affected pericopes and the migration, so the gold
  fixtures and the spec never disagree.
- **Authority:** content/methodology rulings are made by the project lead (Marcia Suzuki);
  the compiler implements and verifies them.

## Spec version pin (current)
`tagset_version`: `TRIPOD_STA_v2_0`. Vendored + drift-checked in the compiler at `_spec/` (see `_spec/pins.json`; `tripod check-drift` enforces these).

| Schema | Version | sha256 |
| --- | --- | --- |
| `validation-rules.json` | `v0.15` | `893b3a490134d4af279bd7e9c7b1d33765fa19d30a87cde6e796b3046fd6e401` |
| `compilation-log.schema.json` | `v0.8` | `ff315586c9e70ea57fa17fa1635226e6f0ed8b8a8b5898cf0803c4f3fe8fdae3` |
| `bcd-delta.schema.json` | `v0.5` | `07a236c957f1429c51d81b8e012ad291298263134ce50dcc2ea7c0640e40d682` |
| `verification-input.schema.json` | `v1.1` | `03e51d5aa0363df6512a40779fb5858c4bfe60d58025a72afe8f3320623835d1` |
| `approved-enumerations.json` | `v0.10` | `8c3c64f9eaf49958c8453d169ce231565d6bdefc307f699abaf4188cc813dec6` |

Governance data files pinned in `pins.json` sources (not schemas): `coverage-exceptions.json`, `lint-exceptions.json`, `id-alignment-exceptions.json`, **`quarantined-vocabulary.json` (`0.2.0`, SC-0023 + SC-0025)**.

Routine vocabulary promotions (growing `approved-enumerations.json`) are logged in [`VOCABULARY_LOG.md`](VOCABULARY_LOG.md), not as a new SC each time (SC-0006 establishes the mechanism); each promotion re-pins the registry above.

---

## SC-ID allocation ledger (canonical)
The authoritative registry of every SC number. IDs are **append-only and never reused** (see Rules).
The chronological entries below carry the detail; this table is the index — one row per number, each
number bound to exactly one decision.

| SC-ID | Decision (current binding) | Status |
| --- | --- | --- |
| SC-0001 | REGISTER 8→7; move COMMUNITY_MEMORY to a NARRATIVE_FRAMING axis | SHIPPED |
| SC-0002 | Propagate the register split into prompts/templates | SHIPPED |
| SC-0003 | Clean residual "elevated register bordering on COMMUNITY_MEMORY" prose | SHIPPED |
| SC-0004 | Deprecate the pre-Wave-3 `_examples/` P01 duplicates | SHIPPED |
| SC-0005 | Widen the `place_id` pattern to `PL<n>_<DESCRIPTOR>` | SHIPPED |
| SC-0006 | Drift convergence: convergent/descriptive split + approved-enumerations registry | SHIPPED |
| SC-0007 | Converge the L1 / discourse / high-risk axes (add a COMPILATION-LOG promotion slot) | SHIPPED |
| SC-0008 | Canonical-home: reconcile the stale vault `_spec/` (frozen ~SC-0014) + build `check-drift --vault` (the load-bearing half) + retire obsolete templates (minor half); **extended 2026-06-06 (SC-0026 analysis) to also reconcile the stale vault `stas/` COMPILATION-LOGs** (same unguarded-stale-vault pattern). Guardrail VERIFIED already-built (exits 1); real work = fix blind-spot + run it + reconcile. | **APPLIED (compiler half; reconcile/writeback pending)** |
| SC-0009 | Merge PL_HA_ARETZ → PL_LAND_OF_JUDAH (same-referent principle, Layer-3) | SHIPPED |
| SC-0010 | Coverage recorded-exception mechanism + P06 "Israel" epithet-internal ruling | SHIPPED |
| SC-0011 | BCD `gender` frontmatter field (authoritative; replaces the prose-guess) | SHIPPED |
| SC-0012 | Level-3 / §3C content discipline (R1–R5) + the `tripod lint` drift-guard | SHIPPED (lint + discipline + template); map remediation follows |
| SC-0013 | Map content remediation under SC-0012 — P01 reference + P02–P06 roll | P01 SHIPPED; P02–P06 §3C+plain-language APPLIED (pending blessing); §4 dialogue re-atomizing = lead's exegetical pass |
| SC-0014 | Rename SPEECH_ACT `ASCRIBES_TO_DIVINE_AGENT_LAMENT_FRAME` → `ASCRIBES_AFFLICTION_TO_GOD_IN_LAMENT` (drop "AGENT" jargon) | APPLIED (pending the lead's blessing) |
| SC-0015 | Extend the Level-3 lint to enforce the operating test: flag cross_ref/inter-proposition-link lines + meta/analytical questions in §4, scan questions (not just answers) incl. same-line Q&A, add comma compounds with an entity-list guard; lint-lexicon v0.1.0→v0.2.0 (re-pinned) | APPLIED |
| SC-0016 | Level-3 §4 content sweep under SC-0012/SC-0013 (remove cross_ref/link lines → relocate figure spans; convert meta-questions to payload; atomize compounds) — P01 reference then P02–P06; + `lint-exceptions.json` recorded sign-off (7 ruled keeps) | SHIPPED (fixtures + vault, blessed + merged 2026-06-01) |
| SC-0017 | De-leak: strip process-commentary (SC-IDs, "per the content discipline", "§3C entities only", "X → Proposition N" relocation trails) from the content layers — maps §3C + FOR_MODEL `objects_in_scene._note` — keeping entities + plain considered-absence; + template/discipline-doc hygiene so P07–P14 are born clean | SHIPPED (fixtures + vault — both PRs merged 2026-06-01); **blessed by Marcia 2026-06-01** |
| SC-0018 | Cross-artifact entity-ID alignment: the locked convention (canonical ID = the bare code; the map carries it in the wikilink target up to the first hyphen) + the `tripod id-check` diagnostic verifier (the 5th deterministic check — aligned) + the empty pinned `id-alignment-exceptions.json`. Diagnostic only; fixes no content. The P01–P06 reconciliation of what it surfaces is a separate later human-gated pass. | PROPOSED |
| SC-0019 | Common-Reader Prose Standard: re-voice the conditioning prose (Level 1, scene descriptions, significant-absence notes, register-tagging block) of P01–P06 to the blessed "good study-Bible note for ordinary readers" register (S1–S4; plain ≠ flat). No spec-version bump (free-text + guidance only). | **SHIPPED 2026-06-01** — P01–P06 prose BLESSED + written back (vault PR #8, squash-merged); methodology note + template + Agent-2/3 prompts seeded so P07–P14 are born in-register |
| SC-0020 | Entity-ID reconciliation under SC-0018: three `id-check` engine refinements (`<NS>?` withheld-referent → INFO; note-title-safe slug normalization; **scene-scoped prose-reference parity bar** — an FM scene-entity the map narrates in that scene's §3 prose is ALIGNED) + the ruled signed-off `id-alignment-exceptions.json` coverage-difference one-siders (now incl. 3 P05 cross-scene/FM-only) + the P01 map TM_→TH_ alignment + AUDIT-relic removal. No spec-version bump (validation-rule tweaks + exceptions data + artifact edits). | **SHIPPED 2026-06-01** — + B31 alias & name-binding engine tweak (beings consult `referential_forms`; canonical preserved) + TM_TEN_YEARS retired (TH_ unverifiable) + written back (vault PR #8); `id-check` **6 clean / 0 findings** |
| SC-0021 | Vocabulary consolidation (corpus-independent triage): promote the 11 cleaned tone/pace bares into `approved-enumerations.json` (tone `RISING/STILLED/URGENT`; pace `BRISK/SLOWED/PAUSED/NARROWS/RISES/SETTLES/HOLDS/WIDENS` — locus-stripped, `NARROW` unified into `NARROWS`), restore `fixtures/for-model ≡ vault` (the tone/pace/`LAMENT_FRAMED` triage), and re-point the COMPILATION-LOG `vocabulary_additions` tone/pace to the bares (SC-0007 intake invariant). `approved-enumerations v0.4 → v0.5`, re-pinned. | **MERGED (PR #23, 2026-06-04)** — gate board green (validate 12/12 · lint 6 clean · coverage 6/6 · check-drift · 136/136 tests); tone/pace convergent drift across P01–P06 = 0 |
| SC-0023 | Quarantined-vocabulary mechanism: un-settle the 8 used-once `communicative_function_element` coin-flips SC-0022 erroneously promoted (`TRANSMITS/ANSWERS/PLACES/ANCHORS/INTRODUCES/POSITIONS/DISTRIBUTES/RECITES`) into a pinned `quarantined-vocabulary.json`; new `quarantined` finding severity + corpus `quarantine watch` that surfaces a RECURRENCE (value in ≥2 pericopes) as an explicit notice (not silent exclusion); `promote` gains a `skippedByQuarantine` guard. `approved-enumerations v0.8→v0.9`, `quarantined-vocabulary 0.1.0`, re-pinned. | **MERGED (PR #25, 2026-06-05)**. UNSETTLED/UNRESOLVED_AT_CLOSE tone-pair unify left as a separate Marcia ruling |
| SC-0022 | Vocabulary consolidation (reuse-dependent triage — the enforcement gate): **promote** 56 cleaned bares (13 `scene_kind` + 27 `proposition_kind` + 16 `communicative_function_element`); **enforce** beings `role_in_scene` descriptive→convergent + seed the 21 ruled relations (controlled slot); **schema-drop** place/object/time `role_in_scene`/`function_in_scene` (entries id-only); **deprecate** 133 orphaned superseded values. `approved-enumerations v0.5→v0.8`, `validation-rules v0.7→v0.9`, re-pinned. | **MERGED (PR #24, 2026-06-04)** |
| SC-0024 | Thread A — nested component `action`/`speech_act` collapse: **closed-list** — collapse the six `VOWS_*_BINDING` SPEECH_ACT values to a single `VOWS` (31→26), synced across `validation-rules.json` + `compilation-log.schema.json`; **content** — reduce ~57 sentence-shaped `action` values across P01–P06 to a small reusable verb set (DIRECTED/STATED/VOWED/ASKED/REPORTED/ASCRIBED/IDENTIFIED + bare verbs), each proven by a per-value survival table (sibling slot or map §4 home); **route** the load-bearing vessels detail to a new `drink_source` sibling slot; **hold** 7 load-bearing labels for Thread B; **migrate** P03 (incl. a P03-D3 supersession note). `action` stays uncontrolled (enforcement = SC-0025). `validation-rules v0.10→v0.11`, `compilation-log.schema v0.5→v0.6`, re-pinned. | **MERGED (PR #26, 2026-06-05)** (gate board green: 142 tests) |
| SC-0025 | Action-slot enforcement (the SC-0024 durability follow-on): reclassify the nested component `action` slot from uncontrolled free-text to a controlled bounded-open axis, seeded with the verb set SC-0024 produced, so P07–P14 cannot re-introduce sentence-shaped action values (drift→review→promote-with-provenance). Clean-then-enforce, the same shape as triage→SC-0022. **SC-0024's `action` cleanup is only durable once this lands.** | **MERGED (PR #27, 2026-06-06)** |
| SC-0026 | COMPILATION-LOG schema gate: wire the 6 gold CLs into the gate (validate against `compilation-log.schema.json`) so a malformed CL fails the board; **finding routed to SC-0008** — the vault `stas/` CLs are content-stale (a strict subset of the fixtures), the same unguarded-stale-vault pattern as `_spec/` | **APPLIED (compiler, gate board green: 155 tests)** 2026-06-06 — MERGED (PR #30, origin/main a6a2d04) |
| SC-0027 | **Thread B fidelity model (THE GOAL):** two-axis fidelity flag (`preserve_meaning` × `preserve_form`; the license = `preserve_form=false`) at element/component level + the (α) shared-group-id (a group entry carries the relationship's own flag) + a dangling-group-id integrity check. `validation-rules v0.12→v0.13` (new `fidelity` + `fidelity_group_entry` defs; `fidelity_groups` on `proposition`, `fidelity` on `register_override_entry`); new engine fidelity pass; **P03 vow re-annotated** (2 groups `people_god_inseparability` + `unto_the_end`; `preserve_form=false` ×6; structure-flag; POETIC_SUNG+BLESSING+INTIMATE as preserve_meaning). Held-7 seeding = follow-up. | **APPLIED (compiler, gate board green: 159 tests + 1 skipped)** 2026-06-07 — PR open; held-7 seeding + P03 vault writeback pending |
| SC-0029 | **Held-7 fidelity seeding** (SC-0027 follow-up; the fidelity model on its designated test corpus — `docs/SC-0029-PROPOSAL.md`): annotate the 7 held `action` labels with `fidelity` + 2 `fidelity_groups`. All 7 `preserve_meaning=true`; `preserve_form=false` on courtroom-lament (A, P04), *nokhriya* (B, P06), both blessing wishes (C, P06), comforted (P06); `preserve_form=true` on the two *shifchah* self-abasement elements (D, P06). Figures: wire orphaned `FIG_0085` (Testify-Against-Legal-Image) into P04 FOR_MODEL **+ map** (closes a pre-existing gap); reference already-wired `FIG_0011` (wings) / `FIG_0132` (Amah-Vs-Shifchah) in `meaning`; B's *nokhriya* figure **DEFERRED** (single-verse, recurrence-gated, e.g. Jonah/Genesis). Two new groups: `blessing_reward_and_refuge` (E), `ruth_reply_grace_received_in_self_abasement` (F). **No schema change** (F single-membership → no spec-version bump). Forward flag: #6 is a *mixed-fidelity element* (preserve-token + free idiom in one component); meaning-field workaround now, token-level refinement recurrence-gated. | **APPLIED (compiler; gate board green: validate 6/6 0-block · held-7 still quarantined · gold-diff P04 38→39 · id-check 6 clean 0-dangling · lint 0/7 · coverage 6/6 · 159 tests + 1 skipped; adversarial dangling-group + fidelity-shape confirmed blocking)** **MERGED (compiler PR #35 + vault PR #10, 2026-06-08)** — `fixtures ≡ vault` byte-identical on origin/main |
| SC-0030 | **Level-3 purity** (`docs/SC-0030-PROPOSAL.md`): the fidelity `meaning` free-text + the fidelity FLAGS (introduced by SC-0027/0029) had crept into the FM **Level-3 supervision target**. Remove `meaning` (rationale → governance §9 table); relocate the flags out of the L3 content objects into a **parallel top-level `fidelity` layer** (Option A — Marcia-ruled) — `elements`/`groups`/`structure_flags`/`register_overrides` that reference L3 by `{prop_id, slot, list_position}` (structure by `{prop_id, field}`; override by `{scene_id\|verse}`); L3 reverts to pure inventory. Removed the old `fidelity` + `fidelity_group_entry` $defs, `proposition.fidelity_groups`, `register_override_entry.fidelity`. Engine: ref-resolution pass (dangling component-ref blocks) + dangling-group-id; ajv validates the layer shape; `lintForModel` gains `l3_free_text` (the guard for the vector). Migrated P03/P04/P06 (19 flags relocated, **no L3 content lost**). The SC-0027 model + all A–F/vow rulings unchanged — only placement. `validation-rules v0.13→v0.14`, re-pinned. **OUT of scope:** the ~49 sentence-shaped slot VALUES (separate post-Jonah triage — `[[tripod-sentence-token-triage]]`). | **APPLIED (compiler; gate board green: validate 6/6 0-block · held-7 still quarantined · gold-diff unchanged · id-check 6 clean · lint 0/7 · coverage 6/6 · 163 tests + 1 skipped; adversarial — dangling-ref, dangling-group, ajv-shape, l3_free_text all confirmed blocking; no L3 content lost vs origin/main)** **MERGED (compiler PR #36 + vault PR #11, 2026-06-08)** — `fixtures ≡ vault` byte-identical on origin/main; `check-drift:vault` exit 0 (the v0.12→v0.14 lag cleared) |
| SC-0031 | **Harness-hardening** (the recurring-error discipline applied to ourselves — `docs/RECURRING-ERROR-DISCIPLINE.md`): (1) commit the discipline as a byte-faithful durable copy of the `tripod-recurring-error-discipline` memory note (the evaluator can't repo-write); (2) `tools/adversarial_check.py` — the **measurement-trap helper** (the 4× PROMOTE candidate): a Python adversarial-check helper that captures the exit code by subprocess return (no pipe), parses `--json` findings as data (no needle/code-guess), applies mutations in Python + **asserts the bytes changed** (no BSD-`sed` no-op), and runs a clean control (no false-alarm). **No `_spec/` change; no vault writeback** (docs + dev tool, compiler-only). | **APPLIED (compiler; `--self-test` ALL PASS — the 4 historical traps + the no-op guard; doc byte-faithful to the memory note; floor unchanged 163 + 1 skipped)** **MERGED (PR #37, 2026-06-08)** |
| SC-0032 | **Book-generalization (Phase 0 of Jonah, the held-out generality test — `docs/JONAH-PLAN.md`).** Generalize the compiler for a second book. **Code (book-general, Ruth byte-identical):** extractor book-filter + out-path derive from the pericope spec; `sta_id` book-prefix derived from the map's `bcv` first token (`Ruth …`→`ruth`); verse-anchor strip generalized; + `extractor/pericopes.jonah.json` (Hebrew-versified J01–J05). **Governed schema change (the evaluator's finding — the plan's "4 code spots" undercounted the 5th, schema-side):** widen the 3 `ruth`-hardcoded id-patterns to fully-general `^[a-z]+_…` — validation-rules v0.14→**v0.15** + compilation-log v0.7→**v0.8** + bcd-delta v0.4→**v0.5**, re-pinned. A `jonah_pericope_…` artifact now validates (was ajv-blocked). | **APPLIED (compiler; Ruth board byte-identical green: validate 6/6 0-block · gold-diff unchanged · 163 tests + 1 skipped; `jonah_pericope_` sta_id validates; check-drift clean)** 2026-06-08 — diff for review; **vault writeback MERGED (verified: `check-drift:vault` exit 0, all 3 schemas `vault:ok` at v0.15/v0.8/v0.5)** |
| SC-0033 | **Jonah Phase 1 — extract + scaffold (the held-out generality test — `docs/JONAH-PLAN.md`).** Extract + pin the **J01** pilot packet (Jonah 1:1-3 = Heb 1:1-3; 33 referents, full cast YHWH/Jonah/Amittai/Nineveh/Tarshish/Joppa) from BHSA `tf/2021`; generalize the two registry-builders (`build_aliases.py` + `build_concept_figure_registry.py`) behind a `--book` param (Ruth byte-identical; optional `--bcd`/`--vault` → empty scaffold for a book with no cast yet, typo-guard preserved); scaffold the empty Jonah registry (`jonah.{aliases,concepts,figures}.json`); pin all 4 in `pins.json` `sources`. **Generality finding:** BHSA's `book` feature is Latin — Jonah = **"Jona"** (not "Jonah"); corrected `pericopes.jonah.json` `bhsa_book` (Ruth was Latin/English-identical, so SC-0032 never hit it). **No schema change · no closed-list change · no vault writeback** (source/registry are compiler-only `sources` pins — `check-drift --vault` does not compare them). | **APPLIED (compiler; Ruth board byte-identical green: 163 + 1 skipped · validate 6/6 0-block · lint 0/7 · coverage 6/6 · id-check 6 clean · gold-diff unchanged · check-drift + check-drift:vault exit 0; Jonah loaders parse all 4 pinned artifacts)** 2026-06-08 — diff for review |
| SC-0034 | **Gate the measurement-trap self-test** (SC-0031 follow-up; Marcia ruled **Option B** — "only a check that *runs* enforces"). Wire `tools/adversarial_check.py --self-test` into `npm test` as a conditional vitest guard (`tests/adversarial-check.test.ts`) + a `check:adversarial` npm script. The guard runs the helper (the 4 historical traps + the no-op refusal) and goes red on any regression; on a host without `python3` it **skips VISIBLY** (the SC-0008 idiom) so the otherwise-TypeScript gate is never hard-broken. **No `_spec/` change; no vault writeback** (test + dev-tooling, compiler-only). *(Renumbered from a concurrent SC-0033 claim — PR #40; SC-0033 went to Jonah Phase 1, which merged first, so per the append-only rule this took the next free number.)* | **APPLIED (compiler; gate green: 164 passed + 1 skipped — +1 vs the 163 floor; self-test ALL PASS in-gate; python3-absent skip-branch verified)** 2026-06-08 — diff for review |
| SC-0035 | **Refresh Agent-2 + remove the cordel-lineage reference** (plan-first, Marcia-ruled; brings the canonical map-author guidance current before the J01 Jonah map). **Task A:** remove the `forja-de-cordel` cordel-poetry-app reference from `CLAUDE.md` (compiler + Downloads backup), leaving the Facilitator/ML-pilot as the only named downstream consumer (live/content footprint **zero** — CLAUDE.md + the agent-2 draft + the backup; the only repo grep-hits are this SC-0035 record of the removal = the audit trail, per the SC-0017 content-scoped zero-grep precedent). **Task B:** refresh vault `_spec/agent-2-system-prompt.md` v0.2→v0.3 — retire AUDIT (→ the four pilot-2 artifacts); re-point the example to today's blessed P01; **§3C realigned to entities-only (R1 — v0.2 instructed the opposite)**; paste in the forbidden-grammar list + substitution table, Level-3 purity (SC-0030), the de-leak (SC-0017), the mapping discipline (no sentence-shaped slot values), the by-layer bootstrapping rule (closed HOLD / open GROW / forced-closed = governed finding); book-generalize (~14 Ruth refs). Agent prompts aren't pinned → no version-pin ritual; blessed + written back to the vault (PR #13), staging draft removed. | **APPLIED (compiler: Task A done, cordel live-footprint 0; Task B blessed + vault writeback DONE, vault PR #13). Floor unchanged (164 + 1).** 2026-06-08 |
| SC-0036 | **Jonah Phase 2 — J01: apply the map rulings + bootstrap the Jonah registry** (Marcia ruled all 5 J01 calls; Architect applies, evaluator verifies). **Map (rulings 1-3):** genre stays `HISTORICAL_NARRATIVE` + an irony/didactic note in the L1 conditioning prose; v.2 divine command → scene-level `ELDER_AUTHORITY` override (one of the 7 — the closed list **holds**; the register *value* flagged for the lead's ear, not a finding); call/flight scene division confirmed; the draft author-note removed (R6). **Registry (ruling 4 — approved cast):** built the Jonah BCD (10 entities + 2 concepts + 3 figures) → populated `jonah.{aliases,concepts,figures}.json` (was the empty SC-0033 scaffold) → **re-pinned** (aliases 0.1.0→0.1.1). **Coverage now anchors: J01 20/20 explicit accounted · 0 unanchored** (the empty-registry false-positive cleared; the map verified faithful — no hallucination). **Prompt (ruling 5):** patch the v0.3 agent-2 §11 template — remove the stale `cross_ref:` line the lint caught on the first Jonah map (figure↔prop links live in §5B) [vault]. | **APPLIED (compiler: map lint 0 + L1 closed lists hold; registry re-pinned, check-drift 0; coverage J01 anchors; Ruth board 164+1 · 6/6 unaffected). Prompt fix = vault writeback.** 2026-06-09 |
| SC-0037 | **Global Concept Bank + Figure Registry** (Marcia ruled BOTH global 2026-06-09; plan-first `docs/SC-0037-PROPOSAL.md`, evaluator-reviewed + amended). The CB/FIG registries are now **canon-wide** — one code per concept/figure across the whole Bible (the per-book CAST B/PL/O/TM/I stays per-book). One global `registry/{concepts,figures}.json` (52 CB / 118 FIG); **Ruth immutable base** (`CB_0001-0050` / FIG max `0195` — codes unchanged); **Jonah renumbered** `CB_0051/52`, `FIG_0196-198`; per-entry `appears_in`. Readers→global (fixes the latent Ruth-default bug); J01 renumbered; the 4 per-book CB/FIG files+pins retired; re-pinned `concepts.json`/`figures.json` @ 0.2.0. **New `tripod concept-check`** (new-book guard: suggest-reuse on a near-dup; diagnostic). **Vault pass:** Jonah's renumbered CB/FIG notes homed in the vault canon bank + `build_concept_figure_registry.py` global-mode (canon notes → the global files; `appears_in` book-mapped from each note's pericope list) — **the pinned registry reproduces byte-identical from the vault notes**; compiler-side Jonah CB/FIG notes retired (one source). | **SHIPPED — compiler core PR #44 MERGED `f64a322`; vault pass = this compiler PR + the vault notes PR. Acceptance MET:** Ruth byte-identical (166\|1 · gold-diff 0 · lint/coverage 6/6 · id-check --corpus 6 clean) · codes canon-unique · J01 lint-0/coverage 20/20-0-unanchored · verifier suggests-reuse on a planted dup · check-drift 0 · reproducible-from-notes. 2026-06-09 |
| SC-0038 | **J01 graduation — the map into fixtures + the generality regression anchor** (Marcia ruled **Option 1 — map now, FOR_MODEL later** — 2026-06-09, builder + evaluator concurring; her exegetical confirm: **all 5 Jonah codes canon-new** — CB_0051/52 + FIG_0196-198, CB_0052 *panim*-presence distinct from Ruth's CB_0037 *kanaph*-refuge). The blessed J01 map **moves** `_working/J01/` → `fixtures/meaning-map/` (2 declared edits: `status: draft→complete`; the stale PROVISIONAL §5 blockquote removed — false since SC-0037, and an SC-ID process leak per the SC-0017 rule). The FM **skeleton** + its CL **stay in `_working/`** (23 schema blocks + 5 lint findings, ALL on unfilled `__TODO__` judgment fields, ZERO on closed-list axes — graduates when its judgment half is authored / the Slice-4 drafter lands). **New `tests/jonah-j01.test.ts` (8 tests)** = the anchor: L1 closed-list **membership** + **zero-blocks-on-closed-axes** (bite **proven** — a planted fake register fails exactly those 2 and nothing else) + coverage 20/20·0-unanchored + book-aware id-check fully clean + the 5 codes registered (`appears_in` JONAH) + concept-check quiet + map lint 0. **Cast-name alignment:** 4 Jonah cast notes' `english:` aligned to the Ruth bare-Capitalized idiom = the map's approved slugs (Ship Crew · People of Nineveh · Ship · Fare; the bootstrap had written lowercase+article glosses) → rebuilt + **re-pinned `jonah.aliases.json` 0.1.1→0.1.2**. **Book-aware plumbing:** `id-check` derives the alias book per map from its `bcv` (a Jonah map no longer resolves against Ruth's table) + visible unpaired-map skip under `--corpus`; `coverage` pericope-id `^P\d+$`→`^[A-Za-z]\d+$` and FM resolution filtered to `-FOR-MODEL.md` (by-prefix had grabbed J01's COMPILATION-LOG); `gold-diff` unpaired-map skip made VISIBLE; 2 stale engine messages (`ruth.aliases.json` hardcode; the retired `ruth.concepts/figures.json`) made book/global-true. | **APPLIED (compiler-only; PR open).** Board **174 + 1 skipped** (166 floor + 8); Ruth **byte-identical** (gold-diff 6/6 at baseline · lint 13 artifacts 0 findings · coverage 245/245 · id-check 6 clean + J01 visibly skipped) · check-drift + check-drift:vault exit 0 · concept-check 0. **No `validation-rules.json` change, no spec-version bump** (pins: the aliases re-pin only). **Named-deferred:** a Jonah CL/VERIFICATION-INPUT cannot validate yet (CL `pericope_id` + VI patterns still pin `^P\d{2}$` — an SC-0032-class governed widening owed when a Jonah FM/CL graduates); the Jonah cast-home (SC-0037 amendment 2) unchanged; **vault writeback of the J01 map blocked on the book-2-home ruling** (the fixtures≡vault invariant stays Ruth-scoped until then). 2026-06-09 → **MERGED 2026-06-10: PR #46 → `main` `c546926`; board re-verified green on the merge commit (174\|1).** |
| SC-0039 | **Jonah Phase 3 setup — the ruled J02–J05 pericope division + pinned BHSA packets** (Marcia ruled the division 2026-06-10: **the psalm stands ALONE** — "a pericope that is purely the poem… no narrative frame muddying the measurement; the swallowing verse is narrative and sits naturally as the storm's resolution"). The derived tiling (Heb = BHSA-native; Eng = human-facing `bcv`): **J02** Jonah 1:4–2:1 (Heb 1:4–2:2 — storm + sailors + the swallowing + the one-line "Jonah prayed" intro, both frame verses thus OUT of the psalm) · **J03** Jonah 2:2–9 (Heb 2:3–2:10 — **the poem only**, "I called…" → "salvation belongs to YHWH") · **J04** Jonah 2:10–3:10 (Heb 2:11–3:10 — opens with the release/vomit, then Nineveh) · **J05** Jonah 4:1–11 (unchanged). `pericopes.jonah.json` updated with the ruling + provenance; **4 packets extracted offline from BHSA tf/2021 + pinned** (`source/jonah/J02–J05.json`, extractor-0.1.0/bhsa-2021, file-bytes shas). **Verified against the data:** the 5 packets tile the whole MT book (48 verses, union exact, 0 overlaps — first cross-chapter ranges, tuple-compare logic verified); boundary content confirmed (J02 ends at the prayer-intro gloss "pray…from bowels of the fish"; J03 opens "I called" + closes "salvation"; J04 opens at the vomit); cast sanity (sailors/chief-sailor/fish · psalm vocabulary · Nineveh/king · qiqayon/worm/wind). | **APPLIED (compiler-only; PR open).** Board **174 + 1 skipped** unchanged; check-drift exit 0 on the 4 new source pins; check-drift:vault exit 0 (source pins are compiler-only). **No schema change, no spec-version bump, no registry change.** Next: Marcia maps J02 (or the psalm first — her order); each map then runs the J01 pipeline (compile → gates → by-layer read). The J03 psalm = the first non-narrative genre to hit the closed lists — the genre/register stress test. 2026-06-10 |

| SC-0040 | **J02 (the storm, Jonah 1:4–2:1) — map authored, ruled, applied** (Marcia ruled all four groups 2026-06-10: **A** map + absences + the "three days and three nights" TIME_PAIR + the 3 register shifts (ELDER_AUTHORITY 1:6 · CONSULTATIVE 1:7/1:11 · RELIGIOUS_WORSHIP 1:14) · **B** the 12-entity cast, fish = **being** · **C** the 3 cross-book reuses (CB_0052 + FIG_0197 + FIG_0198 continue into J02) · **D** the 5 new canon codes). **Cast registered:** B6-Captain · B7-Great-Fish · PL4-Sea · PL5-Dry-Land · O3-Wind · O4-Storm · O5-Cargo · O6-Lots (BCD notes + `jonah.aliases.json` re-pinned **0.1.3**, 18 entities). **Canon codes minted (vault notes first — reproducible-from-notes preserved):** CB_0053-Fear-of-YHWH · CB_0054-Innocent-Blood · FIG_0199-Hurling-Refrain · FIG_0200-Fear-Escalation · FIG_0201-Captain-Echoes-the-Command; global `concepts.json`/`figures.json` re-pinned **0.3.0** (54 CB / 121 FIG); the 5 SC-0037 Jonah notes settle PROVISIONAL→REGISTERED (condition met at SC-0038). Two no-code dispositions per the approved table: "this evil" + "the sailors' gods" → prose/Q&A (relocated, not deleted). The duration = `TH_THREE_DAYS_AND_THREE_NIGHTS` (the Ruth TH_ duration precedent). TIME_PAIR signed off in `lint-exceptions.json` (**0.1.2**; header book RUTH→CANON, engine-unread). **Reader generality fixes the draft surfaced:** cross-chapter + `; Heb c:v` scene ranges (Eng/Heb versification seam — J03/J04 will need it); book-general prop-anchor strip (latent J01 bug: anchors read "Jonah", scene_links lost — anchor test re-pinned 23→18 deliberately); id-check FM-pairing suffix (2nd instance of the SC-0038 prefix-grab class). | **APPLIED (PR pair open: compiler + vault #16).** J02 gates: **lint 0 drift · 1 accepted** · validate 54 blocks ALL judgment-gaps **0 closed-axis (L1 HELD on the storm)** · coverage **79/79 · 0 unanchored** (entities 12/16 anchored; 4 abstract-overlay warns, the Ruth pattern) · id-check **1 clean / 0 findings** (2 TH_ unverifiable, accepted class) · concept-check **0 suggestions** (Fear-of-YHWH vs Hand-of-YHWH = 0.33 < 0.34, the documented calibration case) · board **174 + 1 skipped** · check-drift + :vault exit 0. Map stays `_working/` (graduation = a later call, the J01 pattern). 2026-06-10 |

| SC-0041 | **The Jonah writeback — book folders inside this vault** (Marcia ruled the book-2 vault-home 2026-06-10; closes the SC-0037 amendment-2 deferred item). **Vault gains:** `pericopes/jonah/` with the two blessed maps (J01 from `fixtures/meaning-map/`, J02 from `_working/J02/` — **byte-identical copies**, shas proven; Ruth's six stay flat in `pericopes/` exactly where they are) + **`bcd/jonah/{beings,places,objects}/`** — the 18 Jonah cast notes come home (book-foldered, avoiding the B1-Judges/B1-Jonah flat-namespace collision; Ruth's `bcd/` grandfathered flat). **The vault is now the cast's source of truth** (the SC-0037 CB/FIG pattern applied to the cast): compiler-side `_spec/registry/jonah-bcd/` **retired** (18 files deleted; zero runtime consumers — verified by grep); the pinned `jonah.aliases.json` **rebuilds byte-identical from the relocated vault notes** (proven by diff; pin **aliases-0.1.3 unchanged**; rebuild recipe: `build_aliases.py --book jonah --bcd <vault>/bcd/jonah`). **The byte-identity guard extends to Jonah:** vault `pericopes/jonah/*` ≡ the compiler's blessed copies (J01 ↔ fixtures; J02 ↔ `_working` until its graduation). Machine-only artifacts stay compiler-side (BHSA packets, `pericopes.jonah.json`, pins). NOT in scope (deliberate): the J0x FOR_MODEL skeletons + CLs (unfinished machine artifacts — no vault `stas/` entries until authored); J02 graduation. | **APPLIED (PR pair open: compiler + vault).** No pin changes (aliases byte-identical); no schema change; no spec-version bump. Board **174 + 1 skipped** · check-drift exit 0 · J02 coverage 79/79 · Ruth byte-at-baseline. Evaluator bar: byte-identity on the REMOTE + the alias rebuild from relocated notes + board green + Ruth untouched. 2026-06-10 |

| SC-0042 | **Promote the merge-discipline rule: documented → enforced** (Marcia's promotion ruling 2026-06-10; the verify-MERGED-before-branch-delete class hit its 2nd instance at the SC-0041 merge — a chained `gh pr merge && git push --delete` deleted the head branch of a PR whose merge had transiently failed, closing the open PR, and let the paired vault merge land out of rhythm order). Per the recurring-error fix-hierarchy, the promotion tier is **make-impossible + gate** (the SC-0034 "only a check that runs enforces" pattern): **new `tools/merge_pr.py`** owns the merge rhythm — (1) after any push, WAIT for GitHub mergeability (UNKNOWN → poll; CONFLICTING → abort, nothing done); (2) merge WITHOUT gh's `--delete-branch`; (3) **re-read the PR until verified MERGED** (merge sha in hand) — a failed verify aborts with NO deletion; (4) only then delete the head branch; (5) optional `--require-merged owner/repo#N` = the pair-order guard (vault merge aborts unless the compiler PR is already MERGED). **Gated:** `tests/merge-discipline.test.ts` runs `merge_pr.py --self-test` in `npm test` — 6 offline state-machine scenarios incl. T2 = the literal SC-0041 trap replay and T6 = verify-MERGED-fails→NO-delete; visible-skip without python3 (the SC-0008/0034 idiom); npm script `check:merge-discipline`. | **APPLIED (PR open).** Board **175 + 1 skipped** (174 floor + the gated self-test); self-test ALL PASS (6/6); no `_spec/` change, no pins, no schema. The recurrence tally for this class closes as PROMOTED. 2026-06-10 |

| SC-0043 | **The external review portal (Option C, ruled 2026-06-10) — Tier B, the Portal Builder's first ship.** A read-only static site (GitHub Pages off this public repo) where external reviewers **without GitHub accounts** read every approved pericope artifact and send questions/suggestions through a Google Form — no edit surface. **Built ONLY from blessed copies on `main`:** `fixtures/meaning-map` (`status: "complete"`) + `fixtures/for-model` & `fixtures/compilation-log` (`status: "valid"`); `_working/`, branches, and the private vault are never read. **The approved-only rule is a failing BUILD TEST, not a convention:** any wrong/missing status, wrong type, unparseable frontmatter, or real-path escape from `fixtures/` (symlink smuggling) aborts the whole build (exit 2, nothing written) — proven by 7 end-to-end bite tests that plant drafts in synthetic trees and assert total failure. Wikilinks resolve via the public `_spec/registry/` JSONs (entity hover = code · Hebrew · English · kind; unresolvable degrades to plain styled text — registry coverage stays the pipeline id-check's job). FOR_MODEL/CL render as native `<details>` collapsible outlines + raw-JSON fallback; `dir="auto"` for Hebrew/RTL; **zero client-side JavaScript**; all source HTML neutralized before markdown rendering. Feedback buttons prefill pericope/artifact/section/kind; they ship **visibly disabled** until Marcia creates the Form (field list in `portal/README.md`) — connecting it is a one-line `portal.config.json` edit later. Rebuild trigger: **push to `main` ONLY** (workflow `review-portal`; off-main runs refuse), so unmerged drafts can never appear; J02's map auto-appears when the Architect's graduation PR merges (no portal change needed). **Token-scope reality:** the local git/gh tokens lack the `workflow` OAuth scope, so the workflow file ships at `portal/ci/portal.yml` with a one-time activation step (Marcia copies it to `.github/workflows/portal.yml` in the GitHub web UI — ~1 min; steps in the file header + README). **Files: `portal/` (self-contained npm package — `marked`+`yaml` lockfile-pinned, zero imports from compiler `src/`) + `vitest.config.ts` (adds `portal/**` to the board's exclude — board count proven unchanged) + this row. NO canon paths touched** (no `_spec/`, `fixtures/`, `_working/`, `src/`, no vault writes). | **MERGED (PR #51 → main `23b1e9f`, 2026-06-10, under the evaluator's recorded conditional green) — LIVE 2026-06-11 at https://marciasuzuki.github.io/tripod_compiler/ (workflow activated + Pages enabled + deploy runs green; serving 8 pericopes / 20 approved artifacts incl. the graduated J02) — Google Form CONNECTED (follow-up PR: formBase + 4 entry ids in `portal.config.json`).** Portal tests **25/25** (the bite suite + units + real-tree smoke: 7 pericopes / 19 approved artifacts = Ruth P01–P06 map+FM+CL triples + the J01 map alone, FOR_MODEL honestly "not yet authored"). Compiler board at the SC-0042 floor: **175 + 1 skipped, 13/13 files** (re-proven after rebasing onto the merged #50; the claim-time proof was at the 174-floor). **Marcia ruled the go-live sequencing (Option A, 2026-06-10): activate the deploy right after merge — the reading room goes live with the buttons visibly disabled-but-explained; the Form connects whenever convenient.** Remaining ~5-min dependency (not merge-blocking): create the Form from the README field list + return the pre-filled link. 2026-06-10 |

| SC-0044 | **J02 graduation + the pinned versification convention** (the two ruled pre-J03 riders, from the 2026-06-10 adopt-package; the J01/SC-0038 pattern). The blessed J02 map **moves** `_working/J02/` → `fixtures/meaning-map/` (git-mv, zero edits — vault `pericopes/jonah/` byte-identity verified across the move; the guard's J02 pointer flips `_working` → fixtures); the FM skeleton + CL stay `_working/J02/` until authored. **New `tests/jonah-j02.test.ts` (9 tests)** = the storm anchor: L1 membership incl. the 3 ruled register overrides · zero-closed-axis blocks (pinned 54) · coverage 79/79·0-unanchored·36-implied · book-aware id-check clean (TH_ = the accepted class) · the 8 SC-0040 codes registered · concept-check quiet · lint 0-drift-1-accepted (the TIME_PAIR, asserted exactly) · **the CONVENTION PIN (the ruled rider): scene `verse_range` = HEBREW (BHSA-native, what coverage reconciles), prop `verse_anchor` = ENGLISH (human-facing) — asserted on the divergent S5 (Heb 2:1-2 vs Eng 1:17/2:1)**. Bite proven: a fake register fails the 2 closed-list guards; dropping the `; Heb` qualifier fails the convention + coverage guards — 4 failures, each caught by its own assert. | **APPLIED (PR open).** Board **184 + 1 skipped** (175 floor + 9); no `_spec/` change, no pins, no vault change (byte-identity preserved). Jonah now has TWO graduated maps + two anchors; J03 (the psalm) builds on the pinned convention throughout. 2026-06-10 |

| SC-0046 | **Genre-aware pericope register — the fix for the FIRST REAL L1 FINDING** (Marcia's A-ruling 2026-06-10 on the J03 psalm finding; the JONAH-PLAN's "prize finding" delivered). The finding: `validation-rules.json` hard-coded `pericope_classification.register` as **`const INFORMAL_CASUAL`** — a narrator assumption invisible while every pericope was narrative; the psalm (the ruled division: purely the poem, **no narrator voice**) was structurally blocked from carrying its true register. The closed lists themselves HELD (POETIC_SUNG ✓ PRAYER ✓ RELIGIOUS_WORSHIP ∈ the 7 — nothing minted); the gap was schema structure, not vocabulary. **The ruling, encoded (v0.15 → v0.16):** `register` widens to the closed `register_value` enum + an `allOf` conditional keeps the narrator constant for NARRATIVE (`genre_group: NARRATIVE → register must be INFORMAL_CASUAL`) — option (a); option (c) rejected as forcing a false register onto a narrator-less prayer. **Proven 4 ways** (+ the permanent gate `tests/register-genre-aware.test.ts`): Ruth-as-is green · NARRATIVE+RELIGIOUS_WORSHIP **blocks** (teeth) · POETIC_SUNG+RELIGIOUS_WORSHIP **clears** (the psalm's register block 1→0; its other 54 = judgment TODOs) · POETIC_SUNG+FAKE **blocks** (membership). Re-pinned (file-bytes sha); supersedes-history line added. **Folded rider:** the J02 map status flip draft→complete (the evaluator's PR-#52 catch) — compiler side on PR #52, **vault side on this SC's vault PR #18** (the byte-identity pair, coherence diff-proven pre-PR). | **APPLIED (stacked on PR #52; PR pair open: compiler + vault #18; merge order #52 → compiler SC-0046 → vault #18 via `tools/merge_pr.py --require-merged`).** Board **188 + 1 skipped** (175 floor + the 9 J02-anchor + these 4); check-drift exit 0 @ v0.16; check-drift --vault green against the vault-PR worktree; Ruth/J01/J02 narrative artifacts validate unchanged. J03's apply-cycle (SC-0045) proceeds on Marcia's B–E rulings once this lands. 2026-06-10 |

| SC-0045 | **J03 — the psalm (Jonah 2:2–9 = Heb 2:3–10): mapped, ruled, applied — THE EXPERIMENT PASSES** (Marcia ruled A–E 2026-06-10: **A** the schema finding → SC-0046; **B** genre = **PRAYER**; **C** the 9 enumerated cast entries (8 distinct entities); **D** the reuses — CB_0011 Hesed + FIG_0198, with "driven from before your eyes" = **prose** (no CB_0052 cross-ref, no new code); **E** two new figures — the Temple-Turn pair + the Drowning-Engulfment cluster; the optional candidates stay unminted). **The first non-narrative artifact of the corpus:** POETIC_SUNG / PRAYER / RELIGIOUS_WORSHIP (pericope-level, under the v0.16 genre-aware rule — no narrator voice in the ruled division; one narrative word, "and he said," rides Heb 2:3's front and is named in §1). **Cast registered (vault-first, `bcd/jonah/`):** B8-Idol-Keepers · PL6-Sheol · PL7-Temple · PL8-Pit · O7-Deep · O8-Weeds → `jonah.aliases.json` **0.1.4** (24 entities). **Canon codes (vault-notes-first):** CB_0055-Vain-Idols · CB_0056-Salvation-Belongs-to-YHWH · FIG_0202-Temple-Turn-Pair · FIG_0203-Drowning-Engulfment → registries **0.4.0** (56 CB / 123 FIG). **THE CROSS-BOOK MOMENT: CB_0011 Hesed `appears_in: [JONAH, RUTH]`** — the first canon-wide concept spanning both books (Ruth's note gains J03; the global bank doing what SC-0037 built it for); FIG_0198 gains J03 (the descent's bottom: "to the roots of the mountains"). | **APPLIED (stacked on SC-0046/PR #53; PR pair open: compiler + vault #19; merge chain #52 → #53 → #18 → this → #19).** J03 gates: lint **0 — a poem, born clean** · validate 35 blocks ALL judgment-gaps, **closed-axis 0 (L1 GREEN on poetry)** · coverage **43/43 · 0 unanchored** (entities 9/12; 3 abstract-overlay warns, the Ruth pattern) · id-check **1 clean / 0 findings** · concept-check **0** · board **188 + 1 skipped** · check-drift 0 (aliases 0.1.4, registries 0.4.0). Map stays `_working/J03/` (graduation later, the pattern). **The generality verdict: narrative ×2 + poetry ×1 — the interlingua holds; the one structural gap it ever forced is fixed and gated.** 2026-06-10 |

| SC-0047 | **J04 — Nineveh (Jonah 2:10–3:10 = Heb 2:11–3:10): mapped, ruled, applied** (Marcia ruled A–E 2026-06-10: **A** 6 scenes incl. the people-before-king split + registers (ELDER_AUTHORITY 3:2 · FORMAL_OFFICIAL oracle 3:4 + decree 3:7–9); **B** the Elohim question = **the Ruth-style two-code split** — new **B9-God** (the narrator's Nineveh-scenes name; the insider/outsider pattern made structural, the same referent stated in the note); **C** the five cast codes — B10-King-of-Nineveh · B11-Nobles · B12-Animals · O9-Sackcloth · O10-Ashes; **D** reuses — B5 (onstage at last) · B7 · PL1 · PL5 · CB_0051 @P16+P18 (the evil of 1:2, turned from) · FIG_0196 @P3 (the formula's literal "second time"); **E** firm mints = **CB_0057-God-Relents** (nicham, the pivot) + **FIG_0204-Not-Perish-Refrain** (captain 1:6 · sailors 1:14 · king 3:9 — three pagan mouths); Man-and-Beast + the optional set stay unminted). **The folded parser fix (the evaluator's flag, found via my own "flags carried 4≠3" anomaly):** the §5 flag-pointer parser scanned parentheticals, so the cross-pericope note "pair with J01 **Proposition 1**" wrongly flagged local P1 — pointers now stop at the first parenthetical (`tests/flag-pointer-parse.test.ts`, bite proven; Ruth inert, gold-diff at baseline; P1 verified flag-free on the real artifact, exactly 6 declared instances landing). Cast → **30 entities** (aliases **0.1.5**); banks → **0.5.0** (57 CB / 124 FIG); 6 reuse notes gain J04. | **APPLIED (PR pair open: compiler + vault).** J04 gates: lint **0 born-clean** · validate 54 blocks all judgment-gaps, **closed-axis 0** (narrative under the v0.16 conditional) · coverage **67/67 · 0 unanchored** (matched 30; entities 12/14; 2 abstract-overlay warns) · id-check **1 clean / 0 findings** · concept-check **0** · board **189 + 1 skipped** · check-drift 0. Map stays `_working/J04/` (graduation later). **One pericope remains: J05.** 2026-06-10 |

| SC-0048 | **J05 — the qiqayon (Jonah 4:1–11; ch.4 Eng=Heb): mapped, ruled, applied — THE BOOK OF JONAH IS FULLY MAPPED** (Marcia ruled A–E "as laid out" 2026-06-10: **A** 4 scenes + the furious-prayer register (RELIGIOUS_WORSHIP 4:2–3 — prayer carries the address, not the mood; the questions + closing argument stay plain); **B** the name-weave = **single code as drafted** — B2 with referential forms tracking YHWH → **YHWH-Elohim (4:6, the fusion hinge)** → ha-Elohim → Elohim → YHWH, one speaker in one dialogue, the weave referential not structural (the J04 split governed scene-coherent blocks; ch. 4 alternates inside an exchange — interpretation stated for the evaluator); **C** the four cast codes — **B13-Worm** (a being: an appointed creature that acts, the fish's precedent) · O11-Qiqayon · O12-East-Wind (distinct from J02's storm-wind O3) · O13-Booth; the sun stays prose; **D** reuses — B5 (the hundred-twenty thousand) · **B12-Animals (the book's closing word)** · PL1 · **PL2-Tarshish back in the confession** · **CB_0011 + CB_0057 BOTH at P4** (the creed quotes "abounding in hesed" and "relenting of evil" — the withheld motive lands as accusation; CB_0057 completed in the prophet's own mouth); **E** the strong set minted ("the Appointing refrain is as strong as figure evidence gets in this book") — **FIG_0205-Appointing-Refrain** (*vayman* ×4: the fish (J02), the qiqayon, the worm, the east wind — @P9/P12/P14, the J02-P22 cross-reference held safely in commentary by the SC-0047 parser fix) + **FIG_0206-Anger-Question-Pair** (4:4 ↔ 4:9) + **CB_0058-Compassion-Creed** (the Exodus-34 formula, @P4 — P4 carries the triple); the optional five (death-wish refrain · shade-pair · pity-pair · the name-weave · *gadol*'s last turns) stay unminted, carried in prose. One born-clean reword: the English number-word "and" ("a hundred and twenty thousand") was my rendering's artifact, not the Hebrew's (שְׁתֵּים־עֶשְׂרֵה רִבּוֹ has no conjunction) — reworded, not excepted. Cast → **34 entities** (aliases **0.1.6**); banks → **0.6.0** (58 CB / 126 FIG); 6 reuse notes gain J05 (CB_0011 now spans P02→J05). | **APPLIED (PR pair open: compiler + vault).** J05 gates: lint **0 born-clean** · validate 44 blocks all judgment-gaps, **closed-axis 0** · coverage **70/70 · 0 unanchored** (matched 24; entities 10/13; 3 abstract-overlay warns) · id-check **1 clean / 0 findings** · concept-check **0** · board **189 + 1 skipped** · check-drift 0. Flags verified landing exactly as declared (P4 = the creed triple; no parenthetical leak). Map stays `_working/J05/`. **All 48 verses of Jonah are mapped: 5 pericopes · 2 books · 3 genres-in-use · the closed lists byte-unchanged since Ruth.** 2026-06-10 |

| SC-0049 | **The Jonah Generality Report — the held-out test, written up** (`docs/JONAH-GENERALITY-REPORT.md`; the JONAH-PLAN §2 Phase-3 final deliverable; Marcia's sequencing ruling 2026-06-10: the report first, then the Ruth-remainder arc). The experiment's formal record: the question (does the vocabulary describe Scripture or just Ruth?), the by-layer verdict (**L1 HELD, zero values added · one structural prize-finding, ruled + fixed + gated (v0.16) · L3 born clean ×5 · the registry filled as designed, with the first cross-book concept anchor — CB_0011 Hesed P02→J05** · L2 explicitly deferred to the drafter phase), the numbers (48/48 verses · **279/279 explicit referents, 0 unanchored book-wide** · board 166→189+1 · pins aliases 0.1.0→0.1.6, banks →0.6.0, schema v0.15→v0.16, all reproducible-from-notes), the second thesis (**five pipeline defects only held-out drafting could find** — the Ruth-only anchor strip, cross-chapter ranges, the versification machinery, the prefix-grab class ×2, the §5 parenthetical leak — each now bite-gated), and the honest limits (the judgment half untested on Jonah; one judge; 2 of 4 genre groups exercised; 48 verses is a small book). | **DRAFTED (PR open — docs-only, Tier B by files-touched: evaluator number-verify + Marcia's content blessing, then merge).** No `_spec/` change, no pins, no code. 2026-06-10 |

| SC-0050 | **The P07 packet — the Ruth-remainder arc's deterministic opener** (Marcia's sequencing ruling 2026-06-10: report first, then Ruth — interleaving explicitly allowed; this is the interleaved deterministic half, the SC-0033 pattern applied to P07). `extractor/pericopes.json` gains **P07 = Ruth 2:17–23** (the range settled by the June-2nd exegesis-blessed build; P08–P14 stay OUT, awaiting her division ruling; the file's note updated to say so). Packet extracted offline from BHSA tf/2021 + **pinned** (`source/ruth/P07.json`, file-bytes sha). **Verified against the data:** 7 verses (2:17–23 exactly) · 90 referents · proper nouns Boaz/Naomi/Ruth/YHWH (the gleaning-return cast). A process correction recorded honestly: the first extraction attempt assumed P07 was already in `pericopes.json` — it never was (the May PROGRESS note said so); the data corrected the memory at the first command. | **APPLIED (PR #58, stacked on #57 — the ruled merge order, report → Ruth).** Board **189 + 1 skipped** unchanged; check-drift exit 0 on the new pin. Next in the arc (post-report): the P07 **fresh rebuild** under the proven pipeline (the June-2nd draft beside it as exegetical reference), then the P08–P14 division ruling. 2026-06-10 |

| SC-0051 | **The P07 fresh rebuild — Ruth 2:17–23 mapped under everything Jonah proved (the Ruth-remainder arc's first pericope)** (Marcia ruled **Option 1, blessed as drafted**, 2026-06-10: "every divergence is data-supported, honestly recorded, and reversible by a word"). The first Ruth pericope authored under Agent-2 v0.3 + the pinned conventions + the global banks; the June-2nd draft served as exegetical reference only (its readings honored; its format predates ~20 governed changes). 4 scenes (the field 2:17 · the homecoming 2:18 · the house-talk 2:19–22 · the season-close 2:23) · 16 propositions · INTIMATE scene-level on S3 + one CEREMONIAL moment at the 2:20 blessing · narrator INFORMAL_CASUAL (the v0.16 NARRATIVE const satisfied). **Zero minting — the homecoming dividend:** all 10 CB + 7 FIG flags are canon reuses whose notes already declared P07 (swept 17/17): **CB_0011 Hesed returns to a Ruth source verse (2:20) — the cross-book concept's first homecoming**; **CB_0001 Kinsman-Redeemer fires at its declared first appearance (2:20)**; FIG_0104 closes P06's declared forward-link at the leftover (2:18); FIG_0110/0111/0112 open at 2:20 toward chs. 3–4; FIG_0012 closes the 1:14↔2:23 clinging rhyme. **The three blessed divergences from the June-2nd reference** (numbered for the application-check): (1) the 2:20 blessing reads "blessed be he — the man — by YHWH" (the Genesis-14:19 construction; June-2nd's proposition had "she blesses the LORD" though its own scene title said "the blessing on Boaz"); the asher-clause ambiguity — whose hesed has not been forsaken, YHWH's or the man's — is held open in the proposition (the floating "he"), the concept entry, and the scene's Significant Absence; (2) 2:21 keeps Ruth's masculine "the young men" (B17) where June-2nd smoothed to "his workers" — Boaz's 2:8 and Naomi's 2:22 say "young women" (B16); the report-ripple is stated in one Context sentence, no motive guessed; (3) Scene 3 = the unbroken dialogue 2:19–22 with Scene 4 = the 2:23 close (June-2nd split 19–21/22–23, forcing its INTIMATE override to straddle scenes). Also per the sentence-token discipline: June-2nd's invented `TH_LEFTOVER_FROM_SATIETY_FORM` dropped — the leftover food and "the dead of the household" stay **deliberately uncoded** (plain surface forms; the FM records the pending pair). **One registry correction (the vault half): FIG_0165's stray P07 struck** (appears-in [P07, P09, P11] → [P09, P11], opens-at → P09; the note's own body says "Surfaces in: p11" and its content is the ch.4 gate scene — Ruth 2:17–23 has no two-stage disclosure); registry JSONs rebuilt from the edited vault worktree **byte-identical to the pins** (book-level unchanged — no pin move). | **APPLIED (PR pair open: compiler + vault; merge on Marcia's word via `tools/merge_pr.py`, vault half `--require-merged`).** Gates at draft: lint **0 — born clean** · validate 47 blocks all judgment/pending-registry, **closed-axis 0** · coverage **40/40 explicit · 0 unanchored** (24 implied flagged · 19 reviewer ticks · 8 abstract-overlay warns, the Ruth class) · id-check **0 errors** (the 2 uncoded-by-design pending lines) · concept-check **0** · board **189 + 1 skipped** · gold-diff 0 · check-drift 0. Map stays `_working/P07/` (graduation = a later call, the J01 pattern). **Next: Marcia's P08–P14 division ruling, then pericope by pericope.** 2026-06-10 |

| SC-0052 | **The P08–P14 division — Marcia's ruling pinned; Ruth's homestretch packets extracted and pinned (the SC-0039 pattern applied to the second half of Ruth)** (Marcia ruled **Option 1, the table as proposed**, 2026-06-11 — the two contested seams ruled **3:5\|3:6** (plan\|execution, the locale change; over the 3:8\|3:9 alternative the old feet-entry implied) and **4:8\|4:9** (the So-and-so movement closes with his sandal; "you are witnesses" opens the attestation — the June-era discourse threads' own granular reading; over 4:6\|4:7); the four other seams uncontested: 2:23\|3:1 fixed by SC-0051 · 3:13\|3:14 speech-close · 4:12\|4:13 "so Boaz took Ruth" · 4:17\|4:18 the *toledot* formula. Her note: locale changes and speech-closes have cut every scene boundary P01→J05 — the seams rhyme with the corpus). `pericopes.json` gains **P08 = 3:1–5 (Naomi's plan) · P09 = 3:6–13 (the threshing-floor night) · P10 = 3:14–18 (before-recognition dawn) · P11 = 4:1–8 (the gate: So-and-so, the decline, the sandal) · P12 = 4:9–12 (the attestation + the triple blessing) · P13 = 4:13–17 (marriage, the women's word, Obed) · P14 = 4:18–22 (the generations of Perez — genre GENEALOGY, first use, flagged as the next structural watch-point)**. Seven packets extracted offline from BHSA tf/2021 → `source/ruth/P08–P14.json`, **pinned by file bytes**. **Verified against the data:** ranges exact 7/7 · re-extraction **7/7 byte-identical** · **whole-book tiling 85/85, no gaps, no overlaps, P01–P14 contiguous** (chapter counts re-derived from BHSA, not memory) · 453 new referents (40 vv) · the proper-noun spot-checks corroborate the exegesis — **P10 carries ZERO proper nouns** (the secrecy scene is nameless in the data) while **P12 pours 15 proper nouns into 4 verses** (the attestation is where the names land, **Mahlon and Kilion among them — the P01-D2 withheld pairing's landing site confirmed at 4:9–10**) · **pin-bite proven** (1-byte tamper → check-drift exit 1 naming the file; restore → exit 0, byte-identical to the independent re-extraction). | **APPLIED (PR open; Tier A by files-touched — pinned source packets; evaluator verify + Marcia's merge word).** Board **189 + 1 skipped** unchanged · check-drift exit 0 on all 7 new pins (pins.json +28 lines, purely additive) · gold-diff 0. **Next: P08 — Naomi's plan — opens the map cycle; pericope by pericope to the child named by women and the king's name in the genealogy.** 2026-06-11 |

| SC-0053 | **P08 — Naomi's plan (Ruth 3:1–5): mapped, ruled, applied (Ruth's homestretch opens)** (Marcia ruled **Option 1, blessed as drafted**, 2026-06-11 — groups A–D: **A** two scenes on the P03 dialogue shape (the plan spoken 3:1–4 · the consent 3:5), BOTH scene-level INTIMATE (the whole pericope is the two women's private talk), narrator INFORMAL_CASUAL; **B** cast 100% registered reuse — B3 ("Naomi her mother-in-law", name + kinship-word together) · B9 ("my daughter", her name absent until the consent) · B13 REFERENCED ("our kinsman" → "the man") · B16 REFERENCED · PL6-Threshing-Floor (the plan's destination) · PL_NAOMIS_DWELLING implied · O2-Barley · O13-Your-Garments — **zero new entities, zero uncoded, id-check FULLY clean at draft (a project first)**; **C** flags CB_0014-Rest-Menucha @P1 (1:9's wish becomes the plan's goal) + CB_0042-Uncover-Feet-Margelot @P7, figures FIG_0120 (the 1:9↔3:1 rest-pair CLOSES) + FIG_0121 (wash–anoint–garments triple) + FIG_0122 (the "he will tell you" handoff OPENS toward P09's reversal) + FIG_0123 (the absolute assent OPENS; Boaz mirrors at 3:11) — **plus the blessed NON-flag: CB_0001 NOT flagged at 3:2's moda ("our kinsman" — the family word; the legal word goel is withheld until Ruth's own mouth at 3:9/P09; the P05 kinsman-in-prose precedent; the withholding recorded as Significant Absence)**; **D1** the one lint finding = the Hebrew's own pair (le'ekhol ve-lishtot, "eating and drinking") → the **9th signed-off exception** (`lint-exceptions.json` 0.1.2 → **0.1.3**, reason ACTIVITY_PAIR, the ENTITY_PAIR/TIME_PAIR family — kept verbatim, not reworded, not split); **D2** FIG_0113's declared close-at-P08 = a FIG_0165-class stray (no leftover content in 3:1–5) — correction DEFERRED to P10's cycle on the evidence; **D3** FIG_0136 fires at P09 (its P08 listing is the pair-anchor only), no action). | **APPLIED (PR open — compiler-only, no vault half; Tier A by files-touched; evaluator verify + Marcia's merge word via `tools/merge_pr.py`).** Gates: lint **0 findings · 1 accepted** (the ruled ACTIVITY_PAIR, surfaced ✓ ACCEPTED) · validate 21 blocks all judgment placeholders, **0 closed-axis** · coverage **16/16 explicit · 0 unanchored** (18 implied flagged · 8 ticks · 2 abstract-overlay warns) · id-check **fully clean** · concept-check 0 · board **189 + 1 skipped** · gold-diff 0 · check-drift 0 @ lint-exceptions 0.1.3. Map stays `_working/P08/` (graduation later, the pattern). **Next: P09 — the threshing-floor night (3:6–13), where the handoff reverses and Ruth speaks the withheld word.** 2026-06-11 |

| SC-0054 | **P09 — the threshing-floor night (Ruth 3:6–13): mapped, ruled, applied (the book's hinge)** (Marcia ruled **Option 1, blessed as drafted**, 2026-06-11 — groups A–D: **A** 3 scenes (the approach 3:6–7, narrator plain · midnight 3:8–9 · Boaz's answer 3:10–13), INTIMATE scene-level on S2+S3, one CEREMONIAL moment at the 3:10 blessing, **the 3:13 chai-YHWH oath stays INTIMATE on the P03 precedent** (the weight in the formula's figure, not the register); **B** cast all registered — B9 ("Ruth your servant", amah) · B13 ("the man" at the startle) · B3 REFERENCED · B18 (the role at Ruth's claim) · **B19-Mr.-So-and-so enters the book REFERENCED-unnamed ("a redeemer nearer than I")** · B21 ("all the gate of my people") · B10 (blessed-by + sworn-by) · PL6 · PL7 (pointed at) — 4 deliberately uncoded surface forms (the grain-heap's edge · "at half of the night" · the bachurim, a different word from B17's field-workers · "the night … the morning") = the id-check 4 pending; **C** the densest reuse set in the canon, all pre-declared: 7 CB (CB_0042 ×2 · CB_0037 · **CB_0001 ×4 — the goel-word arrives in Ruth's mouth at 3:9** · CB_0008 · CB_0011 — hesed's third station 1:8·2:20·3:10 · CB_0032 · CB_0006) + 15 FIG incl. **five cross-pericope pairs CLOSING on declared sites** (FIG_0131 wing 2:12→3:9 · FIG_0132 amah 2:13→3:9 · FIG_0111 hesed 2:20→3:10 · FIG_0123 assent 3:5→3:11 · FIG_0134 chayil 2:1→3:11) + FIG_0112 at the 3:12 nearness + the P09-locals (0130 startle · 0133 greater-hesed · 0135 oath · 0137 blessing-formula · 0138 nearer-redeemer · 0139 lie-down · 0140 reverse-handoff — the P08 handoff mirrored) + FIG_0136 riding FIG_0123's prop (the double-flag precedent); **D** FIG_0141 carried (OPTIONAL keep, declared+real) · the loose-list leave-outs each registry-checked (FIG_0142/0194/0161/0165 list P09 with no firing site in 3:6–13 — FIG_0165's expected-not-firing here strengthens the [P11]-only case; CB_0020's P09 listing loose — 3:13 is the chai-YHWH formula, FIG_0135's, not the self-curse) · one note-nit logged: FIG_0122's note cites nonexistent "FIG_0124" — the actual mirror is FIG_0140). **Two self-caught rewords pre-gate, recorded:** my own "verb" in a §3C conditioning line (grammar jargon — lint caught it; reworded "the redeem-word"; the J05 never-sign-off-my-own-English class) and the B19 slug missing the BCD name's period (id-check caught it). | **APPLIED (PR open — compiler-only, no vault half; Tier A by files-touched; evaluator verify + Marcia's merge word via `tools/merge_pr.py`).** Gates: lint **0 — born clean** (post-reword) · validate all judgment placeholders, **0 closed-axis** · coverage **39/39 explicit · 0 unanchored** (23 implied flagged · 25 ticks · 7 abstract-overlay warns) · id-check **0 errors** (the 4 uncoded-pending) · concept-check 0 · board **189 + 1 skipped** · gold-diff 0 · check-drift 0. Map stays `_working/P09/`. **Next per Marcia's order: the VISIBILITY CYCLE (J03+J04+J05+P07 graduations → fixtures + vault + portal) BEFORE P10.** 2026-06-11 |

| SC-0055 | **The visibility cycle — J03 + J04 + J05 + P07 graduate together (fixtures + vault + portal)** (Marcia's sequencing order with the P09 blessing, 2026-06-11: **"P09 then visibility"** — Obsidian and the reviewers' portal catch up to everything blessed before P10 opens). The four blessed maps move byte-identical from `_working/<id>/` into `fixtures/meaning-map/` with the declared **status draft → complete flip** (the SC-0044 J02 pattern; one line per file, proven by the diff); the FM skeletons + COMPILATION-LOGs stay `_working/` until authored (the SC-0038 J01 Option-1 pattern). **The batch anchor (`tests/graduation-batch-1.test.ts`, 21 tests):** per pericope — parses + status complete · L1 closed lists HOLD · the fresh skeleton's blocks are judgment-only with the gap signature PINNED (J03 **35** · J04 **54** · J05 **44** · P07 **47** — each matching its apply-cycle record) · the frozen packet reconciles (43/43 · 67/67 · 70/70 · 40/40, 0 unanchored ×4) · the flag codes registered canon-globally; plus J03 re-pins the versification convention on the fully divergent pericope. **Bite proven** (P07 status tampered → exactly the named test fails; restored → 21/21). **The vault half (PR pair):** `pericopes/jonah/` gains J03+J04+J05, `pericopes/` gains P07 — each **byte-identical to its graduated fixture** (cmp-proven pre-push). **The portal catches up on merge:** local build verified — **12 pericope pages from 24 approved artifacts** (was 8/20; the psalm, Nineveh, the qiqayon, and the gleaning brought home all appear; rebuild rides the push-to-main Action). | **APPLIED (PR pair open, stacked on SC-0054: compiler + vault; Tier A by files-touched; evaluator verify + Marcia's merge word; merge protocol: retarget the stacked PR to main BEFORE the base merges — the #19→#20 lesson — then compiler first, vault `--require-merged`).** Board **210 + 1 skipped** (189 + the 21 anchors) · gold-diff 0 (6 map-only skips, all visible) · lint corpus 18 artifacts · 0 findings · check-drift 0 · check-drift:vault exit 0 (the vault half touches no `_spec/`). **Next: P10 — the nameless dawn — on a portal that finally shows the whole runaway prophet.** 2026-06-11 |

| SC-0056 | **P10 — the nameless dawn (Ruth 3:14–18): mapped, ruled, applied** (Marcia ruled **Option 1, blessed as drafted**, 2026-06-11 — the two INTIMATE scenes (the floor at dawn 3:14–15 · home 3:16–18; no ceremonial moment) · the registered cast with **the mitpachat-vs-simlah note visible** (O13 kept per the BCD's own declared "P10 — 3:15 the cloak" reading; no new code — the word-difference stated in the entry, hers to make structural later) · the declared pairs closing where their notes always pointed · the FIG_0113 one-line vault correction · the FIG_0163 leave-out · **the measure-unit left unflagged exactly because the text withholds it**). The map keeps the pericope's own discipline: **not one personal name in five verses** — "he"/"the man" (FIG_0155 ×2), "the woman," "my daughter," "her mother-in-law" — matching the packet's zero proper nouns. 2 scenes · 13 propositions · 4 CB + 7 FIG, zero minting: CB_0042 (the night-word's 4th and final margelot) · **CB_0044 + CB_0024 at the empty-answer (the declared 1:21 ↔ 3:17 pair)** · **CB_0043 Mother-in-Law at its declared first appearance** (Boaz's reported word aims the gift at the role); **FIG_0153 Do-Not-Return-Empty CLOSES P04→P10 as declared** · FIG_0154 (the doubled who-are-you — 3:9's question in the morning's mouth, the triple ambiguity held per its note) · FIG_0150+0151 (the secrecy pair) · FIG_0152 ×2 (six measures, unit unsaid) · **FIG_0156 OPENS toward the gate** (the wait-formula P11 fulfills). **The FIG_0113 deferral resolved on the evidence (the vault half):** P10 holds no leftover-from-satiety image (the six measures are fresh-weighed; the empty-answer is FIG_0153's) → appears-in [P07, P08] → **[P07]**, closes-at → P07; registries rebuilt from the edited vault **byte-identical to the pins** (no pin move). **Leave-outs recorded:** FIG_0163's P10 anchor misreads 3:18's "the man" (plainly Boaz, who will not rest — not the nearer redeemer; its true scope = P11's anonymity formula; hers to overrule) · CB_0040 not flagged at the six measures (its own note says "possibly seah" — flagging a unit asserts what the text withholds) · CB_0026/CB_0001/CB_0020 loose listings. **Two self-caught comma rewords pre-gate** (my phrasing, not the text's: the vocative split to its address-line; the quote-frame moved into the question — the P07/P08 idiom, no exceptions needed). **Text point mapped per the data:** 3:15 ends "and HE went into the town" (BHSA; some ancient versions read "she"). | **APPLIED (PR pair open: compiler + vault; Tier A by files-touched; evaluator/Marcia pass + her merge word via `tools/merge_pr.py`, vault half `--require-merged`).** Gates: lint **0 — born clean** (post-rewords) · validate all judgment placeholders, **0 closed-axis** · coverage **23/23 explicit · 0 unanchored** (22 implied flagged · 10 ticks · 4 abstract-overlay warns) · **id-check fully clean, zero pending** (the second after P08) · concept-check 0 · board **210 + 1 skipped** · gold-diff 0 · check-drift 0. Map stays `_working/P10/`. **Next: P11 — the gate (4:1–8) — Mr. So-and-so meets his non-name, and the sandal comes off; FIG_0165 finally fires where its corrected note points.** 2026-06-11 |

| SC-0057 | **P11 — the gate (Ruth 4:1–8): mapped, ruled, applied** (Marcia ruled **Option 1, blessed as drafted**, 2026-06-11 — including **the corpus's register first: FORMAL_OFFICIAL at SCENE level on S2–S4** (the convened proceeding, offer through attestation; J04's decree carried it at moment level; all within the closed 7) · the two registry completions as the vault half · the leave-outs). 4 scenes (convening 4:1–2, plain · the field offer + "I will redeem" 4:3–4 · Ruth named + the decline 4:5–6 · the custom + the sandal 4:7–8) · 15 propositions · **zero minting: 9 CB + 18 FIG — the registry's legal cluster firing entire, the densest flag set of the project.** **FIG_0165 FINALLY FIRES where its corrected note points** (the field-then-Ruth staging, props 5+9); **FIG_0015 closes the chance-pair** (2:3 "her chance chanced" ↔ 4:1 the redeemer passing); FIG_0156 + FIG_0112 close; **six figures open toward P12–P13** (FIG_0002 + FIG_0110 → 4:10 · FIG_0005 the sandal pair · FIG_0003 gate-as-remembered-name · FIG_0016 the women's absence arc · FIG_0014 the divine name silent through the whole proceeding, returning at 4:11). Cast: **B19 at his named site (peloni almoni — the non-name spoken to his face)** · **B20-Elders onstage (ten men)** · B2 "our brother Elimelech" · B3 "Naomi, who returned from the fields of Moab" (the seller) · B9 "Ruth the Moabite, the wife of the dead" — **the husband still unnamed, one scene longer** · 3 uncoded-by-design (the field-portion · the dead · the buying-day). **The vault half — both anonymity-figure corrections completed on the gate's own evidence:** FIG_0165 [P09, P11] → **[P11]** (SC-0051 struck the stray P07; SC-0054 found no P09 firing site; it fires here — its body always said "Surfaces in: p11") and FIG_0163 [P10, P11] → **[P11]** (SC-0056 ruled the 3:18 anchor a misread — that "man" is Boaz; the pattern's true site is 4:1's formula); registries rebuilt **byte-identical to the pins** (no pin move). **Text point per the data:** 4:5 mapped as the qere "you acquire Ruth" (the ketiv reads "I acquire") — flagged like P10's vayyavo. **Leave-outs recorded:** FIG_0161 (FIG_0160 already carries 4:1's hinneh) · CB_0009 (its P11 listing marks an ABSENCE — FIG_0014 carries that structure) · CB_0010/CB_0027 (4:11's, P12) · O18/O19 (CB_0003 + CB_0007 cover them). **Three self-caught rewords pre-gate:** my "clause" jargon (the 3rd grammar-word lint-catch of the homestretch) · the 4:4 conditional restructured to condition-in-question form · an anti-entry of mine removed. | **APPLIED (PR pair open: compiler + vault; Tier A by files-touched; her pass + merge word via `tools/merge_pr.py`, vault half `--require-merged`).** Gates: lint **0 — born clean** · validate all judgment placeholders, **0 closed-axis** · coverage **53/53 explicit · 0 unanchored** (27 implied flagged · 33 ticks · 9 abstract-overlay warns — the biggest packet of the homestretch fully reconciled) · id-check **0 errors** (the 3 uncoded pendings) · concept-check 0 · board **210 + 1 skipped** · gold-diff 0 · check-drift 0. Map stays `_working/P11/`. **Next: P12 — the attestation (4:9–12): "you are witnesses this day," the names pour back — MAHLON AT LAST — and the gate blesses the house like Rachel and Leah's.** 2026-06-11 |
| SC-0058 | **The register-rule relocation — vocabulary governance out of the maps, into its home** (Marcia's eye caught the sentence in the P11 draft; the evaluator classified the class and proposed the fix; the Architect's completing sweep proved the full extent — the family wears four wordings, so the first grep under-counted: **the CONSULTATIVE-rule sentence in 13 maps** (not 1), **the definitional gloss in 15**, **the root cause = the Agent-2 prompt's own §8 mandate** ("must explain… why CONSULTATIVE is not used"), **the template suggesting CONSULTATIVE as an example pericope default against the hard rule**, and **no methodology home anywhere** — the maps were the rules' only record. Marcia ruled **Option 1: the full proven family in one SC**, merging before P11). **The fix, the SC-0017 de-leak pattern:** (1) the rules get their home — `_methodology/register-usage-rules.md`: the narrator constant (cross-ref v0.16/SC-0046) · CONSULTATIVE-never-narrator · prayer-carries-the-address (SC-0048, kept in J05's map too as her ruled rationale — relocate-as-copy) · COMMUNITY_MEMORY-is-framing (SC-0001) · the boundary (maps = values + passage-specific reasons; rules = stated once, governed); (2) **27 cuts across 15 maps, each asserted unique** — 12 fixtures byte-paired with their vault copies (one line per file, both trees) + P08/P09/P10 compiler-side + **P11 trimmed on its open branch** (the gate merges under the corrected discipline, honoring her sequencing); every passage-specific statement KEPT (the captain's ELDER_AUTHORITY reason, J02's value-application to the sailors, P07's verse-specific framing line…); (3) the prompt fixed at the source — §8 restated (values + passage-specific reasons; **no vocabulary rules, no value-definitions**), §13's do-not-include gains the class, §14 check 3 reworded, §5 keeps the rule as drafter-knowledge with the home cross-referenced; (4) the template's CONSULTATIVE example corrected. **Zero-grep proven:** the gloss 0/15; the rule-sentence 0/13; the one surviving in-map CONSULTATIVE is J02's value-application (the keep-class). | **APPLIED (PR pair open: compiler + vault; Tier A by files-touched; her pass + merge word via `tools/merge_pr.py`; merge chain: this pair → #67 → #26 — #67 takes its mechanical ledger rebase after this lands).** Gates on the trimmed corpus: board **210 + 1 skipped** (the 21 graduation anchors assert VALUES, not prose — all pass untouched) · gold-diff **exit 0 at baseline** (the §1 prose is not compiled; nothing moved) · lint corpus 18 artifacts · 0 findings · check-drift 0 · the 12 vault pairs byte-identical by construction. **No schema change, no closed-list change, no pin move.** 2026-06-11 |

| SC-0059 | **P12 — the attestation (Ruth 4:9–12): mapped, ruled, applied (the names pour back; the first map born clean of the leak)** (Marcia ruled **Option 1, blessed as drafted**, 2026-06-11 — FORMAL_OFFICIAL on the declaration · CEREMONIAL on the people's blessing, the book's weightiest · the invoked matriarchs and ancestors as registered cast · the 10th signed-off exception (Chilion-and-Mahlon, ENTITY_PAIR) · the two lexeme-disciplined leave-outs). **The first map drafted under the SC-0058 corrected discipline — born clean of the register-rule class (0 gloss, 0 rule-sentence): the prompt fix proven at the source.** 2 scenes (the declaration 4:9–10 · the people's blessing 4:11–12) · 11 propositions · **zero minting: 11 CB + 12 FIG, all pre-declared.** **THE NAMES POUR BACK after the gate's namelessness: Elimelech, Chilion, and Mahlon spoken aloud — and MAHLON named as Ruth's husband at 4:10, closing the P01-D2 withheld pairing carried through six maps.** **Five cross-pericope pairs CLOSE on their declared sites:** FIG_0002 raise-the-name (P11 4:5 → 4:10, stated-then-enacted) · FIG_0110 living-and-dead (P07 2:20 → 4:10) · FIG_0005 the sandal's attestation (P11 → 4:9) · **FIG_0014 the divine name returns at the blessing after the legal silence** (P11's silence → 4:11's return) · FIG_0001 the-Moabitess arc (P01 1:4 → 4:10, its last firing). **Three OPEN toward the close:** FIG_0187 elders-and-women blessing pair (→ 4:14, P13) · FIG_0017 Ephrathah-Bethlehem doubling · FIG_0191 Tamar-Perez (→ the genealogy, P14). Cast all registered: the three dead by name (B2/B5/B4) · B9 "the wife of Mahlon" · B20+B21 the assembly · B10-YHWH returning · B22-Rachel-and-Leah · B23-Tamar · B30-Judah · B27 (Perez) · PL7/PL_ISRAEL/PL8/PL1. **The 10th lint exception (group D1):** "all that was Chilion **and** Mahlon's" (4:9) = the two dead brothers, the C3 ENTITY_PAIR class — the exceptions file's own "his two sons Mahlon and Chilion" precedent; kept verbatim, `lint-exceptions.json` 0.1.3 → **0.1.4** re-pinned. **Lexeme-disciplined leave-outs:** CB_0031-Ephrathite (4:11's אֶפְרָתָה is the place PL8 + FIG_0017, not the gentilic — the P11-CB_0040 discipline) · FIG_0168 + FIG_0007 (their P12 listings are loose — the ruin-word and the chronicle-frame are not in 4:9–12). One self-caught "verb" reword pre-gate; the proper-noun "Israel" anchored to PL_ISRAEL (coverage 49→50). | **APPLIED (PR open — compiler-only, no vault half; Tier A by files-touched; her pass + merge word via `tools/merge_pr.py`).** Gates: lint **0 findings · 1 accepted** (the ruled ENTITY_PAIR, surfaced ✓ ACCEPTED) · validate all judgment placeholders, **0 closed-axis** · coverage **50/50 explicit · 0 unanchored** (4 implied flagged · 18 ticks · 11 abstract-overlay warns) · id-check **0 errors** (1 "the dead" uncoded-by-design) · concept-check 0 · board **210 + 1 skipped** · gold-diff 0 · check-drift 0 @ lint-exceptions 0.1.4. Map stays `_working/P12/`. **Next: P13 — Obed (4:13–17): the conception YHWH gives, the women's blessing answering the men's, the child on Naomi's lap; then P14, and the book is closed.** 2026-06-11 |

| SC-0060 | **P13 — Obed (Ruth 4:13–17): mapped, ruled, applied (the book's reversal — empty to full, dead line to living name)** (Marcia ruled **Option 1, blessed as drafted**, 2026-06-11 — S1 plain · the women's blessing 4:14–15 **CEREMONIAL** (the female-civic counterpart to the men's gate-blessing) · the lap + naming 4:16–17 **INTIMATE** · the child carried by three uncoded surface forms · the two lexeme-disciplined leave-outs). **The second map born clean of the SC-0058 leak** (0 gloss, 0 rule-sentence). 3 scenes (marriage + gift + birth 4:13 · the women's blessing 4:14–15 · the lap + naming 4:16–17) · 12 propositions · **zero minting: 6 CB + 16 FIG, all pre-declared.** **THE BOOK'S TWO REVERSALS LAND:** the conception YHWH gives (4:13 — **the story's second and final direct divine act**, answering 1:6's famine-relief) and Ruth measured **better than seven sons** (the foreign widow past a full house). The redeemer-word lands on the child himself (4:14); the neighbor-women name him **Obed** for Naomi, closing their failure to place her at 1:19; the narrator reaches to **David**. **Five cross-pericope pairs CLOSE:** FIG_0187 (the women's blessing answers the men's, P12 4:11 → 4:14) · FIG_0142 (blessed-be-YHWH-redeemer, P09 → 4:14) · FIG_0181 (communal-naming-for-Naomi, P04 1:19 → 4:17 — grief to joy) · FIG_0016 (the women absent from the gate **return at the cradle**, P11 → here) · FIG_0014 (the divine name, returned at P12, sounds again in the women's). **Two OPEN toward the close:** FIG_0192 (the Obed–Jesse–David names repeat in the genealogy) + FIG_0189 (the book-closing inclusio with the prologue) → P14. Plus the climax locals (rapid-agent-shifts · cheq-lap · omenet-nurse · four-step-naming · name-famous-in-Israel · FIG_0007 the narrator naming David from a later vantage, as at 1:1). The child carried uncoded by son/redeemer/child = the id-check 3, one entity un-minted until "Obed." **Lexeme-disciplined leave-outs:** CB_0010 (the 4:14 lexeme is "in Israel" = PL_ISRAEL + FIG_0184, not "house of Israel") · CB_0004 (no Moabite lexeme in 4:13–17 — "your daughter-in-law who loves you"; FIG_0001 had its last firing at P12). Honest note disclosed: FIG_0014's registry span runs through P13, so the P12 ledger's "closes here at 4:11" parenthetical was a touch early (the return is P12; it sounds once more at 4:14) — framed, not relitigated. | **APPLIED (PR open — compiler-only, no vault half; Tier A by files-touched; her pass + merge word via `tools/merge_pr.py`).** Gates: lint **0 — born clean** · validate all judgment placeholders, **0 closed-axis** · coverage **33/33 explicit · 0 unanchored** (7 implied flagged · 20 ticks) · id-check **0 errors** (the 3 child-surface-form pendings) · concept-check 0 · board **210 + 1 skipped** · gold-diff 0 · check-drift 0. Map stays `_working/P13/`. **Next: P14 — the genealogy (4:18–22): GENEALOGY's first appearance from the closed 31 — the structural watch-point flagged at the division; the gates tell us honestly whether the machinery bends. Ten names, and Ruth closes.** 2026-06-11 |

| SC-0061 | **P14 — the genealogy (Ruth 4:18–22): mapped, ruled, applied — THE WATCH-POINT HOLDS; THE BOOK OF RUTH IS FULLY MAPPED** (Marcia ruled **Option 1, blessed as drafted**, 2026-06-11 — affirm the watch-point classification (GENEALOGY under NARRATIVE, the narrator's plain voice), the B27 cast collection, the four pair-closes, and the Salmah one-liner as the vault half). **GENEALOGY's first appearance from the closed 31 — and it VALIDATES CLOSED-AXIS CLEAN.** genre_group **NARRATIVE** · genre **GENEALOGY** · register **INFORMAL_CASUAL** holds under the v0.16 rule: the narrator recites the line in his plain chronicle voice (the same voice that opened the book at 1:1), the weight carried by the figures, not a register shift. **Unlike the psalm (a finding — no narrator, the const forced a false register), the genealogy IS narrator-recited, so the const FITS — a HOLD, not a finding; the closed lists end the book byte-unchanged from where they began.** The third map born clean of the SC-0058 leak (0 gloss, 0 rule-sentence). 1 scene (the *toledot*) · 10 propositions (the begettings) · **zero minting: 4 CB + 6 FIG, all pre-declared.** **Four cross-pericope pairs CLOSE at the book's very end:** FIG_0191 (Tamar-Perez, the elders' "house of Perez" P12 4:12 → 4:18) · FIG_0192 (the Obed–Jesse–David naming, P13 4:17 → 4:22) · FIG_0189 (the book-closing inclusio with the prologue, P13 → 4:22) · FIG_0194 (the long consequence of YHWH's gift of conception, P06 → 4:22). The genealogy-form figures FIG_0193 (the Perez-to-David lineage) + FIG_0190 (the "X fathered Y" formula) fire across the list. Cast: **B27-Genealogy-Figures** collects the six ancestors (Perez, Hezron, Ram, Amminadab, Nahshon, Salmon) + B13-Boaz (the 7th name — the redeemer now a link in the line he served) + B25-Obed (the hinge) + B26-Jesse-and-David (the arrival). **The vault half — the Salmah/Salmon dual-spelling fixed in the registry:** the text spells Boaz's father two ways (שַׂלְמָה 4:20 · שַׂלְמוֹן 4:21); B27's note recorded only Salmon, so coverage read 19/20. Added שַׂלְמָה to B27's vault BCD note (`bcd/beings/B27-Genealogy-Figures.md` hebrew + entry_verse); `ruth.aliases.json` **rebuilt with the canonical NER recipe, the diff EXACTLY the two B27 fields** (everything else byte-identical), re-pinned **aliases-0.1.3 → 0.1.4** → **coverage 20/20.** | **APPLIED (PR pair open: compiler + vault; Tier A by files-touched; her pass + merge word via `tools/merge_pr.py`, vault half `--require-merged`).** Gates: lint **0 — born clean** · validate 17 blocks **all judgment, 0 closed-axis** · coverage **20/20 explicit · 0 unanchored** · id-check **fully clean** · concept-check 0 · board **210 + 1 skipped** · gold-diff 0 · check-drift 0 @ ruth.aliases 0.1.4. Map stays `_working/P14/`. **THE BOOK OF RUTH IS FULLY MAPPED — 14 pericopes · 85/85 verses; with Jonah's 48, the seed corpus is two complete books.** 2026-06-11 |

| SC-0062 | **The Ruth-homestretch visibility batch — P08–P14 graduate; the whole book of Ruth is visible** (Marcia's commission 2026-06-11: run the SC-0055 visibility idiom, third time through, on the seven blessed-and-merged homestretch maps that still sat in `_working/` with `status: draft`). The seven maps move byte-identical from `_working/<id>/` → `fixtures/meaning-map/` with the declared **status draft → complete flip** (one line per file); the FM skeletons + COMPILATION-LOGs stay `_working/` until authored (the SC-0038 J01 Option-1 pattern, honestly labeled). **The batch-2 anchor (`tests/graduation-batch-2.test.ts`, 36 tests):** per pericope — parses + status complete · L1 closed lists HOLD (each pinned: P08–P13 NARRATIVE/HISTORICAL_NARRATIVE/INFORMAL_CASUAL, **P14 NARRATIVE/GENEALOGY/INFORMAL_CASUAL**) · the fresh skeleton's blocks are judgment-only with the gap signature PINNED (21·52·27·52·36·38·17) · the frozen packet reconciles (16/16·39/39·23/23·53/53·50/50·33/33·20/20, 0 unanchored ×7) · the flag codes registered — **plus a dedicated watch-point gate: P14's GENEALOGY-under-NARRATIVE-with-the-narrator-constant validates closed-axis clean, the "it holds" made permanent the way the psalm's finding is gated.** **Bite proven** (P14 register → CEREMONIAL fails the watch-point's 3 tests; a status → draft fails its anchor; both restore → 36/36). **The vault half (PR pair):** `pericopes/` gains P08–P14, each **byte-identical to its graduated fixture** (cmp-proven) — the vault now reads P01–P14, the whole book of Ruth, beside jonah/J01–J05. **The portal catches up on merge:** local build verified — **19 pericope pages from 31 approved artifacts** (was 12/24; the whole of Ruth + the whole of Jonah). | **APPLIED (PR pair open: compiler + vault; Tier A by files-touched; evaluator pass + Marcia's merge word via `tools/merge_pr.py`, vault half `--require-merged`).** Board **246 + 1 skipped** (210 + the 36 anchors) · gold-diff 0 (13 map-only skips, all visible) · lint corpus 25 artifacts · 0 findings · 10 accepted · check-drift 0 · check-drift:vault 0. **No schema change, no closed-list change, no pin move.** **THE SEED CORPUS IS VISIBLE: two complete books, 19 pericopes, in Obsidian and on the reviewers' portal.** 2026-06-11 |
| SC-0063 | **The FOR_MODEL drafter (Slice 4 phase 1) — built, calibrated, ruled on P08, batch-run: ALL 19 PERICOPES NOW CARRY FOR_MODELs** (Marcia approved the $25 ceiling 2026-06-12; phases A→D each on her word). **Phase A** — fresh pinned prompt `_spec/drafter/fm-drafter-prompt.md` (authored against spec v0.16; the vault agent-3 prompt is STALE — discipline language only, evaluator-verified), deterministic request assembly (spec digests + worked P01 pair + book registry; byte-stable sha), the PATCH-ONLY merge layer (gap report = sole write-authorization; granularity gap note-only; constrained beings-append), guarded Opus path (`claude-opus-4-8`; no key → locked; `.env` walk-up fallback). **Phase B** — P02 calibration vs gold (65%→61% strict-identity de-skewed; verdict: gold-parity measures style-vs-one-hand, diminishing returns); scorer de-skew + overrides normalization + the ORIGIN-AWARE mint audit (drafter vs map-borne; the round-1 presence misattribution corrected — the prose-suffixed values were MAP-borne, P02/P05 old-generation maps only). **Spend governance (her condition):** receipts on EVERY paid call incl. failures (`_working/DRAFTER-RECEIPTS.jsonl`, append-only; the truncated first call RECONSTRUCTED at | SC-0062 | **The Ruth-homestretch visibility batch — P08–P14 graduate; the whole book of Ruth is visible** (Marcia's commission 2026-06-11: run the SC-0055 visibility idiom, third time through, on the seven blessed-and-merged homestretch maps that still sat in `_working/` with `status: draft`). The seven maps move byte-identical from `_working/<id>/` → `fixtures/meaning-map/` with the declared **status draft → complete flip** (one line per file); the FM skeletons + COMPILATION-LOGs stay `_working/` until authored (the SC-0038 J01 Option-1 pattern, honestly labeled). **The batch-2 anchor (`tests/graduation-batch-2.test.ts`, 36 tests):** per pericope — parses + status complete · L1 closed lists HOLD (each pinned: P08–P13 NARRATIVE/HISTORICAL_NARRATIVE/INFORMAL_CASUAL, **P14 NARRATIVE/GENEALOGY/INFORMAL_CASUAL**) · the fresh skeleton's blocks are judgment-only with the gap signature PINNED (21·52·27·52·36·38·17) · the frozen packet reconciles (16/16·39/39·23/23·53/53·50/50·33/33·20/20, 0 unanchored ×7) · the flag codes registered — **plus a dedicated watch-point gate: P14's GENEALOGY-under-NARRATIVE-with-the-narrator-constant validates closed-axis clean, the "it holds" made permanent the way the psalm's finding is gated.** **Bite proven** (P14 register → CEREMONIAL fails the watch-point's 3 tests; a status → draft fails its anchor; both restore → 36/36). **The vault half (PR pair):** `pericopes/` gains P08–P14, each **byte-identical to its graduated fixture** (cmp-proven) — the vault now reads P01–P14, the whole book of Ruth, beside jonah/J01–J05. **The portal catches up on merge:** local build verified — **19 pericope pages from 31 approved artifacts** (was 12/24; the whole of Ruth + the whole of Jonah). | **APPLIED (PR pair open: compiler + vault; Tier A by files-touched; evaluator pass + Marcia's merge word via `tools/merge_pr.py`, vault half `--require-merged`).** Board **246 + 1 skipped** (210 + the 36 anchors) · gold-diff 0 (13 map-only skips, all visible) · lint corpus 25 artifacts · 0 findings · 10 accepted · check-drift 0 · check-drift:vault 0. **No schema change, no closed-list change, no pin move.** **THE SEED CORPUS IS VISIBLE: two complete books, 19 pericopes, in Obsidian and on the reviewers' portal.** 2026-06-11 |
| SC-0064 | **The batch ruling, Section A — Marcia's four map-side rulings applied; ALL 13 machine-drafted FOR_MODELs VALIDATE** (the follow-on to SC-0063 per her close-first word; ruled from the merged floor 2026-06-12). **A-4 (first ruled): THE DISCLOSURE RULE** — *bind when the referent set is fixed by the text at that point in the book's own disclosure (named in-verse, or an unambiguous set); where the text withholds, the FOR_MODEL withholds with it.* Applied: P07 2:20 hammetim → **B2** (set fixed by 1:3–1:5; gold-P02 idiom — the entry binds the head; the map-marked surface withholding preserved: HAMETIM_THE_DEAD + the merism slots; three-entry split flagged as her optional refinement) · P12 4:10 ha-met → **B4** (Mahlon named in-verse) · P11 4:5 ham-met **stays B?** (pre-disclosure — the model's own fill anticipated the rule). **A-1: P10 re-coded TM_NIGHT_TO_MORNING** (new `bcd/times` note; **O15 retired in place** — its recorded P08 span was a phantom; map 3D re-linked BOTH trees; **ruth.aliases rebuilt by the canonical recipe, diff EXACTLY the two entities, re-pinned 0.1.4→0.1.5**). **A-2: P11's FIG_0167 3C entry dropped** (the figure fires via figure_flags; frontmatter declaration kept). **A-3: P07 S1 gains the blessed none-marked Significant Absence wording** (her option-1 bless of the offered sentence, hers to amend; gaps 95→94, the draft's obsolete null-fill rejects by design). FMs regenerated from the edited maps + original fills, recorded amendments re-applied; **P10 + P11 UNBLOCKED → 13/13 FOR_MODELs validate**; anchor pins moved with the rulings (P10 27→24 · P11 52→49 — deltas exactly the removed tension cascades, SC-cited in the test); sheet Section A marked RULED; amendments/regen records in every touched run dir. | **SECTION A APPLIED (PR pair open: compiler + vault map/registry half; Tier A; evaluator standing-green conditional on heads 6dc17a0/cbdb1fb + this row only; Marcia's merge word pending → `tools/merge_pr.py`, vault half `--require-merged`).** Board **268 + 1 skipped** · gold-diff 0 · check-drift 0 @ aliases 0.1.5 · corpus lint 25 · 0 findings · 10 accepted · cmp-proven map pairs ×3. **§B–§E RULED + MERGED axis-by-axis (Marcia, 2026-06-12→19):** §B action+tone→enum v0.12 · proposition_kind→v0.16 · role_in_scene_being→v0.17 · scene_kind→v0.18 · **arc_element→v0.19 (2026-06-19; 56 of 58 promoted — 5 strip-to-type renames + 2 collapses to a survivor; PR [#81](https://github.com/MarciaSuzuki/tripod_compiler/pull/81) / [#37](https://github.com/shemaobt/ruth-pilot-b-wiki/pull/37)) — §B COMPLETE (all 5 axes)**; §C 5 registry codes (ruth.aliases 0.1.5→0.1.6) · §D TOOK+ASKED · §E 2 amendments confirmed. **THE CLOSE — part 1 (this PR pair): the 13 reviewer-ruled FOR_MODELs GRADUATE** `_working/` → `fixtures/for-model/` + vault `stas/` (the SC-0055/0062 visibility idiom applied to FOR_MODELs now that their judgment halves are ruled; the draft→valid frontmatter/title flip; new `tests/graduation-batch-3.test.ts` — 41 tests pinning status-valid + 0-drift + L1-holds incl. the J03-psalm & P14-genealogy watch-points, bite-proven; `validate.test` count 6→19; gold-diff re-baselined 6→19 entries, 0 skips). **APPLIED (PR pair open; Tier A; Marcia's merge word pending → `tools/merge_pr.py`, vault half `--require-merged`).** Board **333 + 1 skipped** · check-drift 0 · check-drift:vault 0 · lint 38 artifacts / 0 findings / 10 accepted · coverage 14/14 block-clean · 19/19 FOR_MODELs validate 0 drift · enum **v0.19** (arc_element 92). **THE CLOSE — part 2 (this PR pair): the COMPILATION-LOGs mechanized + graduated.** The 12 skeleton gap-reports → ruled logs (status skeleton→valid · sta_compilation_status SKELETON_DETERMINISTIC→MODEL_DRAFTED_REVIEWER_RULED · vocabulary_additions assembled from each pericope's per-axis ruling-logs · D2/D3 decisions + honest confidence/known-limitations added); P08's hand-built ruled log graduated as-is; the 12 `_working/` skeletons RETIRED. `fixtures/compilation-log/` + vault `stas/` 6→19; `validate.test` CL count 6→19; `graduation-batch-3` gains 13 CL anchors (status-valid + MODEL_DRAFTED_REVIEWER_RULED + schema-valid, bite-proven). **APPLIED (PR pair open; Tier A; Marcia's merge word pending → `tools/merge_pr.py`, vault half `--require-merged`).** Board **373 + 1 skipped** · check-drift 0 · check-drift:vault 0 · 19/19 FOR_MODELs + 19/19 COMPILATION-LOGs validate. **SC-0064 COMPLETE on merge** — the full machine-drafted seed corpus (19 pericopes, each FOR_MODEL + COMPILATION-LOG) is canonical, on the portal, with provenance; §A–§E + all five §B axes ruled, the artifacts graduated. (Noted follow-up: the CL `decision_id` schema pattern is Ruth-only `^P\\d{2}-D\\d+$` and complog.ts emits a `P`-prefix for Jonah skeletons — cosmetic; a small governed widen if Jonah decision_ids should read `Jnn`.) 2026-06-12→19 |
.137, sha-identical input + 32k cap output); the $25 ceiling enforced PRE-DIAL ($2 reserve); pricing single-sourced. **Phase C (P08 pilot, $0.941)** — 50/50 gaps, 0 rejected, 13 declared mints 0 undeclared, all four gates green; **Marcia ruled bless-with-amendments: THE ACTION-IDIOM RULING** (commanded-not-yet-performed steps = `action: DIRECTED` + `commanded_step` content; imperatives NEVER enter the past-tense action axis — 7 of 13 mints dissolved), P1/P9 speech acts as drafted, **6 mints PROMOTED → approved-enumerations v0.11** (first machine-drafted promotion; `promote --sc` now MANDATORY after the SC-0006-default stamp had to be hand-fixed; sc_refs corrected, log line repo-relative). Ruled FM at `_working/P08/` status:valid; log MODEL_DRAFTED_REVIEWER_RULED. **Phase D (the batch, 12 pericopes)** — TWO SEAM FIXES mid-flight (code-less being_id → gap → `B?` withheld idiom; code-less time_id → gap → TM_ proposal; P07+P09 redrafted); prompt 0.1.3→0.1.5 (idiom ruling baked in · B?/TM_ taught). **10/12 VALID** (P07 `B?` + J01 key-placement = mechanical conformance amendments, each RECORDED in its run dir's `amendments.json` + flagged in the ruling sheet §E); **2 INVALID by MAP↔schema tension surfaced by first FM compilation: P10 (O15 object-code under 3D Times) · P11 (FIG_0167 under 3C Objects)** — tabled. Lint 12/12 · coverage 100% explicit 0 unanchored ×12 · id-check clean modulo registry PROPOSALS + `B?` INFOs. **CLOSED LISTS: 0 violations across all 15 paid calls; J03 the psalm drafted POETIC_SUNG/PRAYER/RELIGIOUS_WORSHIP by the model itself (the v0.16 rule generalized).** **THE BATCH-RULING SHEET prepared** (`docs/SC-0063-BATCH-RULING-SHEET.md`: 196 checkboxes — 4 map-side items · 189 mint values w/ justifications · 5 registry proposals · 2 audit-caught undeclared (TOOK, ASKED) · 2 amendments to confirm) — **ruled under the follow-on SC from the merged floor (her word: close first, rule next).** | **APPLIED (PR pair OPEN: compiler [#73](https://github.com/MarciaSuzuki/tripod_compiler/pull/73) · vault registry-v0.11 half [#30](https://github.com/MarciaSuzuki/ruth-pilot-b-wiki/pull/30); Tier A; evaluator pass + Marcia's merge word via `tools/merge_pr.py`, vault half `--require-merged`).** Board **268 + 1 skipped** (246 + 22 drafter/calibration/receipts) · check-drift 0 @ enumerations v0.11 + fm-drafter-0.1.5 · gold-diff 0 · lint corpus 25 · 0 findings · 10 accepted. **Spend: $21.776 of $25 · 18 receipts (17 ok · 1 truncated).** Drafted FMs stay `_working/` status:draft (P08 status:valid, ruled) pending the batch ruling. 2026-06-12 |
| SC-0065 | **The ORAL PROFILE — `TRIPOD_STA_GENERAL_v0_1` admitted; the gold Cardume oral STA validates under the same machinery; biblical byte-identical** (the validation slice; Marcia's go-ahead 2026-06-17). **OPENS BY SUPERSEDING, on her word (Option A, 2026-06-17), two locked decisions — for the oral profile ONLY, before any schema work: Decision C** (artifact_profile FORBIDDEN) → an oral artifact now carries `artifact_profile: "LA_RECORDING"` on its header; biblical stays implicit-by-context (the forbid is RETAINED on the biblical branch, so biblical is byte-identical). **Decision D** (do NOT build the LA_RECORDING profile) → the oral pipeline is now an active stream; this builds the LA_RECORDING profile as a governed derivation of the biblical master. **Spec:** `validation-rules.json` v0.16→v0.17 — a top-level `if/then/else` on `source_domain` routes biblical (no `source_domain`) to `$defs/biblical_for_model` (the pre-SC-0065 body, verbatim) and oral (`source_domain=oral_archive`) to `$defs/oral_for_model` (admits the oral header w/ `artifact_profile=LA_RECORDING`, `provenance`/`speaker_role=LISTENER_NOT_STORYTELLER`, `bead_span` on header/scene/proposition, `level_1_prose`, the embedded cb/figure/compilation surfaces; reuses pericope_classification + upper_token + scene/prop ids + the register/genre/closed lists — one controlled vocabulary). Oral `sta_id` pattern `^[a-z]+_passage_\d{2}_v\d+_\d+$`; oral tagset const `TRIPOD_STA_GENERAL_v0_1`. **No closed-list change; no change to any biblical `$def`.** **Engine:** `structuralFindings` drops the ajv if/then/else wrapper error (biblical findings byte-identical); `reader/obsidian.ts` accepts a raw `.json` (oral has no Obsidian envelope); `detectArtifact` recognises an oral STA by signature; new `src/engine/oral.ts` **bead-span integrity** pass (tiling + nesting — the audio analogue of scene/verse coverage, invariant 4) runs for oral only. **The 14 oral NEW_VALUEs surface as DRIFT for ruling — not coined** (Stage 2: arc/tone/scene_kind/proposition_kind/role/presence/action). Fixture `fixtures/oral/cardume-P01-sta.json` + `tests/oral-cardume.test.ts` (9). | **APPLIED (PR [#77](https://github.com/MarciaSuzuki/tripod_compiler/pull/77) open — compiler-only, no vault half; Tier A; awaiting evaluator pass + Marcia's merge word via `tools/merge_pr.py`).** Board **277 + 1 skipped** (268 biblical byte-identical + 9 oral) · Cardume `tripod validate` **0 block · 14 drift · 2 info** · check-drift **0** @ validation-rules v0.17 (sha 34a7db46…). **DEFERRED to a follow-on SC: the 3 sibling schemas (compilation-log/bcd-delta/verification-input) for downstream oral compilation · the Compiler's first git tag (the field-app pin target) · the field-app seam-doc path reconciliation · Stage-2 vocabulary rulings.** 2026-06-17 |
| SC-0066 | **The oral source Prose Meaning Map committed beside its STA** (`fixtures/oral/cardume-P01-meaning-map.md` joins `cardume-P01-sta.json`; Marcia's word 2026-06-17, on the SC-0065 merged floor). The `.md` map is the human-readable SOURCE the STA compiles from — the STA's `compilation_log.compiled_from` names it. **No oral map reader exists yet** (a fuller map↔STA derivation-consistency check is future Evaluator work); this SC keeps the pairing **load-bearing** via 2 tests in `tests/oral-cardume.test.ts`: the map is committed beside the STA, and the STA's `compiled_from` names the map — so the map can't be silently dropped/renamed nor the STA lose its provenance. **No schema change, no closed-list change, no pin move.** | **APPLIED (PR [#78](https://github.com/MarciaSuzuki/tripod_compiler/pull/78) open — compiler-only, no vault half; Tier A; full suite + check-drift green; Marcia's merge word via `tools/merge_pr.py`).** Board **279 + 1 skipped** (277 + 2 pairing) · check-drift **0** · no spec/pin/closed-list change. 2026-06-17 |
| SC-0067 | **The referential-form bare-form strip (R1) — surface-form slots carry the bare referring expression; analytic tails stripped** (Marcia's R1, ruled 2026-06-20; the sentence-token triage / seed-freeze gate — [[tripod-sentence-token-triage]] bin 2). **Scope:** the `referential_form` family (3 sub-families — `referential_form` 81 · `<participant>_referential_form` 43 · `referential_form_at_verse` 12 = **136 occ**) **+ the (a) surface-address family** (`address_form` + kin, **24 occ**) per Marcia's **Option-A scope extension** (the sibling `*_form` landscape = 56 occ / 31 keys, bucketed (a) 24 / (b) 22 / (c) 10). Strips the analytic annotations (`_NARRATOR_EPITHET`, `KINSHIP_FORM`, `_RELATIONAL`, `_*_OCCURRENCE`, `_THIRD_PARTY_EPITHET`, `_INTIMATE`/`_DEFERENTIAL` register tags, `_SPELLING_AT_*` positional tails, `WITHHELD_PER_*`, …) to the bare surface form; the stripped content's home is the structural code (B-code / `B?` / `invoked_divine_agent`) or a note (`significant_absence` / CL prose). **31 strip-values + 1 drop** (`SELF_REFERENCE_REFUSING_OWN_NAME` slot — redundant with `action:REFUSED`+`refused_name`) across 17 FOR_MODELs; coupled CL `referential_forms[]` echoes + SHADDAI dedup + 2 divergent variants (`HUSBAND_OF_NAOMI_FRAMING`→`HUSBAND_OF_NAOMI`, `UNNAMED_DIVINE_AGENT_…`→`UNNAMED_DEITY`, the latter also shedding the SC-0014 "AGENT" jargon) + CL prose citations all reconciled (P02–P06 CLs). **3 HELD** (`ELOHIM_TO_YHWH_SHIFT` transition; `NAMED_THEN_ADDRESSED` + `ALTERNATES_MOTHER_IN_LAW_AND_NAOMI` patterns → Phase 3). **Blessed devices untouched:** P01-D2 wife-pairing withholding (`B?` + significant_absence + lint-exception); `UNNAMED_*`/`STRIPPED_TO_HA_ISHAH`/`*_PRONOMINAL` kept; SALMAH/SALMON dual spelling preserved. **No schema change, no closed-list change, no enum/pin move** — `referential_form` is a descriptive axis (never promoted). (b) speech-content `*_form` → Phase 2 (R2); (c) structural `*_form` → Phase 3. Instrument `docs/SC-0067-REFERENTIAL-FORM-BARE-FORM-SHEET.md` (RULED) + apply record `docs/SC-0067-APPLY-RECORD.md`. | **MERGED 2026-06-20 — compiler `142fa9f` (PR #85) · vault shemaobt#40 `6d2f067`; both branches deleted, vault on clean main; post-merge re-verified (Tier A).** Board **373 + 1 skipped** (unchanged — descriptive-axis change) · validate 19/19 FOR_MODELs + 19/19 COMPILATION-LOGs · 0 block · 0 drift · check-drift 0 · check-drift:vault 0 · lint 38 / 0 findings / 10 accepted · coverage 14/14 · gold-diff 100% / 0 divergent · id-check 19 clean · **0 in-scope leaks (re-grep proven)** · vault 23 files cmp byte-identical. 2026-06-20 |
| SC-0068 | **Retire the committed coverage ledgers — they're a generated VIEW that rotted** (Marcia's ruling 2026-06-20, the durable fix for the SC-0067 application-verify finding). The six `fixtures/coverage/*-COVERAGE-LEDGER.md` were stale since **2026-05-30 (SC-0010)** — `TH_→CB_` (SC-0037), a `B9→B8` entity reassignment, drifted unanchored counts — and **nothing caught it (no test pinned them; the coverage gate regenerates fresh).** Proven independent of SC-0067 (floor-regen ≡ branch-regen byte-identical, all six → 0% SC-0067). **Fix (retire, not one-time refresh):** `git rm` the six ledgers · git-ignore `*-COVERAGE-LEDGER.md` (rot-proof — a generated ledger can no longer be committed anywhere) · `fixtures/coverage/README.md` documents the convention. **Nothing is lost:** the substance stays gated **live** by `tests/coverage-corpus.test.ts` (reconciles every P01–P06 FOR_MODEL → 0 unanchored + block-clean) and `tripod coverage --corpus`; the rendered note is regenerated on demand (`coverage --out-dir`, which `mkdir`s the dir). Compiler-only (coverage ledgers were never vaulted). **No schema/closed-list/enum/pin change.** | **APPLIED (PR open — compiler-only; Tier A; full suite + check-drift green; Marcia's merge word via `tools/merge_pr.py`).** Board **373 + 1 skipped** (unchanged — no test read the deleted files) · coverage gate live-green (P01–P06 block-clean) · check-drift 0. 2026-06-20 |
| SC-0069 | **The speech-content strip (R2) — speech/content slots carry only `speech_act` + structure; the words of speech are dropped (they live in the Meaning Map)** (Marcia's R2, ruled 2026-06-20 via the calibration widget; the sentence-token triage / seed-freeze gate Phase 2 — [[tripod-sentence-token-triage]]). **Enumerated by value-shape** across all 19 FOR_MODELs (436 speech-context slot-values / 211 keys; §K complete inventory — speech is the default bucket, nothing slips). **Rulings:** §A speech-content (92 keys) + §B (b)-forms (`question_form` words / `witness_summons_form` / `response_form` / `decree_authority_form`) + §F-leave (analytic labels) **APPROVED**; **§C** command/instruction content (`commanded_step` ×34 + kin) **DROP as speech**; **§D** load-bearing formulas (creed / confession / `AS_YHWH_LIVES` / `BARUKH_YHWH` / `SALVATION_BELONGS_TO_YHWH`) **DROP with Marcia's explicit sign-off**; **§E the P03 vow rungs KEPT as structure → Phase 3** (Marcia's conservative pole); **`agent_named` explicit KEEP** (Evaluator condition — NONE/B-code structural; `NONE` load-bearing = significant-absence of a named agent). **Per-value splits:** `question_form` keeps the 3 `RHETORICAL_*`/`DOUBLED_*` metas; `question_subject` drops the 6 question-words, keeps the 7 analytic topics. **202 value-drops across 17 FOR_MODELs** (187 scalar + 15 array-elements in 5 all-dropped arrays: `divine_descriptors`/`creed_attributes`/`hoped_divine_turning`/`jonah_non_involvement`/`not_cut_off_from`; P04/P14 untouched); per-value count-assert + re-grep every surface; content-only diff (6 ins / 218 del). **SAFETY (Evaluator draft-verify):** the realized NOT_TO_BE_NORMALIZED is the FM **`preserve_form` flag** (SC-0030 two-axis fidelity — `SPEC_CHANGES` SC-0030 says it supersedes the README note), NOT the CL keep-image notes; the only **2 `preserve_form=true`** (P06 P11 `response_components`, Ruth 2:13) ride on the **`action` axis which R2 excludes → corpus SAFE**; map-presence 100% on the genuine-speech sample (the words live in the map, often with Hebrew). **R2 CL touch is lighter than R1** — the speech lives in CL keep-image PROSE which STAYS as the protection (no structured array to reconcile). **No schema change, no closed-list change, no enum/pin move** — the speech-content slots are descriptive/free-form (`event_specific_slots` additionalProperties). Instrument `docs/SC-0069-R2-SPEECH-CONTENT-STRIP-SHEET.md` (RULED + Evaluator-corrected) + apply record `docs/SC-0069-APPLY-RECORD.md`. **Phase 3** (the (c) structural `*_form` + the 3 held R1 values + action-axis decompositions) + **Phase 4** (the value-shape guard, reads `preserve_form`) remain. | **APPLIED (branch — compiler half; Tier A; full suite + all gates green; Evaluator application-verify + Marcia's merge word via `tools/merge_pr.py`, vault half `--require-merged`; vault cp pending).** Board **373 + 1 skipped** (unchanged — descriptive change) · validate 19/19 · 0 block · 0 drift · check-drift 0 · lint 38 / 0 findings / 10 accepted · coverage 14/14 · gold-diff 100% / 0 divergent · id-check 19 clean. 2026-06-20 |
| SC-0070 | **The slot-NAME role-vocabulary rename + the slot-name lint (make-impossible)** (Marcia's catch during the SC-0069 R2 verify, ruled 2026-06-20; the seed-freeze gate — [[tripod-slotname-role-vocab-leak]]). FM `event_specific_slots` **key names** leaked the project's own banned grammatical/thematic-role vocabulary (`agent_named`, `invoked_divine_agent`, `blessing_recipient`, `beneficiary`, …) **with lint GREEN** because `lint.ts` R4 ran prose-only, never slot-names. **RENAMED 55 keys / 99 key-occurrences across 18 FOR_MODELs** (P14 untouched) to event-participant / non-role-theory names: Tier-1 banned (`invoked_divine_agent`→`invoked_deity`, `agent_named`→`named_doer`, `blessing_recipient`→`blessed_party`, `beneficiary`→`born_for`, the recipient family), Tier-2 role-flavored (`question_subject`→`question_about`, `argument_role`→`comparison_rank`, `*_source`→`*_from`, `*_owner`→`*_belongs_to`, the subject/object/locus/owner/antecedent families), and the `referent` family (drop the suffix — `bound_to_referent`→`bound_to`, `clan_referent`→`clan`). **KEPT** `speaker` + `addressee`/`addressees` (Marcia — fundamental communicative-event roles). **Scope reconciled 58→55:** Marcia ruled 58 keys/108 occ PRE-R2; R2 (SC-0069) incidentally removed 3 (`inheritance_locus`/`report_subject`/`violence_locus`, all §A speech-content) + trimmed `question_subject` 13→7. **DURABLE FIX (make-impossible):** new `slot_name_role_vocab` lint rule (`src/engine/lint.ts`) + `lint-lexicon.json` **0.2.0→0.3.0** (`forbidden_slot_name_tokens` = {agent,recipient,beneficiary,subject,object,source,locus,owner,argument,antecedent,referent} + `slot_name_allow`); walks ess keys **token-wise** (so `referential_form` is NOT false-flagged by the `referent` prefix), tier-1 BLOCK, allow-lists speaker/addressee, **scoped to event_specific_slots ONLY** (never the L2 `objects_in_scene`/`object_id` namespace); **bite-proven both ways** (`tests/slot-name-lint.test.ts`, 7 tests — planted leak blocks; renamed corpus / keep-list / referential_form / L2 namespace stay silent). Drafter-prompt self-contradiction fixed (§4 ban vs line-138 endorse → reconciled to ruled names; `fm-drafter-0.1.5`→`0.1.6`, re-pinned). Mechanism: key-anchored token-rename (formatting preserved) + per-key count-assert + 0-banned-token sweep. Instrument `docs/SC-0070-SLOT-NAME-RENAME-SHEET.md` (RULED) + `docs/SC-0070-APPLY-RECORD.md`. | **APPLIED (branch — compiler + vault FM pair; Tier A; Evaluator application-verify + Marcia's merge word via `tools/merge_pr.py`, vault half `--require-merged`; vault = the 18 FMs only, `_spec` is compiler-vendored).** Board **380 + 1 skipped** (373 + 7 slot-name bite-tests) · validate 19/19 · 0 block · 0 drift · check-drift 0 @ lint-lexicon 0.3.0 + fm-drafter-0.1.6 · lint 38 / **0 `slot_name_role_vocab`** / 10 accepted · coverage 14/14 · gold-diff 100% / 0 divergent · id-check 19 clean · **0 banned slot-names repo-wide**. 2026-06-20 |
| SC-0071 | **Phase 3 (the triage residue) — structural-form keeps, redundant-form drops, action-axis decompositions, the 3 held values** (the close of the sentence-token-triage content cleanup — [[tripod-sentence-token-triage]]; ruled by Marcia 2026-06-20 after the Evaluator's draft-verify). **11 changes across 8 FOR_MODELs + 1 COMPILATION-LOG:** §C2 **5 action-axis decompositions to already-governed verbs** (`ASCRIBED_COURTROOM_TESTIMONY_TO_YHWH`→`ASCRIBED` · `STATED_SELF_AS_FOREIGNER` / `STATED_THAT_HE_COMFORTED_HER`→`STATED` · 2×`WISHED_*`→`BLESSED`; **no new verbs minted** — all in the 31-verb SC-0025 seed); §A2 **3 redundant prose-form drops** (`movement_form` / `tie_form` / `resulting_relation_form`, each duplicating already-structured slots — `descent_components`, `named_tie`, `taker`/`wife_taken`); §B **the 3 held R1 values resolved to the form** (`NAMED_THEN_ADDRESSED`→`YHWH` J03 · `ALTERNATES_MOTHER_IN_LAW_AND_NAOMI`→`NAOMI` P07 · `ELOHIM_TO_YHWH_SHIFT`→`YHWH` J05). **The J05 divine-name call (Marcia's §B ruling, after the Evaluator verified the premise):** resolve the slot AND record the deliberate Elohim→YHWH alternation as an explicit `NAMING_SHIFT` entry in the J05 CL `high_risk_register_audit` (completing R1's deferred naming-shift placeholder) — the load-bearing feature stays explicit, not left to re-inference; the per-moment names remain in the appointer/questioner slots. **KEPT:** the structural forms (`vow_structural_form`/`listing_order_form`/`report_variation_form`/`duration_form` + the R2 §E vow forms), the already-governed short compounds (`TOOK_AS_WIFE`/`SAT_ON_ASHES`/`DREW_OFF_SANDAL`/`AROSE_TO_RETURN`), and **the two `preserve_form=true` Ruth 2:13 clauses verbatim** (`STATED_THAT_HE_SPOKE_TO_HEART_OF_HIS_SHIFCHAH` / `STATED_SHE_IS_NOT_AS_ONE_OF_HIS_SHIFCHOT`). **Quarantine:** the 5 decomposed values removed from `quarantined-vocabulary.json` (**0.2.0→0.3.0**; action-held 7→2 — the 2 kept = the preserve_form pair), re-pinned; `tests/drift.test.ts` re-baselined (watch 15→10, action-held 7→2). Per-value count-assert; **no `approved-enumerations` change**. Instrument `docs/SC-0071-PHASE-3-RESIDUE-SHEET.md` (RULED) + `docs/SC-0071-APPLY-RECORD.md`. **Phase 4** (the automatable value-shape guard) is the final close of the triage. | **APPLIED (branch — compiler + vault pair; Tier A; Evaluator application-verify + Marcia's merge word via `tools/merge_pr.py`, vault half `--require-merged`; vault = the 8 FMs + the J05 CL + the `quarantined-vocabulary.json` sync).** Board **380 + 1 skipped** (drift assertions re-baselined; net 0) · validate 38/38 · 0 block · 0 drift · check-drift 0 @ quarantined-vocabulary 0.3.0 · lint 38 / 0 findings / 10 accepted · coverage 14/14 · gold-diff unchanged vs baseline (15 at 100% + the 4 standing MM-coverage divergences) · id-check 19 clean. 2026-06-20 |
| SC-0072 | **Phase 4 CONTENT cleanup — the residual prose-shaped slot VALUES** (the precursor to the Phase-4 value-shape guard; ruled by Marcia 2026-06-26 — the 22 clear ≥5-token clauses + the 28 four-token borderline = 50 distinct values / 51 occ — [[tripod-sentence-token-triage]] · [[tripod-phase4-census-finding]]). **47 edits across 15 FOR_MODELs** (P04/P08/P09/J03 untouched): **22 DROP** (meaning preserved in a sibling slot / `significant_absence` / the Meaning Map — `elder_designation`, `identified_as`, `naming_order`, `narrator_interior_access_scope`, `name_known_status`×2, `hesed_from`, `dissuasion_target`×2, `question_about`×4, `quoted_prior_action`, `epithet_voice_layer`, `oath_conditional_structural_force`, `blessing_content_kind`×4, `moab_naming_status`, `divine_act_marker`); **18 SHORTEN** (prose tail → compact token: `BEHIND_HARVESTERS`, `MARGELOT`, `BEFORE_RECOGNITION`, `DAWN_NEXT_DAY`, `GOING_DOWN`, `NEAR_BREAKING`, `SHIP_HOLD`, `ROWED_HARD`, `FEARED`, `TURNED_FROM_EVIL`, `DISASTER`, `SEE_CITY_FATE`, `WIDOW`, `WENT_UP`, `ON_BOSOM`, `LINE_TERMINUS`, `NOT_EXECUTED`, `CONFIRMATION`); **7 DECOMPOSE** to governed event-beat verbs (P11 `custom_act`→`DREW_OFF_SANDAL`+`GAVE` & collapse the dup `attestation_form: SANDAL_DRAWN_OFF`; P07 `brought_out_first`→`BROUGHT_OUT`+`GAVE`; P11 `compliance`→`TURNED_ASIDE`+`SAT`; J02 `subsequent_response`→`CRIED`; J05 `post_build_position`→`SAT`; J02 `basis_of_knowledge`→`TOLD`+teller; J02 `reason`→ the compact token `SEA_GROWING_STORMIER`, **not** a verb). **The 40 drops/shortens change NO vocabulary.** **The decomposes PROMOTE 6 NEW `action` verbs** (`GAVE`, `BROUGHT_OUT`, `TURNED_ASIDE`, `SAT`, `CRIED`, `TOLD`; `DREW_OFF_SANDAL` was already approved) → `approved-enumerations.json` **v0.19→v0.20** (action 39→45, sc_ref SC-0072), re-pinned (sha `d3069ba6…`) + `VOCABULARY_LOG.md` line; the drift seed is pinned by provenance (`sc_ref===SC-0025`===31) so **no test re-baseline**. **4 KEEPS allow-listed for the guard, not edited** (`ACCORDING_TO_WORD_OF_YHWH`, `ET_HACHAYIM_VEET_HAMETIM`, `IDENTITY_RECOGNITION_AND_STANDING`, `THE_ATTESTATION_IN_ISRAEL`). **The 2 `preserve_form=true` Ruth 2:13 clauses + the `significant_absence` anchors (P03·S3/P06·S1/P06·S2/P07·S3) verified untouched.** Method: structural editing (10 round-trip FMs) + surgical comma-aware line-removal (5 hand-formatted FMs); per-value count-assert + grep every surface (all 47 old values 0 in `fixtures/for-model/`). Instrument `docs/SC-0072-APPLY-RECORD.md`. **Phase 4** (the value-shape guard) is the final close of the triage. | **APPLIED (branch — compiler + vault pair; Tier A; Evaluator application-verify + Marcia's merge word via `tools/merge_pr.py`, vault half `--require-merged`; vault = the 15 FMs + `approved-enumerations.json` v0.20 sync; `pins.json`/`VOCABULARY_LOG.md` compiler-only).** Board **380 + 1 skipped** · validate 19/19 · 0 block · 0 drift · check-drift 0 + **check-drift:vault 0** @ approved-enumerations v0.20 · lint 38 / 0 findings / 10 accepted · coverage 14/14 · gold-diff unchanged vs floor · id-check 19 clean · vault 15/15 FMs + enum byte-identical. 2026-06-26 |
| SC-0073 | **Phase 4 — the value-SHAPE prose guard (make-impossible; the WHOLE close of the sentence-token triage)** (ruled by Marcia 2026-06-26 after the Architect calibration sheet + Evaluator draft-verify — [[tripod-phase4-guard-handoff]] · [[tripod-sentence-token-triage]]). The VALUE analogue of SC-0070 (which guarded slot NAMES): a new `value_shape_prose` lint rule (`src/engine/lint.ts`) that flags a sentence-shaped UPPER_SNAKE VALUE in `event_specific_slots`, so the now-clean seed (post-SC-0072) can't drift back into prose. **Detector = membership-exempt FIRST, then a ≥4-token shape test on the remainder:** E1 approved-enumerations (any axis) · E2 validation-rules closed_lists (genre/register/speech_act/…) · E3 the B/PL/O/TM/TH/CB/FIG/I/D code namespaces · E4 the referential-form family (slot KEYS ending `referential_form`/`referential_form_at_verse` + the R1 address slots `address_form`/`divine_address_form`/`blessed_party_form`/`role_form`/`line_form`) · E5 the 17 kept-structural-form slot KEYS (`vow_structural_form`/`listing_order_form`/`duration_form`/… ) · E6 the 4 ruled KEEPs **by value** (`ACCORDING_TO_WORD_OF_YHWH`/`ET_HACHAYIM_VEET_HAMETIM`/`IDENTITY_RECOGNITION_AND_STANDING`/`THE_ATTESTATION_IN_ISRAEL` — 3 sit in non-kept-form slots, so value-based) · E7 the 2 Ruth 2:13 `preserve_form=true` clauses via `quarantined-vocabulary` membership. **E1/E2/E3/E7 derive LIVE from the pinned spec files (not re-listed) so the allow-list can't drift from canon; only the guard-specific lists (threshold + E4 address slots + E5 kept-form slots + E6 keeps) live in `lint-lexicon.json`.** **Calibration (census regenerated on `9f7f64c`):** of 72 unique ≥4-token ess values ALL are exempt (UNCOVERED=[]) → corpus = **0 findings**; the ~92 kept compact values are all 2–3 tokens (49+43), below the line. tier-1 **BLOCK** (Marcia ruled BLOCK · ≥4-token line, no verb-shape · accept the 9 gloss-y 3-token borderlines · allow-list in the lexicon). **DURABLE FIX:** `lint-lexicon.json` **0.3.0→0.4.0** (re-pinned, sha `4a7ecddd…`) + the rule; **bite-proven both ways** (`tests/value-shape-lint.test.ts`, 11 tests — planted clause/4-token boundary/nested-component BLOCK; the 4 keeps / 2 preserve clauses / long speech_act / referential / kept-form / code+action-verb SILENT; whole-corpus 0; + the preserve_form-⊆-exempt anti-drift invariant). **Compiler-only — NO vault half** (`lint-lexicon.json` is compiler-vendored, not vaulted; corpus already clean → 0 FM edits). Instrument `docs/SC-0073-VALUE-SHAPE-GUARD-SHEET.md` (RULED) + `docs/SC-0073-APPLY-RECORD.md`. **After merge the sentence-token triage is WHOLLY CLOSED.** | **APPLIED (branch — compiler-only; Tier A; Evaluator application-verify + Marcia's merge word via `tools/merge_pr.py` pending).** Board **391 + 1 skipped** (380 + 11 value-shape bite-tests) · validate 19/19 · 0 block · 0 drift · check-drift 0 @ lint-lexicon 0.4.0 · lint 38 / **0 `value_shape_prose`** / 10 accepted · coverage 14/14 · gold-diff unchanged vs floor · id-check 19 clean. 2026-06-26 |
| SC-0074 | **Esther onboarding — Phase 1: SOURCE (the frozen BHSA packets)** (deliberate seed-expansion; division ruled by Marcia 2026-06-26, Evaluator-verified — [[tripod-esther-onboarding]] · book-onboarding checklist). Adds the book of **Esther (prefix `E`, E01–E18)** to the source layer: `extractor/pericopes.esther.json` (the pericope→Hebrew-verse-range table, mirroring `pericopes.json`/`pericopes.jonah.json`) + the 18 frozen, pinned source-linguistic packets `_spec/source/esther/E01.json … E18.json`, extracted OFFLINE by `extractor/extract_bhsa.py` from local BHSA tf/2021 (`extractor-0.1.0/bhsa-2021`), pinned by sha256 in `_spec/pins.json`. **BHSA book name verified = `Esther`** against the tf/2021 `book` feature (10 ch / 167 verses; chapter totals 22/23/15/17/14/14/10/17/32/3); **Esther's Hebrew & English versification COINCIDE** (no Jonah-style fish offset), so `start/end` == English bcv. **Division (18 pericopes, tiles 1:1–10:3, no gaps/overlaps, max unit 14v):** E01 1:1–9 · E02 1:10–22 · E03 2:1–4 · E04 2:5–18 · E05 2:19–23 · E06 3:1–6 · E07 3:7–15 · E08 4:1–8 · E09 4:9–17 · E10 5:1–14 · E11 6:1–14 · E12 7:1–10 · E13 8:1–8 · E14 8:9–17 · E15 9:1–10 · E16 9:11–19 · E17 9:20–32 · E18 10:1–3 (new seams 1:9│10, 2:4│5, 4:8│9). Mirror pairs recorded for the maps (cross-pericope figures/cross_ref): decree E07↔counter-decree E14 · Haman rise E06↔fall E12 · reversal fulcrum E11. **Recipe note:** extracted WITHOUT `--places-ner` (matching the Jonah precedent → `place_aliases:{}`) — the local NER sheet is un-pinned and injects only book-irrelevant Beth-* places, a reproducibility risk; omitting it keeps every packet rebuildable from repo + BHSA alone. **NO closed/bounded-list edit, NO enum bump, NO vault half** (source packets are compiler-vendored, not vaulted; this is pure source-layer ingestion — the `validation-rules.json` version is unchanged). Phase 2 (vault BCD + `_spec/registry/esther.aliases.json`) is the next SC; mapping/compile (Phases 3–4) follow. | **APPLIED (branch `sc-0074-esther-source` — compiler-only; Tier A; Evaluator phase-verify GREEN + Marcia's merge word 2026-06-26).** Acceptance ALL green: `check-drift` exit 0 — all **18** `source/esther/*.json` `vendored:ok`; 18 packets = 18 pericopes; per-packet verse counts == targets (9/13/4/14/5/6/9/8/9/14/14/10/8/9/10/9/13/3); `place_aliases` empty on all 18; board **391 + 1 skipped** (unchanged); pins.json valid JSON. 2026-06-26 |
| SC-0075 | **The note-`type` envelope guard + the 13 mislabeled COMPILATION-LOG envelopes (unblock the Review Portal)** (Evaluator-diagnosed 2026-06-26 — [[tripod-portal-builder-kickoff]]). The Review Portal auto-deploy had **failed silently on every merge since June 20** (live site frozen at `626b8b7`, showing 6/19 STA): the `review-portal` workflow runs `npm test` → `portal/test/smoke-real-tree.test.mjs`, whose build hit the portal **approved-only gate** (`portal/src/lib/gate.mjs`, exit 2). Gate reason: **13 SC-0064-mechanized compilation-logs carried frontmatter `type: "compilation-log"` but the canonical envelope is `"sta-compilation-log"`** (the 6 hand-built CLs + all 19 FMs were correct). The compiler **validator checks the JSON body, not the note envelope**, so `validate` stayed 19/19 green and never caught the drift. **DATA FIX (Tier A pair, byte-identical):** normalized the 13 → `"sta-compilation-log"` — Jonah J01–J05 (`stas/jonah/`) + Ruth P07–P14 (`stas/`) in the vault, and `fixtures/compilation-log/<id>-COMPILATION-LOG.md` in the compiler (one line each, nothing else; all 13 compiler↔vault byte-identical after). **ROOT-CAUSE FIX:** the `tripod compile` CLI generator (`src/cli/tripod.ts`) emitted the wrong envelope — corrected to `"sta-compilation-log"` (this minted the drift). **DURABLE FIX (make-impossible):** new `note-type` BLOCK in `src/engine/validate.ts` — a `CANONICAL_NOTE_TYPE` map (FOR_MODEL→`sta-for-model`, COMPILATION-LOG→`sta-compilation-log`, BCD-DELTA→`bcd-delta`, VERIFICATION-INPUT→`verification-input`); when a note carries a `type` envelope that mismatches its detected class, validate now hard-blocks (oral raw-`.json` STA carry no envelope and are exempt). `detectArtifact` updated to recognize the canonical CL value. **Bite-proven both ways** (`tests/validate.test.ts`, 4 new tests — the exact portal-blocker drift + a cross-class mislabel BLOCK; the canonical corpus + a clean CL SILENT). Also corrected a **latent stale portal assertion** the gate-fix unmasked: `smoke-real-tree.test.mjs` asserted J01 shows "Not yet authored" (day-one map-only state) — but all 19 are now authored, so it asserts J01 renders FOR_MODEL + compilation-log. **No `validation-rules.json` / enum / schema-version change** (code-level validator rule + envelope-data normalization; no re-pin). Independent of SC-0074. Instrument `docs/SC-0075-APPLY-RECORD.md`. **Result: `cd portal && npm ci && npm test && npm run build` → exit 0, manifest = 19 pericopes / 19 for-model / 19 compilation-log → portal auto-deploys all 19 Ruth+Jonah STA.** | **APPLIED (branch — compiler + vault pair; Tier A; Evaluator application-verify + Marcia's merge word via `tools/merge_pr.py` pending, vault half `--require-merged`; vault = the 13 CL envelopes only).** Board **395 + 1 skipped** (391 + 4 note-type bite-tests) · validate 38/38 · 0 block · 0 drift · 0 `note-type` findings corpus-wide · check-drift 0 @ lint-lexicon 0.4.0 · portal `npm test` 25/0 + build exit 0 (19/19/19) · vault 13/13 CLs byte-identical. 2026-06-26 |
| SC-0076 | **Esther onboarding — Phase 2: REGISTRY / BCD (the cast)** (deliberate seed-expansion; **cast proposal approved by Marcia 2026-06-26** — [[tripod-esther-onboarding]] · `tripod-eval-artifacts/ESTHER-CAST-PROPOSAL-SC0076.md`). Seeds the Esther Layer-3 registry: **41 vault BCD notes** under `bcd/esther/{beings,places,objects,times,institutions}/` → the derived, pinned alias table `_spec/registry/esther.aliases.json` via `extractor/build_aliases.py` (no `--places-ner`, matching Jonah). **41 entities: 17 PERSON** (B1–B17: Mordecai, Esther/Hadassah, Haman, Ahasuerus, Vashti, Zeresh, Hegai, Hathach, Shaashgaz, Memucan, Bigthana, Teresh, Harbona, + 4 group beings: the seven eunuchs, the seven princes, the ten sons of Haman, **the Jews**) **· 7 PLACE** (PL1–PL7: Susa, India, Cush, Jerusalem, Babylon, Persia, Media) **· 8 THING** (the lot/Pur, signet ring, gallows, edict, royal horse, royal robe, scepter, **the royal annals**) **· 4 TIME** (Adar/Nisan/Sivan/Tebeth) **· 5 INSTITUTION** (banquet/dat/Purim/harem/the-127-provinces). **Every `hebrew` field = BHSA voc_lex** (vocalized lemma) pulled from the pinned `_spec/source/esther/*.json` packets — not transcribed. **`appears_in`:** proper nouns = computed lemma span; group/common-noun entities hand-anchored to the specific referent. **Esther=Hadassah = ONE being (B2)** with referential_forms; **Harbona (B13)** one being / two spellings. **CB/FIG GLOBAL — no re-mint** (concepts/figures untouched). **NO closed/bounded-list edit, no enum bump** — pure per-book registry seeding (`validation-rules.json` unchanged). **Judgment calls:** §3d Susa collapsed to one place (citadel/city as referential note); patronyms/genealogy not registered; **God = no divine being** (the significant_absence signature). **Adversarial verification** (5-lens workflow re-deriving from the BHSA packets; 0 refuted; Hebrew + format lenses 100% clean) drove **2 objective fixes** (B16 ten-sons `appears_in` +E17/9:25; B1 Mordecai +referential_form "the Jew") **+ 2 cast expansions RULED IN by Marcia** (B17 the Jews; TH_ANNALS the royal annals). Instrument `docs/SC-0076-APPLY-RECORD.md`. Phase 3 (maps) + Phase 4 (compile/vocab) follow. | **APPLIED (compiler `sc-0076-esther-bcd` + vault `sc-0076-esther-bcd` pair; Tier A; Evaluator phase-verify + Marcia's merge word via `tools/merge_pr.py` pending, vault half `--require-merged`).** Acceptance ALL green: `check-drift` exit 0 — `registry/esther.aliases.json vendored:ok` (pin `aliases-0.1.6`, sha `697569be…`) · **byte-identical rebuild** from the vault BCD · **no CB/FIG re-mint** (concepts/figures `vendored:ok`) · `id-check` 0 ref-integrity / 0 name-binding / 0 dangling · board **395 | 1 skipped** (unchanged) · 41 entities (17/7/8/4/5). 2026-06-26 |
| SC-0077 | **Esther onboarding — Phase 4a: REGISTRY-DELTA (the map-surfaced cast)** (the cast Phase-3 mapping surfaced; **Marcia ruled the delta 2026-06-26/27** incl. the eunuchs-own-group-being call; Evaluator-vetted spans + 3 citation-fixes — [[tripod-esther-onboarding]] · `tripod-eval-artifacts/ESTHER-REGISTRY-DELTA-FROM-MAPS.md`). Grows the Esther Layer-3 registry **41 → 51 entities**: **7 new group beings B18–B24** (the king's servants/avadim · the empire's officials/satraps-governors-provincial-princes · the enemies of the Jews/oyvim · the royal scribes · the royal couriers · Haman's friends · **the king's eunuchs/sarisim**) · **PL8** the king's gate · **TH_ROYAL_CROWN** · **TH_THRONE** · **referential-forms** added to B3 (the Agagite / son of Hammedatha / **the enemy of the Jews** צֹרֵר הַיְּהוּדִים), B16 (the 10 sons' names), B17 (**a certain people** עַם אֶחָד / the seed of the Jews / Jews of Susa / Jews of the open towns), TM_ADAR (the 13th/14th/15th of Adar). **Every `hebrew`=BHSA voc_lex; every `appears_in` BHSA-computed/anchored** (B18 = eved∪na'ar union E01·E03·E04·E06·E07·E09·E10·E11·E12; crown drop-E06 + throne drop-E05 citation-fixes confirmed against BHSA — keter never at E06, kisse never at E05, the E06 kisse = Haman's seat). **No closed/bounded-list edit, no enum bump, NO CB/FIG re-mint** — per-book Layer-3 growth only. Vault `bcd/esther/` = 10 new notes + 4 patched → `_spec/registry/esther.aliases.json` regenerated + re-pinned (`aliases-0.1.6`, sha `e2d05cd3…`). Instrument `docs/SC-0077-APPLY-RECORD.md`. Phase 4b (SC-0078) = commit the 18 maps + compile → FOR_MODELs + vocab promotes. | **APPLIED (compiler `sc-0077-esther-registry-delta` + vault same; Tier A; Evaluator phase-verify + Marcia's merge word via `tools/merge_pr.py` pending, vault half `--require-merged`).** Acceptance ALL green: `check-drift` exit 0 — `registry/esther.aliases.json vendored:ok` · **byte-identical rebuild** from the vault BCD · **no CB/FIG re-mint** (concepts/figures `vendored:ok`) · `id-check` unchanged baseline · board **395 | 1 skipped** · 51 entities (24/8/10/4/5). 2026-06-27 |
| SC-0078 | **Level-3 MODALITY — the realis `status` axis + the SPEECH_ACT growth (the schema SC that precedes the Esther compile)** (**Marcia ruled the model 2026-06-27, option 1**; the 3 ⟐ sub-choices + the make-impossible call ruled after the Architect build-spec + Evaluator draft-verify — [[tripod-esther-onboarding]] · `tripod-eval-artifacts/SC-0078-MODALITY-BUILD-SPEC.md`). Marks whether L3 content is narrated fact vs. said/permitted/intended/conditional content, so the model can't read "the Jews were *granted leave* to destroy" as "the Jews destroyed". Two axes, three mechanisms — **mostly reuse, one new field. (1) NEW `status` field** (optional, on a proposition AND a component; `$ref upper_token`), a **Layer-2 bounded-open realis axis** (seed PERMITTED/FORESEEN/RECALLED/COUNTERFACTUAL/NORM/HABITUAL; `ASSERTED` = the unmarked default, omit it). Drifts like `proposition_kind` (a non-seed value → drift warning → promote-with-provenance), wired at both levels in `vocabulary.ts` (proposition-level beside `proposition_kind`; component-level in the `action`-walk); seeded into `drift_detector.canonical_p01_enumerations.status` + `approved-enumerations.json` `axes.status` (v0.20→**v0.21**). **(2) closed SPEECH_ACT 26 → 33** (+`PROPOSES_COURSE_OF_ACTION`, `PETITIONS_FOR_GRANT`, `ASKS_DELIBERATIVE_QUESTION`, `PRESCRIBES_AS_LAW`, `ADVISES_COURSE_OF_ACTION`, `RESOLVES_TO_ACT`, `ALLEGES_AGAINST` — Esther court-speech illocutions; `PRESCRIBES_AS_LAW` is the illocution of decreeing, **orthogonal** to `status: NORM`; `DELIBERATES_INWARDLY` dropped → inner speech = base illocution + `addressee` = the speaker's own being-code). **★ A2 make-impossible (Marcia ruled):** `speech_act` rides on components inside the permissive `event_specific_slots` that ajv never inspects → it was **un-enforced** by the validator (the closed list was consumed only by the drafter; the stale `structural.ts` comment that claimed otherwise is corrected). The closed-list BLOCK is now **engine-enforced** in `vocabulary.ts` (mirror of the `action`-walk) — a component `speech_act` outside the 33 hard-blocks. **Zero-regression empirically met** (the 19 Ruth/Jonah FMs use 25 distinct speech_acts, all ⊆ the prior 26 → 0 new blocks). **(3)** `purpose_of`/`condition_of` stay relation LINKS (no double-encoding as `status`); component-level links keep targeting sibling action labels, so the prop_id resolver is unchanged. **Born-clean:** the SC-0073 value-shape guard now covers `status` at both levels (ess walk + a one-line proposition-level feed); the 6 seed values are 1 token → below the ≥4-token line → never fire; a prose `status` BLOCKs. `validation-rules.json` v0.17→**v0.18** (`for_model_schema.$id` v0-10→v0-11), both re-pinned. **Ruth/Jonah retrofit DEFERRED to a later SC** (their FMs carry no `status` and only the 26 → the generality test stays byte-identical). **Conscious deferral (own SC):** component `speech_act` *presence* (the "mandatory" half) is still unenforced — A2 enforces membership, not presence. Instrument `docs/SC-0078-APPLY-RECORD.md` + the one-page authoring guide `docs/SC-0078-MODALITY-AUTHORING-GUIDE.md`. The Esther Phase-4b compile is the following SC (SC-0079). | **APPLIED (branch `sc-0078-l3-modality` — compiler + vault pair; Tier A; Evaluator application-verify + Marcia's merge word via `tools/merge_pr.py` pending, vault half `--require-merged`; vault = `validation-rules.json` v0.18 + `approved-enumerations.json` v0.21 byte-identical).** Board **406 + 1 skipped** (395 + 11 modality bite-tests, `tests/modality.test.ts`) · validate 19/19 · 0 block · 0 drift (Ruth/Jonah byte-unchanged) · check-drift 0 @ v0.18/v0.21, closed-list sync invariant holds · lint 19/0 · id-check 0 ref-integrity/0 name-binding/0 dangling · coverage 14/14 · gold-diff 100% agreement · tsc clean. 2026-06-27 |
| SC-0079 | **Esther Phase-4b — compile + MAPS PUBLISH (IN FLIGHT; this row covers the maps-publish phase)** (governing SC for the Esther compile; **Phase 1 cast register-3 ruled by Marcia 2026-06-28** — `esther.aliases.json` 51→54 (`TM_DAYS_OF_AHASUERUS`, `TM_THIRD_YEAR_OF_THE_REIGN`, `TM_TWELVE_MONTHS`), re-pinned aliases-0.1.6, Evaluator-verified GREEN; **Part-1/2 meaning-calls ruled by Marcia 2026-07-04** — `tripod-eval-artifacts/ESTHER-PART1-DECISION-CARD.md`, all 7 boxes as recommended: the FORMAL_OFFICIAL decree-key principle ratified corpus-wide (actually-voiced/written edict wording only — never narrator report, spoken proposal, in-room verdict, or hyperbole); override scopes E02 = 1:19–20 · E07 = 3:13 only (prose fixed from two overrides to one) · E13 = 8:8b only (authorization ≠ voicing) · E17 = the two letter-acts (Scenes 1+3; ruled span implemented at the prop layer by tagging Prop 12/9:31 — Architect judgment call flagged for Evaluator verdict); E01/E18 COMMUNITY_MEMORY framing envelope confirmed; the 18 scene cuts batch-confirmed). **The 18 Esther Meaning Maps flip `draft → complete` and publish** (maps-first: the portal renders map-only pericopes with the FOR_MODEL section pending; the Atlas graduates Esther registry-only → compile-pending automatically). Complete-map convention applied: inline meaning-call parentheticals + the draft-only "Mapper's notes" tail sections resolved out (Ruth/Jonah complete maps carry no process commentary; SC-0017 de-leak rule); 11 dangling see-notes pointers cleaned; `sta-status: pending` (map done, FM pending). Landing = compiler branch `sc-0079-esther-maps-publish` (the 18 maps + the Phase-1 registry ride-along) + vault companion `pericopes/esther/` ×18, byte-identical 18/18. Atlas real-tree acceptance updated (Esther registry-only assertions were stale post-landing; registry-only rendering stays covered by the synthetic-tree test). **The compile (FOR_MODELs + CLs + enum bump + quarantine ledger) remains open on `sc-0079-esther-compile`** — this row extends at that close. ⸻ **COMPILE PHASE (CLOSED 2026-07-06):** **Esther onboarding — Phase 4b: COMPILE (the 18 FOR_MODELs) + the vocabulary consolidation** (deliberate seed-expansion; the FM-rewrite that applies the Evaluator-QA'd `SC-0079-remapping-LOCKED.json` + the modality cold-read). **Fix-stream B** (per-axis remap; QUARANTINE = keep+ledger; SPLIT = base proposition_kind + `status`; per-pericope **dedup-by-parking** on level-1 arrays + action `*_components` — keep the canonical, park the co-occurring variant as itself) deterministically reproduces the Evaluator's exact numbers: **PROMOTE 69, dedup-park 57 (47 list + 10 action), 0 blocks**. **Scene #33/#34** un-merge per QA-FINAL (E12→`EXPOSURE_SCENE`; E08/E09 + E02×2 revert+park — #34 SUMMONS split RULED (Marcia 2026-06-29): both originals kept parked). **Fix-stream A** — the 15 deterministic modality `status` edits (the 8 supervision-poisoning relabels: a realis `status` of PERMITTED/NORM/HABITUAL/RECALLED on a qatal/wayyiqtol *occurred* clause → bare; +"if I perish"→COUNTERFACTUAL); A is authoritative for `status` over B. **enum v0.21 → v0.22** (+68: action 4 [DESTROY/KILL/ANNIHILATE/PLUNDER] · arc 13 · comm 11 · context 2 · pace 2 · prop_kind 3 · role 17 · scene 10 · tone 6); **quarantined-vocabulary 0.3.0 → 0.4.0** (+226 parked); both re-pinned. No Ruth/Jonah FM touched. **THE CLOSE (2026-07-06, all human rulings in):** Marcia's speech-act decision card applied verbatim (`tripod-eval-artifacts/ESTHER-SPEECH-ACT-DECISION-CARD.md`, all 6 + the E10 rider: E06 P6 speech_act dropped/NORM kept · E06 P8→ASKS_RHETORICAL_QUESTION_AS_PROTEST · E10 P9/P14 comps→PROPOSES_COURSE_OF_ACTION + ASKED comps→ASKS_INFORMATION_SEEKING_QUESTION · E12 P5→ASKS_INFORMATION_SEEKING_QUESTION · E17 P10 PRESCRIBES_AS_LAW kept · E18 P2 colophon→STATES_AS_TRUE, no new closed-list member). The 18 CLs mechanized (SC-0064 pattern: fan-out authorship + fix-streams A/B + card decisions; vocabulary_additions = enum-v0.22 per-value provenance, 68 mints across 15 pericopes) and flipped `skeleton`→`valid`. **Rider:** compilation-log.schema.json carried a stale pre-SC-0078 26-value speech_act enum — aligned verbatim to the ruled 33, schema v0.8→v0.9, re-pinned (the Esther CLs legitimately use the 7 newer values). gold-diff baseline regenerated 19→37; **Marcia ruled the agreement bar 2026-07-06: the six Esther passages at 81–88 (E16/E08/E03/E07/E02/E11) are a designed seed trait — ≥90 scopes to Ruth/Jonah (P01/P03 exact), Esther asserts a ≥80 floor.** Atlas real-tree acceptance graduated: absence-divs flip doesNotMatch→match (the God-absence signature renders from the FMs) + book status compile-pending→complete. VOCABULARY_LOG SC-0079 entry appended. Branch rebased onto main post-#103/#111 keeping main's ruled maps (byte-identical 18/18). | **MAPS-PUBLISH APPLIED (PR pending — Tier A: Evaluator application-verify + Marcia's merge word; vault half `--require-merged`).** Board 406 + 1 skipped · portal suite 49/0 · portal build green: 18 map-only Esther pages + 19 Ruth/Jonah intact · 0 unresolved registry tooltips · id-check --corpus 19 clean + 18 map-only skipped by design · check-drift ok, closed-list sync invariant holds. 2026-07-04 ⸻ **COMPILE: CLOSED — PR pair pending (Tier A: Evaluator application-verify + Marcia's merge word; vault half `--require-merged`).** Corpus validate 74/74 (37 FMs + 37 CLs) 0 block / 0 drift / 241 quarantined · check-drift exit 0 + :vault ok (closed-list sync invariant holds) · id-check --book esther on E01–E18: 0 ref-integrity / 0 flag-mismatch (name-binding/misalignment/dangling = pre-existing map diagnostics, human-ruled inventory) · lint 37/37 clean · coverage --corpus 14/14 block-clean · portal suite 57/0 on the real tree (the CL `valid` gate satisfied; Esther Atlas status `complete`) · board 442 pass · 1 skipped · 0 fail incl. the two graduation tests (count 19→37, agreement scoped per the ruling) · tsc clean · gold-diff Ruth/Jonah 100%, Esther 81–100 as ruled. Standing note: 5 parked values now RECUR (the quarantine ledger's designed review trigger — future ruling). 2026-07-06|
| SC-0080 | **The Meaning-Coordinates RENAME — FOR_MODEL → Meaning Coordinates, whole-ecosystem** (Marcia's four rulings 2026-07-05, executed per `tripod-eval-artifacts/RENAME-BUILD-SPEC-MEANING-COORDINATES.md`: full `-MEANING-COORDINATES` suffix · STA stays — the only `sta-*` string moved is the envelope type `sta-for-model`→`sta-meaning-coordinates` · historical freeze ratified — SPEC_CHANGES rows, apply-records, `_working/`, old handoffs keep the old name; this row is the log's only edit · own SC, one atomic compiler+vault PR pair). Script-applied from the committed kit (`tools/rename-map.json` + engine + exempt/live lists + the census-of-record; machine-enforced SC-0079-close gate 953f2f9·d1d0fb4): 39 git-mv (`fixtures/for-model/`→`fixtures/meaning-coordinates/` + 37 notes + the drafter prompt) · 1,359 global replacements across 188 files (9 ordered variants, Scope A/B) · 409 word-boundary identifier replacements (37 patterns; bare `fm`/`FM` per-file dispositions, frontmatter sense untouched) · idempotence proven (run-2 zero). Version chain: validation-rules v0.18→v0.19 (inner `$id` →meaning-coordinates-v0-12) · compilation-log.schema v0.9→v0.10 · verification-input v1.1→v1.2 · approved-enumerations v0.22→v0.23 (168 `source_artifact` live pointers renamed; growth_log frozen) · lint-lexicon 0.4.1 · drafter `fm-drafter-0.1.6`→`mc-drafter-0.2.0` (prompt git-mv'd; history lines frozen) · id-alignment-exceptions re-pinned (RECONFIRM-AT-APPLY addition) · all pins recomputed. Portal: gate/dirs/type/labels renamed, display label `Meaning Coordinates (STA)`, hidden legacy `#for-model` anchors on every pericope page (old shared links land), tour narration + Method page renamed as Scope-B public prose (quoted in the PR for Marcia's tour-wording gate). Pre-rename tags: compiler `v0.5.0`+`v0.5.0-pre-rename`, vault `pre-rename-mc`. Spec-table additions found at apply, flagged in the PR: `src/drafter/assemble.ts` prompt-loader path · `src/drafter/digest.ts` comment · `pages.mjs` `fmSection` (artifact sense) · `docs/RECURRING-ERROR-DISCIPLINE.md` + `_spec/id-alignment-exceptions.json` prose-`FM` scope. | **APPLIED (branch `rename-meaning-coordinates`; Tier A — Evaluator full verify: grep-zero + forward-replay proof + gates + vault lockstep + live-portal legacy anchors; Marcia's merge word after their green; vault half `--require-merged`). Gates at build: board green · validate 0 block / 0 drift · check-drift 0 + :vault 0 · gold-diff baseline byte-identical (no-semantic-change proof) · portal suite green. 2026-07-06 |

**Superseded / void allocations (recorded, never rebound):**
- **SC-0006 — "Template relics" (planning-time allocation; never committed to this log) → VOID.**
  Cross-session history shows the template-relics fix was tentatively numbered SC-0006 in an earlier
  planning session, but it was never written into SPEC_CHANGES.md under that number: the committed log
  ran SC-0001→SC-0005 (`84bd95c`), and SC-0006 first appears already bound to "Drift convergence"
  (`9fdef18`, 2026-05-29). That binding is now load-bearing — every seeded value in
  `approved-enumerations.json` carries `sc_ref:"SC-0006"` and `promote.ts` stamps it by default — so
  SC-0006 stays bound to drift convergence. The template-relics decision is reinstated below as
  **SC-0008**. This cross-session collision is why the append-only-IDs rule now exists.
- **SC-0007 — double-booked in `docs/PROGRESS.md`** between L1-axis convergence (Next #1) and template
  relics (open-thread (c): "pick one"). Reconciled: **SC-0007 = L1-axis convergence** (it closes
  SC-0006's flagged known limitation), **SC-0008 = template relics**.

---

## Entry template
```
## SC-XXXX — <short title>
- Date: YYYY-MM-DD
- Decided by: <name>
- Status: PROPOSED | APPROVED | SHIPPED | SUPERSEDED(→SC-YYYY)
- Type: closed-list change | bounded-list change | registry/BCD | schema-shape | axis reclassification | other
- Summary: <one line>
- Rationale: <why; link to discussion/doc>
- Spec change (exact): <fields/lists added, removed, moved; before → after>
- Artifact migration: <affected pericopes + what changes in each; or "none">
- Validator impact: <new/changed rule behaviour>
- Version: <old spec version> → <new spec version> (sha256 <hash>)
- Verification: <how we confirmed: fixtures re-validate clean, etc.>
```

---

## SC-0062 — The Ruth-homestretch visibility batch (P08–P14 graduate; Ruth fully visible)
- **Date:** 2026-06-11
- **Decided by:** Marcia's commission — run the visibility batch (the SC-0055 idiom) on the seven blessed homestretch maps still in `_working/` with `status: draft`, so the portal and Obsidian show the whole book of Ruth. Full ceremony.
- **Status:** APPLIED (PR pair open — compiler + vault; Tier A; evaluator pass + her merge word; vault half `--require-merged`).
- **Type:** fixture graduation ×7 + the batch-2 anchor test + vault map copies. **No schema change, no closed-list change, no registry change, no pin move.**
- **The moves (the SC-0044/SC-0055 pattern ×7):** `_working/{P08..P14}/<map>.md` → `fixtures/meaning-map/`, byte-identical except the declared **status draft → complete flip** (one line per file; the portal filters on complete). The FM skeletons + COMPILATION-LOGs stay `_working/` until their judgment halves are authored.
- **The batch-2 anchor (`tests/graduation-batch-2.test.ts`, 36 tests, the batch-1 idiom):** per pericope — parses with status complete · the L1 closed lists HOLD (classification pinned exactly, incl. **P14 = NARRATIVE / GENEALOGY / INFORMAL_CASUAL**) · the skeleton's only blocks are judgment fields with the gap signature **pinned** (P08 21 · P09 52 · P10 27 · P11 52 · P12 36 · P13 38 · P14 17) · the frozen packet reconciles (16/16 · 39/39 · 23/23 · 53/53 · 50/50 · 33/33 · 20/20 explicit, 0 unanchored, implied pinned) · the flag codes registered canon-globally. **Plus the dedicated watch-point gate:** P14's GENEALOGY-under-NARRATIVE-with-the-narrator-constant **validates closed-axis clean** — the watch-point's "it holds" made permanent, the way the psalm's finding is gated by `tests/register-genre-aware.test.ts`. **Bite proven:** P14 register → CEREMONIAL fails the watch-point's 3 tests; a status flip → draft fails its anchor; both restore → 36/36.
- **The vault half:** `pericopes/` gains P08–P14, each **byte-identical to its graduated fixture (cmp-proven pre-push)**. The vault now reads P01–P14 — the whole book of Ruth — beside `pericopes/jonah/` J01–J05.
- **The portal catches up on merge:** verified by local build against this tree — **19 pericope pages from 31 approved artifacts** (was 12/24); the whole of Ruth and the whole of Jonah; the rebuild rides the push-to-main Action, no portal code touched.
- **Verification:** board **246 + 1 skipped** (210 + the 36 anchors) · gold-diff 0 (**13 map-only skips, all visible**: J01–J05, P07–P14) · lint corpus 25 artifacts · 0 findings · 10 accepted · check-drift 0 · check-drift:vault 0.
- **THE MILESTONE:** with the homestretch graduated, **the seed corpus is visible** — two complete books, 19 pericopes, in Obsidian and on the reviewers' portal: Ruth P01–P14 and Jonah J01–J05.
- **Next:** the FM judgment halves (all skeletons await the Slice-4 drafter or hand) · the whole-of-Ruth generality note (the Jonah-report analogue) · [[tripod-sentence-token-triage]].

---

## SC-0061 — P14, the genealogy: the watch-point holds; Ruth fully mapped
- **Date:** 2026-06-11
- **Decided by:** Marcia — **"Option 1"**: affirm the watch-point classification (GENEALOGY under NARRATIVE, the narrator's plain voice), the B27 cast collection, the four pair-closes, and the Salmah one-liner as the vault half.
- **Status:** APPLIED (PR pair open — compiler + vault; Tier A; her pass + merge word; vault half `--require-merged`).
- **Type:** map authoring + one registry/BCD correction (B27 dual spelling). **No schema change, no closed-list change; one pin moved (ruth.aliases 0.1.3 → 0.1.4).**
- **THE WATCH-POINT (the structural test flagged at the division, SC-0052):** GENEALOGY is the book's first use of that genre from the closed 31. Classified faithfully — genre_group **NARRATIVE**, genre **GENEALOGY**, register **INFORMAL_CASUAL** — it **validates closed-axis clean** under the v0.16 conditional. **The honest result is a HOLD, not a finding:** the psalm forced a finding because it had no narrator and the register-const imposed a false value; the genealogy IS narrator-recited (the plain chronicle voice, weight in the figures not the register), so the const fits. The closed lists close the book byte-unchanged from where they began. The seed's own treatment concurs (GENEALOGY_SCENE is a scene_kind within narrative; DESCENDED_FROM's sub-thread is NARRATOR_CHRONICLE).
- **The map:** `_working/P14/P14-Ruth-4-18-22.md` + FM skeleton + COMPILATION-LOG. 1 scene (the *toledot* of Perez) · 10 propositions (the begettings, Perez → David). The **third map born clean of the SC-0058 leak.**
- **Flags (zero minting, 4 CB + 6 FIG):** four cross-pericope pairs CLOSE at the book's end — FIG_0191 (Tamar-Perez, P12→4:18) · FIG_0192 (the four-step naming, P13→4:22) · FIG_0189 (the book-closing inclusio, P13→4:22) · FIG_0194 (the divine-gift consequence, P06→4:22); the genealogy-form figures FIG_0193 + FIG_0190 across the list. Concepts: CB_0049 (Perez the founder), CB_0005 (name-preservation enacted name by name), CB_0047 (Obed), CB_0048 (David — the last word). Cast: B27 collects the six ancestors; B13/B25/B26 the named rest.
- **The vault half (the Salmah fix):** the text spells Boaz's father two ways — שַׂלְמָה (4:20) and שַׂלְמוֹן (4:21); B27's note recorded only Salmon, so coverage read 19/20 (the 4:20 token unanchored). Added שַׂלְמָה to `bcd/beings/B27-Genealogy-Figures.md` (hebrew + entry_verse note); rebuilt `ruth.aliases.json` with the canonical NER recipe — **the diff is exactly the two B27 fields, all else byte-identical** (reproducibility preserved) — re-pinned aliases-0.1.3 → 0.1.4. Coverage → **20/20.** The map also documents the variant in B27's referential-form line.
- **A process note (honest, self-caught):** the alias + pin edits were first made on the session branch by mistake (the P13 merge had switched the working branch); caught immediately via the missing `_working/P14` and a measurement-glitch board read, and moved cleanly to the P14 branch via stash. No bytes lost; recorded as the lesson (after a merge, confirm the working branch before editing).
- **Verification:** lint **0 — born clean** · validate 17 blocks all judgment, **0 closed-axis** · coverage **20/20 explicit · 0 unanchored** · id-check **fully clean** · concept-check **0** · board **210 + 1 skipped** · gold-diff 0 · check-drift 0 @ ruth.aliases 0.1.4 · the alias rebuilds byte-identical from the edited vault BCD.
- **THE MILESTONE:** with P14, **the book of Ruth is fully mapped — 14 pericopes, 85 of 85 verses.** With Jonah's 5 pericopes / 48 verses, the seed corpus is two complete books across three genres-in-use, the closed lists byte-unchanged throughout.
- **Next:** P13/P14 graduations + the remaining graduations when Marcia calls them; the FM judgment halves; the whole-of-Ruth generality note (the Jonah-report analogue) at the arc's close.

---

## SC-0060 — P13, Obed: mapped, ruled, applied (the book's reversal)
- **Date:** 2026-06-11
- **Decided by:** Marcia — **"Option 1, blessed as drafted"**: the women's blessing CEREMONIAL, the child's three uncoded surface forms, and the two lexeme-disciplined leave-outs.
- **Status:** APPLIED (PR open — compiler-only, no vault half; Tier A by files-touched; her pass + merge word via `tools/merge_pr.py`).
- **Type:** map authoring. **No schema change, no closed-list change, no registry change, no pin move.**
- **The milestone:** the **second map born clean of the SC-0058 leak** — the corrected drafting discipline holds across a new author-pass.
- **The map:** `_working/P13/P13-Ruth-4-13-17.md` + FM skeleton + COMPILATION-LOG. 3 scenes — marriage + gift + birth (4:13) · the women's blessing (4:14–15) · the lap + naming (4:16–17) — 12 propositions. S1 INFORMAL_CASUAL; S2 CEREMONIAL (the women's "blessed be YHWH" benediction, the female counterpart to the gate's); S3 INTIMATE (the grandmother and the child, the neighbor-women over the cradle).
- **The reversals:** the conception YHWH gives (4:13) is the book's second and final direct divine act (answering 1:6); Ruth is measured "better than seven sons" (empty → full); the redeemer-word lands on the child; the women name him Obed for Naomi (closing 1:19); the narrator reaches to David.
- **Flags (zero minting, 6 CB + 16 FIG):** five cross-pericope pairs CLOSE — FIG_0187 (women answer the men's blessing, P12→4:14) · FIG_0142 (blessed-be-YHWH-redeemer, P09→4:14) · FIG_0181 (communal-naming-for-Naomi, P04→4:17) · FIG_0016 (the women return at the cradle, P11→here) · FIG_0014 (the divine name sounds again); two OPEN toward P14 — FIG_0192 (the four-step naming repeats in the genealogy) + FIG_0189 (the book-closing inclusio); plus the climax locals. The child is carried uncoded by three surface forms (son/redeemer/child) — one entity until "Obed"; the id-check's 3 pendings.
- **Lexeme-disciplined leave-outs:** CB_0010 (4:14 is "in Israel" the place = PL_ISRAEL + FIG_0184, not "house of Israel"); CB_0004 (no Moabite lexeme — "your daughter-in-law who loves you"; the label gone, FIG_0001 last-fired at P12).
- **Honest note (disclosed, not fixed):** FIG_0014's registry span runs through P13, so the SC-0059/P12 ledger's "closes here at 4:11" parenthetical was a touch early — the divine name's *return* is at P12 (4:11) and it *sounds again* here at 4:14; P13's flag is framed as the continued sounding, the P12 record left as written.
- **Verification:** lint **0 — born clean** · validate all judgment placeholders, **0 closed-axis** · coverage **33/33 explicit · 0 unanchored · 7 implied flagged · 20 reviewer ticks** · id-check **0 errors** (3 child-surface-form pendings) · concept-check **0** · board **210 + 1 skipped** · gold-diff 0 · check-drift 0.
- **Next:** P14 — the genealogy (4:18–22): GENEALOGY's first appearance from the closed 31; the structural watch-point flagged at the division. The gates tell us honestly whether the narrative-narrator rule bends. Ten names, and the book of Ruth closes.

---

## SC-0059 — P12, the attestation: mapped, ruled, applied (the names pour back)
- **Date:** 2026-06-11
- **Decided by:** Marcia — **"Option 1, blessed as drafted"**: FORMAL_OFFICIAL on the declaration, CEREMONIAL on the people's blessing (the book's weightiest), the invoked matriarchs and ancestors as registered cast, the 10th signed-off exception (Chilion-and-Mahlon, ENTITY_PAIR, kept verbatim), and the two lexeme-disciplined leave-outs.
- **Status:** APPLIED (PR open — compiler-only, no vault half; Tier A by files-touched; her pass + merge word via `tools/merge_pr.py`).
- **Type:** map authoring + one pinned-file edit (`lint-exceptions.json` 0.1.3 → 0.1.4). **No schema change, no closed-list change, no pin move beyond the exceptions file.**
- **The milestone:** the **first map drafted under the SC-0058 corrected discipline** — born clean of the register-rule class (0 definitional gloss, 0 rule-sentence). The prompt fix works at the source: a new map written after the relocation does not carry the leak.
- **The map:** `_working/P12/P12-Ruth-4-9-12.md` + FM skeleton + COMPILATION-LOG. 2 scenes — the declaration (4:9–10) · the people's blessing (4:11–12) — 11 propositions. FORMAL_OFFICIAL on the declaration (the legal performative completing the gate proceeding); CEREMONIAL on the blessing (the full communal blessing invoking YHWH, the matriarchs, Perez/Tamar — the weightiest in the book).
- **The names:** after eight verses of gate-namelessness (P11), the dead are named — **Elimelech, Chilion, Mahlon** — and **Mahlon is named as Ruth's husband (4:10), closing the pairing the narrator withheld since P01 (the P01-D2 thread carried through six maps).**
- **Flags (zero minting, 11 CB + 12 FIG):** five cross-pericope pairs CLOSE — FIG_0002 (raise-the-name, P11→4:10, the formula enacted) · FIG_0110 (living-and-dead, P07→4:10) · FIG_0005 (the sandal's attestation, P11→4:9) · **FIG_0014 (the divine name returns at the blessing after the legal silence — P11's silence → 4:11)** · FIG_0001 (the Moabitess arc, P01→4:10, last firing); three OPEN — FIG_0187 (elders-and-women blessing pair → 4:14) · FIG_0017 (Ephrathah-Bethlehem doubling) · FIG_0191 (Tamar-Perez → the genealogy, P14).
- **The 10th signed-off lint exception:** "all that was Chilion **and** Mahlon's" (4:9) — the two dead brothers, the ENTITY_PAIR/C3 class, the exceptions file's own "his two sons Mahlon and Chilion" precedent. Kept verbatim — not split, not reworded; reason ENTITY_PAIR; proposed by Architect 14, accepted by Marcia, sc_ref SC-0059; re-pinned 0.1.4 by file bytes.
- **Lexeme-disciplined leave-outs:** CB_0031-Ephrathite (the 4:11 lexeme is אֶפְרָתָה, the place — carried by PL8 + the FIG_0017 doubling — not the gentilic אֶפְרָתִי; the P11-CB_0040 discipline) · FIG_0168 (the ruin-word is P11's, not in 4:9–12) · FIG_0007 (the chronicle-frame "in former times" is not here).
- **Two self-caught fixes pre-gate:** my "verb" in a §3C line (grammar jargon — reworded "the act at the center"); the proper-noun "Israel" anchored to PL_ISRAEL as a §3B place (coverage read 49/50 until the place block carried it — the matcher reads §3B, not inline links).
- **Verification:** lint **0 findings · 1 accepted** · validate all judgment placeholders, **0 closed-axis** · coverage **50/50 explicit · 0 unanchored · 4 implied flagged · 18 reviewer ticks** (11 abstract-overlay warns — the blessing's referred-to concepts) · id-check **0 errors** (1 "the dead" uncoded-by-design) · concept-check **0** · board **210 + 1 skipped** · gold-diff 0 · check-drift 0 @ lint-exceptions 0.1.4.
- **Next:** P13 — Obed (4:13–17): the marriage and the conception YHWH gives, the women's blessing answering the men's, the child on Naomi's lap, the neighbor-women naming him; then P14, the genealogy, and the book is closed.

---

## SC-0058 — The register-rule relocation (vocabulary governance out of the maps)
- **Date:** 2026-06-11
- **Decided by:** Marcia — **"Option 1"**: the full proven family in one SC, full ceremony, merging before P11. (The chain of eyes: Marcia caught the sentence in the P11 draft as "a rule about the vocabulary, not a fact about the passage"; the evaluator swept the merged main, classified the class — leak vs keep — and found the rules had no methodology home; the Architect's completing sweep proved the family's full extent across its four wordings.)
- **Status:** APPLIED (PR pair open — compiler + vault; Tier A; her pass + merge word; merge chain: this pair → #67 → #26).
- **Type:** content-discipline cleanup (the SC-0017 de-leak pattern) + methodology home + drafting-discipline fix. **No schema change, no closed-list change, no pin move.**
- **The extent (sweep-proven, not assumed):** the CONSULTATIVE-rule sentence in **13 maps** (four wordings: "is never used" / "is not used" / "never put in a consultative key" / "(a fixed rule)"); the definitional gloss ("the plain default for the biblical narrator's voice") in **15**; the **root cause: the Agent-2 prompt's §8** mandated writing the rule into every map — every v0.3 map obeyed its instructions; the **template** suggested CONSULTATIVE as an example pericope default, against the hard rule; **no methodology home** — the maps were the rules' only record.
- **The fix:** the home (`_methodology/register-usage-rules.md` — the narrator constant cross-referencing v0.16/SC-0046 · CONSULTATIVE-never-narrator, worked case J02's sailors · prayer-carries-the-address, SC-0048, **kept in J05's map as Marcia's ruled rationale, relocate-as-copy** · COMMUNITY_MEMORY-is-framing, SC-0001 · the boundary) · **the trims: 27 cuts across 15 maps, each replacement asserted unique** (12 fixtures byte-paired with vault; P08–P10 compiler-side; **P11 on its open branch** so the gate merges corrected) — every passage-specific statement kept · **the source fixed** (prompt §8 restated: values + passage-specific reasons, no vocabulary rules, no value-definitions; §13 gains the class; §14 check 3 reworded; §5 keeps the rule as drafter-knowledge, home cross-referenced) · the template's register line corrected.
- **Zero-grep proof:** the gloss 0 remaining; the rule-sentence 0 remaining; the one surviving in-map "CONSULTATIVE" is J02's value-application to the sailors — the tag applied to the passage, which is what the prose is for.
- **Verification:** board **210 + 1 skipped** (the 21 graduation anchors assert classification values, not prose — all pass untouched; the generality record undisturbed) · gold-diff **exit 0 at baseline** (the §1 register prose is not consumed by the compiler — measured, not assumed) · lint corpus 18 · 0 · check-drift 0 · the 12 vault pairs byte-identical by construction.
- **Two sibling observations logged, deliberately out of scope:** P09's "on the P03 precedent" and P10's "the P07 and P08 house-talk register" name pericope-IDs in conditioning prose (process-pointer-shaped, milder class — for a future eye, not this SC); the prompt's §2 "match P01" framing predates the corpus's growth (cosmetic).
- **Next:** the merge chain (this pair → #67 + its ledger rebase → #26), then P12 — the attestation.
## SC-0057 — P11, the gate: mapped, ruled, applied (the legal cluster fires entire)
- **Date:** 2026-06-11
- **Decided by:** Marcia — **"Option 1: blessed as drafted — including the FORMAL_OFFICIAL scene-register first, the two registry completions as the vault half, and the leave-outs."**
- **Status:** APPLIED (PR pair open — compiler + vault; Tier A by files-touched; her pass + merge word via `tools/merge_pr.py`, vault half `--require-merged`).
- **Type:** map authoring + two registry-note completions (the vault half). **No schema change, no closed-list change, no pin move.**
- **The map:** `_working/P11/P11-Ruth-4-1-8.md` + FM skeleton + COMPILATION-LOG. 4 scenes — the convening (4:1–2, plain narration) · the field offer and "I will redeem" (4:3–4) · Ruth named and the decline (4:5–6) · the custom told and the sandal drawn (4:7–8) — 15 propositions. **The corpus's register first: FORMAL_OFFICIAL at scene level on S2–S4** — the convened proceeding at the town gate (Jonah's royal decree carried the register at moment level; the gate sustains it; all within the closed 7, no L1 strain). The narrator's 4:7 custom-aside stays his plain voice inside the formal scene.
- **Flags (zero minting — the registry's legal cluster entire, 9 CB + 18 FIG):** CB_0001 ×4 (the role enters · claims · declines · transfers) · CB_0045 (the 3:12 disclosure embodied) · CB_0006 (the venue convened) · CB_0003 (the hinge: the dead's inheritance vs "lest I ruin my own") · CB_0005 + CB_0002 + CB_0039 (the raise-up-the-name term) · CB_0004 (the Moabite named into the family's law) · CB_0007 (the attestation custom). Figures: **three pairs CLOSE** — FIG_0015 the chance-pair (2:3 "her chance chanced" ↔ 4:1 the redeemer passing at the very moment) · FIG_0156 (the man does not rest — 3:18 fulfilled at 4:1) · FIG_0112 (the 2:20 nearness resolved into the queue's answer); **FIG_0165 FIRES at its true home** (props 5+9 — the field first, Ruth second, the held card); **six OPEN toward P12–P13** — FIG_0002 + FIG_0110 (→ 4:10) · FIG_0005 (the sandal pair) · FIG_0003 (the gate where a name is remembered → 4:11) · FIG_0016 (Ruth and Naomi absent from the proceeding that decides them → P13) · FIG_0014 (the divine name silent through the whole legal scene; its return at the 4:11 blessing). Twin figures ride shared propositions per precedent (0162+0163 at the non-name; 0166+0168 at the decline).
- **Cast:** B19-Mr.-So-and-so **at his named site** — peloni almoni, the non-name spoken to his face; **B20-Elders step onstage** (ten men of the town); B2 referenced as "our brother Elimelech"; B3 referenced as "Naomi, who returned from the fields of Moab" — the seller, absent; B9 referenced as "Ruth the Moabite, the wife of the dead" — **her husband unnamed one scene longer (the P01-D2 thread's last verse of silence)**; PL7 working; PL2 · PL_ISRAEL · O26-Sandal · TM_FORMER_TIMES. Three uncoded-by-design: the field-portion (chelqat hassadeh) · "the dead" · "on the day you buy."
- **The vault half — both anonymity-figure corrections completed on the gate's own evidence:** `FIG_0165-Two-Stage-Disclosure`: appears-in [P09, P11] → **[P11]**, opens-at → P11 (SC-0051 struck its stray P07; the P09 cycle found no firing site; its body always said "Surfaces in: p11"). `FIG_0163-Unnamed-Kinsman-Pattern`: appears-in [P10, P11] → **[P11]**, opens-at → P11 (the P10 cycle ruled its 3:18 anchor a misread — that "man" is Boaz). Registries rebuilt from the edited vault worktree **byte-identical to the pins** — no pin move.
- **Text point, mapped per the data:** 4:5 follows the qere — "you acquire Ruth the Moabite"; the written ketiv reads "I acquire." Flagged for her eyes, the P10-vayyavo class.
- **Leave-outs recorded:** FIG_0161 Hinneh-Family (FIG_0160 already carries 4:1's hinneh — two figures would double-count one word) · CB_0009 Divine-Name (its P11 listing marks an absence; FIG_0014 carries that structure — a concept does not flag where its word is not) · CB_0010 / CB_0027 (4:11's, P12 territory) · O18 / O19 (CB_0003 and CB_0007 cover the inheritance and the custom's "every matter").
- **Three self-caught rewords (pre-gate, recorded):** my "clause" (grammar jargon — the third grammar-word the lint has caught in my own conditioning prose this homestretch; reworded "the stated purpose") · the 4:4 conditional restructured to condition-in-question form ("What if he will redeem? — let him redeem") · an anti-entry of mine ("not this field") removed as noise.
- **Verification:** lint **0 — born clean** · validate all judgment placeholders, **0 closed-axis** · coverage **53/53 explicit · 0 unanchored · 27 implied flagged · 33 reviewer ticks** (9 abstract-overlay warns — the biggest packet of the homestretch fully reconciled) · id-check **0 errors** (the 3 uncoded pendings) · concept-check **0** · compile carried exactly the declared 37 flag instances · board **210 + 1 skipped** · gold-diff 0 · check-drift 0.
- **Next:** P12 — the attestation (4:9–12): "you are witnesses this day," the names pour back in — **Mahlon at last** — and the people at the gate bless the house like Rachel and Leah's.

---

## SC-0056 — P10, the nameless dawn: mapped, ruled, applied
- **Date:** 2026-06-11
- **Decided by:** Marcia — **"Option 1, blessed as drafted"**: the two INTIMATE scenes, the registered cast with the mitpachat-vs-simlah note visible (O13 kept; a new code only if the word-difference should be structural — not minted, resist over-coding), the declared pairs closing where their notes always pointed, the FIG_0113 one-line vault correction, the FIG_0163 leave-out, and the measure-unit left unflagged exactly because the text withholds it.
- **Status:** APPLIED (PR pair open — compiler + vault; Tier A by files-touched; her pass + merge word via `tools/merge_pr.py`, vault half `--require-merged`).
- **Type:** map authoring + one registry-note correction (the vault half). **No schema change, no closed-list change, no pin move.**
- **The map:** `_working/P10/P10-Ruth-3-14-18.md` + FM skeleton + COMPILATION-LOG. 2 scenes — the floor at dawn (3:14–15) · home (3:16–18) — 13 propositions; both scenes INTIMATE (the dawn still inside the night's privacy; the homecoming the two women's house-talk); no ceremonial moment; narrator INFORMAL_CASUAL. **The pericope's own discipline kept: not one personal name in five verses** (the packet's zero proper nouns) — "he"/"the man," "the woman," "my daughter," "her mother-in-law," all carried as referential forms.
- **Flags (zero minting, all pre-declared):** CB_0042 (margelot, the night scenes' fourth and final) · **CB_0044 Riqam-Empty + CB_0024 Full-and-Empty at the empty-answer — the declared 1:21 ↔ 3:17 pair: Naomi's lament-word returns negated in Boaz's reported speech, aimed at her role** · **CB_0043 Mother-in-Law-Chamot at its declared first appearance** · figures: the secrecy pair (FIG_0150 before-recognize + FIG_0151 let-it-not-be-known) · FIG_0152 ×2 (six measures — the unit unsaid, the vagueness kept as the text's) · **FIG_0153 Do-Not-Return-Empty CLOSES (P04 → P10, REQUIRED)** · **FIG_0154 Doubled-Who-Are-You** (3:9's question in the morning's mouth; recognition / identity / changed standing all held open) · FIG_0155 ×2 (only "the man"; his name rests until the gate) · **FIG_0156 The-Man-Will-Not-Rest OPENS toward P11**.
- **The FIG_0113 deferral resolved (the vault half):** deferred at SC-0053 (no firing site in P08), ruled on P10's evidence — the six measures are freshly weighed, not the meal's leftover; the empty-answer belongs to FIG_0153. `figures/FIG_0113-Leftovers-After-Satiety.md`: appears-in [P07, P08] → **[P07]**, closes-at P08 → P07. Registries rebuilt from the edited vault worktree **byte-identical to the pins** (book-level unchanged — no pin move).
- **Leave-outs recorded:** FIG_0163 (its note reads 3:18's "the man" as the nearer redeemer — the plain reading is Boaz, who will not rest; FIG_0155 covers the naming-down; its true scope is P11's anonymity formula; hers to overrule) · CB_0040-Ephah (its own note says the six-measure unit is "possibly seah" — flagging would assert what the text withholds) · CB_0026 / CB_0001 / CB_0020 (the loose-list class).
- **Two self-caught comma rewords (pre-gate, recorded):** both my phrasing, not the text's — the vocative split to its own address-line ("who are you" · "my daughter") and the quote-frame moved into the question ("What did she report he said?") — the P07/P08 idiom; no exceptions needed.
- **Text point, mapped per the data:** 3:15 closes "and HE went into the town" (BHSA 3ms; some ancient versions read "she") — the movement told plainly, flagged for her eyes.
- **Verification:** lint **0 — born clean** · validate all judgment placeholders, **0 closed-axis** · coverage **23/23 explicit · 0 unanchored · 22 implied flagged · 10 reviewer ticks** (4 abstract-overlay warns) · **id-check fully clean, zero pending — the second after P08** · concept-check **0** · compile carried exactly the declared 13 flag instances · board **210 + 1 skipped** · gold-diff 0 · check-drift 0.
- **Next:** P11 — the gate (4:1–8): Mr. So-and-so meets his non-name, the field then Ruth, the sandal comes off; FIG_0165 finally fires where its corrected note points.

---

## SC-0055 — The visibility cycle: J03 + J04 + J05 + P07 graduate (fixtures + vault + portal)
- **Date:** 2026-06-11
- **Decided by:** Marcia's sequencing order, given with the P09 blessing: **"P09 then visibility"** — the Architect applies P09, then runs the graduation cycle before P10 opens, "so your Obsidian and the reviewers' portal catch up to everything blessed."
- **Status:** APPLIED (PR pair open, stacked on SC-0054 — compiler + vault; Tier A by files-touched; evaluator verify + Marcia's merge word via `tools/merge_pr.py`; **merge protocol: retarget the stacked compiler PR to main BEFORE SC-0054's branch merges-and-deletes** — the #19→#20 lesson — then compiler first, vault `--require-merged`).
- **Type:** fixture graduation ×4 + anchor tests + vault map copies. **No schema change, no closed-list change, no registry change, no pin move.**
- **The moves (the SC-0044 J02 pattern ×4):** `_working/{J03,J04,J05,P07}/<map>.md` → `fixtures/meaning-map/`, byte-identical except the declared **status draft → complete flip** (one line per file, the diff is the proof; the portal filters on complete). The FM skeletons + COMPILATION-LOGs stay `_working/` until their judgment halves are authored (the SC-0038 J01 Option-1 pattern).
- **The batch anchor (`tests/graduation-batch-1.test.ts`, 21 tests, the J01/J02 idiom):** per pericope — the map parses with status complete · the L1 closed lists HOLD on the graduated fixture · the fresh skeleton's only blocks are judgment fields with the gap signature **pinned** (J03 **35** · J04 **54** · J05 **44** · P07 **47** — each equal to its apply-cycle record) · the frozen packet reconciles (**43/43 · 67/67 · 70/70 · 40/40 explicit, 0 unanchored ×4**, implied counts pinned) · the map's flag codes are registered canon-globally. J03 additionally **re-pins the versification convention** on the fully divergent pericope (scene range Heb 2:3+, prop anchors Eng 2:2–2:9). **Bite proven:** P07's status tampered back to draft → exactly the named anchor fails (1/21); restored → 21/21.
- **The vault half:** `pericopes/jonah/` gains J03 + J04 + J05; `pericopes/` gains P07 — each **byte-identical to its graduated fixture, cmp-proven pre-push**. The psalm, Nineveh, the qiqayon, and the gleaning brought home become visible in Obsidian.
- **The portal catches up on merge:** verified by local build against this tree — **12 pericope pages from 24 approved artifacts** (was 8 pages / 20 artifacts); the rebuild rides the existing push-to-main Action; no portal code touched.
- **Verification:** board **210 + 1 skipped** (189 + the 21 anchors) · gold-diff 0 (**6 map-only skips, all visible**: J01 J02 J03 J04 J05 P07) · lint corpus 18 artifacts · 0 findings · check-drift 0 · check-drift:vault exit 0 (the vault half touches no `_spec/`).
- **Next:** P10 — the nameless dawn (3:14–18) — on a portal that finally shows the whole runaway prophet; then P11–P14 to the book's close.

---

## SC-0054 — P09, the threshing-floor night: mapped, ruled, applied (the book's hinge)
- **Date:** 2026-06-11
- **Decided by:** Marcia — **"Option 1, bless as drafted"** on groups A–D: the oath staying INTIMATE on the P03 precedent, the CEREMONIAL blessing moment, the four uncoded surface forms, FIG_0141 carried, the loose-list leave-outs (each registry-checked). Her sequencing order with the blessing: **"P09 then visibility"** — the graduation cycle (J03 + J04 + J05 + P07 → fixtures + vault + portal) runs before P10 opens.
- **Status:** APPLIED (PR open — compiler-only, no vault half; Tier A by files-touched; evaluator verify + Marcia's merge word via `tools/merge_pr.py`).
- **Type:** map authoring. **No schema change, no closed-list change, no registry change, no pin move.**
- **The map:** `_working/P09/P09-Ruth-3-6-13.md` + FM skeleton + COMPILATION-LOG. 3 scenes — the approach (3:6–7, the narrator's wordless telling) · midnight (3:8–9) · Boaz's answer (3:10–13) — 21 propositions; INTIMATE scene-level on S2+S3; CEREMONIAL moment at the 3:10 blessing; **the 3:13 oath INTIMATE per P03** ("the weight is carried by the figures, not by a grander way of speaking").
- **Cast:** all registered — B9 (amah, her own word) · B13 ("the man" until he answers) · B3 REFERENCED · B18 (the role) · **B19-Mr.-So-and-so's first entrance, REFERENCED-unnamed** · B21 · B10 · PL6 · PL7. Four deliberately uncoded surface forms (grain-heap's edge · midnight · the bachurim · night-and-morning) = the id-check 4 pending, by design.
- **The flags (22, all pre-declared):** 7 concepts — CB_0042 ×2 · CB_0037 (the 2:12 wing returns as petition) · **CB_0001 ×4 (the goel-word in Ruth's mouth at 3:9; the queue opens at 3:12)** · CB_0008 · CB_0011 (1:8 · 2:20 · 3:10) · CB_0032 · CB_0006 — and 15 figures: **five cross-pericope pairs close on their declared sites** (wing 2:12→3:9 · amah 2:13→3:9 · hesed-not-forsaken 2:20→3:10 · assent 3:5→3:11 · chayil 2:1→3:11), FIG_0112 fires at the 3:12 nearness, the P09-locals (startle · greater-hesed · blessing-formula · nearer-redeemer · chai-YHWH oath · lie-down-authority · reverse-handoff), FIG_0136 riding FIG_0123's proposition (the P07 double-flag precedent), FIG_0141 carried (OPTIONAL keep).
- **The leave-outs (registry-checked):** FIG_0142 / FIG_0194 / FIG_0161 / FIG_0165 list P09 without a firing site in 3:6–13 (FIG_0165's expected-not-firing strengthens its [P11]-only case); CB_0020's P09 listing is loose — 3:13 is the chai-YHWH formula (FIG_0135), not the self-curse. Note-nit logged: FIG_0122's note cites nonexistent "FIG_0124"; the actual mirror is FIG_0140.
- **Two self-caught rewords (pre-gate, recorded):** my own "verb" in a §3C line — grammar jargon, lint-caught, reworded (the J05 never-sign-off-my-own-English class); the B19 slug missing the BCD name's period — id-check-caught, fixed.
- **Verification:** lint **0 — born clean** (post-reword) · validate all judgment placeholders, **0 closed-axis** · coverage **39/39 explicit · 0 unanchored · 23 implied flagged · 25 reviewer ticks** (7 abstract-overlay warns, the known class) · id-check **0 errors** (4 uncoded-pending) · concept-check **0** · compile carried exactly the declared 27 flag instances · board **189 + 1 skipped** · gold-diff 0 · check-drift 0.
- **Next (Marcia's order):** the **visibility cycle** — J03 + J04 + J05 + P07 graduate (fixtures + vault copies + the portal catches up) — then P10, the nameless dawn.

---

## SC-0053 — P08, Naomi's plan: mapped, ruled, applied (Ruth's homestretch opens)
- **Date:** 2026-06-11
- **Decided by:** Marcia — **"Option 1, bless as drafted"** on groups A–D: the two scenes with INTIMATE, the cast, the flags with the *moda* withholding, the eating-and-drinking pair as a signed-off exception (her name + provenance, the standing pattern), and the FIG_0113 deferral.
- **Status:** APPLIED (PR open — compiler-only, no vault half; Tier A by files-touched; evaluator verify + Marcia's merge word via `tools/merge_pr.py`).
- **Type:** map authoring + one pinned-file edit (`lint-exceptions.json` 0.1.2 → 0.1.3). **No schema change, no closed-list change, no registry change.**
- **The map:** `_working/P08/P08-Ruth-3-1-5.md` + FM skeleton + COMPILATION-LOG. Two scenes on the P03 dialogue shape — the plan spoken (3:1–4) · the consent (3:5) — 9 propositions; **both scenes INTIMATE at scene level** (the entire pericope is the two women's private talk; no public or ceremonial moment); narrator INFORMAL_CASUAL under the v0.16 NARRATIVE constant.
- **Zero minting; cast 100% registered:** B3-Naomi ("Naomi her mother-in-law" — name + kinship-word together) · B9-Ruth (addressed only "my daughter"; her name absent until the consent) · B13-Boaz REFERENCED ("our kinsman," then only "the man") · B16-Young-Women REFERENCED · PL6-Threshing-Floor (the plan's named destination) · PL_NAOMIS_DWELLING (implied) · O2-Barley · O13-Your-Garments. **id-check fully clean at draft — zero pending registry items, a project first** (the registry now covers a new pericope's text completely before mapping begins).
- **Flags (all pre-declared on these verses):** CB_0014-Rest-Menucha @P1 (1:9's *menucha* wish becomes the plan's *manoach* goal) · CB_0042-Uncover-Feet-Margelot @P7 (first of the night scenes' four occurrences). Figures: **FIG_0120 Manoach-Rest-Place CLOSES here** (opened P02 1:9) · FIG_0121 Bridal-Preparation-Triplet @P4 (wash–anoint–garments) · **FIG_0122 He-Will-Tell-You-Handoff OPENS @P8** (the reversal lands at P09, where she tells him) · **FIG_0123 Absolute-Assent OPENS @P9** ("all that you say I will do"; Boaz speaks it back at 3:11).
- **The blessed non-flag (group C):** CB_0001 Kinsman-Redeemer **not** flagged, though its note lists P08 — the word at 3:2 is *moda* ("our kinsman," the family word), not *goel*; the text withholds the legal word until Ruth herself speaks it at 3:9 (P09). The P05 precedent (kinsman in prose, no flag until the word appears); the withholding recorded as the scene's Significant Absence. The note's P08 listing = old-generation thread-span residue, left untouched (unlike FIG_0165, it contradicts nothing in its own body).
- **The 9th signed-off lint exception (group D1):** Proposition 5's "until he has finished eating **and** drinking" — the Hebrew's own pair (לֶאֱכֹל וְלִשְׁתּוֹת), one meal-activity in the ENTITY_PAIR/TIME_PAIR family ("her father and her mother"; "three days and three nights"). Kept verbatim — not split, not reworded; reason ACTIVITY_PAIR; proposed by Architect 14, accepted by Marcia, sc_ref SC-0053; `lint-exceptions.json` re-pinned **0.1.3** by file bytes.
- **Deferrals recorded (groups D2–D3):** FIG_0113's declared close-at-P08 is a FIG_0165-class stray (no leftover content in 3:1–5) — the note correction waits for P10's evidence (3:17 territory, where FIG_0153 may already own the image). FIG_0136 fires at P09; its P08 listing is the pair-anchor only.
- **Verification:** lint **0 findings · 1 accepted** (surfaced ✓ ACCEPTED with reason — the engine stays pure) · validate 21 blocks, all judgment placeholders, **0 closed-axis** · coverage **16/16 explicit · 0 unanchored · 18 implied flagged · 8 reviewer ticks** (2 abstract-overlay warns, the known class) · id-check **fully clean** · concept-check **0** · compile carried exactly the declared 6 flag instances · board **189 + 1 skipped** · gold-diff 0 · check-drift 0 @ lint-exceptions 0.1.3.
- **Next:** P09 — the threshing-floor night (3:6–13): the handoff reverses, the assent comes back in Boaz's mouth, and Ruth speaks the word the plan kept silent.

---

## SC-0052 — The P08–P14 division ruled and pinned (Ruth's homestretch packets)
- **Date:** 2026-06-11
- **Decided by:** Marcia — **"Option 1, bless the table as proposed — seams at 3:5|3:6 and 4:8|4:9."** Her reasoning note, recorded: the recommended shapes rhyme with the corpus's established scene-logic — locale changes and speech-closes have cut every scene boundary from P01 through J05. (The proposal: candidate boundaries derived from the text's seams, the June-era division reconstructed from the vault's own discourse-threads + registry entries as reference, each seam glossed — the SC-0039 pattern.)
- **Status:** APPLIED (PR open; Tier A by files-touched — pinned source packets; evaluator verify + Marcia's merge word).
- **Type:** pericope-spec entries + source-packet extraction/pin ×7. **No schema change, no closed-list change, no registry change.**
- **The ruled division (Ruth 3:1–4:22, 40 vv):** **P08** 3:1–5 *Naomi's plan* · **P09** 3:6–13 *the threshing-floor night* · **P10** 3:14–18 *before-recognition dawn* · **P11** 4:1–8 *the gate: So-and-so, the field-then-Ruth disclosure, the decline, the sandal* · **P12** 4:9–12 *"you are witnesses": the attestation + the triple blessing* · **P13** 4:13–17 *marriage, conception, the women's word, Obed named* · **P14** 4:18–22 *the generations of Perez*.
- **The two contested seams, as ruled:** **3:5|3:6** — the locale change (home → floor); P08 mirrors P07's two-women house-dialogue; the alternative 3:8|3:9 (the old feet-entry's implication) declined. **4:8|4:9** — the word changes hands (the So-and-so movement closes with his sandal; Boaz's "you are witnesses" opens the attestation); matches the June-era discourse threads' granular lines; the alternative 4:6|4:7 (the old sandal-entries' lean) declined. The four uncontested seams: 2:23|3:1 (fixed by SC-0051) · 3:13|3:14 (speech-close → narrator resume) · 4:12|4:13 ("so Boaz took Ruth") · 4:17|4:18 (the *toledot* formula is the seam).
- **Verification (against the data, not memory):** chapter counts re-derived from local BHSA (Ruth = 22+23+18+22 = 85) · ranges exact 7/7 · re-extraction **7/7 byte-identical** · **whole-book tiling 85/85 — no gaps, no overlaps, P01–P14 contiguous** · 453 new referents across 40 vv (P08 51 · P09 88 · P10 62 · P11 105 · P12 68 · P13 58 · P14 21) · proper-noun spot-checks corroborate the exegesis: **P10 = zero proper nouns** (the secrecy scene is nameless in the data); **P12 = 15 proper nouns in 4 verses** incl. **Mahlon + Kilion — the P01-D2 withheld-pairing's landing site (4:9–10) confirmed**; P14 = the ten genealogy names to David · **pin-bite proven** (1-byte tamper → check-drift exit 1 naming `source/ruth/P08.json`; restored → exit 0, byte-identical to the independent re-extraction) · pins.json +28 lines, purely additive · board **189 + 1 skipped** · gold-diff 0.
- **Watch-points recorded for the coming cycles:** P14's genre = **GENEALOGY** (closed-31, first use — whether its register behaves under the v0.16 narrative-narrator constant is the next structural test of the psalm's kind; the gates will tell us honestly) · 4:13 = the book's second explicit divine act ("YHWH gave her conception") · 3:17's "do not go **empty**" answers Naomi's 1:21 in her own word · the wing-pair (P06 2:12) closes at P09 3:9 · FIG_0165 lives whole inside P11, where its corrected note now points.
- **Next:** P08 — Naomi's plan — opens the map cycle under the proven pipeline (packet → Agent-2 v0.3 draft → her ruling groups → apply → gates → PR pair → her merge word); pericope by pericope to the book's close.

---

## SC-0051 — The P07 fresh rebuild (the Ruth-remainder arc's first pericope)
- **Date:** 2026-06-10
- **Decided by:** Marcia — **"Option 1, blessed as drafted. Every divergence is data-supported, honestly recorded, and reversible by a word."** (Ruling groups A–D from the same-day report, blessed as laid out; the cycle = the J02/J04 pattern: draft → her ruling → apply → gates → PR pair → her merge word.)
- **Status:** APPLIED (PR pair open — compiler + vault; merge on Marcia's word via `tools/merge_pr.py`, vault half `--require-merged`).
- **Type:** map authoring (the first Ruth pericope under the post-Jonah pipeline) + one registry-note correction. **No schema change, no closed-list change, no pin move.**
- **The map:** `_working/P07/P07-Ruth-2-17-23.md` + the deterministic FOR_MODEL skeleton + COMPILATION-LOG (the J0x working pattern). 4 scenes — the field (2:17) · the homecoming (2:18) · the house-talk (2:19–22) · the season-close (2:23) — 16 propositions; INTIMATE scene-level on Scene 3, one CEREMONIAL moment at the 2:20 blessing; narrator INFORMAL_CASUAL under the v0.16 NARRATIVE constant. Versification note carried (Ruth Eng = Heb, no divergence).
- **Zero minting — the homecoming dividend:** every flag is a canon reuse whose note already declared P07 (swept 17/17 pre-PR): 10 concepts (the gleaning custom · the ephah · *chavat* hand-threshing · the blessing form, at both blessings · **CB_0011 Hesed at 2:20 — the cross-book concept's first return to a Ruth source verse** · the living-and-dead formula · **CB_0001 Kinsman-Redeemer at its declared first appearance, 2:20** · the Moabite marker at 2:21 · *devek* clinging · the harvest frame) + 7 figures (FIG_0104 Abundance-Triplet **closes** P06's declared forward-link at the leftover, 2:18 · FIG_0113 opens toward P08 · FIG_0110 Living-and-Dead + FIG_0111 Hesed-Not-Forsaken + FIG_0112 Close-to-Us all **open at 2:20** toward chs. 3–4 · FIG_0001 Ruth-the-Moabitess fires at 2:21 · FIG_0012 **closes** the 1:14 ↔ 2:23 clinging image-rhyme).
- **The three blessed divergences from the June-2nd reference draft** (numbered for the application-check; each reversible by her word): **(1)** the 2:20 blessing's target — "blessed be he — the man — by YHWH" (בָּרוּךְ הוּא לַיהוָה, the Genesis-14:19 construction; June-2nd's proposition read "she blesses the LORD" though its own scene title said "the blessing on Boaz"); the relative-clause ambiguity — whose hesed has not been forsaken, YHWH's or the man's — is held open three ways: the proposition's floating "he," the CB_0011 scene entry, and the scene's Significant Absence. **(2)** 2:21 keeps Ruth's masculine "the young men" (עִם־הַנְּעָרִים, B17-Young-Men) where June-2nd smoothed to "his workers"; Boaz's 2:8 word and Naomi's 2:22 counsel say "young women" (B16); the ripple is stated in one §2.2 sentence, no motive guessed. **(3)** Scene 3 = the unbroken dialogue 2:19–22, Scene 4 = the 2:23 narrator close (June-2nd's 19–21 / 22–23 split forced its INTIMATE override to straddle two scenes).
- **The sentence-token discipline applied:** June-2nd's invented `TH_LEFTOVER_FROM_SATIETY_FORM` is gone — the leftover food ("what she had left over from her satiety") and "the dead of the household" stay **deliberately uncoded** as plain surface forms; the FM skeleton records the pending pair (the 2 id-check lines are this, by design).
- **The registry correction (the vault half):** FIG_0165-Two-Stage-Disclosure listed P07 in `appears-in`, but the note's own body says "Surfaces in: p11" and its content (the nearer redeemer agrees to the field before Ruth is brought in) is the chapter-4 gate scene — Ruth 2:17–23 has no two-stage disclosure. Struck: appears-in → [P09, P11], opens-at → P09. Residual observation flagged, not acted on: the body's P11-only claim still disagrees with the remaining P09 in the frontmatter — a future registry-hygiene item, out of this SC's blessed scope.
- **Verification:** lint **0 — born clean** · validate 47 blocks, all judgment placeholders or pending-registry, **0 closed-axis** (L1 holds; NARRATIVE / HISTORICAL_NARRATIVE / INFORMAL_CASUAL) · coverage **40/40 explicit · 0 unanchored · 24 implied flagged · 19 reviewer ticks** (8 abstract-overlay warns, the Ruth class — CB codes have no surface form) · id-check **0 errors** across all five passes · concept-check **0 suggestions** · compile carried **exactly the declared 19 flag instances** (the §5 parenthetical parser holding) · board **189 + 1 skipped** · gold-diff 0 (Ruth fixtures byte-identical) · check-drift 0 · registries rebuilt from the edited vault worktree **byte-identical to the pins**.
- **Next:** Marcia's P08–P14 division ruling (the threshing floor · the gate · the sandal · the genealogy), then pericope by pericope; P07's graduation (fixtures + vault copy + status flip) when she calls it.

---

## SC-0050 — The P07 packet (the Ruth-remainder arc opens, deterministic half)
- **Date:** 2026-06-10
- **Decided by:** Marcia's sequencing ruling (report → Ruth; interleaving allowed — "they don't compete for your time until the report needs your blessing"). This is the interleaved deterministic half: the frozen referent set P07's fresh rebuild will be coverage-checked against.
- **Status:** APPLIED (PR #58, stacked on #57; Tier A by files-touched — a pinned source packet; evaluator verify + Marcia's merge word, in the ruled order report → Ruth).
- **Type:** pericope-spec entry + source-packet extraction/pin. **No schema change, no closed-list change, no registry change.**
- **The entry:** `pericopes.json` gains **P07 = Ruth 2:17–23** — the range settled by the June-2nd exegesis-blessed P07 build (her readings remain valid input for the rebuild); **P08–P14 deliberately stay out** pending her division ruling, and the file's note now says exactly that.
- **Verification:** 7 verses (2:17–23, no gaps) · 90 referents · proper nouns Boaz, Naomi, Ruth, YHWH; pinned by **file-bytes** sha; `check-drift` exit 0; board **189 + 1 skipped** untouched.
- **Process note (the data corrected the memory):** the first extraction attempt assumed P07 was already in `pericopes.json`; it never was (PROGRESS.md recorded this in May). Caught at the first command — re-confirming the standing rule: verify against the data, not the recollection. A second instance the same hour: the ledger insert asserted on SC-0049's entries, which live on the unmerged #57 branch — resolved by stacking (this branch now sits on #57, matching the ruled merge order).
- **Next:** post-report, the P07 **fresh rebuild** (Agent-2 v0.3, the pinned conventions, the global banks) with the June-2nd draft open beside it as exegetical reference; then Marcia's P08–P14 division ruling; then pericope by pericope.

---

## SC-0049 — The Jonah Generality Report (the held-out test, written up)
- **Date:** 2026-06-10
- **Decided by:** Marcia's sequencing ruling ("the generality report first — it's about the held-out test, which is complete — then the Ruth-remainder arc: P07 rebuilt fresh under the proven pipeline → the P08–P14 division ruling → pericope by pericope").
- **Status:** DRAFTED for Marcia's blessing (PR open; docs-only → Tier B mechanics: the evaluator verifies every number against the merged mains, Marcia blesses the content, then merge).
- **Type:** documentation — the JONAH-PLAN §2 Phase-3 named final deliverable. **No `_spec/` change, no pins, no code.**
- **The document (`docs/JONAH-GENERALITY-REPORT.md`):** §1 the question and the held-out method (by-layer pass conditions; the prize-finding framing); §2 the verdict by layer — L1 **held with zero values added**, the schema's one structural finding (the narrator-register constant) ruled/fixed/gated, L3 **born clean five for five**, the registry filled as designed with reuse proven at every scale (within-pericope, cross-pericope, and **cross-book: CB_0011 Hesed, P02 → J05**), L2 explicitly deferred (the skeletons' judgment half is the next experiment); §3 the numbers, each re-runnable (48/48 verses · 279/279 explicit referents, 0 unanchored book-wide · board 166 → 189+1 · the pin lineage, all reproducible-from-notes); §4 the second thesis — **five real pipeline defects only held-out drafting could find**, each now bite-gated; §5 honest limits (judgment half untested on Jonah; single judge; 2 of 4 genre groups exercised; small book); §6 the verdict — the supervision is schema-valid, version-stable, and drift-controlled **on held-out Scripture**.
- **Next (per the same ruling):** the Ruth-remainder arc — P07 **rebuilt fresh** under the proven pipeline (the June-2nd draft open beside it as exegetical reference; its rulings remain valid input, but the artifact format predates ~20 governed changes), then Marcia's P08–P14 division ruling, then pericope by pericope.

---

## SC-0048 — J05, the qiqayon: mapped, ruled, applied (the book of Jonah is fully mapped)
- **Date:** 2026-06-10
- **Decided by:** Marcia — "Rule A–E as laid out," on verified ground. **A** the four scenes (the anger and the reason for the flight · the question and the watcher · the three appointings · the second question and the argument the book ends inside) + the furious-prayer register: 4:2–3 → RELIGIOUS_WORSHIP (it is prayer, addressed by name — the register carries the address, not the mood); the two divine questions and the closing argument stay plain. **B** the name-weave — **single code with referential forms, as drafted**: chapter 4 alternates YHWH (4:2–4) → the doubled **YHWH-Elohim at the qiqayon's appointing (4:6 — the fusion hinge between the books' two names)** → ha-Elohim (4:7) → Elohim (4:8–9) → YHWH for the final word (4:10–11), all one speaker inside one continuous dialogue; the J04 split governed scene-coherent blocks, and fragmenting a single exchange across two codes would cut the speaker in half — the weave is carried as referential forms + prose, and stays available as a figure candidate. *(Interpretation note for the evaluator: her ruling named both conventions with "as laid out / as drafted" — applied as drafted.)* **C** the four cast codes: **B13-Worm** (ruled a being — an appointed creature that acts, the fish's precedent) · O11-Qiqayon (acted upon — object) · O12-East-Wind (a different wind on a different errand than J02's O3 storm-wind) · O13-Booth (his own shade, against God's). The sun stays prose (strikes once, unappointed). **D** reuses: B5 · **B12-Animals — the book's last word** · PL1 · **PL2-Tarshish, named again in the confession** · **CB_0011-Hesed + CB_0057-God-Relents, both at P4** — the creed verse pays the book's oldest debt (the J01 withheld motive) and completes the J04 concept in Jonah's own mouth. **E** the strong candidates minted (her emphasis: "the Appointing refrain is as strong as figure evidence gets in this book"): **FIG_0205-Appointing-Refrain** (*vayman* four times — the fish, the plant, the worm, the wind: the whole creation on God's errands; @P9/P12/P14 with the J02-P22 cross-reference safely parenthetical) · **FIG_0206-Anger-Question-Pair** (4:4 ↔ 4:9, the second sharpened to the plant) · **CB_0058-Compassion-Creed** (the Exodus-34 formula as a referred-to concept). *(Interpretation note: the E-ruling said "the candidates… as laid out" — the laid-out STRONG set of three is minted; the five optional stay unminted, carried in prose.)*
- **Status:** APPLIED (PR pair open — compiler + vault; merge on Marcia's word via `tools/merge_pr.py`, vault half `--require-merged`).
- **Born-clean note:** one lint finding on the first draft — the «and» inside my English number-rendering ("a hundred and twenty thousand"); the Hebrew (שְׁתֵּים־עֶשְׂרֵה רִבּוֹ, "twelve myriads") has no conjunction, so the honest fix was rewording my rendering, not a sign-off exception. Relint: 0.
- **Registries:** cast → `jonah.aliases.json` **aliases-0.1.6** (34 entities: 13 beings · 8 places · 13 things); canon banks → **0.6.0** (58 CB / 126 FIG); 6 reuse notes gain J05 — **CB_0011-Hesed's pericope list now runs P02 → J05**, the canon bank's longest-spanning concept.
- **Verification:** lint **0 — born clean** (post-reword); validate 44 blocks, all judgment placeholders, **0 closed-axis**; coverage **70/70 explicit · 0 unanchored · 26 implied** (matched 24; entities 10/13; the 3 unanchored are CB abstract overlays, the Ruth warn-class); id-check **1 clean / 0 findings**; concept-check **0 suggestions**; flags verified landing exactly as declared — **P4 carries the triple (CB_0011 + CB_0057 + CB_0058)**, FIG_0205 at the three appointings with no parenthetical leak (the SC-0047 fix holding); board **189 + 1 skipped**; check-drift exit 0.
- **The milestone:** with J05 applied, **all 48 verses of the book of Jonah are mapped** — five pericopes, every gate green, on a held-out book the interlingua was never tuned for. Across two books and three genres-in-use, the closed lists remain **byte-unchanged since Ruth**; the one structural assumption the whole exercise surfaced (the narrator-register constant) was found by the psalm, fixed by ruling, and is permanently gated.
- **Next:** the FM judgment halves (the five Jonah skeletons await the Slice-4 drafter or hand-authoring); J03/J04/J05 graduations when Marcia calls them; the by-layer generality report (the JONAH-PLAN's final deliverable) at the arc's close.

---

## SC-0047 — J04, Nineveh: mapped, ruled, applied (one pericope remains)
- **Date:** 2026-06-10
- **Decided by:** Marcia — A–E on the draft: **A** the 6-scene division (the fish's one verse · the second call · five words in the city · the people believe before the king hears · throne-to-ashes + the decree · God relents) + the three register shifts (ELDER_AUTHORITY for the renewed charge 3:2, matching the 1:2 ruling; FORMAL_OFFICIAL for the herald's oracle 3:4 and the royal decree 3:7–9). **B** the Elohim question — **the Ruth-style two-code split**: the narrator's switch to "God" (Elohim) in every Nineveh scene (3:5→3:10) becomes a distinct being **B9-God**, the insider/outsider naming pattern made structural; B9's note states the identity (the same God as B2-YHWH under the outsiders' name). **C** the five cast codes: B10-King-of-Nineveh (never named by the text) · B11-Nobles · B12-Animals (herd and flock, decreed into the fast) · O9-Sackcloth · O10-Ashes. **D** the reuses: B5-People-of-Nineveh steps onstage; B7-Great-Fish (the vomiter); PL1-Nineveh (entered at last); PL5-Dry-Land (the landing); CB_0051 at P16+P18; FIG_0196 at P3 — the word-formula's literal "a second time," the cross-pericope pair with J01's opening. **E** the firm mints: **CB_0057-God-Relents** (*nicham* — the book's theological pivot, P17+P19) and **FIG_0204-Not-Perish-Refrain** (the same hope in three pagan mouths across the book: 1:6, 1:14, 3:9); Man-and-Beast and the optional set (who-knows echo, greatest-to-least, the *ra'ah* wordplay, the "overthrown" ambiguity — the last carried un-flattened in the prose) stay unminted.
- **Status:** APPLIED (PR pair open — compiler + vault; merge on Marcia's word via `tools/merge_pr.py`, vault half `--require-merged`).
- **The folded parser fix:** my own compile output betrayed it ("flags carried 4" vs 3 declared) — the §5 flag-pointer parser scanned the whole line, so the cross-pericope parenthetical "pair with J01 Proposition 1" flagged **local** P1 (YHWH speaks to the fish) with FIG_0196: a wrong supervision signal. Pointers now stop at the first parenthetical (the §5 idiom: pointers before the paren, commentary inside). Bite test `tests/flag-pointer-parse.test.ts`; Ruth proven inert (gold-diff at baseline — no Ruth §5 parenthetical names propositions); the real J04 artifact verified: P1 flag-free, exactly the 6 declared instances landing where declared.
- **Registries:** cast → `jonah.aliases.json` **aliases-0.1.5** (30 entities: 12 beings · 8 places · 10 things); canon banks → **0.5.0** (57 CB / 124 FIG); the 6 reused notes gain J04 in their appearance lists. All rebuilt from the vault worktree; reproducible-from-notes maintained.
- **Verification:** lint **0 — born clean**; validate 54 blocks, all judgment placeholders, **0 closed-axis** (narrative under the v0.16 conditional — the teeth hold and are satisfied); coverage **67/67 explicit · 0 unanchored · 25 implied** (matched 30 — the Elohim tokens now anchor to B9; entities 12/14, the 2 unanchored are the CB abstract overlays, the Ruth warn-class); id-check **1 clean / 0 findings**; concept-check **0 suggestions**; board **189 + 1 skipped**; check-drift exit 0.
- **Next:** J05 (4:1–11 — the anger and the qiqayon; the held-breath pericope the whole book has been winding toward), then the book is mapped; the FM judgment halves; graduations when Marcia calls them.

---

## SC-0045 — J03, the psalm: mapped, ruled, applied (the experiment passes)
- **Date:** 2026-06-10
- **Decided by:** Marcia — the full ruling set on the draft: **A** the register finding → option (a), shipped as SC-0046; **B** genre = **PRAYER** (over HYMN_WORSHIP_SONG); **C** the 9 machine-enumerated cast entries approved; **D** the reuses confirmed (CB_0011 Hesed at 2:8; FIG_0198 at the descent's bottom) with "driven from before your eyes" (2:4) ruled **prose** — no CB_0052 cross-ref, no new code; **E** two new figures approved — the **Temple-Turn pair** (2:4 ↔ 2:7) and the **Drowning-Engulfment cluster** (2:5); the optional candidates (the vertical axis, the sailors-echo) stay unminted.
- **Status:** APPLIED (stacked on SC-0046; PR pair open — compiler + vault #19; the merge chain runs #52 → #53 → vault #18 → this → vault #19, each via `tools/merge_pr.py`, the vault halves with `--require-merged`).
- **Type:** map authoring (the first POETIC_SUNG artifact) + cast registration (vault-first) + canon-registry growth + the SC-0046 schema fix exercised. **No further schema change.**
- **The headline:** the corpus's first non-narrative artifact validates **closed-axis clean** under v0.16 — POETIC_SUNG / PRAYER / pericope-level RELIGIOUS_WORSHIP (the ruled division has no narrator voice; the one narrative word riding Heb 2:3 is named in §1, not hidden). Every scene heading carries the pinned `; Heb` qualifier (English 2:2–9 = Hebrew 2:3–10 — divergent on every verse); scene ranges land Hebrew, prop anchors English, exactly per the SC-0044 convention.
- **Cast (vault `bcd/jonah/`, the SC-0041 source of truth):** B8-Idol-Keepers · PL6-Sheol · PL7-Temple · PL8-Pit · O7-Deep · O8-Weeds; `jonah.aliases.json` rebuilt from the vault notes → **aliases-0.1.4** (24 entities: 8 beings · 8 places · 8 things).
- **Canon codes (vault-notes-first; registries → 0.4.0, 56 CB / 123 FIG):** CB_0055-Vain-Idols · CB_0056-Salvation-Belongs-to-YHWH · FIG_0202-Temple-Turn-Pair · FIG_0203-Drowning-Engulfment. **The cross-book moment: CB_0011 Hesed now reads `appears_in: [JONAH, RUTH]`** — the first concept the canon bank anchors across two books (Ruth's own note gains J03 in its pericope list); FIG_0198 Going-Down-Descent gains J03 at its lowest point.
- **Verification:** lint **0 findings — a poem, born clean** under the atomic-bare-plain discipline; validate **35 blocks, all judgment placeholders, 0 closed-axis**; coverage **43/43 explicit · 0 unanchored · 15 implied** (entities 9/12 anchored; the 3 unanchored are the CB abstract overlays, the Ruth warn-class); id-check **1 clean / 0 findings**; concept-check **0 suggestions** (all four new codes genuinely new); board **188 + 1 skipped**; check-drift exit 0 on the three re-pins.
- **The generality verdict, three pericopes in:** narrative held twice; poetry held once the one structural assumption (the narrator-register constant) was found and fixed — and that fix is now a four-case permanent gate. The closed lists have not grown by a single value across two books and three genres-in-use.
- **Next:** the FM skeletons' judgment halves (the Slice-4 drafter, or hand-authoring); J04–J05 (narrative again — the machinery is proven); J03 graduation when Marcia calls it.

---

## SC-0046 — Genre-aware pericope register (the first real L1 finding, fixed)
- **Date:** 2026-06-10
- **Decided by:** Marcia — option (a), genre-aware, concurring with the Architect: "the narrator constant keeps its teeth for NARRATIVE pericopes; the field opens to the closed seven for non-narrative groups. Option (c) would force a false register onto a narrator-less prayer — the single kind of error this pipeline exists to prevent." A governed schema change, cleanly its own SC.
- **The finding (J03, the psalm — the JONAH-PLAN's "prize finding"):** drafting the first POETIC_SUNG pericope surfaced that the pinned schema's `pericope_classification.register` was **`const INFORMAL_CASUAL`** — the narrator default baked in as a structural constant, invisible across six Ruth and two Jonah pericopes because all were narrative. Marcia's psalm division (purely the poem) produced the first pericope with **no narrator voice**: a prayer addressed to YHWH throughout cannot truthfully carry the narrator's register, and the schema blocked the true one. The precision of the result matters: the closed LISTS held (all three values the psalm needs exist — nothing was minted); the gap was one structural assumption.
- **Status:** APPLIED (stacked on PR #52; PR pair open — compiler + vault #18; merge order #52 → this → #18, the vault half with `--require-merged`).
- **Type:** governed schema change. `validation-rules.json` **v0.15 → v0.16** ($id bumped, supersedes-history line added, re-pinned by file-bytes sha); vault writeback in the pair. No closed-list change — REGISTER stays the same 7.
- **The change:** `register: {const: INFORMAL_CASUAL}` → `register: {$ref: #/$defs/register_value}` (the closed 7) **plus** an `allOf` conditional on `pericope_classification`: `if genre_group == NARRATIVE then register == INFORMAL_CASUAL`. The narrator hard-rule survives exactly where narrators live; a non-narrative pericope carries its true register; membership is enforced everywhere.
- **Proven (4 cases, now the permanent gate `tests/register-genre-aware.test.ts`):** (1) Ruth P01 as-is — green, floor unchanged; (2) NARRATIVE + RELIGIOUS_WORSHIP — **blocks** (the teeth); (3) POETIC_SUNG + RELIGIOUS_WORSHIP — the register axis **clears** (the psalm's blocker 1 → 0; its remaining 54 blocks are all judgment TODOs); (4) POETIC_SUNG + FAKE_REGISTER — **blocks** (membership unconditional).
- **Folded rider (the evaluator's PR-#52 catch):** the graduated J02 map's `status: draft → complete` (the J01 idiom; `sta-status: pending` stays true). Compiler side landed on PR #52; **the vault-copy flip rides this SC's vault PR #18**, so the byte-identity guard re-closes the moment both halves merge — pair coherence diff-proven before the PRs opened.
- **Verification:** board **188 + 1 skipped** (the 175 floor + the 9 J02-anchor tests + these 4); `check-drift` exit 0 at v0.16; `check-drift --vault <worktree>` green (vendored ≡ the vault-PR canonical); J01/J02 anchors unaffected (the narrative path is byte-equivalent).
- **Next:** J03's apply-cycle (SC-0045) on Marcia's B–E rulings — genre (PRAYER vs HYMN_WORSHIP_SONG), the 9-entity cast, the reuse confirms (CB_0011 Hesed — the first Ruth concept recurring in Jonah; FIG_0198's bottom; the CB_0052 cross-ref question on 2:4), and the figure candidates.

---

## SC-0044 — J02 graduation + the pinned versification convention (the pre-J03 riders)
- **Date:** 2026-06-10
- **Decided by:** Marcia — both riders ruled in the 2026-06-10 adopt-package: the mechanical J02-graduation PR, and "pin the verse_anchor-Eng/verse_range-Heb convention" before J03 (where English and Hebrew diverge on every verse).
- **Status:** APPLIED (PR open; merge via `tools/merge_pr.py` on Marcia's call after the evaluator's verify).
- **Type:** fixtures promotion + a regression anchor. **No `_spec/` change, no pins, no schema, no vault change** (the vault copy was already byte-identical; the byte-identity guard's J02 pointer flips from `_working` to fixtures).
- **The move:** `_working/J02/J02-Jonah-1-4-2-1.md` → `fixtures/meaning-map/` (git-mv, zero content edits — unlike J01's graduation, no metadata was stale: the SC-0040 cycle wrote the map post-rulings). The FM skeleton + COMPILATION-LOG stay in `_working/J02/` until the judgment half is authored (the Option-1 pattern, Marcia-ruled at SC-0038).
- **The anchor (`tests/jonah-j02.test.ts`, 9 tests):** the J01-anchor pattern plus the convention pin — (a) L1 closed-list membership, including the three ruled register overrides (ELDER_AUTHORITY · CONSULTATIVE · RELIGIOUS_WORSHIP) as members; (b) **the versification convention, asserted where it diverges** — scene `verse_range` is HEBREW (S5 = `2:1-2`; coverage reconciles against the Hebrew-versified packet) while prop `verse_anchor`s are ENGLISH (P22 = `1:17`, P24 = `2:1`); (c) zero-closed-axis blocks, gap inventory pinned at 54; (d) coverage 79/79 · 0 unanchored · 36 implied; (e) book-aware id-check fully clean (TH_ = the known unverifiable-accepted class); (f) CB_0052-54 + FIG_0197-0201 registered with `appears_in: JONAH`; (g) concept-check quiet; (h) lint = 0 drift + exactly the one signed-off TIME_PAIR.
- **Bite proven (the discriminating-test bar):** a planted fake register fails the two closed-list guards; deleting the `; Heb` qualifier from the S5 heading fails the convention pin AND coverage (Heb 2:2 falls outside every scene) — four failures, each caught by the assert built for it; restored byte-identical, 9/9.
- **Verification:** board **184 passed + 1 skipped** (the 175 floor + 9); vault ≡ fixtures for both Jonah maps; Ruth untouched.
- **Why now:** J03 (the psalm, Eng 2:2–9 = Heb 2:3–10) diverges on EVERY verse — the convention this pins is the rail J03's headings, anchors, and coverage run on.

---

## SC-0043 — The external review portal (Tier B; the Portal Builder's first ship)
- **Date:** 2026-06-10
- **Decided by:** Marcia — the Option-C portal ruling + the risk-tiered-verification adoption (both 2026-06-10). Built by the **Portal Builder** (the third role); SC number claimed after checking `gh pr list` (SC-0042 = open PR #50, the merge-discipline tool) + this ledger.
- **Status:** MERGED (PR #51 → main `23b1e9f`, 2026-06-10, under the recorded conditional green; **Tier B**) — **LIVE 2026-06-11** (Marcia activated `.github/workflows/portal.yml`; Pages enabled `build_type: workflow`; deploy runs green; 8 pericopes / 20 approved artifacts) — **Google Form connected** by the follow-up PR (formBase + 4 `entry.N` ids in `portal.config.json`; form verified publicly reachable, prefill verified)
- **Type:** other (derived read-only artifact — no spec, schema, vocabulary, registry, or fixture change)
- **Summary:** Read-only GitHub Pages site where external reviewers without GitHub accounts read every approved pericope artifact (map + FOR_MODEL + compilation log) and feed back through a prefilled Google Form; the approved-only rule is a failing build test.
- **Rationale:** External reviewers (translation consultants, community checkers) have no GitHub and must never need write access. Publishing the blessed copies read-only, with feedback routed through a form into the normal ruling → Architect → gates pipeline, gets review traffic without opening any edit surface on canon.
- **Spec change (exact):** none. New files only: `portal/` (self-contained static-site builder: gate + reader + wikilink resolver + renderer + 25 tests; deps `marked`+`yaml`, lockfile-pinned; no imports from compiler `src/`), `portal/ci/portal.yml` (the deploy workflow — build+test+deploy on push to `main` only; ships outside `.github/` because the local tokens lack the `workflow` OAuth scope; one-time web-UI activation step in its header), `vitest.config.ts` (root: adds `portal/**` to vitest's default excludes so the compiler board doesn't discover the portal's node:test files — board proven identical before/after at claim time (174 + 1 skipped, 12/12 files) and re-proven after the SC-0042 rebase at the new floor (175 + 1 skipped, 13/13 files; the merge-discipline gate runs untouched)), this ledger entry.
- **Artifact migration:** none. Day-one content by the status filter alone: Ruth P01–P06 (map `complete` + FOR_MODEL/CL `valid`) + Jonah J01 (map only; FOR_MODEL section renders "not yet authored"). J02 appears automatically when its Tier-A graduation PR merges.
- **Validator impact:** none on the compiler validator. The portal adds its own independent gate: every rendered artifact must sit (real-path, symlinks resolved) inside its `fixtures/` class directory AND carry the approved status (`complete` / `valid`) — any violation kills the whole build (exit 2, nothing written, all violations listed). Proven by end-to-end bite tests (planted draft, wrong status, missing status, wrong type, unparseable frontmatter, smuggling symlink).
- **Version:** no spec version touched; no pins moved.
- **Verification (Tier B light pass):** portal tests 25/25 locally (bite suite + units + real-tree smoke); real-tree build → 7 pericopes / 19 approved artifacts, zero unresolved `[[wikilinks]]` in output, Hebrew tooltips present, zero client JS. Evaluator bar per the kickoff: scope-check (no Tier-A paths in the diff) + plant-a-draft bite confirmation + smoke.

## SC-0042 — Promote the merge-discipline rule (documented → enforced)
- **Date:** 2026-06-10
- **Decided by:** Marcia — "Promote the merge-discipline rule now" (the recurring-error discipline's promotion ruling: the class hit its threshold). The class, two instances: (1) 2026-06-01 — a branch deleted before its PR was verified MERGED; (2) 2026-06-10, the SC-0041 merge — `gh pr merge && git push --delete` chained in one command; the merge transiently failed ("not mergeable" while GitHub recomputed mergeability seconds after a push to the open PR), the delete ran anyway and closed the PR, and the paired vault merge proceeded out of rhythm order (vault before compiler). Recovered same-session (re-push + reopen + merge-after-MERGEABLE); the recovery steps are now the tool's state machine.
- **Status:** APPLIED (PR open; merge on Marcia's call after the evaluator's verify).
- **Type:** process tooling + gate (the SC-0034 class). **No `_spec/` change, no pins, no schema, no spec-version bump.**
- **The tool (`tools/merge_pr.py`):** one command owns the merge rhythm and cannot violate it — wait-for-MERGEABLE before merging (UNKNOWN polls, CONFLICTING aborts with nothing done) → merge (never gh's own `--delete-branch`) → **re-read the PR until state == MERGED with the merge sha in hand** → only then delete the head branch; `--require-merged owner/repo#N` makes the pair order (compiler first, vault follows) a checked precondition rather than a remembered one. Aborts are side-effect-free past the abort point and say so explicitly ("NO branch deletion attempted").
- **The gate (`tests/merge-discipline.test.ts` + `npm run check:merge-discipline`):** the offline self-test replays 6 state-machine scenarios with a scripted fake gh — T1 happy-path (delete strictly after verified MERGED), **T2 the literal SC-0041 trap** (merge fails → abort, no delete), T3 UNKNOWN-recompute (waits, then proceeds), T4 CONFLICTING (aborts before merging), T5 the pair-order guard (prerequisite not MERGED → exit 2, target untouched), **T6 the core rule** (merge command "succeeds" but the PR never reads MERGED → abort, no delete). Wired into `npm test` with the visible-skip-without-python3 idiom (SC-0008/0034) — never a silent green.
- **Verification:** self-test ALL PASS (6/6); board **175 passed + 1 skipped** (the 174 floor + this gate); typecheck clean; no fixture, registry, or spec diffs.
- **Bootstrap note:** this SC's own PR is the first to be merged under the rule — run `python3 tools/merge_pr.py <PR#> --delete-branch` from the branch (the tool needs only gh + the remote PR, not a merged copy of itself).
- **Tally:** the verify-MERGED-before-delete class closes as **PROMOTED** (documented → enforced); recorded in the memory register.

---

## SC-0041 — The Jonah writeback: book folders inside this vault
- **Date:** 2026-06-10
- **Decided by:** Marcia — the book-2 vault-home ruling ("book folders inside this vault"; the evaluator recorded it and set the expected-scope list as the acceptance contract). Closes the SC-0037 amendment-2 deferred item: "where does book #2 live in a Ruth-named vault."
- **Status:** APPLIED (PR pair: compiler #49 + vault #17; merged on Marcia's call after the evaluator's remote verify).
- **Type:** vault structure (writeback) + registry source-of-truth handover. **No `validation-rules.json` change, no closed-list change, no pin change, no spec-version bump** — the one acceptance-critical invariant is that `jonah.aliases.json` does NOT move.
- **Vault gains (PR #17):** `pericopes/jonah/` with the two blessed maps — J01 (from `fixtures/meaning-map/`) and J02 (from `_working/J02/`), **byte-identical copies, shas proven on the remote refs**; Ruth's six stay flat in `pericopes/`, untouched. `bcd/jonah/{beings,places,objects}/` — the 18-entity Jonah cast comes home, book-foldered ("book folders" applied literally; also necessary — both books use the same code series, so a flat merge would put Ruth's B1-Judges and Jonah's B1-Jonah in one folder). Ruth's `bcd/` grandfathered flat.
- **Compiler side (PR #49):** `_spec/registry/jonah-bcd/` **retired** (18 files; grep-verified zero runtime consumers — the alias builder takes `--bcd` as an argument). **The vault is now the cast's source of truth** — the SC-0037 CB/FIG pattern applied to the cast. Rebuild recipe: `build_aliases.py --book jonah --bcd <vault>/bcd/jonah`.
- **Acceptance, proven (both by the builder pre-PR and the evaluator on the remote):** the pinned `jonah.aliases.json` rebuilds **byte-identical from the relocated vault notes** — pin **aliases-0.1.3 unchanged**; all 20 moved/copied files sha-identical across the repos; board 174 + 1 skipped; check-drift exit 0; Ruth byte-at-baseline. **The byte-identity guard now covers Jonah:** vault `pericopes/jonah/*` ≡ the compiler's blessed copies (J01 ↔ fixtures; J02 ↔ `_working` until its graduation).
- **Deliberately NOT in scope:** the J0x FOR_MODEL skeletons + COMPILATION-LOGs (unfinished machine artifacts — no vault `stas/` entries until their judgment half is authored); J02's graduation (a separate later call, the J01 pattern); machine-only artifacts (BHSA packets, `pericopes.jonah.json`, pins) stay compiler-side.
- **Process note:** this chronological entry was added at the evaluator's pre-merge catch — the original PR carried only the allocation-ledger row (the house norm is both). The recurrence class: ledger-completeness checks belong in the pre-PR sweep.

---

## SC-0040 — J02 (the storm): map authored, ruled, applied
- **Date:** 2026-06-10
- **Decided by:** Marcia — J02-before-the-psalm ("the storm grows the Jonah cast on familiar narrative ground"), then all four ruling groups on the draft: **A** the map (scene division, absences, prose) + the "three days and three nights" **TIME_PAIR** + the three moment-level register shifts (ELDER_AUTHORITY v.6 · CONSULTATIVE v.7/v.11 · RELIGIOUS_WORSHIP v.14); **B** the 12-entity cast as proposed, **fish = being**; **C** the three cross-book reuses (CB_0052 · FIG_0197 · FIG_0198 continue into J02); **D** the five new canon codes. Builder drafted as Agent-2 v0.3; evaluator verifies the application against her words before the merge call.
- **Status:** APPLIED (PR pair open — compiler + vault #16; merge gated on the evaluator's verify + Marcia's word).
- **Type:** map authoring + cast registration (aliases re-pin) + canon-registry growth (CB/FIG re-pin, vault-notes-first) + a lint-exception sign-off + reader generality fixes. **No `validation-rules.json` change, no closed-list change, no spec-version bump.**
- **The by-layer read (the result):** **L1 closed lists HELD on the storm** — genre/genre-group/register validate; the three register shifts all live inside the closed 7; zero blocks on any closed axis. L2 grows by design (24 propositions, judgment gaps awaiting the drafter). The registry grew exactly as a new book should: 8 cast entities (B6-Captain, B7-Great-Fish, PL4-Sea, PL5-Dry-Land, O3-Wind, O4-Storm, O5-Cargo, O6-Lots → `jonah.aliases.json` **aliases-0.1.3**, 18 entities) + 5 canon codes (CB_0053-Fear-of-YHWH, CB_0054-Innocent-Blood, FIG_0199-Hurling-Refrain, FIG_0200-Fear-Escalation, FIG_0201-Captain-Echoes-the-Command → global registries **0.3.0**, 54 CB / 121 FIG, **vault notes first** so reproducible-from-notes holds; the five SC-0037 Jonah notes settle PROVISIONAL→REGISTERED, their stated condition met at SC-0038).
- **Application notes for the evaluator (interpretation of the approved table):** "this evil" (offered CB-or-plain-prose; group D omitted it) → **plain prose** — its content lives in P8/P10 Q&A + §3E, the §3C entry removed (relocate-never-delete); "the sailors' gods" (proposed no-code) → prose mention in §3E + P3/P7 Q&A, the §3A entry removed; the duration's open code-class → **`TH_THREE_DAYS_AND_THREE_NIGHTS`** (the Ruth `TH_` duration precedent — unverifiable-by-registry, the accepted class); the three **optional** figure candidates (the *gadol* greatness chain · "the sea kept growing stormier" · the promise↔fulfillment calm pair) were flagged-only and are **not minted**; `lint-exceptions.json` header `book: RUTH→CANON` (truth-fix; the engine never reads it).
- **Reader generality fixes (code-only; the storm surfaced them):** (1) scene headings accept chapter-qualified + cross-chapter ranges and an explicit **`; Heb c:v–v` qualifier** where English/Hebrew versification diverge (J02 S5 = "(1:17–2:1; Heb 2:1–2)"; **J03/J04 headings will need it throughout**); (2) the proposition-anchor strip is book-general (the old regex stripped only "Ruth" — a Jonah map's anchors captured the word "Jonah" and lost the [Scene N] tags; latent in J01's skeleton since SC-0036, the J01 anchor test re-pinned 23→18 **deliberately** with the cause recorded); (3) id-check pairs by `-FOR-MODEL.md` suffix (a bare prefix-find grabbed the COMPILATION-LOG — second instance of the SC-0038 coverage class).
- **Verification:** lint **0 drift · 1 accepted (TIME_PAIR ✓ signed off)** · validate 54 blocks, all judgment placeholders, **0 closed-axis** · coverage **79/79 explicit · 0 unanchored** (matched 51; entities 12/16 anchored; 4 abstract-overlay warns = the Ruth asymmetry) · id-check **1 clean / 0 findings** (2 `TH_` unverifiable, accepted class) · concept-check **0 suggestions** (Fear-of-YHWH↔Hand-of-YHWH = 0.33 < 0.34 — the documented threshold-calibration case held) · board **174 + 1 skipped** · check-drift + check-drift:vault exit 0 · Ruth byte-at-baseline.
- **Not done (deliberate):** the J02 map stays in `_working/` — graduation (map → fixtures + anchor-test extension) is a separate later call, the J01 pattern; the agent-2 prompt's scene-heading guidance gains the `; Heb` form at the next prompt refresh (vault rider, noted).

---

## SC-0039 — Jonah Phase 3 setup: the ruled J02–J05 division + pinned packets
- **Date:** 2026-06-10
- **Decided by:** Marcia — the pericope-division ruling (the §5.1 sub-call from `docs/JONAH-PLAN.md`): **the psalm stands alone.** Her criteria, applied strictly: "purely the poem" (no narrative frame verse inside the psalm pericope) and "the swallowing verse is narrative and sits naturally as the storm's resolution." With tiling-completeness those two criteria **determine** the remaining boundaries: the one-line prayer-intro (Heb 2:2 = Eng 2:1) has no home but J02's tail; the release/vomit (Heb 2:11 = Eng 2:10) has no home but J04's head.
- **Status:** APPLIED (compiler-only; PR open — evaluator verify, then Marcia's merge call).
- **Type:** pericope-spec ruling + source-packet extraction/pins. **No schema change, no closed-list change, no spec-version bump, no registry change.**
- **The tiling (Heb drives the extractor; Eng is the `bcv` label):** J02 = Jonah 1:4–2:1 (Heb 1:4–2:2, 15 vv — storm, sailors, swallowing, prayer-intro) · **J03 = Jonah 2:2–9 (Heb 2:3–2:10, 8 vv — the poem only)** · J04 = Jonah 2:10–3:10 (Heb 2:11–3:10, 11 vv — release + Nineveh) · J05 = Jonah 4:1–11 (11 vv). With J01 (3 vv): **48 verses, the whole MT book, union exact, zero overlaps** — verified from the extracted packets, not the plan. First cross-chapter ranges in the project; the extractor's chapter-major tuple comparison verified correct.
- **Verification against the data (the `Jona` lesson):** boundary content — J02's final verse carries the "pray … from the bowels of the fish" glosses (the ruled placement); J03 opens "I called" (Heb 2:3) and closes "salvation belongs to YHWH" (Heb 2:10); J04 opens at the vomit (Heb 2:11). Cast sanity per packet — J02 mariners + chief-sailor + the fish (166 referents); J03 the psalm vocabulary incl. Sheol + the temple/palace (84); J04 Nineveh + the king (111); J05 the qiqayon + the worm + the east wind (125). Four spot-check misses during verification were all probe-term errors (BHSA glosses: "chief + sailor", "palace", "qiqayon"), each re-verified against the actual glosses — the data was right every time.
- **Pins:** `source/jonah/J02–J05.json` added to `pins.json` `sources` (extractor-0.1.0/bhsa-2021; sha256 of the **file bytes** — the builder-stdout body-sha trap avoided again). `check-drift` exit 0; `check-drift:vault` exit 0 (source pins are compiler-only, not vault-compared).
- **Rider:** the SC-0038 allocation-ledger row updated APPLIED→MERGED (`c546926`), per the post-merge touch-up noted at ship time.
- **Next (the mapping, Marcia's):** J02 or the psalm first — her order. Each map runs the J01 pipeline: she authors (Agent-2 v0.3 discipline) → compile → the gate board → the by-layer read. **J03 is the genre/register stress test** — the first non-narrative genre to hit the closed lists; a value the poem genuinely needs that isn't in GENRE/GENRE_GROUP/REGISTER would be the first forced-L1 finding (a governed addition, its own SC — that's the experiment).

---

## SC-0038 — J01 graduation: the map into fixtures + the generality regression anchor
- **Date:** 2026-06-09
- **Decided by:** Marcia — two rulings. (1) The **exegetical confirm**: all 5 Jonah codes are **canon-new** (CB_0051 Evil-of-Nineveh · CB_0052 Before-the-Face-of-YHWH · FIG_0196 Word-of-YHWH-Came-Formula · FIG_0197 Away-from-Before-YHWH-Refrain · FIG_0198 Going-Down-Descent); the one near pair examined and held distinct — CB_0052 (*panim*, the presence fled FROM) ≠ Ruth's CB_0037 (*kanaph*, the wing-refuge fled TO). (2) The **graduation shape**: Option 1 — *map now, FOR_MODEL later* — after the builder surfaced (and the evaluator independently reproduced) that the J01 FOR_MODEL is an unfilled deterministic skeleton (23 schema blocks + 5 lint findings, all on `__TODO__` judgment fields, zero on closed lists), so it cannot enter the gold set clean; both Claudes recommended Option 1.
- **Status:** APPLIED (compiler-only; PR open — evaluator verify, then Marcia's merge call).
- **Type:** fixtures promotion + registry/BCD (cast-name alignment, aliases re-pin) + book-aware CLI plumbing + a new regression test. **No `validation-rules.json` change, no closed-list change, no spec-version bump.**
- **The move:** `_working/J01/J01-Jonah-1-1-3.md` → `fixtures/meaning-map/` (git-mv), with exactly 2 declared edits, both metadata-truth: frontmatter `status: "draft"→"complete"` (the map is blessed — SC-0036; `sta-status: "pending"` stays TRUE), and the §5 PROVISIONAL blockquote removed (false since SC-0037 registered the codes; an SC-ID process leak the SC-0017 de-leak rule forbids in content artifacts). De-leak grep zero on the graduated map; the regenerated skeleton from the edited map is **JSON-identical** to the `_working` FM (the edits are compile-neutral). The FM skeleton + COMPILATION-LOG stay in `_working/J01/` until the judgment half is authored (or the Slice-4 drafter lands).
- **The anchor (`tests/jonah-j01.test.ts`, 8 tests):** the generality result, mechanized — (a) J01's classification values are **members of the pinned closed lists**; (b) validating the regenerated skeleton yields blocks **only** on the known judgment-field paths, **zero** on `pericope_classification`/`speech_act`, pinned at 23; (c) coverage vs the frozen BHSA packet: **20/20 explicit · 0 unanchored · 7 implied**; (d) book-aware id-check map↔skeleton **fully clean**; (e) CB_0051/52 + FIG_0196-198 registered in the global banks with `appears_in: JONAH` (the confirm, made durable); (f) `suggestReuse` quiet for Jonah (both kinds); (g) map lint 0. **Bite proven:** corrupting the fixture's register to a non-member fails exactly the two generality assertions and nothing else (the evaluator's discrimination bar).
- **Cast-name alignment (the one content-adjacent finding):** id-check surfaced 4 name-binding errors — the bootstrapped Jonah cast notes carried lowercase natural-language glosses ("the ship's crew", "the people of Nineveh", "ship", "fare") where the Ruth idiom (and the map's approved wikilink slugs) use bare Capitalized names. Aligned the 4 `english:` fields (Ship Crew · People of Nineveh · Ship · Fare — Hebrew untouched), rebuilt via `build_aliases.py --book jonah --bcd _spec/registry/jonah-bcd`, **re-pinned `jonah.aliases.json` aliases-0.1.1 → 0.1.2** (sha of the FILE, per the measurement-trap rule — the builder's stdout sha is the body-only trap, hit live again this SC). Marcia may overrule any of the 4 names; the diff is in the PR.
- **Book-aware plumbing (4 contained fixes, all code-only):** (1) `id-check` derives each map's alias book from its `bcv` frontmatter (explicit `--book` forces one book for all; single-word book names cover the pilot) — a Jonah map no longer silently resolves against Ruth's table; under `--corpus` an unpaired map (no FOR_MODEL yet) is a **visible skip** per the command's own documented contract, while an explicitly-passed unpaired map still errors. (2) `coverage` accepts `J01`-style pericope ids (`^P\d+$` → `^[A-Za-z]\d+$`) and resolves the FM note by `-FOR-MODEL.md` suffix (the bare-`.md` prefix-find had grabbed `J01-COMPILATION-LOG.md` from a mixed dir — found by measurement when coverage read 9/20; correct artifact reads 20/20). (3) `gold-diff` skips a map-only fixture **visibly** (J01 prints as skipped; the Ruth baseline array is unchanged). (4) Two stale engine messages corrected (`ruth.aliases.json` → the actual per-book table; `ruth.concepts/figures.json` → the global bank names, retired in SC-0037).
- **Verification:** board **174 passed + 1 skipped** (the 166 floor + the 8 new); `validate fixtures/for-model/` 6/6 0-block; `lint --corpus` 13 artifacts · 0 findings · 7 accepted; `id-check --corpus` 6 clean + J01 visibly skipped, exit 0; `coverage --corpus` 245/245 · 0 unanchored; `coverage J01 --book jonah` 20/20 · 0 unanchored; `gold-diff` P01-P06 at baseline (J01 visibly skipped, baseline file untouched); `check-drift` + `check-drift:vault` exit 0 on the re-pin; `concept-check` 0 suggestions.
- **Named-deferred (not lost):** (a) a Jonah COMPILATION-LOG / VERIFICATION-INPUT **cannot validate yet** — `compilation-log.schema.json` `pericope_id` and the VI patterns still pin `^P\d{2}$`; widening is an SC-0032-class governed schema change (version bump + re-pin + vault writeback), owed when a Jonah FM/CL graduates, deliberately not smuggled into this SC. (b) The Jonah **cast-home** (SC-0037 amendment 2) — unchanged, still compiler-side. (c) **Vault writeback of the graduated J01 map is blocked on the book-2-home ruling** (where does Jonah live in a Ruth-named vault?); until ruled, the fixtures≡vault byte-identity invariant remains Ruth-scoped (P01–P06), explicitly not extended to J01.

---

## SC-0037 — Global Concept Bank + Figure Registry (canon-wide CB/FIG)
- **Date:** 2026-06-09
- **Decided by:** Marcia ruled the architecture — the Concept Bank AND the Figure Registry are global/canon-wide (one code per concept/figure across the whole Bible). Plan-first (`docs/SC-0037-PROPOSAL.md`), evaluator-reviewed; three amendments folded at approval (move+renumber, cast-home deferred-and-named, vendored-vs-notes split). Surfaced by the J01 collision (SC-0036 restarted Jonah at `CB_0001`/`FIG_0001`, colliding with Ruth).
- **Status:** SHIPPED. Compiler core = **PR #44 MERGED** (`origin/main` `f64a322`). Vault pass (this entry) = a compiler PR (builder global-mode + retire the duplicate Jonah CB/FIG notes + this record) **+ a vault PR** (Jonah's renumbered CB/FIG notes homed in the canon bank).
- **Type:** registry architecture (per-book → canon-wide CB/FIG) + a new diagnostic verifier. **No `validation-rules.json` change, no closed-list change, no spec-version bump** (the `CB_####`/`FIG_####` patterns are book-agnostic in the schema — confirmed; only the engine/readers resolved a book-specific registry).
- **The model:** one global `registry/concepts.json` (52) + `figures.json` (118). **Ruth is the immutable base** — its `CB_0001-0050` / FIG (115, max `0195`, with retired-slot gaps) keep their exact codes (in 6 shipped pericopes); a new book **continues from the max** and **reuses** an existing code for a recurring concept. Per-entry `appears_in` (book tags). **Jonah renumbered:** `CB_0001/2`→`CB_0051/52`, `FIG_0001-3`→`FIG_0196-198` (Marcia's read, builder + verifier agree: all five genuinely new). The per-book **CAST** (B/PL/O/TM/I — the `*.aliases.json` tables) stays per-book (Jonah's cast ≠ Ruth's).
- **Code:** readers `loadConceptRegistry()/loadFigureRegistry()` drop the book param → global path — which **fixes a latent bug** (`id-align` loaded with no book → defaulted every book to *Ruth's* registry). J01 map/FOR_MODEL/BCD-notes renumbered. The 4 per-book `*.{concepts,figures}.json` + their pins retired; `concepts.json`/`figures.json` pinned `0.2.0`.
- **New verifier — `tripod concept-check`** (the cross-canon consistency check / new-book guard, `src/engine/concept-check.ts` + 2 tests): when a book introduces a concept/figure resembling one the canon bank already has, surface the suggested reuse (token-set similarity over name_slug+aliases; diagnostic — the human rules). Reference-integrity stays `id-check` (now global). Demonstrated: a planted Jonah "Famine" → `⚠ resembles CB_0028 (Famine) [RUTH]`; real J01 → 0 suggestions.
- **Vault pass (this entry):** homed Jonah's 5 renumbered CB/FIG **notes** in the vault canon `concepts/`+`figures/` (with `appears-in: [J01]`); finalized `build_concept_figure_registry.py` to **global mode** — harvest the canon notes → the two global files, `appears_in` book-mapped from each note's `appears-in` pericope list (`PERICOPE_BOOK`: P→RUTH, J→JONAH). **The pinned global registry rebuilds byte-identical from the vault notes** (the vendored↔notes link restored — the merge-built bootstrap is now reproducible). Retired the compiler-side Jonah CB/FIG notes (`_spec/registry/jonah-bcd/{concepts,figures}/`) so there is one source. *(The reproducibility check earned its keep: it caught 3 Jonah notes whose glosses had embedded double-quotes → invalid YAML → silently skipped; fixed.)*
- **Deferred + named:** the Jonah **CAST** notes' vault home (`_spec/registry/jonah-bcd/{beings,places,objects}` is still compiler-side, parallel to Ruth's vault-canonical cast) — the same "where does book #2 live in a Ruth-named vault" question; deferred, not lost.
- **Verification (acceptance bar):** Ruth board **byte-identical** (gold-diff 0; lint/coverage 6/6/id-check --corpus all green — Ruth's CB/FIG resolve vs the global) · codes canon-unique · J01 lint-0 / coverage 20/20 · 0-unanchored · **`concept-check` suggests-reuse on a planted near-dup** · 166 tests + 1 skipped · check-drift 0 · the registry reproduces byte-identical from the vault notes.
- **Sequencing:** with the vault pass landed, **J01 may now graduate** from `_working/` into the Jonah fixtures with its final, locked global codes (the next step). Until merged, Jonah's codes remain provisional.

---

## SC-0036 — Jonah Phase 2 (J01): apply the meaning-map rulings + bootstrap the Jonah registry from the approved cast
- **Date:** 2026-06-09
- **Decided by:** Marcia ruled all five J01 calls (genre; v.2 register shift; scene division; the cast; cross-refs out of L3). Architect applies; evaluator verifies. The first end-to-end Jonah pericope — the generality test in practice.
- **Status:** APPLIED (compiler — map + registry + re-pin + governance) 2026-06-09. **Prompt fix (ruling 5) = a human-gated vault writeback** (the v0.3 agent-2 prompt is vault-only). The Jonah BCD source lives at `_spec/registry/jonah-bcd/` (compiler-side bootstrap; its canonical vault home for a second book is an open structure question — the alias table rebuilds identically wherever it's homed).
- **Type:** map authoring (draft) + registry population (re-pin) + a prompt fix. **No `validation-rules.json` change, no closed-list change, no spec-version bump.**
- **The by-layer generality read (the headline):** Jonah 1:1-3 maps with the existing interlingua. **L1 closed lists + schema structure HELD** — `NARRATIVE` / `HISTORICAL_NARRATIVE` / `INFORMAL_CASUAL` all validate (no forced new value); the map compiled to `jonah_pericope_01_v2_0` (SC-0032 widening end-to-end). L2 bounded-open (scene/proposition kinds) grows by design (judgment gaps for the drafter). L3 lint 0 — atomic/bare/plain, entities-only §3C, born clean under v0.3. Registry all-new (expected).
- **Map rulings applied (`_working/J01/J01-Jonah-1-1-3.md`, on branch `jonah-j01-map`):**
  - **(1) Genre** stays `HISTORICAL_NARRATIVE` (closed list held); the didactic-irony — the called prophet who runs — added to the §2.4 L1 conditioning prose, where genre nuance belongs (not a forced genre).
  - **(2) Register** — v.2 YHWH's command takes a **scene-level `ELDER_AUTHORITY` override** (one of the 7 closed values → holds). ⚠ The register *value* (ELDER_AUTHORITY vs. another of the 7) is flagged for the lead's ear; if she hears one outside the 7, that is the first real Jonah L1 finding.
  - **(3) Scene division** (call v.1-2 / flight v.3) confirmed.
  - **(5) Cross-refs** out of L3 already removed (the lint `link_in_level3` catch); the draft **author-note removed** (R6 — no process-commentary in the final map).
- **Registry bootstrap (ruling 4):** constructed the Jonah BCD (5 beings, 3 places, 2 objects, 2 concepts, 3 figures — Hebrew forms from the pinned J01 BHSA packet) and built the registries with the SC-0033 generalized builders: `jonah.aliases.json` (10 entities) + `jonah.concepts.json` (2) + `jonah.figures.json` (3), replacing the empty scaffolds; **re-pinned** in `pins.json` (aliases `0.1.0→0.1.1`; concepts/figures new sha). `tripod coverage --book jonah J01` now **anchors: 20/20 explicit accounted · 0 unanchored · 13 matched + 7 to-tick + 5 minor** — the empty-alias-table false-positive is gone, and coverage (the fidelity floor) confirms the map invents nothing.
- **Prompt fix (ruling 5, vault writeback):** the v0.3 agent-2 §11 proposition template still carried a `cross_ref:` line — a stale carry-over the de-leak/L3-purity discipline forbids (figure↔prop links live in §5B). The lint caught it on the **first** Jonah map (the operating-test gate working). Remove that line + note where the links go. Vault-gated (Marcia pauses Obsidian).
- **Process finding (recorded):** coverage is **blind on a fresh book until its alias table is built** — the first map of any book can't be auto-verified for fidelity until its cast is registered. The fix is exactly this BCD→alias bootstrap; sequence it before relying on coverage for a new book's first pericope.
- **Verification:** map lint 0 · validate L1 closed lists hold (judgment-field gaps are the skeleton's, filled by the drafter) · coverage J01 20/20 0-unanchored · `check-drift` exit 0 (Jonah re-pins) · Ruth board unchanged (164 + 1 · coverage 6/6 · check-drift:vault 0).
- **Still the lead's / next:** confirm the ELDER_AUTHORITY register value; the L2 bounded-open values (scene/proposition kinds) when a drafter fills them; J02–J05 division (esp. the psalm); whether the J01 map + cast graduate from `_working/` to the Jonah fixtures + a vault home.

---

## SC-0035 — Refresh Agent-2 to current methodology + book-generalize; remove the cordel-lineage reference
- **Date:** 2026-06-08
- **Decided by:** Marcia (plan-first sign-off): §3C → R1 entities-only; paste the disciplines in (self-contained); log as SC-0035 covering both tasks, drafted next; the Evaluator verifies the draft before the prompt is used. Sequencing: must land before the J01 Jonah map (else the map is born against stale rules).
- **Status:** APPLIED (compiler — Task A + this record) 2026-06-08. **Task B blessed (evaluator + Marcia) + written back to the vault — DONE:** v0.3 transcribed byte-identical to vault `_spec/agent-2-system-prompt.md` (**vault PR #13**); the compiler-side staging draft removed (the canonical lives in the vault). Agent prompts are vault-only + unpinned → **no version-pin ritual, no `check-drift`/`pins.json` step.**
- **Type:** methodology-doc refresh + project-doc cleanup. **No `validation-rules.json` change, no closed-list change, no spec-version bump.**
- **Task A — delete the cordel lineage.** The cordel / Psalm→poem work is a separate project; its footprint in the Tripod repo invited confusion about which map-author is canonical. Removed the `forja-de-cordel` (cordel-poetry-app) sentences from the "Downstream contract to preserve" section of `CLAUDE.md` (compiler repo + the frozen `Downloads/CLAUDE.md` backup), keeping the legitimate point (shared-TS-types → compiler-checked contract; the Facilitator/ML pilot is the consumer). Scope = project docs only; the installed poetry-project *skills* (Marcia's separate project) are untouched. Gate: `git grep -inE "cordel|forja|sertanejo"` is **0 in the live/content docs** (CLAUDE.md + the agent-2 draft + the Downloads backup); the only remaining repo occurrences are in **this SPEC_CHANGES record of the removal** — the audit trail. The zero-grep gate is **content-scoped**, exactly as SC-0017's de-leak cleaned the content layers while the SC audit docs kept the SC-provenance. (If literal repo-wide zero is preferred over the named audit trail, the prose here can be reworded to point at the diff instead — Marcia's call.)
- **Task B — refresh `_spec/agent-2-system-prompt.md` v0.2 → v0.3** (last substantive touch SC-0020; ~14 cycles stale). A refresh, not a rewrite — the core principles hold. Folds in disciplines already documented in `_methodology/` (consolidate, don't invent):
  - **Retire the AUDIT** — every AUDIT reference → the pilot-2 reality (FOR_MODEL + COMPILATION-LOG + BCD-DELTA + VERIFICATION-INPUT; there is no AUDIT). The ~7 refs the SC-0017/0020 pass missed.
  - **Re-point the canonical example** from the old P01 to **today's blessed P01** (post-de-leak SC-0017, post-Level-3-purity SC-0030) — "match P01" now means the current one.
  - **§3C realigned to entities-only (R1) — THE load-bearing fix.** v0.2 instructed the *opposite* (put formulas/framings/wordplay/repetition *into* §3C); R1 (`level3and3Ccontentdiscipline.md`, SC-0012/0013) makes §3C entities-only (concrete objects + referred-to concepts), with events → propositions, naming → referential forms, literary patterns → figures. A Jonah map authored on v0.2 would be born drifted.
  - **Pasted in (self-contained, Marcia's call):** the forbidden grammatical-vocabulary list + plain-language substitution table (R4); Level-3 purity (SC-0030 — L3 = bare proposition inventory, no meaning/commentary); the de-leak (SC-0017/R6 — no process-commentary in content); the **mapping discipline** (no sentence-shaped UPPER_SNAKE slot values — strip to type; the guard the auto-lint can't draw yet, that keeps Jonah's L3 born-clean); the Common-Reader Prose Standard (SC-0019) retained.
  - **By-layer bootstrapping** (`vocabulary-bootstrapping-for-new-books.md`, distilled to the operational rule): registry starts empty + fills as you map; closed lists (genre/register/speech-act) HOLD; open lists GROW by design; a forced closed-list value is a governed finding (its own SC), never invented. (The Jonah by-layer expectation — NOT the full 4-phase pre-scan workflow, a separate setup activity; the JONAH plan went pilot-first.)
  - **Book-generalized** — removed the Ruth-Pilot-2 hardcoding; the book is supplied per run; Jonah's specifics (the `Jona` BHSA spelling, Hebrew versification) are handled upstream by the extractor (SC-0032/0033).
  - **Header** → v0.3, date 2026-06-08, "tagset locked to TRIPOD_STA_v2_0 / spec v0.15", book-neutral title.
  - **Kept the still-valid core:** don't translate; no grammar-frame slot names; never infer a withheld relation; describe-what-must-be-reconstructed.
- **Review flow (done):** the full v0.3 was staged at `docs/SC-0035-agent-2-prompt.DRAFT.md` for evaluator-verify + Marcia's read; **both blessed it** (compiler PR #42). The prompt was then transcribed byte-identical to vault `_spec/agent-2-system-prompt.md` (**vault PR #13**) and the staging draft removed. The cordel/AUDIT/§3C/discipline changes are now in the canonical vault prompt.
- **Flagged (not this task):** Agents 1/3/4 are similarly stale; lower priority (Agent 3's deterministic compile is largely superseded by `tripod compile`), so Agent 2 — the irreducibly-exegetical map author Phase 2 needs — went first. A later pass for 1/3/4.
- **Verification:** Task A grep 0 (repo + backup); compiler floor unchanged (no code touched — 164 + 1 skipped, check-drift exit 0); the staged draft is the reviewable artifact.

---

## SC-0034 — Gate the measurement-trap self-test (SC-0031 follow-up)
- **Date:** 2026-06-08
- **Decided by:** Marcia ruled **Option B** at the SC-0031 review — wire the self-test into the gate, "precisely because it's what this whole cycle preaches (only a check that *runs* enforces)," as a small, non-urgent, PR-gated follow-up. Builder did it the same cycle (context hot; deps already installed) rather than defer.
- **SC-ID note:** this work was authored concurrently as "SC-0033" (PR #40) by a separate session; SC-0033 was also taken by Jonah Phase 1 (PR #39), which **merged first** and is now immutably bound to that number on `main`. Per the append-only rule (merged-first keeps the number; the other takes the next free), this is **renumbered SC-0033 → SC-0034**. No content change beyond the number + a rebase onto the merged `main`.
- **Status:** APPLIED (compiler; gate green) 2026-06-08 — diff for review. **No `_spec/` change, no vault writeback** (test + dev-tooling, compiler-only).
- **Type:** process-hardening — promote the SC-0031 helper from a *manually-run* check to a *gated* one (fix-hierarchy tier 2 — "the real run-it-every-turn"), per `docs/RECURRING-ERROR-DISCIPLINE.md`.
- **(1) `tests/adversarial-check.test.ts`** — a vitest guard that runs `python3 tools/adversarial_check.py --self-test` (T1 dangling-group · T2 ajv-shape · T3 exit-without-pipe · T4 clean-control · T5 no-op-refusal) and asserts exit 0 + `ALL PASS`; any regression turns `npm test` red. The helper is a Python dev tool, so the guard **skips VISIBLY** when `python3` is absent (the SC-0008 conditional-skip idiom via `it.skipIf`) — never a silent green, never a hard-broken TypeScript gate.
- **(2) `package.json` script `check:adversarial`** — `npm run check:adversarial`, a named manual handle parallel to `check-drift:vault`.
- **Verification:** full suite **164 passed + 1 skipped** (was 163 + 1; the +1 is this guard, in-gate ALL PASS); `npm run check:adversarial` → ALL PASS exit 0; build clean. **Both branches proven:** `python3` present → the guard runs and passes; an absent binary → `python3Available()` returns false → the test skips visibly (not a false-green). Also corrected a stale `docs/PROGRESS.md` gate line (read "159 passed", ~4 SCs behind the 163 floor → now the true count + the two conditional guards).

---

## SC-0033 — Jonah Phase 1: extract + pin the J01 pilot packet; generalize the registry-builders; scaffold the Jonah registry
- **Date:** 2026-06-08
- **Decided by:** Builder (deterministic Phase-1 infra per `docs/JONAH-PLAN.md` §2, ruled into motion by Marcia's Jonah-cycle sign-off at SC-0032). No new meaning/exegetical call — the J01 boundary (Jonah 1:1-3) is the plan's firm pilot; J02–J05 division stays Marcia's.
- **Status:** APPLIED (compiler; Ruth board byte-identical green) 2026-06-08 — diff for review.
- **Type:** source-extract + tooling generalization. **No `validation-rules.json` edit, no closed-list change, no schema-version bump, no vault writeback** — the new artifacts are compiler-only `pins.json` `sources` (BHSA extracts + derived registries have no wiki-canonical original; `check-drift --vault` does not compare them).
- **What shipped:**
  1. **J01 pilot packet.** Ran `extract_bhsa.py J01 --pericopes extractor/pericopes.jonah.json` → `_spec/source/jonah/J01.json` (Jonah 1:1-3 = Heb 1:1-3; **33 referents** — 20 explicit / 7 implied-subject / 6 minor; full cast YHWH ×3, Jonah ×2, Amittai, Nineveh, Tarshish ×3, Joppa, correctly typed PERSON/PLACE). Pinned `extractor-0.1.0/bhsa-2021` sha `655575f6…`.
  2. **Book-general registry-builders.** `build_aliases.py` + `build_concept_figure_registry.py` gain a `--book` param (default `ruth`); the output `book` label, `source` string, and default out-path now derive from it. `--bcd`/`--vault` made **optional**: omit → scaffold an EMPTY registry for a book with no registered cast yet; an explicitly-given missing path still errors (typo guard preserved); a missing `concepts/`/`figures/` subdir → empty for that collection. **Ruth proven byte-identical** (regenerated all 3 `ruth.*` registries with `--book ruth` against the vault BCD + NER sheet → sha-identical to the pinned files).
  3. **Empty Jonah registry scaffold.** `jonah.aliases.json` / `jonah.concepts.json` / `jonah.figures.json` (builder-produced, empty, `book: JONAH`), pinned (`aliases-0.1.0` `755e8bb5…`, `registry-0.1.0` `63bcfcd6…` / `8ce3216c…`). The book-aware readers (`loadAliasTable/Concept/Figure("jonah")`, `loadSourcePacket("source/jonah/J01.json")`) parse all four — so `coverage`/`id-check --book jonah` are unblocked the moment a Jonah map exists.
- **Generality finding (the first "Jonah isn't Ruth" at the data layer).** BHSA's `book` feature uses **Latin** book names; Jonah is **`Jona`**, not `Jonah` (verified against `tf/2021`). Ruth is identical in Latin/English, so SC-0032 set `pericopes.jonah.json` `bhsa_book: "Jonah"` and never hit the mismatch — the extractor found 0 verses until corrected to `"Jona"`. The `bhsa_book` field exists precisely to carry the BHSA-native spelling; only its value was wrong. (`bcv` stays English "Jonah 1:1-3"; `book: JONAH` drives the out-path.) This is exactly the kind of finding Phase-1-verifies-the-Heb-ranges-at-extraction was meant to surface.
- **Measurement note (SC-0031 trap, avoided).** `extract_bhsa.py` + `build_aliases.py` print the sha of the JSON body *before* the trailing newline, but write `body + "\n"` — so their stdout shas (`a37e4469…`, `43a76275…`) are **not** the file shas. Pins use `shasum -a 256` of the file bytes (what `check-drift`'s `sha256OfFile` computes), verified directly.
- **Verification:** `check-drift` exit 0 (4 new Jonah `sources` pins `vendored:ok`); full Ruth board green and unchanged — 163 passed | 1 skipped, validate 6/6 0-block, lint 0/7-accepted, coverage 6/6 (245/245), id-check 6 clean, gold-diff unchanged, `check-drift:vault` exit 0, `adversarial_check.py --self-test` ALL PASS.
- **Housekeeping (not part of the Jonah change, flagged for the lead):** corrected the stale "Spec version pin (current)" summary table above — it read `validation-rules v0.13 / compilation-log v0.7 / bcd-delta v0.4` (unmaintained since before SC-0030; SC-0030 and SC-0032 both bumped schemas without updating it) → now the authoritative `v0.15 / v0.8 / v0.5` from `pins.json`. That summary table duplicates `pins.json` and is ungated, so it drifts — candidate for a future SC (gate it against `pins.json`, or delete the duplicate and point at `pins.json`). The SC-0032 vault writeback is confirmed landed (its row said "pending" pre-merge; `check-drift:vault` now shows all 3 schemas `vault:ok`).
- **Next (Phase 2, Marcia-gated):** author the **J01 Meaning Map** (the meaning-map skill + mapping discipline), compile → FOR_MODEL, run the full board, and produce the **first by-layer generality read** (`docs/JONAH-PLAN.md` §0: L1+structure should HOLD; L2/registry GROW by design; a forced L1 value = the prize finding, its own SC). The Jonah cast surfaced there populates the (currently empty) BCD/registries via a BCD-delta.

---

## SC-0032 — Book-generalization (Phase 0 of the Jonah held-out generality test)
- **Date:** 2026-06-08
- **Decided by:** Marcia ruled the Jonah cycle + the widen pattern (**fully-general** `^[a-z]+_…`). The evaluator caught that the plan's "4 code hardcodings" undercounted — the 3 schema id-patterns are a 5th, governed, vault-mirrored hardcoding. Plan: `docs/JONAH-PLAN.md`.
- **Status:** APPLIED (compiler; Ruth byte-identical green) 2026-06-08 — diff for review; **vault writeback (3 schema files) pending** (the SC-0008 ritual).
- **Type:** book-generalization — a book-general **code** refactor (behind the existing `--book`/`bcv` derivation) + a **governed schema change** (3 id-pattern widenings).
- **Code (Ruth byte-identical):** `extract_bhsa.py` book-filter + out-path derive from the pericope spec (`bhsa_book`/`book`); `skeleton.ts` + `complog.ts` derive the `sta_id` book-prefix from the map's `bcv` first token (`Ruth …`→`ruth`); `meaning-map.ts` verse-anchor strip generalized (`^[A-Za-z]+\s+`). New `extractor/pericopes.jonah.json` (Hebrew-versified J01–J05; **J01 firm pilot, J02–J05 provisional** — Marcia rules boundaries at authoring + Phase 1 verifies the Heb ranges).
- **Schema (governed; fully-general so no future book ever re-touches it — the make-it-impossible fix-tier):** `^ruth_pericope_…`→`^[a-z]+_pericope_…` in `validation-rules.json` (v0.14→**v0.15**) + `compilation-log.schema.json` (v0.7→**v0.8**); `^ruth_bcd_delta_…`→`^[a-z]+_bcd_delta_…` in `bcd-delta.schema.json` (v0.4→**v0.5**). Re-pinned (`893b3a49…` / `ff315586…` / `07a236c9…`).
- **Verification:** Ruth board byte-identical green (validate 6/6 0-block; gold-diff unchanged; 163 tests + 1 skipped); `check-drift` clean against the 3 new pins; **proof:** a `jonah_pericope_01_v2_0` sta_id validates (0 block) where it was ajv-blocked at v0.14. Versification finding recorded (Eng 1:17 = Heb 2:1). **OUT of scope (Phase 1+):** running the Jonah extraction + authoring the maps.
- **Pending:** vault writeback of the 3 schema files (the SC-0008 ritual → `check-drift:vault` exit 0; the evaluator verifies it like SC-0030).

---

## SC-0031 — Harness-hardening: the recurring-error discipline + the measurement-trap helper
- **Date:** 2026-06-08
- **Decided by:** Marcia ruled the harness-hardening cycle (before Jonah) + the helper location (`tools/`). Discipline + helper scope from `[[tripod-recurring-error-discipline]]` + `[[tripod-my-role-is-evaluator]]` (the measurement-trap, recurred 4×). The 3-role loop: evaluator flags the recurrence, builder builds, Marcia rules.
- **Status:** APPLIED (compiler; self-test green) 2026-06-08 — diff for review. **No `_spec/` change, no vault writeback** (docs + dev tool, compiler-only).
- **Type:** process-hardening — promote a 4×-recurring measurement error from a *note* to a *tool* (fix-hierarchy tier 2), per the discipline.
- **(1) `docs/RECURRING-ERROR-DISCIPLINE.md`** — byte-faithful durable copy of the memory note (escalate-by-recurrence; the 4-tier fix hierarchy; the 3-role loop; the recurrence-tally + retrospective ritual). Evaluator verifies byte-faithfulness.
- **(2) `tools/adversarial_check.py`** — makes a "break X, expect a block" check unfoolable, closing each of the 4 traps that bit the evaluator this arc: pipe-eaten exit (→ subprocess return value, no pipe), needle/code-guess on prose (→ parse `--json` findings as data), BSD-`sed` silent no-op (→ apply the mutation in Python + assert the bytes changed; **refuse a no-op mutation**), mis-measured DIFFERS false-alarm (→ run the unmutated clean control). `--self-test` replays the 4 historical instances + the no-op guard against the real fixtures.
- **Verification:** `python3 tools/adversarial_check.py --self-test` → ALL PASS (T1 dangling-group · T2 ajv-shape · T3 exit-without-pipe · T4 clean-control · T5 no-op-refusal); doc body byte-identical to the memory note (3107 chars); floor unchanged (163 tests + 1 skipped, check-drift clean).

---

## SC-0030 — Level-3 purity: relocate the fidelity apparatus out of the supervision target
- **Date:** 2026-06-08
- **Decided by:** Marcia Suzuki — **Option A** (parallel fidelity layer) ruled in review; sub-points delegated to Architect 11 ((i) move the L1 override flag into the layer + drop its `meaning`; (ii) ref by `{prop_id, slot, list_position}`). The **scope boundary** (sentence-shaped slot values OUT) set by Marcia + the evaluator. Ruling-gate + the relocated-rationale §9 table: `docs/SC-0030-PROPOSAL.md`.
- **Status:** APPLIED (compiler; gate board green) 2026-06-08 — diff for review; vault writeback (3 FMs + `_spec/` schema, the SC-0008 ritual) pending.
- **Type:** schema-shape (FOR_MODEL) + engine + lint. **Fixes a regression introduced by SC-0027/0029** (commentary + conditioning had entered the Level-3 supervision target).
- **The principle:** Level 3 (map + FM) is *only the inventory of propositions the model translates* — the training supervision target. Commentary/conditioning must not live there (it would train the model to emit rationale, not translation).
- **Spec change (exact):** `validation-rules v0.13→v0.14`. Added a top-level `fidelity` layer (`$defs`: `fidelity_layer`, `fidelity_element`, `fidelity_group`, `fidelity_structure_flag`, `fidelity_register_override`, `l3_component_ref`); removed the old `fidelity` + `fidelity_group_entry` `$defs`, `proposition.fidelity_groups`, and `register_override_entry.fidelity`. The layer is strict (ajv-validated shape; **no `meaning` field**). Re-pinned (sha `4784b390…`).
- **Engine:** rewrote the SC-0027 fidelity pass (`vocabulary.ts`) → resolve every layer ref into Level 3 (a dangling component/field/override ref blocks `referential-integrity`) + the dangling-group-id check (every `element.group` resolves). Shape-validation moved to ajv.
- **Lint:** `lintForModel` gains `l3_free_text` — flags a `meaning` key or any prose-shaped value (a space + a lowercase letter) inside a Level-3 `event_specific_slots`. Deliberately does **not** flag UPPER_SNAKE slot values (the post-Jonah triage).
- **Migration:** P03/P04/P06 — 19 `meaning` removed, fidelity flags moved to the layer (P03: 6 el + 2 grp + 1 sf + 1 ro; P04: 1 el; P06: 6 el + 2 grp). **No Level-3 content lost** (deep-compare vs `origin/main` L3-with-fidelity-stripped — identical; all 19 flags preserved). Maps unchanged.
- **Verification:** validate 6/6 0-block (held-7 still quarantined); check-drift clean (v0.14); gold-diff unchanged (fidelity is not on the compared layer); id-check 6 clean / 0-dangling; lint 0/7; coverage 6/6; **163 tests + 1 skipped**. Adversarial (real validate/lint path): dangling component-ref → block; dangling group → block; non-boolean flag → ajv block; `meaning` re-injected into L3 → `l3_free_text`.
- **OUT of scope (deferred):** the ~49 sentence-shaped slot VALUES (`[[tripod-sentence-token-triage]]` — post-Jonah, its own SC; Jonah meanwhile guarded by mapping discipline).

---

## SC-0029 — Held-7 fidelity seeding (the fidelity model on its designated test corpus)
- **Date:** 2026-06-07
- **Decided by:** Marcia Suzuki — each label's preserve/render + the two grouping calls (A–F) ruled in review; the "figure carries the image, `preserve_form=false` frees the wording" resolution for A/B/C; `preserve_form=true` for D's *shifchah* self-abasement; B's figure DEFERRED; the two groups E/F. Architect 11 did the plumbing + figure verification; the ruling-gate is `docs/SC-0029-PROPOSAL.md` (§9 = the rulings + verification).
- **Status:** APPLIED (compiler; gate board green) 2026-06-07 — **diff for review; vault writeback (P04 map + FM, P06 FM) pending** (human-gated).
- **Type:** content/annotation (FOR_MODEL `fidelity` flags + 2 `fidelity_groups`) + one figure-wiring — **no schema change, no spec-version bump** (SC-0027's model already supports it).
- **Summary:** The 7 held `action` labels (quarantined since SC-0024/0025 because meaning and form diverge — their purpose was to be Thread B's first annotated corpus) are seeded with `fidelity`. All 7 `preserve_meaning=true`. `preserve_form=false` on #1 courtroom-lament (P04 1:21), #2 *nokhriya* (P06 2:10), #3 repay + #4 wings (P06 2:12), #5 comforted (P06 2:13); `preserve_form=true` on #6 + #7 (the two *shifchah* self-abasement elements, P06 2:13). The held-7 **stay quarantined** (fidelity ≠ promotion — orthogonal axes).
- **Figures (Marcia's "keep the image, free the wording" resolution):** A → wire orphaned **FIG_0085** (Testify-Against-Legal-Image; `appears-in: P04`, paired `CB_0025` already wired) into P04 FOR_MODEL `figure_flags` **and** the map (frontmatter + §5B) — closes a pre-existing gap (the figure was in neither artifact). C → reference already-wired **FIG_0011** (Wing-of-Refuge). D → reference already-wired **FIG_0132** (Amah-Vs-Shifchah; registers the *shifchah→amah* progression, which reaches forward to 3:9 — reinforcing, not yet load-bearing). B → figure **DEFERRED** (single-verse *nokhriya/hikkirani* wordplay, no recurrence data; the figure-analogue of quarantine; revisit on recurrence, e.g. Jonah/Genesis).
- **Groups (two new, single-membership):** `blessing_reward_and_refuge` (E — P06 P9, members FIRST+SECOND: Boaz's reward and refuge are one prayer); `ruth_reply_grace_received_in_self_abasement` (F — P06 P11, members FIRST+SECOND+THIRD: Ruth receives grace while self-abasing, one bound response). Each member component carries the `fidelity_group` ref (load-bearing) + each group lists `members` (audit), per the vow pattern. **F as one group settles the single-vs-multi membership question: single suffices → no schema change.**
- **Forward flag:** #6 (`STATED_THAT_HE_SPOKE_TO_HEART_OF_HIS_SHIFCHAH`) is a *mixed-fidelity element* — a preserve-token (*shifchah*) + a re-realizable idiom (*dibber al-leb*) in one component, which one `preserve_form` boolean cannot split. The `meaning` field carries the split now (no schema change); a future token-level refinement is recurrence-gated (watch held-out law/poetry, where mixed-fidelity elements will be common).
- **Verification:** validate 6/6 0-block (held-7 still quarantined); check-drift clean (no spec change); gold-diff P04 38→39 matched (FIG_0085; baseline re-recorded), else unchanged; id-check 6 clean / 0 dangling / 0 flag-mismatch (FIG_0085 aligned in both artifacts); lint 0/7; coverage 6/6; 159 tests + 1 skipped. Adversarial (throwaway copies, real `validate` path): a dangling `fidelity_group` ref → block; a malformed component fidelity → `fidelity-shape` block.

---

## SC-0027 — Thread B fidelity model: `preserve_meaning` × `preserve_form` (first applied to the P03 vow)
- **Date:** 2026-06-07
- **Decided by:** Marcia Suzuki — the two Thread C calls + the per-step vow rulings + the genre/pair-scope rulings (2026-06-07); architecture (α) + scope (Option 1) recommended by Architect 10 + the evaluator, confirmed by Marcia in review.
- **Status:** APPLIED (compiler half — schema + engine + P03 vow; gate board green: 159 tests + 1 skipped; validate 6/6, check-drift clean, lint/coverage/id-check/gold-diff exit 0) 2026-06-07 — PR open; **held-7 seeding + the P03 vault writeback pending**.
- **Type:** schema-shape (FOR_MODEL) + new engine validation pass — **THE GOAL** (the fidelity model the interlingua exists to serve).
- **Summary:** The two-axis fidelity flag — `preserve_meaning` (the claim must survive translation) × `preserve_form` (false = the Performer may re-realize the wording; the "means, not mandate" license). Attached at the element/component level; the (α) shared-group-id mechanism carries relationship-level claims (a group entry's own fidelity) referenced by member elements. First applied to the P03 vow.
- **Spec change (exact):** `validation-rules v0.12→v0.13`. New `$defs`: `fidelity` (required boolean `preserve_meaning`/`preserve_form`; optional `meaning`, `fidelity_group`) and `fidelity_group_entry` (`group_id` + the relationship's `fidelity`; optional `members`). `proposition` gains optional `fidelity_groups`; `register_override_entry` gains optional `fidelity` (both strict). Component-level `fidelity` + the structure-flag ride in the permissive `event_specific_slots`.
- **Validator impact:** new engine fidelity pass (`vocabulary.ts`) — validates fidelity-object shape wherever it appears in the permissive slots, and runs the **dangling-group-id integrity check** (every referenced `fidelity_group` must resolve to a declared group; pericope-wide, since groups bind across propositions). New finding code `fidelity-shape`. Strict-def fidelity (groups, register) is ajv-validated.
- **Artifact migration:** P03 FOR_MODEL re-annotated — the vow: `preserve_form=false` on all six rungs; two `fidelity_groups` (`people_god_inseparability` = rungs 3+4 / `unto_the_end` = 5+6; accompaniment 0+1 ungrouped, per "group iff the text structurally binds, never for symmetry"); a `vow_structural_form_fidelity` structure-flag (`preserve_meaning=true`: the escalation must survive); S2 override → `genre_group_override=POETIC_SUNG` + `genre_override=BLESSING` + `override_value=INTIMATE` carrying `preserve_meaning=true` (genre+register must survive; blessing-dominant + a curse-oath note). `NOT_TO_BE_NORMALIZED` (unimplemented README note) is superseded by the `preserve_form=true` pole.
- **Deferred (own follow-up):** seeding the held-7 with fidelity (each one's preserve/render = Marcia's call); the **P03 vault writeback** (fixture↔vault sync owed — SC-0008 discipline); the single-vs-multi group-membership question (the vow needs only single; revisit if a held-7 case needs two).
- **Version:** `v0.12 → v0.13` (sha256 `0323662a643822d469db257655fcdf9c1450e07eaf69a557a1191ebf6bc3b745`). Re-pinned.
- **Verification:** P03 re-validates block-clean (spec v0.13); the dangling-group-id check fails a broken ref (test) and passes the two real groups; a malformed component fidelity → `fidelity-shape` block (test); 159 tests + 1 skipped; check-drift clean; corpus gates exit 0; gold-diff unchanged (P03 100%).

---

## SC-0026 — COMPILATION-LOG schema gate (turn CL validation on)
- **Date:** 2026-06-06
- **Decided by:** recommended by Architect 10, confirmed by Marcia in review (2026-06-06) — scope: schema gate only; vault-CL staleness → SC-0008
- **Status:** APPLIED (compiler, gate board green: 155 tests · validate 6/6 FOR_MODEL + 6/6 CL · check-drift clean · lint/coverage/id-check/gold-diff exit 0) 2026-06-06 — MERGED (PR #30, origin/main a6a2d04)
- **Type:** gate-checks (test wiring; no schema change)
- **Summary:** Wire the six gold COMPILATION-LOGs (`fixtures/compilation-log/P01–P06`) into the gate so each is validated against `compilation-log.schema.json` (v0.7) on every `npm test`, exactly as the six FOR_MODELs already are. The CL-validation machinery already existed (`validateArtifact` dispatches the CL schema at `src/engine/validate.ts:33-34,76`; `tripod validate <dir>` already discovers CLs) — SC-0026 adds the missing **gate test** (6 positive + a vendored-count check + 1 negative) so a malformed CL fails the board instead of entering the corpus as unguarded "provenance."
- **The seam (why SC-0026, not SC-0008):** SC-0026 only turns a check ON against the already-correct schema (the gate-checks side of the seam test). It changes no schema and no data.
- **Finding surfaced (routed to SC-0008, NOT fixed here):** turning CL validation on is **green** — all 6 CLs are *shape*-clean in both trees. But the vault `stas/` CLs are a **strict subset** of the compiler fixtures (vault-only keys = 0 across all 6; each missing `vocabulary_additions.role_in_scene_beings`, 5/6 also missing the `vocabulary_additions.*_elements` breakdown; 54–242 changed lines per pericope). That is **content** staleness, invisible to a schema gate — the 4th unguarded-authoritative-artifact instance, sibling to the stale vault `_spec/`. **Routed to SC-0008** (writeback the 6 vault CLs + a `stas/` content guardrail). One concern per cycle.
- **Spec change (exact):** none — no `validation-rules.json` / `_spec/` edit, no `pins.json` bump.
- **Artifact migration:** none (no fixture content changed).
- **Validator impact:** none new in the engine; the existing CL structural pass is now exercised by the durable test gate.
- **Version:** no spec-version bump.
- **Verification:** `npm test` 147→155 (6 CL fixtures + vendored-count + 1 negative); `tripod validate fixtures/compilation-log/` → 6 valid · 0 block; the negative case (drop required `sta_id`) → a located `block` schema finding naming `sta_id`; `check-drift` clean; `validate fixtures/for-model/` unchanged (6 valid · 0 block · 0 drift · 15 quarantined); `lint/coverage/id-check/gold-diff --corpus` exit 0.

---

## SC-0024 — Thread A: nested component `action` / `speech_act` collapse (REDUCE, not promote)
- **Date:** 2026-06-04
- **Decided by:** Marcia Suzuki (ruled the whole Thread A set: vow collapse; keep the speech_act suffix-variants — same-act-vs-different-act; hold 7 load-bearing labels for Thread B; D1 split — route the load-bearing vessels detail to a sibling slot, collapse the incidental map-only ones; vessels option (a) descriptive slot + the registry gap flagged for a future BCD-delta; courtroom-lament held on the *ʿanah b-* legal/affliction split; audit-log handling option (a); escalate `action` enforcement to a named SC-0025). The compiler implemented + verified.
- **Status:** MERGED (compiler PR merged; PR#/date in the SC-ID allocation-ledger row above).
- **Type:** closed-list change (`speech_act`) + content edit (`action`, uncontrolled)
- **Summary:** The SC-0021/0022 triage cleaned the top-level bounded-open slots but never reached the `action`/`speech_act` slots **nested inside proposition `*_components` arrays**. SC-0024 collapses them: the six `VOWS_*_BINDING` SPEECH_ACT values → one closed-list `VOWS` (31→26), and ~57 sentence-shaped `action` values across P01–P06 → a small reusable verb set, each reduction proven by a per-value **survival table** (the dropped detail must survive in a named sibling slot or map §4 line, else it is held).
- **Rationale:** A sentence-shaped value is never a reusable type; the nested slots were the last uncollapsed layer. Because `action` is free text, the gates **cannot** verify these collapses — so the proof is the survival table (`docs/THREAD-A-PROPOSAL.md`), not gate-green. The vow's six speech-acts are the *same* act (vowing) differing only by domain → collapse; the rhetorical-question suffix-variants are *different* illocutionary acts → kept. 7 labels where preserve-meaning/preserve-form come apart (courtroom lament; wings-of-refuge blessing; *shifchah/shifchot*; *nokhriya*) are **held** as the Thread B starter corpus.
- **Spec change (exact):**
  - `validation-rules.json` `closed_lists.SPEECH_ACT`: remove `VOWS_IDENTITY_BINDING`, `VOWS_ROAD_BINDING`, `VOWS_RESIDENCE_BINDING`, `VOWS_PEOPLE_BINDING`, `VOWS_GOD_BINDING`, `VOWS_PLACE_OF_BURIAL_BINDING`; add `VOWS`. 31 → 26.
  - `compilation-log.schema.json` `speech_act_value.enum`: same edit (kept in sync — SC-0014 precedent).
  - `action` is **uncontrolled free text** → no enum/schema change; the reductions are content edits. (Making `action` controlled = SC-0025.)
- **Artifact migration:**
  - **P03 FOR_MODEL:** vow `speech_act` ×6 → `VOWS`; vow `action` ×6 → `VOWED`.
  - **P01–P06 FOR_MODELs:** ~57 `action` reductions to the reusable verb set (DIRECTED/STATED/VOWED/ASKED/REPORTED/ASCRIBED/IDENTIFIED/REFUSED/PROPOSED/GREETED/BLESSED/PERMITTED/POSITIONED/INTRODUCED/PLACED/NAMED/SAW/WENT_OUT/WALKED + pre-existing bare verbs). 7 held labels untouched.
  - **P06 P4 FOR_MODEL:** vessels routed to a new `drink_source: WATER_VESSELS` sibling slot (the vessels are an unregistered referent — a registry gap flagged for a future BCD-delta, not in scope here).
  - **P03 COMPILATION-LOG:** `speech_act_values_used` six → `VOWS`; a **supersession note** appended to decision P03-D3 recording that the collapse incurs no loss (FIG_0074 + `vow_structural_form` + `list_position` carry the six-way distinction; verified FIG_0074 fires independently of speech_act values). Original P03-D3 reasoning retained as provenance.
  - The CLs' descriptive `action` gap-hints are a frozen compile-time record (already divergent from the FMs, e.g. `DIVINE_AGENT` vs `YHWH`; nothing validates them against FM actions) — left as historical.
- **Validator impact:** none new — the FM validator already enforces SPEECH_ACT as a closed list; `VOWS` now passes, the six `VOWS_*_BINDING` now block (correct).
- **Version:** `validation-rules.json v0.10 → v0.11` (sha256 `bf25ebb116e4e564a5e546304e38838e0321ac4c6de2403e4b3e69b700ed3179`); `compilation-log.schema.json v0.5 → v0.6` (sha256 `cb9c5c8c1a031b468597dcae601f3334f6e70bea393f19dfb88f4d421e3889e0`). Both re-pinned.
- **Verification:** 142 tests · validate 6/6 (0 block · 0 drift · 8 quarantined unchanged) · lint 0/7 accepted · coverage 6/6 (245/245) · id-check 6 clean · gold-diff baseline (exit 0) · check-drift clean. The `action` slot went from ~50 one-offs to a reusable set with exactly the 7 held labels remaining sentence-shaped (verifiable: `grep '"action"'`).
- **Discovered (pre-existing, OUT OF SCOPE — flagged for a governed follow-on):** all 6 COMPILATION-LOGs carry `vocabulary_additions.role_in_scene_beings`, but `compilation-log.schema.json` (`additionalProperties:false`) lacks that property → every CL is invalid against its own schema. An SC-0022 residue (the role_in_scene_being intake slot was never added to the CL schema); latent because the gate board does not validate CLs. Not touched by SC-0024.

## SC-0025 — Action-slot enforcement (the SC-0024 durability follow-on)
- **Date:** 2026-06-05
- **Decided by:** Marcia Suzuki (approved option A: held-7 **quarantined** not seeded; restore the SC-0007 intake invariant by declaring `action_values` in all 6 CLs; the SC-0025/SC-0026 seam holds — declaring provenance in a CL is *schema-says*, not *gate-checks*). The compiler implemented + verified.
- **Status:** MERGED (compiler PR merged; PR#/date in the SC-ID allocation-ledger row above).
- **Type:** axis reclassification (`action` uncontrolled → bounded-open convergent) + compilation-log schema gap fix (`role_in_scene_beings`)
- **Summary:** Reclassify the nested component `action` slot from uncontrolled free-text to a **controlled bounded-open convergent axis**, seeded with the 31 SC-0024-cleaned verbs; the 7 held sentence-shaped labels are **quarantined** (the Thread B corpus — deliberately unsettled, not seeded as types). The drift engine now reaches the nested `event_specific_slots.*_components[].action` values. Also fixes the `role_in_scene_beings` schema gap SC-0024 flagged. **Does NOT** turn on gate-board CL validation — that stays SC-0026 (one concern per gate cycle).
- **Rationale:** SC-0024 *reduces* `action` but the slot stays uncontrolled, so P07–P14 could re-introduce sentence-shaped values and the slot would re-dirty — enforcement prevents that. The held-7 are quarantined, not seeded, because seeding would re-bless sentence-shaped values into the cleaned slot; quarantine hands them to Thread B intact and **pre-judges nothing** (the reason names the meaning/form divergence, not a fate). Clean-then-enforce, the SC-0021/0022 shape.
- **Spec change (exact):**
  - `validation-rules.json` `drift_detector.canonical_p01_enumerations`: add `action` (P01 seed ARRIVED_AT/TOOK_AS_WIFE/WERE_AT). No closed-list change — `action` is bounded-open. `v0.11 → v0.12`.
  - `approved-enumerations.json`: add the `action` axis (31 verbs, P01–P06; `axisClass` auto-classifies non-`_examples`/non-`referential_form` as convergent). `v0.9 → v0.10`.
  - `quarantined-vocabulary.json`: add the `action` axis (the 7 held). `0.1.0 → 0.2.0`.
  - `compilation-log.schema.json` `vocabulary_additions`: add `action_values` (intake slot) **and** `role_in_scene_beings` (the SC-0024-flagged gap fix). Additive, optional; required set unchanged. `v0.6 → v0.7`.
  - Engine (`vocabulary.ts`): fold the `action` drift check into the existing `event_specific_slots` walk — one walk, one added key-targeted leaf rule, `typeof`-string-guarded. `promote.ts`: map `action_values → action`. Quarantine display message made axis-neutral (it called every quarantined value a "used-once coin-flip" — wrong for the action held-7). Skeleton (`complog.ts`) unchanged — both new CL properties are optional; the skeleton emits required-only.
- **Artifact migration:**
  - **All 6 COMPILATION-LOGs:** declare `action_values` (each pericope's first-introduced verbs, 31 total) to restore the SC-0007 intake invariant — the registry must be reconstructible from the CLs (P01's canonical seed *is* CL-declared, so `action` must be too; precedent SC-0021/0022 re-pointed CLs when adding axes). Surgical insert; **P03's pre-existing `*_elements` divergence left untouched** (SC-0026's).
  - **FOR_MODELs:** none — the 31 seeded approved + 7 quarantined cover the whole corpus → **0 new drift**.
- **Validator impact:** `action` now drift-checked (bounded-open); the 7 held surface as `quarantined` notices (never block, never silently approved); recurrence in P07–P14 flags RECURS.
- **Version:** `validation-rules.json v0.11 → v0.12` (sha `61bcbb3b75b5d5fdd6adf161e89e0bc193764ae081b6606b7aa0cf31727464bc`); `approved-enumerations.json v0.9 → v0.10` (sha `8c3c64f9eaf49958c8453d169ce231565d6bdefc307f699abaf4188cc813dec6`); `quarantined-vocabulary.json 0.1.0 → 0.2.0` (sha `dab5f57ba152d83686181c4d56281c07e5fb5cc9299c54426b3a4522f00eecf8`); `compilation-log.schema.json v0.6 → v0.7` (sha `0d7f17d1dc203ae72b4459857ce0135c2ec8f05e044faaabfbb372bd6ad94161`). All re-pinned.
- **Verification:** 147 tests · validate 6/6 (0 block · 0 drift · **15 quarantined** = 8 comm-func + 7 action) · lint 0/7 · coverage 6/6 (245/245) · id-check 6 clean · gold-diff baseline (exit 0) · check-drift clean. Three merge checks green: the SC-0007 convergence test (invariant **restored** by the CL declarations, not silenced); the predicted 8→15 quarantine count; and the load-bearing bare-verb fact now a **gated** test (79 `action` occurrences, all `^[A-Z_]+$`, parsed not grepped).
- **Vault writeback (pending):** the 6 CLs (FMs unchanged) — same no-clobber discipline as SC-0024's `66c58f1` (diff-first byte-identity, let the cron land it, no manual push to vault `main`); the P03 CL writeback must be surgical (only `action_values`, leave the `*_elements` divergence for SC-0026).

## SC-0023 — Quarantined-vocabulary mechanism: un-settle the 8 used-once comm-func coin-flips (surface recurrence)
- **Date:** 2026-06-04
- **Decided by:** Marcia Suzuki (ruled option (a) — a quarantine exception file that surfaces a recurrence as an explicit notice, not mere exclusion from the drift gate; its own SC; full gate board; leave the `UNSETTLED/UNRESOLVED_AT_CLOSE` tone-pair unify as a separate ruling, not folded in); the compiler implemented + verified.
- **Status:** MERGED (compiler PR merged; PR#/date in the SC-ID allocation-ledger row above).
- **Type:** other (governed exception mechanism + registry correction)
- **Summary:** SC-0022 erroneously promoted **all 16** `communicative_function_element` verbs as settled, including the **8 used-once coin-flips** Marcia had quarantined for the P08–P14 frequency revisit. SC-0023 un-settles those 8 into a pinned `quarantined-vocabulary.json` and adds a `quarantined` finding severity + a corpus `quarantine watch`, so a recurrence is surfaced as an **explicit notice**, not silently excluded.
- **Rationale:** Marking a quarantined coin-flip *approved* defeats the quarantine — a later reuse would not drift, so the revisit signal is lost and the one-off gets cemented by accident. Caught in the post-SC-0022-merge spot-check (the green board hid it; only reading the **raw** promoted list found it — the lesson: gates prove schema consistency, not that the promoted set matches the rulings). The quarantine must be a recorded **third state** — neither approved-and-silent nor a failing drift — that makes recurrence visible.
- **Spec change (exact):**
  - **New pinned `_spec/quarantined-vocabulary.json` (`0.1.0`):** the 8 used-once comm-func verbs — `TRANSMITS`(P01), `ANSWERS`(P02), `PLACES`(P03), `ANCHORS`/`INTRODUCES`/`POSITIONS`(P05), `DISTRIBUTES`/`RECITES`(P06) — each with reason + provenance. Added to `pins.json` sources; `check-drift` verifies it (20 pins now).
  - **`approved-enumerations.json` `v0.8 → v0.9`:** removed those 8 from `communicative_function_element` (16 → **8 recurring**: OPENS/ESTABLISHES/WITHHOLDS/PLANTS/CLOSES/ADVANCES/REACTIVATES/STAGES). Re-pinned.
  - **Engine:** new `quarantined` severity (`report.ts`, + optional `value` on `Finding` for aggregation); `vocabulary.ts` emits a `quarantined` notice (never `drift`, never silently-approved) for a quarantined convergent value; `report.ts` `quarantineWatch()` aggregates across artifacts and flags **RECURS** when a value appears in ≥2 pericopes; the `validate` CLI prints the corpus quarantine watch + a `quarantined` count. `promote.ts` `planPromotion` gains a `skippedByQuarantine` bucket — a quarantined value is **never** auto-promoted regardless of status.
  - **Fixture CL intake:** the 5 CLs' `communicative_function_elements` reduced to the recurring approved values (P02→[CLOSES], P03→[ADVANCES], P04→[REACTIVATES, STAGES], P05→[], P06→[]); the quarantined verbs are no longer listed as promotable.
- **Artifact migration:** **no FOR_MODEL / map change** — the 8 verbs are still *used* in the maps (each once); only their registry status changes approved → quarantined. `fixtures ≡ vault` for FOR_MODELs is unaffected (no FM edit; no vault writeback needed for this SC).
- **Validator impact:** comm-func use of a quarantined value → `quarantined` notice (not drift, not block); the corpus quarantine watch flags a recurrence (≥2 pericopes) as RECURS = the revisit trigger. `validate` 6/6 · **0 drift · 8 quarantined** · 22 descr. The "0 *convergent drift*" gate is unchanged (quarantined ≠ drift).
- **Version:** `approved-enumerations.json` `v0.8 → v0.9` (sha256 `56444d9bc43b00d523461ade1d44db8b8f7204b56465e4f0a30ae3c0000a4610`); new `quarantined-vocabulary.json` `0.1.0` (sha256 `97636c1b18fc0191780683ab3df25249f9d8e0369bc041382dff157328b09783`). `validation-rules.json` unchanged (`v0.10`).
- **Verification:** full gate board green — `vitest` **142/142** (+6 SC-0023 tests: un-settled-and-disjoint · quarantined-not-drift · watch-parks-once · watch-RECURS-on-2nd-pericope · promote-skips-quarantined) · `validate` 6/6 (0 drift · 8 quarantined) · `lint --corpus` 0/7 · `coverage --corpus` 6/6 · `id-check --corpus` 6 clean · `gold-diff` baseline unchanged (comm-func is a judgment field) · `check-drift` 20 pins clean.
- **NOT in scope (separate Marcia ruling):** the `UNSETTLED_AT_CLOSE` / `UNRESOLVED_AT_CLOSE` tone-pair unify-or-distinguish call (both sit in `approved-enumerations` from SC-0006; SC-0023 deliberately left them untouched).

---

## SC-0022 — Vocabulary consolidation: enforce the cleaned interlingua (promote · enforce beings · schema-drop · deprecate)
- **Date:** 2026-06-04
- **Decided by:** Marcia Suzuki (the reuse-dependent triage rulings 2026-06-03/04 — the `scene_kind`/`proposition_kind`/`communicative_function` collapses, the 21 beings relations, the place/object/time role-drop "loses nothing", and the deprecation = **delete** ruling 2026-06-04); the compiler implemented + verified them.
- **Status:** MERGED (compiler PR merged; PR#/date in the SC-ID allocation-ledger row above).
- **Type:** bounded-list change + axis reclassification + schema-shape (one consolidation PR, staged in 5 commits)
- **Summary:** Make the reuse-dependent vocabulary triage real — promote the cleaned values, enforce the beings participant-role as a controlled 21-relation slot, drop the redundant place/object/time role/function fields, and deprecate the orphaned old forms. The slots P07–P14 are measured against become a constraint, not a convention. Sits on top of SC-0021 (#23 merged first; SC-0022's promote list deliberately excludes tone/pace).
- **Rationale:** The vault triage (`tripod_cleanup_log.md`) collapsed the low-reuse one-off bounded-open values into reusable types a model can generalize, reduced the beings role gloss to 21 standing relations, and proved the place/object/time role/function glosses content-free (their meaning is owned by registry-kind + the level-3 statement slots + figures/threads). Until promoted + enforced these were a convention nothing stopped the unwritten pericopes from regenerating; SC-0022 is the gate that makes them enforced.
- **Spec change (exact), staged:**
  - **(1) Promote** — `approved-enumerations.json` += 56 values (`scene_kind`×13, `proposition_kind`×27, `communicative_function_element`×16), `sc_ref: SC-0022`, provenance = first-seen FOR_MODEL; re-derived from live `validate` drift (106 instances). COMPILATION-LOG `vocabulary_additions` re-pointed (SC-0007 intake invariant). `v0.5 → v0.6`.
  - **(2) Enforce beings** — drift-detector seed key `role_in_scene_examples_being → role_in_scene_being` (axisClass keys off the `_examples` suffix → flips descriptive→convergent), reseeded with P01's relations; new `role_in_scene_being` axis in `approved-enumerations.json` seeded with all 21 ruled relations; `vocabulary.ts` drifts beings role against it; `promote.ts` `VA_KEY_TO_AXIS` += `role_in_scene_beings`; each CL declares its `approved_in` roles. `validation-rules v0.7 → v0.8`; `approved-enumerations v0.6 → v0.7`.
  - **(3) Schema-drop** — scene_places/scene_objects/scene_times entry schemas now id-only (`role_in_scene`/`function_in_scene` removed from `required` + `properties`; entries stay `additionalProperties:false` → the fields are forbidden); removed the 3 orphaned drift-detector `_examples` seeds; `vocabulary.ts` drops the 3 drift() calls (loops kept for referential integrity); `skeleton.ts` emits id-only entries. `validation-rules v0.8 → v0.9`.
  - **(4) Deprecate** — removed 133 orphaned superseded values unused by any pericope (scene_kind 33→16 · proposition_kind 89→35 · communicative_function_element 56→16 · tone_element 26→17 · pace_element 24→11; arc/context/presence/role unchanged). `approved-enumerations v0.7 → v0.8`. Old→new mapping preserved in `tripod_cleanup_log.md` + git history (Marcia ruled delete over a deprecated-flag crosswalk).
  - **(5) Seed tidy** — refreshed the inert drift-detector `canonical_p01_enumerations` fallback (scene_kind/proposition_kind/communicative_function_element) to P01's cleaned values so the never-consulted seed no longer names deprecated forms (the live baseline is `approved-enumerations.json`; this fires only if an axis is ever absent there). `validation-rules v0.9 → v0.10`.
- **Artifact migration:** `fixtures/for-model/` P01–P06 synced byte-identical to the triaged vault (cleaned scene/prop/comm-func + the 21 beings relations), then the place/object/time role/function fields stripped (56 fields) — so `fixtures` now intentionally **lead** the vault on the field-drop. COMPILATION-LOGs re-pointed (scene/prop/comm-func + the new `role_in_scene_beings` intake; P02 stays the grandfathered PROPOSED exception). `gold-diff-baseline.json` re-baselined (agreement unchanged — P01 100/P02 90/P03 100/P04 95/P05 98/P06 96; only judgment-placeholder counts fall as the role gaps go). **Vault writeback** (remove the place/object/time fields from the vault `stas/` FOR_MODELs, restoring `fixtures ≡ vault`) is the **human-gated follow-up** — pause Obsidian auto-backup, reviewed vault PR — NOT done here.
- **Validator impact:** beings `role_in_scene` outside the 21 → convergent drift (review signal); place/object/time `role_in_scene`/`function_in_scene` now **BLOCK** (additionalProperties); reusing a deprecated old value → drift. Descriptive (open) findings across P01–P06 fall **158 → 22** (only `referential_form` remains open by design).
- **Version:** `approved-enumerations.json` `v0.5 → v0.8` (sha256 `aa1a62d00b442d5841a3966309a131e50a581f0eba2ba973a7729a38a6e79eee`); `validation-rules.json` `v0.7 → v0.10` (sha256 `2d141a58513fe520e81148fd05f8340003fceececd0f3a05e2df3cc9c937cb4e`). Sibling schemas (compilation-log v0.5, bcd-delta v0.4, verification-input v1.1) unchanged.
- **Verification:** full gate board green at each stage — `validate` 6/6 (0 block · 0 drift) · `lint --corpus` 0 drift / 7 accepted · `coverage --corpus` 6/6 block-clean (245/245) · `id-check --corpus` 6 clean · `gold-diff` baseline match · `check-drift` clean · **`vitest` 136/136**. Promote list independently re-derived from live drift; every removed value asserted unused (no FOR_MODEL, no CL) before deprecation.

---

## SC-0021 — Vocabulary consolidation: promote the cleaned tone/pace bares + restore fixtures ≡ vault
- **Date:** 2026-06-03
- **Decided by:** Marcia Suzuki (the triage rulings — tone/pace locus-strip, the `NARROW`↔`NARROWS` UNIFY, `scene_communicative_purpose` no-op, `LAMENT_FRAMED` removal; 2026-06-02/03); the compiler implemented + verified them.
- **Status:** SHIPPED
- **Type:** bounded-list change (convergent L1-element promotion)
- **Summary:** Promote the 11 corpus-independent triage bares into `approved-enumerations.json` and re-establish the `fixtures ≡ vault` + COMPILATION-LOG intake coherence the triage deferred.
- **Rationale:** The tone/pace triage (vault `tripod_cleanup_log.md`) stripped locus suffixes from the L1 mood axes (`BRISK_CHRONICLE_OPENING → BRISK`, `NARROWS_AT_RECOGNITION_FAILURE → NARROWS`, …), dropped redundancies, and — Marcia's UNIFY ruling — merged the `NARROW`(state)/`NARROWS`(movement) split into one bare `NARROWS` (zero full-14 attestation that the contrast does any work). The cleaned bares were convergent drift pending promotion; this is the single governed consolidation that promotes them, re-pins, and runs the full gate board (deferred per-batch to avoid governance churn).
- **Spec change (exact):** `axes.tone_element` += `RISING`(P03), `STILLED`(P04), `URGENT`(P02); `axes.pace_element` += `BRISK`(P02), `SLOWED`(P02), `PAUSED`(P03), `NARROWS`(P03), `RISES`(P04), `SETTLES`(P04), `HOLDS`(P05), `WIDENS`(P05) — all `sc_ref: SC-0021`, provenance = each value's first-seen FOR_MODEL. `version v0.4 → v0.5`, `date → 2026-06-03`, `growth_log` appended. Old locus values (`BRISK_CHRONICLE_OPENING`, …) left in the registry as orphaned-historical (a deprecation sweep is a separate later pass).
- **Artifact migration:** `fixtures/for-model/` P02–P06 synced byte-identical to the triaged vault (tone/pace + `LAMENT_FRAMED` removal). COMPILATION-LOG `vocabulary_additions` tone/pace re-pointed to the bares (CONFIRMED) so the **SC-0007 intake invariant** holds — each pericope's CL declares exactly its `approved_in` convergent values (`drift.test.ts` guards this). The vault canonical FOR_MODELs were already triaged (vault `main`, per-batch approved); the vault's reduced CLs (tone/pace dropped) + the stale vault `_spec/approved-enumerations.json` `v0.1` are a separate deferred `--vault` reconcile.
- **Validator impact:** none (no rule change). The 11 bares move from convergent-drift → approved; P01–P06 now validate with **0 tone/pace convergent drift**.
- **Version:** `approved-enumerations.json` `v0.4 → v0.5` (sha256 `c7d32fb4e8004913406bab4f8e543e6ca43443911585581c3212431235b94258`). `validation-rules.json` unchanged (`v0.7`).
- **Verification:** full gate board green — `validate` 12/12 (0 block · 0 drift) · `lint` 6 clean · `coverage --corpus` 6/6 block-clean · `check-drift` (19 pins; approved-enumerations pin `v0.5`) · `gold-diff` baseline unchanged · **`vitest` 136/136**.

---

## SC-0020 — Entity-ID reconciliation (under SC-0018): two `id-check` engine tweaks + ruled exceptions + P01 map alignment
- **Date:** 2026-06-01
- **Decided by:** Marcia Suzuki (the rulings — "follow your disposition", 2026-06-01); the compiler implemented + verified them.
- **Status:** **APPLIED (P01 worked reference + engine + exceptions) — pending the lead's blessing.** The two engine
  tweaks, the signed-off exceptions, the P01 map TM_/TH_ alignment, and the P01 AUDIT-relic removal are in the
  compiler fixtures + tooling. **DEFERRED to the post-blessing roll/writeback:** the B31 BCD alias (A2, a vault
  edit), the P02–P06 roll (no other map carries the same issues beyond the deferred B31), and the vault writeback.
- **Type:** validation-rule tweak + registry/exceptions data + artifact (map) edits. **No closed-list / schema-shape
  change. No spec-version bump.** Touches only entity *references* (codes/slugs/links) + the `id-check` engine +
  the pinned exceptions file.
- **Principle (SC-0018 carried forward):** the prose Meaning Map and the FOR_MODEL are two halves of one training
  pair; an entity named in one must be the same canonical code the other uses. SC-0018 surfaced the P01–P06
  inventory; this entry applies the lead's rulings on it and removes two classes of false positive at the engine.
- **A1 — two `src/engine/id-align.ts` tweaks (the standing-gate corrections):**
  1. **`<NS>?` is a legal withheld-referent, not an error.** A schema-legal code whose tail after the namespace
     prefix is exactly `?` (e.g. `B?`, from `b_code` `^B(\d+|\?)$`) is an INTENTIONAL withheld referent — the
     artifact deliberately declines to bind the slot to a registered entity (P06's deceased-husband, the
     SC-0016-blessed "her husband (pair withheld; see P01-D2)"). New predicate `isWithheldReferent` + a new INFO
     finding `WITHHELD_REFERENT` (`WithheldReferentFinding`, `counts.withheld`). It is pulled OUT of
     reference-integrity (no more `UNKNOWN_CODE`) **and** out of the structural symmetric difference (it can never
     align — the other side legitimately lacks it). **No content change to P06.** Resolves the P06 `B?` ref-integrity
     ERROR + its S2 misalignment.
  2. **Note-title-safe slug normalization for name-binding.** Obsidian note titles (hence wikilink slugs) cannot
     carry an apostrophe `'` or a forward slash `/`; the BCD canonical names do ("Naomi's Dwelling…", "His People /
     People of YHWH"). New `normalizeSlug` (strip `'`, collapse `/`→`-`, collapse repeats) applied to **both** the
     found slug and the expected slug in the B/PL/O/TM/I name-binding compare. **No slug/content edit** — editing the
     slug would break the wikilink unless the note were also renamed; this is the right fix. Resolves the
     `PL_NAOMIS_DWELLING` (P05) + `PL5_BOAZ_PORTION` (P05, P06) apostrophe mismatches.
  Both tweaks are covered by new `tests/id-align.test.ts` cases (`normalizeSlug` units + a name-binding-accepts case;
  `isWithheldReferent` units + B?-is-INFO-not-error/not-misalign cases; the real-fixture integration locks for
  P01/P05/P06 updated to the post-tweak behaviour).
- **A2 — B31 name (registry, vault-side): DEFERRED.** Ruled call = add `People-of-YHWH` (and `People of YHWH`) to
  B31's BCD `aliases` (vault `bcd/beings/`) + re-extract/re-pin `ruth.aliases.json`. This is a vault edit; it is
  **out of scope for this fixtures-only chunk** and rides the post-blessing writeback. The interim `id-check --corpus`
  still shows the B31 name-binding (P02 @S1, P03 @S2) — expected and noted.
- **A3 — the place/time one-siders → signed-off exceptions (`_spec/id-alignment-exceptions.json`).** The legitimate
  map↔FOR_MODEL coverage differences (off-stage/contextual referents) ruled ACCEPTED, taken verbatim from the
  `id-check --corpus` output, each with `accepted_by:"Marcia Suzuki"`, `accepted_on:"2026-06-01"`, `sc_ref:"SC-0020"`:
  - `PL_LAND_OF_JUDAH` (P03, **S1 + S2** — two entries, FM_NOT_MAP) — the covenant land they return *to*; referenced, not scene-present.
  - `TM_BARLEY_HARVEST_BEGINNING` (P04, S3, FM_NOT_MAP) — the harvest frame named at the close; FM-structural only.
  - `PL_AMONG_SHEAVES` (P06, S4, FM_NOT_MAP) — the gleaning locus in Boaz's command; FM-structural only.
  - `PL1` (P04, S2, MAP_NOT_FM) + `PL2` (P04, S1, MAP_NOT_FM) — the already-documented MM↔gold coverage difference (the P04 95% gold-diff).
  The file goes **EMPTY → 6 entries**; re-pinned **0.1.0 → 0.2.0** (sha256 `4247b9aa46a813a5fd92a2307048b412e8bfade70e31679cfb6ed78f8f1eafcc`) in `_spec/pins.json`; `check-drift` clean.
- **A4 — the TM_/TH_ same-referent (P01) → align the map to the FOR_MODEL.** P01's "about ten years" was
  `TM_TEN_YEARS` in the map §3C but `TH_TEN_YEARS_APPROXIMATELY` in the FOR_MODEL. Ruled canonical = the
  thematic-object form (the map's own §3C note calls it "a length of time named inside the scene — not the scene's
  time-setting"). Edited the P01 **map** §3C wikilink `[[TM_TEN_YEARS-About-Ten-Years]]` →
  `[[TH_TEN_YEARS_APPROXIMATELY-About-Ten-Years]]` (same §3C placement + "About-Ten-Years" slug; only the code
  aligns). After the edit the pair no longer splits as a LIKELY_SAME_REFERENT; `TH_TEN_YEARS_APPROXIMATELY` stays
  `unverifiable` (no TH_ registry) — acceptable, by design. **Vault-note situation (surfaced for the lead, NOT
  acted on):** `bcd/times/TM_TEN_YEARS-About-Ten-Years.md` EXISTS; `TH_TEN_YEARS_APPROXIMATELY` is unregistered (a
  pericope-local TH_ overlay, correctly unverifiable). After A4, P01 was `TM_TEN_YEARS`'s only structural user (the
  P05 compilation-log/BCD-DELTA cite it only as a *precedent reference* in prose). The `TM_TEN_YEARS` BCD note is
  thus left orphaned-as-a-structural-referent; **relocate-never-delete** — proposed to the lead (retire/rename vs
  keep-as-alias), not unilaterally touched.
- **A5 — the AUDIT relic (P01 map) → removed.** Removed the `[[P01-Ruth-1-1-5-AUDIT]]` frontmatter wikilink (pilot-2
  produces no AUDIT; a dangling relic, same family as SC-0017). Removal only. The P01 dangling-note flag clears.
- **Engine/report code:** `src/engine/id-align.ts` (`isWithheldReferent`, `normalizeSlug`, `WithheldReferentFinding`,
  `withheldReferents`/`counts.withheld`, withheld-exclusion in reference-integrity + the structural diff, slug
  normalization in name-binding). `src/audit/id-align-ledger.ts` (the withheld-referents section in the CLI text +
  the wiki ledger note + counts). `src/cli/tripod.ts` (the `withheld-referent` corpus-summary tally).
  `tests/id-align.test.ts` 29 → **36**.
- **Residual inventory after A1+A3+A4+A5 (SURFACED for the lead — NOT signed off this chunk):** `id-check --corpus`
  is **2 clean (P04, P06) · 4 with findings · 0 ref-integrity · 2 name-binding (the deferred B31, P02+P03) · 8
  un-accepted misalignments · 0 flag · 0 dangling · 6 accepted · 1 withheld INFO**. The 8 un-accepted misalignments
  are the **pre-existing** SC-0018 "REFERENCED-being-declared-in-§3A-prose-not-the-§3A-header" gaps + two P05 ones —
  NOT introduced here, NOT in the four A3 categories the lead ruled: P01 S4 `B2`/`B8`/`B9`; P02 S2 `B4`/`B5`; P05 S2
  `PL5`, S3 `B2`, S4 `TH_WITHIN_DAY_FROM_MORNING_UNTIL_NOW`. The brief's "clean except deferred B31" gate did not
  enumerate these; per "sign off **only** what the lead ruled," they are left surfaced for the lead's disposition
  (accept-as-coverage-difference, align, or rule otherwise) — see the STOP-and-report.
- **Gates:** `npm test` **133 green** (126 prior + 7 new); `check-drift` clean (re-pinned exceptions 0.2.0);
  `validate` 6/6; `lint --corpus` 0 drift / 7 accepted / 12 clean; `coverage --corpus` 6/6 (245/245); `gold-diff`
  **byte-identical** to the committed baseline (A edits the map code-string + FM free-text, neither in the
  gold-compared layer — confirmed across all 6 pericopes).
- **Boundaries:** fixtures-only. No vault edit (vault `git status` clean). No schema/closed-list/vocabulary change.
  No B31 alias, no template/methodology/prompt seeding, no writeback — all deferred to the post-blessing roll.

## SC-0020 (continued) — the parity-bar refinement + the 8 standing misalignments resolved
- **Date:** 2026-06-01 (the combined P01–P06 roll chunk, on `sc-0019-0020-p01`).
- **Decided by:** Marcia Suzuki (the parity-bar ruling); the compiler implemented + verified it.
- **Status:** **APPLIED (compiler-side).** Resolves the 8 standing misalignments the prior entry surfaced for the
  lead's disposition.
- **A6 — scene-scoped prose-reference parity bar (`src/engine/id-align.ts`).** The lead's ruling: a FOR_MODEL
  scene-being/place/object that the map **references in that scene's §3 prose** (any wikilink in the scene's
  narration / role / relationship / What-Happens lines — the PROSE bucket, not just the §3A–3D declared-entity
  header) counts as **ALIGNED** — the two artifacts agree the entity is in the scene; one DECLARES it structurally,
  the other NARRATES it. It is a misalignment **only** if the map never references it in that scene. Implemented as
  `mapProseByScene` (scene → entity codes referenced in that scene's prose) + `proseAlignedInScene(code, scope)`;
  an FM-only code with a same-scene prose match is dropped from the symmetric difference before it can become a
  finding. **Scene-scoped on purpose:** a prose mention in a *different* scene does NOT align it here. Also made the
  "present-elsewhere" annotation scene-accurate (`mapProseByScope` + `presentElsewhereLabel`), so a cross-scene
  prose home is labelled with its scene (e.g. "present on other side as §3B prose **in S3**") and never misreads as
  same-scene.
- **The 8 standing misalignments → 0 un-accepted:**
  - **5 resolved by the parity bar (same-scene prose-reference):** P01 S4 `B2`/`B8`/`B9` (B2 in the sons' "son of
    …" relationship lines; B8/B9 in Naomi's "mother-in-law of … Orpah and … Ruth" line, all in S4 §3A); P02 S2
    `B4`/`B5` (in S2's "[[B2]], [[B4]], [[B5]] — collectively 'the dead'" line). After the bar, **P01 is fully
    clean** and P02's only finding is the deferred B31 name-binding.
  - **3 signed off as exceptions (genuinely not same-scene-prose-referenced):** all P05, all FM_NOT_MAP, added to
    `_spec/id-alignment-exceptions.json` with `accepted_by:"Marcia Suzuki"`, `accepted_on:"2026-06-01"`,
    `sc_ref:"SC-0020"`:
    - `PL5` @S2 (`COVERAGE_DIFFERENCE_CROSS_SCENE_PROSE_ONLY`) — the FM declares PL5 in S2, but the map's S2 names
      the field only generically ("the field", untagged) and reserves the `[[PL5-Field]]` wikilink for S3, where
      Ruth actually arrives.
    - `B2` @S3 (`COVERAGE_DIFFERENCE_FM_SLOT_ONLY`) — the FM carries B2 in S3 only as the `clan_eponym` slot value;
      the map's S3 names the clan as `[[B29-The-Clan-of-Elimelech]]` and references B2 itself only in S1.
    - `TH_WITHIN_DAY_FROM_MORNING_UNTIL_NOW` @S4 (`COVERAGE_DIFFERENCE_FM_STRUCTURAL_ONLY`) — the FM carries it as a
      structural §3C object; the map records the same content in S4 §3C as bare Hebrew+gloss text (no `[[TH_…]]`
      wikilink, following the P01 about-ten-years precedent), so the map genuinely never wikilinks the code. Remains
      `unverifiable` (no TH_ registry) — acceptable.
  - The exceptions file goes **6 → 9 entries**; re-pinned **0.2.0 → 0.3.0** (sha256
    `5b2760572a0cb1fda20ce7c1e188b835a60468813acd88614d6b2af947b5a29a`) in `_spec/pins.json`; `check-drift` clean.
- **Tests:** `tests/id-align.test.ts` 36 → **39** — the P01 integration lock flipped (B2/B8/B9 @S4 now ALIGNED,
  P01 fully clean); a new P05 integration lock (the 3 gaps survive as ✓ ACCEPTED, scene-accurate annotation); two
  new hermetic parity-bar units (same-scene prose → ALIGNED; cross-scene prose → still MISALIGN, annotated). The
  `real()` integration helper now loads the pinned exceptions, mirroring the CLI.
- **`id-check --corpus` after the refinement:** **4 clean (P01, P04, P05, P06) · 2 with findings · 0 ref-integrity
  · 0 misalignment · 0 flag · 0 dangling · 2 name-binding (the deferred B31, P02+P03) · 3 unverifiable (TH_) · 1
  withheld INFO (P06 B?) · 9 accepted**. Clean except the explicitly-deferred B31 + the TH_-unverifiable, exactly
  as the chunk gate required.

## SC-0019 — Common-Reader Prose Standard: P01 conditioning-prose re-voicing (worked reference)
- **Date:** 2026-06-01
- **Decided by:** Marcia Suzuki (the register target, blessed 2026-05-31: "good study-Bible note for ordinary
  readers"); the compiler drafts the per-map voice, **the lead reviews per map** (the SC-0013 cadence).
- **Status:** **P01 worked reference APPLIED — pending the lead's voice blessing.** The standard governs the
  *conditioning prose* only. P01's prose is re-voiced in the compiler fixtures as the worked reference. **DEFERRED
  to the post-blessing roll:** P02–P06 prose, the methodology/template/agent-prompt seeding (born-clean machinery),
  and the vault writeback.
- **Type:** free-text + guidance only. **No schema / closed-list / vocabulary change. No spec-version bump.** Does
  **not** touch the `register` token, the Level-3 payload (R1–R6), the framework headings, or any pin/schema.
- **The standard (read `docs/COMMON-READER-PROSE-STANDARD.draft.md`):** four rules — **S1** common-reader register,
  **S2** concrete over abstract, **S3** translate the register / keep the substance (**plain ≠ flat**: if a rewrite
  drops a nuance, it's wrong), **S4** drop the critical apparatus (keep load-bearing concept words; gloss once) —
  plus a flag-word→plain table. Touchstone: explain the passage clearly and warmly to an intelligent friend who
  loves the Bible but never went to seminary.
- **B1 — the two missing calibration examples (PROPOSED for the lead's voice sign-off, not final):** drafted from
  real P01 prose — one **scene-description line** (P01 §3C O1 Function: *"precipitating crisis that drives the
  departure"* → *"the thing that sets the whole story off and drives the family out"*) and one **significant-absence
  note** (P01 S1: *"The narrator does not name YHWH … without any explicit divine action."* → *"The narrator never
  says YHWH sent the famine or drove the family out. The book opens with no word of God doing anything."*). These
  complete the draft's calibration bank (it had Level-1 / tone / narrative pairs but no scene-line or absence-note
  pair); they are **proposals** — the final voice is the lead's.
- **B3-P01 — P01 conditioning prose re-voiced** in `fixtures/meaning-map/P01-Ruth-1-1-5.md`: Level 1 (Arc/Burden,
  Context, Tone/Pace, Communicative Function), the §3A–3D scene descriptions (Role/Type/Meaning/Effect/What-it-is/
  Function/Signals), the four significant-absence notes, and the multi-level register-tagging block. The §3F
  "Communicative Purpose" lines were already plain and left unchanged (so the FM `scene_communicative_purpose`
  mirror is untouched). **Plain ≠ flat honoured** — every nuance preserved (e.g. the §2.3 narrator-withholding +
  weight-by-compression; the §3B PL_LAND_OF_JUDAH referential-form `THE_LAND_AFFLICTED_BY_FAMINE` + the 1:6–7
  cross-reference; "about" ten years as narrator restraint). Load-bearing concept words kept + glossed once (hesed →
  "loyal, covenant kindness"; sojourning).
- **Mechanical tie (the FM free-text):** the four `significant_absence` fields in
  `fixtures/for-model/P01-Ruth-1-1-5-FOR-MODEL.md` were matched to the new map wording (in the FM's terser register).
  `scene_communicative_purpose` unchanged (the §3F lines did not move). **gold-diff** re-baselined
  (`--out fixtures/gold-diff-baseline.json`) and confirmed **byte-identical** to the committed baseline — gold-diff
  compares header/classification/scene-IDs/entity-sets/flag-sets, **not** the free-text fields, so the prose +
  significant_absence edits move the file text (a git diff) but **no gold-diff number**. Exactly as the brief
  required ("only those free-text lines moved").
- **Gates:** `validate` 6/6 · `lint --corpus` **0 drift** / 7 accepted / 12 clean (the re-voicing introduced no
  forbidden vocabulary — R4 and S4 aligned) · `coverage --corpus` 6/6 · `gold-diff` re-baselined (byte-identical) ·
  `npm test` 133 green.
- **Boundaries:** fixtures-only; P01 only. No P02–P06 prose, no seeding, no vault edit. Drafts are PROPOSALS for the
  lead's voice sign-off.

## SC-0019 (continued) — P02–P06 conditioning-prose roll (matching the blessed P01 register)
- **Date:** 2026-06-01 (the combined P01–P06 roll chunk, on `sc-0019-0020-p01`).
- **Decided by:** Marcia Suzuki blessed the P01 worked reference (voice + structure) as the calibration anchor; the
  compiler drafts the per-map voice for P02–P06, **the lead reviews per map** (the SC-0013 cadence).
- **Status:** **APPLIED compiler-side — a PROPOSAL pending the lead's per-map voice blessing.** Do not treat as
  final; do not roll into the writeback until blessed.
- **B3-P02..P06 — conditioning prose re-voiced** in `fixtures/meaning-map/P02..P06`, matching the blessed P01
  register ("held-in and plain"): the multi-level register-tagging block, Level 1 (Arc / Context / Tone / Pace /
  Communicative Function), the §3A–3D scene descriptions (Role / Type / Meaning / Effect / What-it-is / Function /
  Signals), and the significant-absence notes. **Not touched:** the `register` token, the Level-3 §4 payload (the
  Q/A inventory), the framework headings, the §3 entity codes/wikilinks/presence tokens, schemas/vocabulary.
- **Plain ≠ flat — the load-bearing nuances kept (the lead's flagged cases):**
  - **P02:** hesed glossed once as "loyal, covenant kindness" (matching P01); the two-wave road argument; the
    womb/husband/hand-of-YHWH grounding; Orpah's kiss vs Ruth's holding-on.
  - **P03:** the six-step binding vow and the self-curse oath (kept the binding ladder + "calls down a curse on
    herself"); the unnamed-gods (v.15) vs YHWH-named (v.17) contrast; the withheld Moabite epithet at the binding;
    the four open "where" places.
  - **P04 (lament / fourfold naming):** §2.3 uses the lead's blessed calibration "after" verbatim ("…sets the full
    life she left against the empty one she's come back to and names God over and over…"); the **full↔empty**
    antithesis kept throughout (the "out full / back empty" bracket, Mara = "bitter" vs Naomi = "sweet"); the
    **fourfold divine naming** kept precise (Shaddai ×2, YHWH ×2 — "named twice … named twice"); the Moabite epithet
    returning in narrator voice; the narrator passing no verdict on Naomi's God-claim.
  - **P05 (miqreh):** §2.1 uses the lead's blessed calibration "after" verbatim ("…happens to end up in Boaz's field
    — and the narrator tells it so you can't quite tell whether it was luck or God's quiet hand"); the luck-vs-
    providence ambiguity kept at every occurrence (§2.1, §2.3, §2.4, §3C miqreh, S3 absence — never collapsed to
    plain "by chance"); the chayil "worth" thread; the textually-disputed v.7b shelter ("read different ways across
    the major commentaries").
  - **P06:** the wing-of-refuge image (kept "take refuge under his wings", "like a bird sheltering its young");
    the amah/shifchah status-shift (kept "from a lowly servant-girl here to something nearer at 3:9"); the three
    "do not" commands (touch / shame / rebuke); the abundance triplet (eat / be satisfied / have leftover → carry
    back to Naomi); the SC-0016-blessed "her husband (pair withheld; see P01-D2)" note left intact.
- **Mechanical tie (the FM free-text):** in `fixtures/for-model/P02..P06`, the `scene_communicative_purpose` and
  `significant_absence` fields that mirror the map's §3F / absence prose were matched to the new wording (in the
  FM's terser "Narrator…" register), preserving load-bearing parentheticals (e.g. P03 `(FIG_0001)`, P06 "per
  P01-D2"). **gold-diff re-confirmed byte-identical** to the committed baseline: gold-diff compares
  header/classification/scene-IDs/entity-sets/flag-sets, **not** the free-text fields, so the prose + matched
  free-text edits move the file text (a git diff) but **no gold-diff number**. (One incidental edit to a §3A
  `Presence:` parenthetical — which *does* feed the comparable layer via the `being:presence` key — was reverted to
  keep the structural layer truly untouched; the gold-diff baseline did not need re-writing.)
- **Gates:** `validate` 6/6 (0 drift) · `lint --corpus` **0 drift / 7 accepted / 12 clean** (the re-voicing
  introduced NO forbidden vocabulary — R4/S4 aligned) · `coverage --corpus` 6/6 (245/245) · `gold-diff`
  byte-identical · `id-check --corpus` clean except deferred · `npm test` **136 green**.
- **Boundaries:** fixtures-only. **No vault edit (vault `git status` clean).** No seeding (methodology/template/
  agent-prompt) — DEFERRED to the post-blessing writeback. Drafts are PROPOSALS for the lead's per-map voice sign-off.

---

## SC-0018 — Cross-artifact entity-ID alignment: the locked convention + the `tripod id-check` checker
- **Date:** 2026-06-01
- **Decided by:** Marcia Suzuki (the convention + the diagnostic mandate, 2026-06-01); the compiler implemented + verified it.
- **Status:** **PROPOSED.** The convention is recorded and enforced by a new deterministic verifier; the
  **reconciliation** of whatever the checker surfaces over P01–P06 is a **separate, later, human-gated pass**
  (it rides with the SC-0019 prose-standard pass — not this entry). Add-only tooling: no content/schema/vocab
  edit; the one spec-side change is a new empty pinned exceptions file.
- **Type:** verifier / tooling + a locked methodology convention (the canonical entity-ID form). No spec-version bump.
- **The principle:** the prose Meaning Map and the FOR_MODEL are the two halves of **one training pair**. An
  entity named in one must be the **same canonical code** the other uses — verifiable by machine, not by a
  human's eye. Today the map writes `[[B2-Elimelech]]` and the FOR_MODEL writes `B2` (recoverable), but in
  places it is a genuine mismatch (P01's "about ten years" is `TM_TEN_YEARS` in the map's §3C but
  `TH_TEN_YEARS_APPROXIMATELY` in the FOR_MODEL — different code *and* namespace). This SC makes the alignment
  mechanical. It is **diagnostic only** — it fixes nothing; it produces the inventory, Marcia rules it, a later
  pass aligns.
- **The locked convention (recorded; the checker enforces it):**
  - **Canonical entity ID = the bare code** (`B2`, `O1`, `PL_LAND_OF_JUDAH`, `CB_0012`, `TM_*`, `FIG_*`, `I*`) —
    what the FOR_MODEL + BCD use and what the schema *patterns* enforce (`being_id` = `^B(\d+|\?)$`, etc.; a name
    like `B2-Elimelech` is schema-illegal in a FOR_MODEL).
  - **Maps carry the code in the wikilink target:** `[[CODE-Slug]]`, `[[CODE-Slug|Display]]`, or `[[CODE]]`. **The
    code = the wikilink target (the part before any `|`), taken up to but NOT including the first hyphen `-`; if
    there is no hyphen, the whole target is the code.** Sound because **codes never contain a hyphen** (the schema
    id patterns are `[A-Z0-9_]`-only). The checker **asserts this invariant from the pinned schema patterns at
    startup** (`assertNoHyphenInCodePatterns`) and fails loudly if any future pattern admits a `-`.
- **The checker (`tripod id-check [paths… | --corpus] [--json] [--out <ledger.md>] [--out-dir <dir>]`):** per
  pericope (the map + its paired FOR_MODEL), runs five steps. The entity-code **namespaces are derived from the
  pinned validation-rules schema `$defs`** (`b_code`/`place_id`/`object_id`/`time_id`/`cb_id`/`figure_id`), never
  hardcoded; map wikilink→code parsing reuses the reader's rule.
  1. **Extract.** MAP: every `[[…]]` whose code matches an entity namespace, tagged **STRUCTURAL** (declared in a
     §3A–§3D per-scene entity block — confirmed section labels `**3A — Beings**`/`**3B — Places**`/`**3C — Objects
     and Elements**/`**3D — Times**`) vs **PROSE** (a relationship/role bullet, §3E "What Happens", §4, §5, or
     frontmatter). A code on a `- Relationship:`/`- Role:` bullet is PROSE, exactly as the reader declares an entry
     only on a non-bullet, wikilink-led line. A wikilink whose code matches no namespace is a **note link** (kept
     for step 5). FOR_MODEL: scene-container ids + pericope `cb_flags`/`figure_flags`; secondary, being-codes used
     as `event_specific_slots` *values* (`^B\d+$`); excludes referential_form / role / function / action / speech_act.
  2. **Reference integrity (both sides).** Every code must resolve to a real BCD entry — consumes the pinned
     `_spec/registry/ruth.aliases.json`. Unknown code in a **tracked namespace** (B/PL/O/TM/I) ⇒ **ERROR**.
  3. **Name-binding (map slugs).** The slug must equal `slugify(BCD canonical name)`, where **slugify = trim ·
     collapse internal whitespace to a single `-` · preserve the source Title-Case (no lowercasing) · drop a
     leading/trailing `-`** (e.g. `Bethlehem of Judah`→`Bethlehem-of-Judah`, `About Ten Years`→`About-Ten-Years`,
     `In the Days When the Judges Judged`→`In-the-Days-When-the-Judges-Judged`). Mismatch ⇒ **ERROR** (catches a
     typo'd slug and a right-name-wrong-code).
  4. **Cross-artifact alignment (the core).** Per scene where scene IDs align (map §3 scene ↔ FOR_MODEL
     `level_2_scenes[].scene_id`, both `S1`/`S2`…), else per pericope: `MAP_STRUCTURAL` (structural codes) vs
     `FM_STRUCTURAL` (scene-container codes + pericope cb/figure flags); report the **symmetric difference**
     (map-not-FM, FM-not-map). Where an unmatched map code and FM code **share a stem**, tag them
     **LIKELY_SAME_REFERENT** (the highest-value finding — the `TM_/TH_` class). PROSE-only mentions are NOT
     required in the FOR_MODEL (checked by steps 2–3 only).
  5. **Dangling note links (secondary).** A map non-entity `[[Note-Title]]` resolving to no existing note ⇒ flag
     (catches stale `[[…-AUDIT]]`/`[[…-COMPILATION-LOG]]`/`[[…-BCD-DELTA]]` links; pilot-2 has no AUDIT). Reported
     separately.
- **Namespaces the vendored registry does not track (decision, flagged for review):** `ruth.aliases.json` covers
  only the concrete coverage namespaces **B/PL/O/TM/I** — it has **no `CB_`/`FIG_`/`TH_` entries** (those live in
  the Concept Bank / Figure Registry / are thematic overlays, not vendored here). Applying "unknown ⇒ ERROR"
  literally to them would flood the inventory with dozens of false errors for schema-legal codes. So
  reference-integrity (step 2) and name-binding (step 3) run **only** for tracked namespaces; a schema-legal code
  in an untracked namespace is surfaced separately as **UNVERIFIABLE_NO_REGISTRY** (shown, not errored). Each
  cross-artifact finding also records whether the code is **present on the other artifact non-structurally**
  (map: frontmatter/§5-flag/prose; FM: a proposition slot) — so "absent" is distinguished from "present, not in
  the structural block".
- **Exceptions (recorded sign-off, the SC-0010 mechanism):** new pinned **`_spec/id-alignment-exceptions.json`**
  (**0.1.0**, **EMPTY**), added to `_spec/pins.json` → `sources`, covered by `check-drift`. Same shape/mechanism
  as `coverage-exceptions.json` / `lint-exceptions.json`: a signed-off entry (keyed by pericope stem · kind ∈
  {REFERENCE_INTEGRITY, NAME_BINDING, MISALIGNMENT, DANGLING_NOTE} · code | note_title | scope/direction)
  downgrades a finding to **✓ ACCEPTED** (shown with reason, excluded from the failure count). **No exceptions
  added in this build** — the first run is the raw, complete inventory.
- **Spec change (exact):** no closed-list / schema-shape change. One new vendored+pinned governed file
  `_spec/id-alignment-exceptions.json` (**0.1.0**, sha256
  `7197ca55ce3bf0940555c011682d904be7bde478003d32ac06f6e4809132a412`), added to `_spec/pins.json` → `sources` and
  verified by `check-drift`.
- **Code:** `src/engine/id-align.ts` (namespace derivation + invariant + the five-step checker),
  `src/audit/id-align-ledger.ts` (CLI text + wiki ledger note), `id-check` CLI verb, `loadIdAlignmentExceptions()`,
  `tests/id-align.test.ts` (16 tests: the invariant; slugify; the six required cases — clean→0, typo'd slug→ERROR,
  wrong-code→ERROR, TM_/TH_→LIKELY_SAME_REFERENT, off-stage PROSE→not-flagged, unknown→ERROR, seeded exception→
  ACCEPTED; the unverifiable-namespace rule; dangling-note resolution; FM code extraction; + a real-fixture
  integration lock on P01's TM_/TH_ pair and P06's `B?` placeholder).
- **First-run inventory (P01–P06, the deliverable Marcia rules):** 6/6 pericopes have findings · **1
  reference-integrity ERROR** (P06 FOR_MODEL `B?` — an unresolved `wife_taken` slot placeholder; legal per
  `^B(\d+|\?)$`, no BCD entry) · **5 name-binding ERRORs** (`B31` slug `People-of-YHWH` vs BCD `His People /
  People of YHWH` in P02+P03; `PL5_BOAZ_PORTION` slug `Boazs-…` vs `Boaz's-…` in P05+P06; `PL_NAOMIS_DWELLING`
  slug `Naomis-…` vs `Naomi's-…` in P05) · **87 cross-artifact misalignments** of which the **1
  LIKELY_SAME_REFERENT** is P01 `TM_TEN_YEARS` ↔ `TH_TEN_YEARS_APPROXIMATELY` (the headline) — the rest are
  largely cb/figure flags that sit in the map's frontmatter/§5 (not §3 structural) and REFERENCED scene-beings the
  map declares in prose but not §3A · **16 dangling note links** (stale `[[…-AUDIT]]`/`[[…-COMPILATION-LOG]]`/
  `[[…-BCD-DELTA]]`/`[[…-VERIFICATION-INPUT-en]]` frontmatter links + P04's `[[T7-Harvest-Provision]]`, a
  non-namespace code that is neither a valid entity nor a real note — a likely typo) · 157 unverifiable
  (CB_/FIG_/TH_). The reconciliation is the later SC-0019-rider pass.
- **Boundaries:** touched no map / FOR_MODEL / BCD / schema / vocabulary / pin content **except** adding the empty
  exceptions file. No `|Display` additions, no code reconciliation, no template/methodology edit, no vault touch.
- **Gates:** `npm test` **113 green** (97 prior + 16 new); `check-drift` clean (the new exceptions file pinned at
  0.1.0); `validate` 6/6 · `lint --corpus` 0 drift / 7 accepted · `coverage --corpus` 6/6 (245/245) · `gold-diff`
  agreement **UNCHANGED** — confirmed (no content touched).
- **Delivery:** compiler PR only (no vault). Marcia receives the P01–P06 inventory to rule.

### SC-0018 refinement (2026-06-01) — low-noise standing gate (R1/R2/R3)
- **Status:** **PROPOSED** (rides with SC-0018). The v1 checker was GOOD — it found every genuine issue — but
  over-reported from three scoping limits. This refinement removes the noise **without losing any genuine
  finding**. Still **diagnostic / add-only**: no map / FOR_MODEL / BCD / schema / vocabulary content touched; the
  `id-alignment-exceptions.json` stays **EMPTY** (unchanged, same pin). The clean inventory is the deliverable.
- **R1 — CB_/FIG_ codes are FLAGS; compare them in their real homes.** v1 looked for codes only in the map's §3
  entity blocks, so every `CB_`/`FIG_` code (which the map carries in **frontmatter `active-concepts`/`active-figures`
  + the §5 Flags section**, NOT §3) read as a false "FM-not-map" misalignment. Now `CB_`/`FIG_` codes are pulled
  out of the §3-block ↔ scene-container structural diff and compared as **flag SETS**: the map's flag set (the
  frontmatter `active-*` list items + the **first wikilink on each §5 bullet**, mirroring the reader's `applyFlags`
  rule) vs the FOR_MODEL's `cb_flags` + `figure_flags`. Aligned flags never report; only a genuinely one-sided flag
  does (new finding type `FlagMismatchFinding`, kind ∈ {CONCEPT, FIGURE}). A `CB_`/`FIG_` wikilink that appears only
  inside a §5 bullet's *narration* (e.g. P06 §5A's parenthetical cross-pericope ref to `FIG_0131`) is **not** a flag —
  matched by the FOR_MODEL keeping such a code in `cross_ref` free-text, not in `figure_flags`. **Removes ~70 false
  misalignments** (the P01–P06 CB/FIG placement noise → 0 flag mismatches).
- **R2 — vendor + pin a Concept-Bank and Figure-Registry index so `CB_`/`FIG_` are verifiable.** v1 had no `CB_`/`FIG_`
  registry (the alias table covers only B/PL/O/TM/I), so 157 legal codes were "UNVERIFIABLE." New offline extractor
  **`extractor/build_concept_figure_registry.py`** harvests the wiki `concepts/*.md` (frontmatter `cb-code:`, optional
  `aliases:`) — 50 files — and `figures/*.md` (frontmatter `fig-code:`, optional `aliases:`) — 115 files — into two
  vendored, pinned registries, each entry `{code, name_slug (= filename minus the `CODE-` prefix), aliases[]}`:
  - `_spec/registry/ruth.concepts.json` (**registry-0.1.0**, sha256 `788d2f10bda81ac6b1a12e9d7cfc2904a3e42d65e744affd92bb48ff3ce817ff`) — 50 entries.
  - `_spec/registry/ruth.figures.json` (**registry-0.1.0**, sha256 `56ebf4d450099ca1ed58ef2771cfec783d192b1dca8a8b1738a9aae34e9d3803`) — 115 entries.
  Both added to `_spec/pins.json` → `sources` and covered by `tripod check-drift` (the same vendor+pin discipline as
  `ruth.aliases.json`). Reference-integrity + name-binding now apply to `CB_`/`FIG_`: a `CB_`/`FIG_` code with no
  registry entry ⇒ **ERROR**; a map slug matching neither the `name_slug` nor any `alias` ⇒ **name-binding ERROR**.
  Over P01–P06 this surfaces **no new genuine CB/FIG findings** (every map/FM flag code resolves; every map CB/FIG
  slug matches its `name_slug`) — but the machinery is now in place and `unverifiable` drops 157 → 2 (only the two
  `TH_` thematic-overlay codes remain, by design — `TH_` has no registry).
- **R3 — note-resolution knows the real pilot-2 artifact suffixes + discourse-thread refs.** v1 flagged
  `COMPILATION-LOG`/`BCD-DELTA`/`VERIFICATION-INPUT[-en]` links and `[[T7-Harvest-Provision]]` as "dangling" only
  because they live in the vault (`stas/`, `bcd/discourse-threads/`), not the compiler fixtures dir — 15 false flags.
  Now `-FOR-MODEL`/`-COMPILATION-LOG`/`-BCD-DELTA`/`-VERIFICATION-INPUT`/`-VERIFICATION-INPUT-en`/`-COVERAGE-LEDGER`
  are recognized as **valid pilot-2 sibling-artifact suffixes that resolve**, and `T#-…` **discourse-thread** refs
  resolve (a documented biblical-only namespace whose notes live in `bcd/discourse-threads/`; `[[T7-Harvest-Provision]]`
  was a real note, NOT the typo the v1 note guessed). **`-AUDIT` is deliberately NOT a valid pilot-2 artifact** → it
  still flags. **Dangling drops 16 → 1** (only P01's `[[…-AUDIT]]`, the one true relic).
- **Refined inventory (P01–P06):** **1 ref-integrity** (P06 `B?`) · **5 name-binding** (`B31` P02+P03;
  `PL5_BOAZ_PORTION` P05+P06; `PL_NAOMIS_DWELLING` P05) · **17 cross-artifact misalignments** (the genuine entity
  gaps only — the P01 `TM_TEN_YEARS`↔`TH_TEN_YEARS_APPROXIMATELY` **LIKELY_SAME_REFERENT**; place/time gaps
  `PL_LAND_OF_JUDAH` P03, `TM_BARLEY_HARVEST_BEGINNING` P04, `PL_AMONG_SHEAVES` P06, P04 `PL1`/`PL2`; REFERENCED
  scene-beings the map declares in §3A-prose but not the §3A header; P05 `TH_WITHIN_DAY…`; P06 `B?`) · **0 flag
  mismatches** (all CB/FIG flags align) · **1 dangling** (P01 AUDIT) · **2 unverifiable** (`TH_` only). Was
  1/5/87(1 LSR)/16/157; now 1/5/17(1 LSR)/0-flag/1/2. Every genuine v1 finding survives; the ~70 CB/FIG placement
  misalignments + 15 false danglings are gone.
- **Code (refinement):** `src/engine/id-align.ts` — `isFlagCode`, `harvestMapFlags` (map flag-set), `extractForModelFlags`
  (FM flag-set), `buildFlagRegistry` + `loadConceptRegistry`/`loadFigureRegistry` (R2), `FlagMismatchFinding` + the
  flag-set comparison step (R1), `isKnownPilot2Sibling` + the refined dangling resolution (R3), CB/FIG branches in
  reference-integrity + name-binding (R2). `src/reader/source-packet.ts` — `CodeRegistry` types + loaders.
  `src/audit/id-align-ledger.ts` — the flag-mismatch section + updated counts/labels. `id-check` CLI summary gains
  `flag-mismatch`. `extractor/build_concept_figure_registry.py` + `extractor/README.md`. `tests/id-align.test.ts`
  grows 16 → **29** (R1 flag-set comparison incl. the §5-narration exclusion; R2 CB/FIG ref-integrity + name-binding
  + alias tolerance; R3 suffix/thread resolve + AUDIT-still-flags; unit tests for the new helpers; + the refined
  real-fixture integration locks across P01/P03/P04/P05/P06).
- **Note (exceptions `_doc`):** a new exception **kind `FLAG_MISMATCH`** is supported by the engine for signing off a
  one-sided flag; the `id-alignment-exceptions.json` `_doc` comment still lists only the original four kinds. The file
  is left **byte-for-byte unchanged** (still EMPTY, pin intact) per the add-only mandate — re-pinning it is out of scope.
- **Gates (refinement):** `npm test` **126 green** (113 prior + 13 new); `check-drift` clean **incl. the two new
  registry pins**; `validate` 6/6 · `lint --corpus` 0 drift / 7 accepted / 12 clean · `coverage --corpus` 6/6
  (245/245) · `gold-diff` agreement **UNCHANGED** (95/98/96% — no content touched).

---

## SC-0017 — De-leak: strip process-commentary from the content layers
- **Date:** 2026-06-01
- **Decided by:** Marcia Suzuki (the principle + scope); the compiler applied + verified it.
- **Status:** **SHIPPED — BLESSED by Marcia 2026-06-01.** Fixtures applied + Architect-verified, then the
  **vault writeback DONE** (both PRs merged 2026-06-01 — compiler #17 `ebe7847`, vault #7 `6210b3b`). Vault
  `pericopes/` + `stas/` are byte-identical to the deleaked fixtures; the template + discipline-doc (R6) edits
  landed in the vault. See the vault-writeback record at the end of this entry.
- **Type:** artifact remediation + docs/template hygiene (no schema/spec change; no closed-list change).
- **Principle:** a meaning map and a FOR_MODEL describe the **passage**, never the **project**. The
  relocation audit-trail SC-0013/SC-0016 left inline in §3C (`SC-XXXX`, "Relocated per the content
  discipline", "§3C entities only", "X → Proposition N") is a worklog leaking into the source artifact. It
  comes out. The relocate-never-delete record already lives, durably, in `docs/SC-0013-RELOCATION-AUDIT.md`
  + `docs/SC-0016-LEVEL3-SWEEP-AUDIT.md` (confirmed present before stripping) — removing the inline notes
  loses nothing.
- **Verified scope — the leak was in BOTH trees, phrased differently:**
  - **Maps** (`fixtures/meaning-map/P0*.md`): §3C blockquotes (`> §3C lists entities only … Relocated per the
    content discipline: …`) and `- None: … Relocated per the content discipline[ (SC-XXXX)]: X → Proposition N; …`
    trails (single- or multi-line bulleted). Stripped to the bare considered-absence / the header above the entities.
  - **FOR_MODELs** (`fixtures/for-model/P0*-FOR-MODEL.md`): inside `objects_in_scene._note` as
    `"§3C entities only (SC-00XX). [No persistent objects.] Relocated: … → P# (…); … → B# referential_form; …"`.
    A literal-string grep for the map phrasing finds nothing here — this is the trap; targeted by pattern.
    **P01's FOR_MODEL used `(SC-0012)` phrasing** an earlier narrower grep had missed; caught + stripped.
- **Strip vs keep (the cut boundary):** STRIP every span matching the union pattern (case-insensitive)
  `relocat` · `SC-00[0-9][0-9]` · `→ Prop`/`→ Proposition` · `content discipline` · `§3C entities only`.
  KEEP: entity entries (`[[O#]]`/`[[CB_]]`/`[[TM_]]` + What-it-is/Function/Signals; FOR_MODEL `entries[]`);
  the plain considered-absence (`- None: no persistent objects in this scene.` / `_note: "No persistent
  objects in this scene."` where `entries: null`); and every passage-describing note (`times_in_scene`
  "No distinct temporal frame…", `register_overrides._note`, `significant_absence`, and the §3C reasoning that
  the about-ten-years/within-day duration is content carried in §3C — these describe the *text*, not the cleanup).
- **`_note` handling rule (FOR_MODEL):** if a scene has real `objects_in_scene.entries`, the whole `_note`
  was scaffolding → **remove the `_note` key** (the entities carry the content; `_note` is optional, stays
  schema-legal). If `entries: null`, reduce `_note` to the minimal considered-absence
  `"No persistent objects in this scene."`. (10 keys removed; 7 reduced to minimal across P01–P06.)
- **Also removed (ruled by the lead, 2026-06-01):** a stale **`- PENDING (the lead's ruling): the
  doubled-divine-name pattern …`** bullet in P04 §3C — process-commentary that is now false: FIG_0195 captures
  that pattern and is flagged at P04 §5B + active-figures + FOR_MODEL P4/P5 `figure_flags` (SC-0016), so nothing
  is lost. (Its FOR_MODEL twin was already cleaned by the `_note` rule.)
- **Guidance fix (vault-only, in the writeback pass):** the meaning-map template
  (`_templates/meaning-map-template.md`) — §3C = entities or a plain "None: <reason>" only, the
  relocate-never-delete record goes to the SC audit doc, never inline; and the discipline doc
  (`_methodology/level3and3Ccontentdiscipline.md`) — add the principle (the map describes the passage, not
  the project; no process-commentary in the content layers; same family as R5). Neither file exists in the
  compiler repo, so these land in the vault writeback.
- **Verification (achieved):** **zero-grep across both trees** — the union pattern returns 0 matches in
  `fixtures/meaning-map/` and `fixtures/for-model/`. `validate` 6/6 (confirms the `_note`-key removals are
  schema-legal) · `lint --corpus` **0 drift / 7 accepted** (second-order gate held: the 7 §4 keeps resolve by
  `(pericope, rule, match, context_prefix)`, not line number, so the §3C strip did not orphan them) ·
  `coverage --corpus` 6/6 block-clean (245/245, 0 unanchored) · `gold-diff` **agreement layer UNCHANGED**
  (matched/divergent/agreementPct byte-identical pre/post; only the informational `judgmentPlaceholders`
  count fell — the removed §3C prose carried skeleton `__TODO__` spans the compiler enumerated, not a fidelity
  measure — baseline re-written) · `npm test` **97 green** · `check-drift` clean. Removal-only: 21 insertions
  (all minimal-note normalizations) / 101 deletions; no entity, proposition, §4, flag, figure, or
  significant_absence touched.

### SC-0017 vault writeback (2026-06-01) — DONE
The blessed §3C/`_note` de-leak was propagated to the canonical vault (`ruth-pilot-b-wiki`), and the guidance
edits (which don't exist in the compiler repo — the meaning-map template + the discipline doc's new **R6**)
landed there too. Delivered on vault branch `sc-0017-deleak-vault` via **reviewed vault PR #7**, merged to vault
`main` (`6210b3b`); compiler **PR #17** merged to compiler `main` (`ebe7847`). **Architect-verified after
merge:** vault `pericopes/P01–P06` + `stas/*-FOR-MODEL` are **byte-identical** (`cmp`, 12/12) to the deleaked
compiler `fixtures/`; **zero-grep clean** across both trees; `validate` 6/6 · `lint --corpus` 0 drift / 7
accepted · `coverage --corpus` 6/6 · `gold-diff` agreement unchanged · `check-drift` clean · 97 tests green.
The vault working copy was returned to clean `main` and both merged branches deleted. **Process artifacts
intentionally retained provenance** — P04's BCD-DELTA `audit_note` (`SC-0016`) and the COMPILATION-LOG
compilation prose are the audit/governance layer, deliberately out of the content-only scope. This closes the
§3C / Level-3 content-discipline arc (SC-0012 → SC-0017).

---

## SC-0016 — Level-3 §4 content sweep (operating test) under SC-0012/SC-0013
- **Date:** 2026-05-31
- **Decided by:** Marcia Suzuki (per-row rulings 2026-05-31; the compiler applied + verified them)
- **Status:** **SHIPPED + VAULT-SYNCED — BLESSED by Marcia Suzuki 2026-05-31.** Fixtures shipped after an
  Architect fidelity pass (relocate-never-delete verified on the high-stakes drops: P04 lament divine-agency
  atoms "YHWH has testified against me" / "Shaddai has done evil to me" survive; P05 miqreh + verb-chain +
  working-time-code payloads survive; gold-diff independently UNCHANGED). **Vault writeback DONE** (2026-05-31,
  vault branch `sc-0016-vault-writeback`): vault `pericopes/P01–P06` now **byte-identical** to compiler
  `fixtures/meaning-map/` (the §4 sweep), and the figure spans are recorded in the vault `figures/` registry
  (additive proposition-span enrichment, 32 files, 96 insertions / 0 deletions). See the vault-writeback record
  at the end of this entry.
- **Type:** artifact remediation (no schema/spec change) + a new pinned reviewer-sign-off ledger (governed).
- **Summary:** The §4-proposition counterpart to SC-0013's §3C sweep. SC-0015 made the lint *see* the §4 blind
  spots; this acted on the full true inventory (**150 findings** across P01–P06). For every finding, the
  operating test was applied (*would the model SAY this in the translation? if no, it is conditioning and
  leaves §4*): (a) **60 inline `cross_ref`/link lines** removed (P01 2 · P02 2 · P03 5 · P04 19 · P05 21 ·
  P06 13) — the figure span survives in each map's §5A/§5B flags (verified present before removal) and is
  enriched into the figure registry by the deferred vault patch; (b) **21 meta/analytical questions**
  converted to payload questions or dropped where the payload already lived in a sibling Q&A (form-analysis →
  the CB_/FIG_ flag); (c) **39 compound answers** atomized one-act-per-pair; (d) **19 question/answer-side
  forbidden-vocabulary** hits rephrased in plain language; (e) **11 §4-answer interpretive labels** bared to
  the act. Per-proposition ledger + relocate-never-delete verification: `docs/SC-0016-LEVEL3-SWEEP-AUDIT.md`.
- **Rulings applied (Marcia):** bless P01; **C1** drop bracketed glosses (nuance rides on the flag); **C2**
  form-questions → payload (or drop when redundant); **C3** entity-list/party-pairs are payload; split the P06
  prostration doubling, "deliberately, intentionally" → "on purpose"; two batch rewrites (P05 P1 narrator-pause
  → introduction content; P02 P8 keep "(none)" as asserted payload).
- **Recorded reviewer sign-off (the analogue of SC-0010 coverage-exceptions):** **7** §4 lines were ruled
  **KEEP** — genuinely-exegetical false-positives for the compound heuristic (a vocative, a discourse opener, a
  single directive, a fixed oath formula, a me/you party-pair, a withholding-note, and an entity-pair "her
  father and her mother"). Rather than reword the text to dodge the regex, they are signed off in a new pinned
  **`_spec/lint-exceptions.json`** (provenance per entry): `tripod lint` downgrades a matched finding to
  **ACCEPTED** (shown `✓` with reason, excluded from the drift count). New code: `applyLintExceptions()` +
  `recount()` + `loadLintExceptions()` + CLI wiring + tests; the engine stays pure (always surfaces the
  finding; acceptance is a recorded downgrade).
- **Blessing-pass rulings (Marcia, 2026-05-31):** (a) **"her father and her mother"** ruled KEEP as one (an
  entity-pair, like "his two sons Mahlon and Chilion"; an earlier cut wrongly split it) → un-split + recorded as
  the **7th** sign-off (`ENTITY_PAIR`); lint-exceptions re-pinned **0.1.0 → 0.1.1**. (b) **Politeness particles
  ("na"/please) = register, not payload** — confirmed as a **standing precedent** for deference markers
  ("please / your servant / my lord") through P07–P14 (so e.g. P05's "let me glean, please" → atom "let me
  glean", the particle conditioning).
- **Spec change (exact):** no closed-list / schema-shape change. New vendored+pinned governed file
  `_spec/lint-exceptions.json` (**v0.1.1**, sha256 `2d756b6c0374e54598a6084b3fcb4b3c7fa74f56c656d6b0eae79f28533f4279`),
  added to `_spec/pins.json` → `sources` and verified by `check-drift`.
- **Artifact migration:** `fixtures/meaning-map/P01–P06` §4 swept (this repo). The **vault** `pericopes/*.md`
  + `figures/FIG_*.md` writeback is **deferred** per the lead's routing ruling — recorded as a patch in
  `docs/sc-0016/FIG-span-relocations.proposed.md`. No FOR_MODEL / §3C / Levels 1–2 / §5-flag content touched.
- **Verification (achieved, at blessing):** `tripod lint --corpus` → **0 drift (0 tier-1, 0 tier-2) · 7
  accepted (signed off) · exit 0** — the operating-test bar, mechanized. `validate` 6/6 · `coverage --corpus`
  6/6 block-clean (245/245, 0 unanchored, 1 accepted) · `gold-diff` **UNCHANGED at blessing** (P01 100 · P02 90 ·
  P03 100 · P04 95 · P05 98 · P06 96 — proof the entities/flags/structure layer did not move) · `check-drift`
  clean (15 pins) · **97 tests green**.

### SC-0016 vault writeback (2026-05-31) — the deferred canonical sync
The blessed §4 sweep + figure spans were propagated from the compiler fixtures into the canonical vault
(`ruth-pilot-b-wiki`), on a fresh branch `sc-0016-vault-writeback` off `origin/main`. **Faithful transcription,
not authoring** — no §4 answer re-edited.
- **Branch hygiene (verified, not assumed):** the vault working copy's prior branch
  `claude/p02-p06-content-remediation` was **1 empty auto-backup commit** ahead of `origin/main` (`git diff`
  empty); all substantive content (SC-0013 `636feba`) was already merged via PR #5 (`e2fc80f` = origin/main).
  Nothing substantive stranded → based the writeback on `origin/main`.
- **§4 transcription (diff-first, no clobber):** for each P01–P06, confirmed the ONLY delta vs the blessed
  `fixtures/meaning-map/` was inside §4 (frontmatter + §1–3 + §5 Flags byte-identical before edit; §3C already
  shared from SC-0013), then synced. Vault `pericopes/P01–P06` now **byte-identical (`cmp`)** to the fixtures.
- **Figure spans (additive):** each figure that carried a §4 cross_ref span now records it in its own entry —
  `opens-at-proposition`/`closes-at-proposition` frontmatter + one Appearances bullet, from the §5B Figure-Flag.
  32 figure files, 96 insertions, **0 deletions** (verified purely additive). §5B flags + FOR_MODEL cross_ref
  fields unchanged. (Worklist: `docs/sc-0016/FIG-span-enrichment-worklist.md`.)
- **Safety rail:** Marcia paused the Obsidian Git auto-backup for the duration (re-enabled after merge), so the
  edits could not be swept onto the branch before review.
- **Folded-in ruled tasks (separate commits):** **Task A** — created figure **FIG_0195** (Fourfold Divine
  Naming in Lament; the pattern complementing FIG_0006 + FIG_0086) in both vault + fixtures: figure file +
  P04 BCD-DELTA `to_figures_registry` + map §5B/active-figures + FOR_MODEL P4/P5 `figure_flags`; gold-diff
  re-baselined (P04 matched 37→38, the only line changed). **Task B** — this entry's SC-0014 forward-pointer
  (above): the old value still in the Pilot-3 Layer-2 seed CSV, to reconcile at Pilot-3 lock (pointer only,
  no CSV edit).
- **Delivered via** a reviewed vault PR (`sc-0016-vault-writeback` → vault `main`); on merge the vault working
  copy returns to clean `main`, the auto-backup is re-enabled tracking `main`, and the stale
  `claude/p02-p06-content-remediation` branch is removed.

---

## SC-0015 — Extend the Level-3 lint (operating test on §4 blocks)
- **Date:** 2026-05-31
- **Decided by:** Marcia Suzuki
- **Status:** **APPLIED** (repo PR for the lint engine + lexicon + tests; no vault artifact touched).
- **Type:** tooling / drift-guard (no closed-list change; no schema-shape change).
- **Summary:** The SC-0013 relocation audit found the SC-0012 `lintMeaningMap` was **blind in three ways** on
  the Level-3 §4 layer, so the operating test (*a Level-3 block contains ONLY payload Q&A pairs — flag every
  line that isn't one*) was only half-enforced. This extends the lint with **six** changes:
  1. **`link_in_level3` (new rule, tier-1).** The old code `continue`d past every line matching `cross_ref`
     (and `[[FIG_…]]` pointer lines), so inline cross_refs/inter-proposition links in §4 were never flagged.
     That skip is **removed**; such lines are now FLAGGED via a new `link_markers` lexicon list
     (`cross_ref`, `caused_by`, `paired_with`, `forward_link[-]`, `back_reference[-]`,
     `back_reference_to_proposition`). The link rule short-circuits the line, so a `[[FIG_…]]` cross_ref is
     reported **once** (as the link), never double-counted as an interpretive_label.
  2. **Question-side scanning.** The old code looked only at `**A:**` answers. The loop now splits each line
     into `{q, a}` (new `qaParts`, handles same-line `**Q:** … **A:** …` and split forms) and runs the
     forbidden-vocabulary / interpretive-label scan over the **question** too (this is what surfaces
     `«verb»`/`«agent»`/`«lexeme»`/`«clause»`/`«subject»` in analytical prompts).
  3. **`meta_question` (new rule, tier-2).** A new `meta_questions` lexicon list (regex sources:
     `construction`, `verb[- ]chain`, `pattern?`, `stylistic`, `texture?`, `effect?`, `closure label`,
     `root?`, `…-marker`/`marker?`, ordinal `… part?`, `…-form?`) flags analytical/meta questions about
     form/structure/style that aren't payload.
  4. **Comma compounds + entity-list guard.** `compound_markers` gains `", "`; the compound check moves into a
     new `compoundMarker(a)` helper that, for `, ` / ` and `, first strips wikilinks, entity-id tokens and
     proper names, so an answer that merely **names** several entities ("his two sons [[B4]] Mahlon and [[B5]]
     Chilion"; "the family [[B2]] Elimelech, [[B3]] Naomi") is **not** flagged — only a connector still
     joining two word-groups after stripping (two acts/clauses) is.
  5. **Conditioning bleed on the question side** (new `conditioning_qa` values `tone?`, `pace?`, `genre?`,
     `scene-kind?`, `proposition-kind?`) is matched as a whitespace-bounded substring on the question (and on
     stray non-Q&A lines), not a naïve whole-line `includes`.
  6. **More §4-answer labels (scoped, not shared).** The softer dialogue-paraphrase labels the SC-0013 audit
     listed (`declaration`, `full[- ]knowledge`, `instruction about`, `further command`/`further instruction`,
     `reports a`, `recites`/`recital`, `answers with`) go in a **new `answer_labels` lexicon list applied to
     meaning-map §4 ANSWERS ONLY** — deliberately *not* added to the shared `interpretive_labels` (which
     `scanProse` also runs over FOR_MODEL fields and §3C notes). This keeps them from false-positiving on a
     governed closed-list `speech_act` value (e.g. `REFUSES_REQUEST_WITH_COUNTER_DECLARATION` contains
     "DECLARATION") or a §3C relocation `_note` — both **out of scope** for the §4 content sweep and not
     fixable by it. (A first cut merged them into `interpretive_labels`; that surfaced 6 FOR_MODEL findings on
     exactly those governed enums/notes and was corrected to the scoped list.) Also added the **`hifil`**
     spelling to `forbidden_vocabulary` (the list had `hiphil` only; R4 names "hifil/qal/piel").
  7. **Truthful per-proposition reporting.** The finding `location` now carries the current Level-3 block
     (`§4 Prop 5 · A`, etc.) and `finalize`'s de-dup key includes `context`, so distinct lines that share a
     rule+match (e.g. 21 inline cross_refs in P05, each its own relocation) are counted **separately** instead
     of collapsing to one. A genuine re-scan of the identical string (FOR_MODEL `walkStrings`) still collapses.
     This makes `lint --corpus` the true §4 inventory the sweep is measured against, not a coarse undercount.
  `lintForModel`, `scanProse`, `walkStrings`, `section`, `R1_NOT_ENTITY`, `snake` are unchanged
  (FOR_MODEL `cross_ref`/link fields are intentionally left alone), so **no FOR_MODEL finding count changes**.
- **Files touched:** `src/engine/lint.ts` (rule union + `Lexicon` fields incl. `answer_labels` +
  `qaParts`/`compoundMarker` helpers + rewritten `lintMeaningMap` per-line loop, answer_labels applied
  answer-side only); `_spec/lint-lexicon.json` (`schema_version` 0.1.0→0.2.0; `+link_markers`,
  `+meta_questions`, `+answer_labels` (10, §4-answers-only), `+hifil`, `compound_markers += ", "`,
  `conditioning_qa +=` 5, `interpretive_labels` left at its original 15, `_doc` refreshed); `tests/lint.test.ts`
  (rewrote the old "exempts cross_ref" test to assert the new FLAG behavior + once-not-twice count; added
  meta-question, question-side-jargon, comma-compound + entity-list-guard, answer-label-in-§4, and a
  FOR_MODEL scope-guard test proving a soft label inside a closed-list `speech_act`/§3C note is NOT flagged);
  `_spec/pins.json` (re-pin, below); this log.
- **Lexicon re-pin:** `lint-lexicon.json` **0.1.0 → 0.2.0**;
  sha256 `15a67a5fe1a9e646b76efc7195fa1bdbaf8fe6e6079d186b56e269d841b7b825` →
  `5bfa0e3fa9afdb8b90912865185a95c8596a7e1c3c58f90000ed748e2f3bb8f2`. `tripod check-drift` green at the new pin.
- **Relation to SC-0012/SC-0013:** completes SC-0012's drift-guard by closing the cross_ref-skip and
  answer-only blind spots the SC-0013 audit found; it does **not** touch artifact content — the content sweep
  those findings call for is reserved as **SC-0016** (human-gated).
- **Validator impact (corpus lint, before → after):** `tripod lint --corpus` **14 → 150** findings
  (**0 → 62 tier-1**, 88 tier-2); **7 artifacts clean** (P01 meaning-map after its cross_ref removal under
  SC-0016, + all six FOR_MODELs — unchanged from baseline). All newly-surfaced findings are in the
  previously-invisible meaning-map §4/§3C layer; the true per-rule corpus tally is **60 `link_in_level3`**
  (inline cross_refs: P02 2 · P03 5 · P04 19 · P05 21 · P06 13), **21 `meta_question`** (P03 1 · P04 9 ·
  P05 9 · P06 2), **39 `compound`** (P02 7 · P03 5 · P04 8 · P05 10 · P06 9), **19 question/answer-side
  `forbidden_vocabulary`** (`verb`/`agent`/`lexeme`/`clause`/`subject`/`hifil`: P02 5 · P04 7 · P05 4 · P06 3),
  **11 §4-answer `interpretive_label`** (P02 1 · P06 10). (The pre-truthful-reporting view collapsed these to
  54 by de-duping per coarse location.) No FOR_MODEL count changed (lintForModel untouched; answer_labels are
  §4-answer-scoped).
- **Verification:** `npm test` 94/94 green (lint.test.ts 14/14); `tripod check-drift` clean incl.
  lint-lexicon at v0.2.0 + the closed-list sync invariant; `tripod validate fixtures/for-model/` 6/6 valid;
  `tripod gold-diff` unchanged (P01 100 · P02 90 · P03 100 · P04 95 · P05 98 · P06 96);
  `tripod coverage --corpus` 6/6 block-clean (245/245). No fixture content edited.

---

## SC-0014 — Rename SPEECH_ACT `ASCRIBES_TO_DIVINE_AGENT_LAMENT_FRAME` → `ASCRIBES_AFFLICTION_TO_GOD_IN_LAMENT`
- **Date:** 2026-05-31
- **Decided by:** Marcia Suzuki
- **Status:** **APPLIED — pending the project lead's blessing** (vault PR for P02–P06 / repo PR for the spec + fixtures).
- **Type:** closed-list change (L1 SPEECH_ACT) + artifact migration.
- **Summary:** `tripod lint` surfaced that the closed SPEECH_ACT list carried
  `ASCRIBES_TO_DIVINE_AGENT_LAMENT_FRAME` — "AGENT" is role-theory jargon (R4), in a Layer-1 closed-list
  value, so it cannot be fixed by content edits. **Ruling:** rename it to **`ASCRIBES_AFFLICTION_TO_GOD_IN_LAMENT`**
  — drops the "AGENT" jargon; uses **GOD** (not YHWH) because SPEECH_ACT is the cross-corpus interlanguage
  (it must read for the LA_RECORDING profile too). The new value carries no forbidden-vocabulary token.
- **Spec change (exact):**
  - `validation-rules.json` `closed_lists.SPEECH_ACT`: `ASCRIBES_TO_DIVINE_AGENT_LAMENT_FRAME` →
    `ASCRIBES_AFFLICTION_TO_GOD_IN_LAMENT` (one value; no other list touched). Version **v0.6 → v0.7**,
    `$id` + `for_model_schema.$id` bumped, `sibling_schemas.compilation_log` → `v0.5`, `supersedes` appended.
  - `compilation-log.schema.json` `$defs…speech_act` enum: same rename (kept in sync). Version **v0.4 → v0.5**,
    `$id` bumped, `supersedes` appended. (No structural change — additive-only history preserved.)
  - There is **no** `for_model_schema.$defs.speech_act_value` enum (SPEECH_ACT is enforced by the L1
    closed-list block, not an ajv enum), so the closed-list **sync invariant** — which covers
    REGISTER / GENRE / NARRATIVE_FRAMING — is unaffected.
- **Artifact migration (done):** the value occurs as a structured `speech_act` in **P02 FOR_MODEL (×1, P12)**
  and **P04 FOR_MODEL (×4, P4+P5)**; migrated in vault `stas/` + compiler `fixtures/for-model/`. The P02/P04
  **COMPILATION-LOG**s (structured values + audit prose) migrated in vault + compiler fixtures. Propagated to
  `_spec/agent-3-system-prompt.md` (the Agent-3 prompt that emits the value) and the Pilot-2 usage glossary
  `_templates/sta-vocabulary.md`. **Deliberately NOT touched** (out of Pilot-2 scope): `_pilot-3-design/`, the
  bible-wide Layer-2 seed (`tripod-bible-wide-layer-2-vocabulary-seed-*` + `.csv`, `sta-vocabulary-general.md`)
  where it is a `PENDING_PILOT3_LOCK` candidate, and `_archive/` + `_working/` (historical audit trail).
- **⚠ FORWARD-POINTER (Pilot-3 lock) — added under SC-0016 vault writeback (2026-05-31):** the **old**
  `ASCRIBES_TO_DIVINE_AGENT_LAMENT_FRAME` value still lives in the bible-wide Layer-2 **seed CSV** (×2 rows,
  Pilot-3 lane) as a `PENDING_PILOT3_LOCK` candidate. It was intentionally left there (no CSV edit — Pilot-3 is
  out of this lane's scope). **At Pilot-3 lock this must be reconciled** to the renamed `ASCRIBES_AFFLICTION_TO_GOD_IN_LAMENT`,
  or SC-0014's de-jargoning is silently reversed when the seed is promoted. Recorded as a pointer only; do not
  cross into Pilot-3 to fix it now.
- **Validator impact:** `tripod validate` blocks the old value (no longer in the closed list) and accepts the
  new one. The 5 FOR_MODEL `[forbidden_vocabulary] «agent»` lint findings disappear (corpus lint **19 → 14**,
  the residual 14 being the §4 Q&A dialogue reserved for the lead).
- **Version:** `validation-rules.json` **v0.6 → v0.7** (sha256 `8c436b261178517108cb44e39408bca2c99dfd8674fb4954ccf2e72197cadcbe`);
  `compilation-log.schema.json` **v0.4 → v0.5** (sha256 `f009b32781f8a0e4d4d40e4f7500dd64396bdeeb06e3fe16d9ff2214033c36ca`).
  Both re-pinned in `_spec/pins.json` + the pin table above.
- **Note (pre-existing drift observed, flagged):** the vault `_spec/compilation-log.schema.json` was at **v0.3**
  and lacked SC-0007's promotion slots — i.e. SC-0007 had shipped to the compiler's vendored/pinned copy but was
  never written back to the vault canonical. Applying SC-0014, the vault `_spec/` schemas were re-synced to the
  pinned compiler copies (v0.7 / v0.5), incidentally closing that drift. Vault COMPILATION-LOG **artifacts** were
  not back-filled with SC-0007 convergent values (out of scope); flagged for a future vault-writeback pass.
- **Verification:** `tripod check-drift` green against the new pins + the closed-list sync invariant;
  `tripod validate fixtures/for-model/` 6/6 valid; `tripod validate fixtures/compilation-log/` 6/6 valid
  (P02/P04 against the renamed v0.5 enum); `tripod lint --corpus` 14 findings (0 tier-1), the 5 `«agent»`
  findings gone; `npm test` green.

---

## SC-0013 — Map content remediation (SC-0012): P01 reference + P02–P06 roll
- **Corrective second pass — P02–P06 brought to the blessed-P01 standard (2026-05-31):** the first roll
  (below) was sound on the plain-language (R4) fixes, the dialogue intact, and P06's keepers — but it
  over-deleted §3C on a **false premise** ("P02–P05 had zero §3C entities"). Per the lead's rulings, those
  pericopes **do** reference real Concept-Bank entities that belong in §3C (R1, per blessed P01/P06). Restored
  as proper §3C entities (map: Hebrew header + What-it-is/Function/Signals; FOR_MODEL: `objects_in_scene`
  `object_id: CB_00xx` + `function_in_scene`), BCD unchanged (these CB_ were already registered):
  - **P02** — bread `CB_0012` (S1); hesed `CB_0011`, blessing `CB_0008`, rest/menucha `CB_0014` (S2).
    Relocations made real: daughter-in-law (kallah) → B8/B9 `referential_form` (`CB_0017`); mother's-house →
    place `PL_EACH_HER_MOTHERS_HOUSE` (`CB_0013`); paqad / news-hearing / departure verbs → P1–P3.
  - **P03** — §3C "None" confirmed correct; the six vow bindings verified surviving in P3 `vow_components`;
    pairing `CB_0021` + `FIG_0072`/`FIG_0074` and levirate `CB_0019` verified as flags.
  - **P04** — barley-harvest `CB_0026` (S3). Mara `CB_0023` (→ P4 renaming + B3 `referential_form` + `FIG_0082`),
    full/empty `CB_0024`+`CB_0044` (→ `FIG_0084` + P5 payload), testify-against `CB_0025` (→ P5), Moabite
    `CB_0004` + daughter-in-law `CB_0017` (→ B9 `referential_form`) all verified. **STOP / surfaced for the
    lead:** the **doubled-divine-name pattern** (YHWH×2 / Shaddai×2, 1:20–21) has **no figure of its own** —
    its four invocations survive (Shaddai→`FIG_0006`; YHWH→P5+`CB_0025`) but the pattern-as-figure was dropped;
    a new `FIG` is proposed (handoff), not added pending the ruling.
  - **P05** — chayil `CB_0032` (S1); favor `CB_0033`, gleaning `CB_0034` (S2); providence/miqreh `CB_0035`,
    blessing `CB_0008` (S3); foreman-role `CB_0036` verified in B15 `referential_form`, Moabite `CB_0004` added
    to B9 `referential_form`. The within-day "from morning until now" duration restored to §3C (it was a
    content-duration the map's §3D note still pointed at, per the P01 `TM_TEN_YEARS` precedent).
  - **P06** — keepers unchanged; the FOR_MODEL was additively aligned to the map, which retained `CB_0034`
    (S1/S4) and `CB_0033`/`CB_0038`/`CB_0037`/`CB_0008` (S2) as §3C entities the FOR_MODEL had dropped.
- **Per-item relocation notes (both artifacts) replace the first pass's generic per-scene counts** ("N thematic
  items relocated…"), matching P01's item→named-destination standard; a per-pericope relocation audit table
  proves relocate-never-delete. The deleted `*_form` slots were re-verified (grammatical-pattern labels; the
  events survive in `proposition_kind`/components).
- **Result (corrected):** corpus lint **19 → 14** (0 tier-1; the 14 are §4 Q&A dialogue + were 19 before SC-0014
  removed the 5 `«agent»`); validate 6/6; coverage 6/6 block-clean (245/245, 0 unanchored); gold-diff agreement
  unchanged; tests green.
- **Residuals (by design, surfaced for the lead):** (a) the **§4 Q&A dialogue compounds** (re-atomizing is the
  lead's exegetical pass — each surfaced with a proposed atomization, not decided); (b) the **P04 doubled-divine-name
  figure** (above).

### First roll (2026-05-31, superseded above)
- **P02–P06 roll:** applied R1/R3/R4/R5 across P02–P06 (vault + fixtures). **§3C → entities
  only:** 116 thematic objects (events/speech-acts/framings/patterns — *_form/_verb/_formula/_directive/
  _question/_declaration) removed from `objects_in_scene`, with their orphaned `*_form` slots (R5
  conditioning) and the mirrored map §3C entries; only true O#/CB_ entities kept (~~P02–P05 had none~~ —
  **corrected above**; P06 keeps O9/O10/O11 + CB_*). Plain-language fixes (R4): action/referential values
  "DIVINE_AGENT"→"YHWH", significant_absence "agent/verb" prose, P06's marquee items ("speech-act of directive
  instruction"→"telling", "abundance triplet — three verbs"→"she ate · she was satisfied · she had leftover",
  "infinitive-absolute doubling"→plain). P06 conditioning-Q&A (Register?/Self-form?/Forward-link?) removed (R5).
- **Result:** corpus lint **~182 findings (56 tier-1) → 19 (0 tier-1)**; validate 6/6 clean; coverage 6/6
  block-clean (leaner abstract overlays); gold-diff agreement unchanged (leaner placeholders); 88 tests green.
- **Pending the lead's blessing** (vault PR #5 / repo PR #15, superseded by the corrective pass).

---

## SC-0013 — Map content remediation (SC-0012): P01 worked reference
- **Date:** 2026-05-31
- **Decided by:** Marcia Suzuki
- **Status:** **P01 APPLIED — pending the project lead's blessing** (vault PR for P01 map + FOR_MODEL);
  P02–P06 roll once P01 is blessed. The worked reference calibrates the rest.
- **Type:** artifact remediation (no schema/spec change).
- **Summary:** Apply R1–R5 to P01's §3C + Level 3. §3C shrinks from ~17 thematic entries to **3 true
  entities** (O1 famine · CB_0030 sojourn · the about-ten-years duration). Every relocated item's insight
  is re-homed (never deleted), documented inline in `_note`s:
  - **events → propositions:** death of Elimelech→P7, taking of wives→P9, deaths of both sons→P12, prior-death-referenced→P13 link;
  - **framings → referential_forms:** "husband of Naomi"→B2@P7, "the woman"→B3@P13, "her children" (yeladim)→B4·B5@P13, "for themselves"→P9 OWN_INITIATIVE, Ephrathite identity→being B6;
  - **patterns → figures / inherent / order-constraint:** vayhi formula→FIG_0007, residual "she remained"→FIG_0052, "both of them"→inherent in deceased[B4,B5], cumulative listing→P13 listing_order_form (no new figures, per the lead's call).
  - **plain-language (R4):** significant_absence "agent of the famine" → "the one who sent the famine".
- **Artifact migration:** **P01.** Vault `stas/P01-…-FOR-MODEL.md` (objects_in_scene + slot cleanups) and
  `pericopes/P01-…md` (§3C) — mirrored to `fixtures/`. **BCD unchanged** (P01's TH_ codes were never registered).
- **Verification:** `tripod lint` P01 **12 findings → 0 (clean)**; `validate` 0-block; `coverage` P01 still
  block-clean (abstract overlays 15→2); `gold-diff` P01 **100%**, leaner (78→56 placeholders); 88 tests green.

---

## SC-0012 — Level-3 / §3C content discipline (R1–R5) + the `tripod lint` drift-guard
- **Date:** 2026-05-31
- **Decided by:** Marcia Suzuki
- **Status:** **SHIPPED** (the rule, the lint, and the template remediation); the **map remediation is a
  gated follow-on** (P01 first). Wiki side: vault PR [ruth-pilot-b-wiki#3](https://github.com/MarciaSuzuki/ruth-pilot-b-wiki/pull/3).
- **Type:** methodology + tooling — **no `validation-rules.json` change**.
- **Summary:** The content layers — Level 3 propositions and §3C — must hold only the **bare, atomic,
  plain-language payload**; everything else (interpretation, classification, grammatical analysis,
  register, links, figures, cross-refs) is **conditioning** (its own field/layer) or doesn't belong.
  Five rules: **R1** §3C = entities only (events→propositions, framings→`referential_form`,
  patterns→figures); **R2** atomic; **R3** bare (content, not a label for it); **R4** plain-language (no
  grammatical/linguistic jargon — *the locked anti-colonial-frame commitment*: a map that says
  "wayyiqtol" or "patient" teaches downstream models the source grammar and imposes it on unknown target
  languages); **R5** payload-only (conditioning separated).
- **Spec change (exact):** new vendored **`_spec/lint-lexicon.json`** (forbidden vocabulary tiered,
  interpretive labels, conditioning-Q&A patterns, compound markers, substitutions — sourced from the
  methodology doc), pinned in `_spec/pins.json` → `sources`. New engine `src/engine/lint.ts` + CLI
  `tripod lint [paths…] [--corpus] [--tier1]`. Wiki: `_methodology/level3and3Ccontentdiscipline.md` (the
  rule) + `_templates/meaning-map-template.md` remediated (§3C entities-only; §6.2 atomic-bare-plain-payload).
- **Validator impact:** a **4th deterministic verifier** beside validate/coverage — it *surfaces* drift
  (the human judges + relocates insight, never deletes). Completes the stack: **legal (validate) ·
  complete (coverage) · atomic-bare-plain (lint) · true (human review).**
- **Inventory (drives the map remediation):** `tripod lint --corpus` over P01–P06 ⇒ **~182 findings** —
  87 forbidden-vocabulary, 48 §3C-not-an-entity (TH_ objects that are events/framings/patterns), 34
  interpretive labels, 10 compounds, 3 conditioning-in-Q&A. None block; all are review signal.
- **Version:** no spec-version bump; `lint-lexicon.json` pinned `0.1.0`.
- **Verification:** 88 tests green (+8 lint: whole-word discipline so "verb"⊄"adverb" and controlled
  slot-names aren't flagged; R1/label/conditioning/compound detection); `check-drift` ok.
- **Follow-on (gated):** remediate the agent system-prompts + spec vocab prose; then the **map remediation**
  (P01–P06 §3C + Level 3) — exegetical, the project lead adjudicates; P01 first → confirm leaner & truer →
  roll P02–P06; re-validate + re-baseline gold-diff/coverage; **before Slice 4** (clean supervision in, clean drafts out).

---

## SC-0011 — BCD `gender` frontmatter field (authoritative; retires the prose-guess)
- **Date:** 2026-05-30
- **Decided by:** Marcia Suzuki
- **Status:** **SHIPPED** — wiki side **merged** (vault PR [MarciaSuzuki/ruth-pilot-b-wiki#2](https://github.com/MarciaSuzuki/ruth-pilot-b-wiki/pull/2), rebased on `main`); compiler side shipped.
- **Type:** registry/BCD (Layer 3) — **no `validation-rules.json` change**.
- **Summary:** The coverage matcher used entity gender to disambiguate unnamed referents, but the only
  source was a **prose-scan guess** in `build_aliases.py` (count kinship terms in the BCD entry). It was
  unreliable — it read **YHWH** and **the field-foreman** as feminine (off surrounding pronouns) and three
  collectives (whole-city, clan, people-of-YHWH) as single-gendered — and a wrong gender can veto a correct
  match (it was wrongly flagging YHWH as missing until the corpus pass made proper-noun **name** matches
  gender-immune, SC-0010's sibling fix). Replace the guess with an **authoritative `gender` frontmatter
  field** on every being.
- **Spec change (exact):** BCD — add `gender: "m" | "f" | null` to all 31 being notes (after `b-code`).
  `null` = collective / mixed / office (do not guess). `build_aliases.py` reads it authoritatively (explicit
  `null` ⇒ no gender); the prose guess survives only as a fallback for a being lacking the field. Alias table
  re-pinned `aliases-0.1.1 → 0.1.2` (9 gender corrections; all non-gender fields byte-identical).
- **Artifact migration:** none (frontmatter-only; no FOR_MODEL/map/prose changes).
- **Validator impact:** none to block status — the corpus stays 6/6 clean (the proper-noun name match is
  already authoritative). Entity gender is now correct data for unnamed-referent disambiguation.
- **Version:** no spec-version bump; alias-table source re-pinned `0.1.2`.
- **Verification:** 80 tests green; `check-drift` ok; `tripod coverage --corpus` 6/6 clean; B10 (YHWH) gender = `m`.

---

## SC-0010 — Coverage recorded-exception mechanism + the P06 "Israel" epithet-internal ruling
- **Date:** 2026-05-30
- **Decided by:** Marcia Suzuki
- **Status:** **SHIPPED**
- **Type:** tooling/spec (coverage) + a registry ruling — **no `validation-rules.json` change**.
- **Summary:** Coverage's "nothing missing" check blocks on a named referent the map omits. Some findings
  are *legitimately* not entities — the reviewer must be able to **sign off** so the block is recorded as
  accepted rather than silently suppressed (the open item in `docs/COVERAGE.md`: "how the reviewer signs off
  on exceptions"). Add a pinned `_spec/coverage-exceptions.json`; `tripod coverage` downgrades any matched
  finding to **ACCEPTED** (still shown in the ledger, with the reason + provenance), so it no longer fails
  the run. **First exception:** P06 — "Israel" (יִשְׂרָאֵל, 2:12) occurs inside the divine title "the God of
  Israel"; it qualifies the divine name, not a separately-tracked participant, so no `PL_ISRAEL` entity is
  warranted there. Ruling: **EPITHET_INTERNAL**.
- **Spec change (exact):** new vendored artifact `_spec/coverage-exceptions.json` (pinned in
  `_spec/pins.json` → `sources`). Match keys: `UNMAPPED_SOURCE` → (pericope, gloss, verse-prefix);
  `UNANCHORED_ENTITY` → (pericope, entity_id). Each entry carries `reason` + `accepted_by`/`accepted_on`/`sc_ref`.
- **Artifact migration:** none (no FOR_MODEL/map edits — the map intentionally does **not** tag Israel at 2:12).
- **Validator impact:** `reconcile()` gains an `exceptions` arg; accepted findings are excluded from the
  block counts and tallied as `score.accepted`; `ok` ignores accepted findings. Ledger + CLI show an
  "accepted exceptions" section and `· N accepted` in the score line. **Result: the full P01–P06 corpus is
  6/6 block-clean** (245/245 explicit accounted, 1 by this sign-off, 0 unanchored).
- **Version:** no spec-version bump; `coverage-exceptions.json` pinned at `0.1.0`.
- **Verification:** 80 tests green (raw P06 still flags Israel; the sign-off downgrades it to accepted;
  synthetic accept tests for both finding kinds); `check-drift` ok.

---

## SC-0009 — Merge PL_HA_ARETZ into PL_LAND_OF_JUDAH (the same-referent principle)
- **Date:** 2026-05-30
- **Decided by:** Marcia Suzuki
- **Status:** **SHIPPED** — wiki side **merged** (vault PR [MarciaSuzuki/ruth-pilot-b-wiki#1](https://github.com/MarciaSuzuki/ruth-pilot-b-wiki/pull/1), approved by the project lead, rebased as `402f5ef` on `main`); compiler side shipped. Repo fixtures + pinned alias table confirmed identical to the merged canonical BCD.
- **Type:** registry/BCD (Layer 3) — **no `validation-rules.json` change**.
- **Summary:** "the land" (הָאָרֶץ, Ruth 1:1) and "the land of Judah" (אֶרֶץ יְהוּדָה, 1:6–7) are the
  **same geographic referent** (the covenant territory) seen at two moments — the famine that empties it
  and the bread/return that fills it. The P01 working code `PL_HA_ARETZ` is **merged into the existing
  `PL_LAND_OF_JUDAH`** and retired; the two moments are distinguished by **referential_form**, not by a
  second PL-code. (Closes the item `PL_LAND_OF_JUDAH` had flagged: *"pending formal PL-code assignment,
  parallel to P01's PL_HA_ARETZ handling."*)
- **Same-referent principle (new, reusable — the reason this is logged):** when the source text refers to
  one place / being / object under different surface forms across the book, it gets **one** Layer-3 code;
  the load-bearing surface distinctions are carried by `referential_form` (and per-scene `role_in_scene`),
  **never** by minting a second code. Splitting one referent across two codes is **registry drift** — the
  same failure class the compiler exists to prevent (training paper §12), now stated for Layer 3.
- **Spec change (exact):** none to `validation-rules.json`. **BCD `PL_LAND_OF_JUDAH`:** add
  `hebrew_aliases: [הָאָרֶץ, אֶרֶץ]`; `aliases` (referential forms) → `[THE_LAND_AFFLICTED_BY_FAMINE` (1:1)`,
  LAND_OF_RETURN_AND_PROVISION` (1:6–7)`]`; `first-appearance P02→P01`; `appears-in [P02,P03]→[P01,P02,P03]`.
  **`PL_HA_ARETZ`** retired (it never had a BCD file — a P01 working code only).
- **Artifact migration:** **P01.** Vault `stas/P01-…-FOR-MODEL.md`: `PL_HA_ARETZ → PL_LAND_OF_JUDAH` in
  scene S1 `places_in_scene` and proposition P2 `afflicted_place` (`role_in_scene: LAND_AFFLICTED_BY_FAMINE`
  carries the 1:1 referential sense — the schema's `scene_places_container` is `additionalProperties:false`,
  so places take no `referential_form` field; only beings do). Vault `pericopes/P01-…md`: scene-1 "the land"
  → `[[PL_LAND_OF_JUDAH]]`. Mirrored into `fixtures/` (kept byte-identical to the vault). No other pericope
  references `PL_HA_ARETZ` as a live code.
- **Validator / tooling impact:** the FOR_MODEL re-validates **0-block** (`PL_LAND_OF_JUDAH` matches the
  `place_id` pattern). `extractor/build_aliases.py` now reads `hebrew_aliases` → `hebrew_cons_aliases`;
  `src/engine/coverage.ts matchScore()` tests the referent against the entity's primary Hebrew **plus** any
  surface alias. `gold-diff` P01: 43→44 matched (the now-coded place extracts deterministically). `tripod
  coverage P01`: "the land" (אָרֶץ, 1:1) now **MATCHED → PL_LAND_OF_JUDAH** (`via lexical`), where it was
  previously on the reviewer tick-list because `PL_HA_ARETZ` had no BCD entry.
- **Version:** **no spec-version bump** (`validation-rules.json` stays `v0.6`). Layer-3 source re-pinned:
  `_spec/registry/ruth.aliases.json` `aliases-0.1.0 → 0.1.1` (sha `9eba86f6…`).
- **Verification:** 73 tests green (incl. a synthetic `hebrew_cons_aliases` match test + the P01 acceptance
  assertion that "the land" → `PL_LAND_OF_JUDAH` and `PL_HA_ARETZ` is gone); `check-drift` ok; coverage P01
  block-clean (`47/47 explicit · 5 implied · 0 unanchored · 14 ticks`).

---

## SC-0008 — Canonical-home: spec-vault reconcile + `check-drift --vault` guardrail (folds in template-relics retirement)
- **Date:** 2026-05-29 (reinstated as SC-0008)
- **Decided by:** Marcia Suzuki (compiler-half build recommended by Architect 10, confirmed by Marcia in review 2026-06-07)
- **Status:** APPLIED (compiler half — see the **Build** note below; gate board green; the **vault writeback / reconcile is PENDING**). PR open.
- **Type:** governance infrastructure — build the `check-drift --vault` guardrail + reconcile the stale vault spec (the **load-bearing** half); folds in the original template-relics docs hygiene (the minor half)
- **ID note:** Tentatively numbered SC-0006 in an earlier planning session but never committed to this
  log under that number (SC-0006 shipped as drift convergence, `9fdef18`); later surfaced as SC-0007 in
  `docs/PROGRESS.md` open-thread (c), colliding with the L1-axis item now at SC-0007. Reinstated here
  under its own permanent ID — see the allocation ledger.
- **Summary:** Two pre-Wave-3 template files survive in the wiki vault `_templates/`:
  `for-model-template.md` still documents `discourse_threads_active` as a FOR_MODEL field (now
  BCD-DELTA-only), and `audit-template-schema.json` is the schema for the obsolete AUDIT artifact.
  Retire/redirect both.
- **Spec-vault reconcile + `check-drift --vault` — the PRIMARY, load-bearing half of SC-0008 (scoped 2026-06-06, found during SC-0025); the template-relics retirement above is the minor original half.** SC-0008 is the missing guardrail behind a now-verified drift, not an incidental cleanup: the vault `_spec/` schema files are a stale snapshot frozen ~SC-0014 (`validation-rules.json` v0.7 vs compiler v0.12; `approved-enumerations.json` v0.1 vs v0.10; `compilation-log.schema.json` v0.5 vs v0.7; `quarantined-vocabulary.json` **absent**), because every SC since ~SC-0015 edited the spec compiler-side and `check-drift --vault` — the check that would catch it — was never built. SC-0008 has sat **PROPOSED since before SC-0014 with no owner** while ~10 SCs of spec edits accrued unwritten-back; if it stays parked, the same finding recurs at SC-0030 with the spec eleven versions stale. **Scope bound here:** the bulk spec-vault reconcile (catch all four files up + create the quarantine file) ships **together** with `check-drift --vault` (so it can't silently re-drift), as its **own gate cycle** — one concern per cycle; it does **not** ride on SC-0026 (CL gate-validation — different artifact, different check) or Thread B. Interim, per the Canonical-home rule's "Current state": the compiler's pinned `_spec/` is canonical; the vault `_spec/` is a stale snapshot and must not be synced *from*.
- **Build (compiler half — Architect 10, 2026-06-07; PR pending Marcia):** ⚠ verification corrected the framing above — `check-drift --vault` was **NOT "never built"**: it exists and **exits 1** on a differing file. The real gaps were (i) it is **never run** (no gate invokes it; the vault isn't in CI) and (ii) an **absent** canonical file was **silently uncaught** (`vaultOk=undefined`, ignored by the exit). This PR (compiler half): **fixes the blind spot** (absent → `vault:MISSING`, trips exit 1, + a unit test); **reclassifies `quarantined-vocabulary.json` source→schema** (Marcia's call) so it is vault-guarded; adds a **conditional `--vault` vitest test** (runs when `TRIPOD_VAULT_SPEC` is set, **skips visibly** otherwise) + a `check-drift:vault` npm script; folds in a one-line **schema-vs-source criterion**; and **sweeps SC-0021–0025 ledger statuses to MERGED**. The **reconcile** (writeback the 3 stale schemas + the now-schema quarantine file + the 6 stale CLs to the vault) is the **second half — vault writeback, PENDING**; until it lands the conditional test is *correctly red when the vault is configured* (catching real drift, not a regression). `check-drift --vault` now reports **4** drift items (3 stale + quarantine MISSING), up from 3 — the blind-spot fix working.
- **Scope extended (2026-06-06, found during SC-0026; recommended by Architect 10, confirmed by Marcia in review):** SC-0008 also reconciles the stale vault `stas/` **COMPILATION-LOGs**, not just `_spec/`. Verified during SC-0026: all 6 vault CLs are a strict subset of the merged compiler fixtures (vault-only keys = 0; each missing `vocabulary_additions.role_in_scene_beings`, 5/6 also missing the `vocabulary_additions.*_elements` breakdown; 54–242 changed lines each) — the same unguarded-stale-vault class as `_spec/`, an authoritative artifact left adrift because its *content* check is the unbuilt one. SC-0008's deliverable accordingly grows to: (a) **writeback** the 6 vault CLs to match the merged compiler copies (diff-first / surgical, handoff §V.5), and (b) a **`stas/` content guardrail** (the CL analogue of `check-drift --vault`). SC-0026 (the CL **schema** gate) is the separate, already-shipped half — it cannot see content staleness.
- **Rationale:** Stale templates re-introduce retired fields; a future author copying the
  template would re-add `discourse_threads_active` to a FOR_MODEL.
- **Spec change (exact):** no `validation-rules.json` change; wiki-vault `_templates/` edits only.
- **Artifact migration:** none (templates are not validated artifacts).
- **Validator impact:** none.
- **Version:** no spec-version bump.
- **Verification:** grep clean; `_templates/` no longer references retired fields.

---

## SC-0007 — Converge the L1 / discourse / high-risk axes (add a COMPILATION-LOG promotion slot)
- **Date:** 2026-05-30 (shipped)
- **Decided by:** Marcia Suzuki
- **Status:** SHIPPED (2026-05-30)
- **Type:** schema-shape (COMPILATION-LOG) + promotion mechanism
- **Summary:** Closes the SC-0006 known limitation. The COMPILATION-LOG `vocabulary_additions` only
  carried `proposition_kinds`/`scene_kinds`/`presence_values`, so the remaining convergent axes — the
  L1 element axes (arc/context/tone/pace/communicative_function), `discourse_thread_state`, and
  `high_risk_register_kind` — had no promotion slot and kept drifting. Added intake slots so
  `tripod promote` can converge them into `approved-enumerations.json`.
- **Rationale:** Those axes are already classified convergent (SC-0006) and already keyed in the
  registry, but with no intake slot they never accumulated; their drift was permanent noise, not signal.
- **Spec change (exact):**
  - `compilation-log.schema.json` `$defs.vocabulary_additions.properties` gains seven OPTIONAL arrays
    (each `array` of `vocabulary_addition_entry`): `arc_elements`, `context_elements`, `tone_elements`,
    `pace_elements`, `communicative_function_elements`, `discourse_thread_states`,
    `high_risk_register_kinds`. The `required` set is **unchanged**, so v0.3 logs stay valid (additive).
    `version` v0.3 → v0.4, `$id` `…compilation-log-v0-3` → `-v0-4`, `date_locked` → 2026-05-30, `supersedes` appended.
  - `validation-rules.json` `sibling_schemas.compilation_log` pointer `"…v0.3"` → `"…v0.4"`
    (pointer-accuracy only — **no rule change**, so its `version` stays **v0.6**, re-pinned by hash).
  - `src/compiler/promote.ts`: `VA_KEY_TO_AXIS` gains the seven `…s → singular` mappings;
    `UNCOVERED_CONVERGENT_AXES` shrinks to `[]`.
  - `src/cli/tripod.ts`: `promote` prints "all convergent axes are promotable (SC-0007)" when the
    uncovered set is empty.
- **Scope note (honest):** the five **L1-element** axes surface as FOR_MODEL drift, so promoting them
  measurably zeroes drift (proven below). `discourse_thread_state` and `high_risk_register_kind` are
  **not FOR_MODEL fields** (they live in the BCD-DELTA discourse threads and this log's
  `high_risk_register_audit[].kind`); the FOR_MODEL drift detector does not scan them. Their new slots
  make them **promotable / keep the registry complete**, but drift-*detection-from-source* for those two
  is out of scope here (future work — the validator would scan the COMPILATION-LOG / BCD-DELTA, not the FOR_MODEL).
- **Artifact migration:** `fixtures/compilation-log/P02-Ruth-1-6-14-COMPILATION-LOG.md` populated with
  P02's convergent values (arc ×6, tone ×4, pace ×2, communicative_function ×7, all `PROPOSED`) plus
  its five genuinely-new `high_risk_register_kind` values (R1/R2/R3/R11/R15). P01 + P03–P06 unchanged
  (slots optional; they may add the new lists as values are approved).
- **Registry growth — deferred (decision):** SC-0007 ships the **mechanism only**. The vendored
  `approved-enumerations.json` is **not** grown here (stays v0.1); the real P02 promotion is a routine
  `tripod promote --apply` action for a later session (logged in `VOCABULARY_LOG.md`, re-pins the
  registry). The mechanism is proven in tests without mutating the vendored registry.
- **Validator impact:** every convergent axis is now promotable; once a value is promoted into the
  registry it stops drift-warning (the detection side was already wired via `driftBaseline()`).
- **Version:** `compilation-log.schema.json` v0.3 → **v0.4**
  (sha256 `af54950a87b5aeb818a526467e814c2dabbe2ef85fd0386cce213e62789f1400`);
  `validation-rules.json` **v0.6 unchanged**, re-pinned (sha256
  `80be76213d8a4fb6bd34c87641ac58feccf23e36ee160c2f5c73fe6a7e207bf0`). Both re-pinned in `_spec/pins.json` + the table above.
- **Verification:** `npm test` 44/44 green; `tripod check-drift` green against the new pins + closed-list
  sync invariant; `tripod validate fixtures/for-model/` → 6/6 block-clean; `tripod validate
  fixtures/compilation-log/` → 6/6 valid (P02 against v0.4 with the new slots); `tripod promote
  fixtures/compilation-log/P02-…-COMPILATION-LOG.md --status ANY` lists 41 values across all convergent
  axes ("all convergent axes are promotable"); the drift test confirms promoting P02's
  `vocabulary_additions` drives residual convergent FOR_MODEL drift to **zero** (was non-zero on the L1
  axes before).

---

## SC-0006 — Establish drift convergence: convergent/descriptive split + growing approved-enumerations registry
- **Date:** 2026-05-29
- **Decided by:** Marcia Suzuki
- **Status:** SHIPPED (2026-05-29)
- **Type:** axis reclassification + schema-shape (new registry file) + validator mechanism
- **Summary:** Two linked changes so drift becomes a real, converging review signal instead of
  per-pericope noise. (1) **Split** the drift detector's axes into *convergent* (review-signal)
  vs *descriptive* (open, never converge); (2) replace the frozen P01-only baseline with a
  **growing `approved-enumerations.json` registry** that accumulates approved convergent values
  with provenance, so a value approved in one pericope stops drifting in the next.
- **(1) Convergent vs descriptive (rule-based):** an axis whose seed key contains `_examples`
  (`role_in_scene_examples_*`, `function_in_scene_examples_object`, `*_kind_examples`) or equals
  `referential_form` is **descriptive** — per-pericope by nature, surfaced as severity `descriptive`
  (informational), never as drift. Everything else (`proposition_kind`, `scene_kind`,
  `presence_value`, the L1 element axes incl. `context_element`, `discourse_thread_state`,
  `high_risk_register_kind`) is **convergent** → severity `drift`. Example: P02 drops from 86
  lumped "drift" to **37 convergent + 49 descriptive**.
- **(2) Growing registry:** new `_spec/approved-enumerations.json` (v0.1), seeded from the
  convergent axes of `drift_detector.canonical_p01_enumerations` (each value tagged
  `first_seen`/`approved_in`/`source_artifact`/`sc_ref`). The validator reads it as the live drift
  baseline for convergent axes (descriptive axes still use the P01 seed for the informational note).
  `drift_detector.canonical_p01_enumerations` is retained as the documented seed.
- **Promotion-with-provenance:** `tripod promote <COMPILATION-LOG>` reads the log's
  `vocabulary_additions` (the Gate-F-approved values), gates on `status` (default `CONFIRMED`),
  and grows the registry; `tripod propose-vocabulary <FOR_MODEL>` lists convergent-drift values as
  candidate additions. Promotions append to `VOCABULARY_LOG.md` and re-pin the registry — no new SC
  per promotion (this entry establishes the policy).
- **Known limitation (flagged):** the COMPILATION-LOG `vocabulary_additions` only carries
  `proposition_kinds` / `scene_kinds` / `presence_values`, so those converge; the L1-element axes
  (arc/context/tone/pace/communicative_function), `discourse_thread_state`, and
  `high_risk_register_kind` have **no promotion slot** in the COMPILATION-LOG schema yet and will
  keep drifting until that schema gains one (a future SC). `tripod promote` prints these uncovered axes.
- **Version:** new `approved-enumerations.json v0.1`
  (sha256 `3623630868562083bb0c7d35a177db0416bd3a983e43f22808d66bea96a7a282`); pinned in `_spec/pins.json`.
  No change to `validation-rules.json` (the canonical_p01_enumerations seed is unchanged).
- **Verification:** `tripod validate` P01–P06 still block-clean; P02 splits 37 convergent / 49
  descriptive; promoting P02's `vocabulary_additions` zeroes its `proposition_kind`+`scene_kind`
  drift on re-validate, with residual convergent drift only on the uncovered axes. 22 tests green.

---

## SC-0005 — Widen the `place_id` pattern to allow `PL<n>_<DESCRIPTOR>` sub-place codes
- **Date:** 2026-05-29
- **Decided by:** Marcia Suzuki
- **Status:** SHIPPED (2026-05-29)
- **Type:** schema-shape (closed pattern; affects 3 schemas)
- **Summary:** The locked `place_id` pattern `^PL(\d+|_[A-Z][A-Z0-9_]*)$` accepted `PL5` or
  `PL_BOAZ_PORTION` but **not** the hybrid sub-place form `PL5_BOAZ_PORTION` — which the BCD
  itself registers (`bcd/places/PL5_BOAZ_PORTION-…md`) and P05/P06 use. Widen the pattern to
  permit an optional `_<DESCRIPTOR>` after the number. **Surfaced by the Slice 1 validator on
  its first run against the gold fixtures** — the drift-control payoff in action.
- **Spec change (exact):** pattern `^PL(\d+|_[A-Z][A-Z0-9_]*)$` → `^PL(\d+(_[A-Z][A-Z0-9_]*)?|_[A-Z][A-Z0-9_]*)$`
  in all three schemas that carry it:
  - `validation-rules.json` (`$defs.place_id`) → version **v0.5 → v0.6**, `$id` + `for_model_schema.$id` bumped, `sibling_schemas` refs updated.
  - `bcd-delta.schema.json` (`$defs.…place_id`) → version **v0.3 → v0.4**, `$id` bumped.
  - `verification-input.schema.json` (`place_id`) → version **v1.0 → v1.1**.
  - `compilation-log.schema.json` — unchanged (no `place_id` field).
- **Artifact migration:** none. P05/P06 (and the BCD) already use `PL5_BOAZ_PORTION`; the widen
  makes the spec match existing artifacts. No `PL` code anywhere needs renaming.
- **Validator impact:** `place_id` now accepts `PL<n>`, `PL_<DESCRIPTOR>`, and `PL<n>_<DESCRIPTOR>`.
  After the widen, all six gold FOR_MODELs (P01–P06) validate block-clean.
- **Version:** `validation-rules.json v0.5 → v0.6`
  (sha256 `b024e0ea40771ba4a169b936ce57f05686e5333485d937cbed97a80e0d14de3a`); siblings re-pinned
  (see pin table above + `_spec/pins.json`).
- **Verification:** `tripod validate fixtures/for-model/` → 6/6 block-clean (P01 drift 0; P02–P06
  bounded-open drift only); `tripod check-drift` green against the new pins; the closed-list sync
  invariant holds. A reverted (narrow) pattern re-blocks P05/P06 (regression guard in the test suite).

---

## SC-0004 — Deprecate the pre-Wave-3 `_examples/` P01 duplicates; redirect to canonical P01
- **Date:** 2026-05-29
- **Decided by:** Marcia Suzuki
- **Status:** SHIPPED (2026-05-29)
- **Type:** other (examples/docs consistency; **no spec change** — pin/hash unchanged)
- **Summary:** Resolves the examples refresh flagged under SC-0003. The three
  `_examples/{meaning-map,audit,for-model}-P01-worked-example.md` files were pre-Wave-3 **full
  duplicates** of P01 carrying forbidden/obsolete content. Rather than maintain parallel copies (a
  drift vector), they are **deprecated and redirected** to the canonical P01 reference.
- **Spec change (exact):** none.
- **Artifact edits (wiki):** each `_examples/` file replaced with a short deprecation stub
  (`status: deprecated`, `superseded-by`) pointing to the live canonical artifact:
  - `meaning-map-…` → `[[P01-Ruth-1-1-5]]` (was `CONSULTATIVE`; treated COMMUNITY_MEMORY as a register).
  - `for-model-…` → `[[P01-Ruth-1-1-5-FOR-MODEL]]` (was `TRIPOD_STA_v1_0`; had `artifact_profile`,
    a `discourse_threads_active` block in the FOR_MODEL, `CONSULTATIVE`).
  - `audit-…` → `[[P01-Ruth-1-1-5-COMPILATION-LOG]]` + `[[P01-Ruth-1-1-5-BCD-DELTA]]` (the monolithic
    AUDIT artifact type no longer exists; it used `registry_additions` + the combined audit model).
- **Rationale:** `CLAUDE.md` already designates the live `pericopes/` + `stas/` + `verification/` P01
  artifacts as the structural reference for every artifact. Maintaining duplicate worked examples
  would re-introduce drift. Single source of truth.
- **Supersedes:** the "recommend a separate full examples refresh" note in SC-0003 (resolved here by
  deprecation rather than regeneration).
- **Validator impact:** none.
- **Version:** unchanged — `validation-rules.json v0.5` (sha256 `a326dbdd2601089851907c2025517a7f3b076a9432d380e00487ee0ec76f1b4a`).
- **Verification:** post-edit sweep — no live (non-archive, non-working) artifact carries a structured
  `"register": "CONSULTATIVE"`, `artifact_profile`, `discourse_threads_active`, or an AUDIT artifact;
  the `_examples/` stubs contain only deprecation prose + canonical redirects.

---

## SC-0003 — Clean residual "elevated register bordering on COMMUNITY_MEMORY" prose from live artifacts
- **Date:** 2026-05-29
- **Decided by:** Marcia Suzuki
- **Status:** SHIPPED (2026-05-29)
- **Type:** other (artifact-prose consistency; **no spec change** — pin/hash unchanged)
- **Summary:** Resolves the residual prose flagged under SC-0002. Live artifacts described the
  *wayhi bi-yemei* opening as an "elevated register bordering on COMMUNITY_MEMORY" — post-SC-0001 a
  category error, since `COMMUNITY_MEMORY` is a `NARRATIVE_FRAMING` value, not a register.
- **Spec change (exact):** none (no `validation-rules.json` edit; closed lists untouched).
- **Artifact edits (wiki):**
  - `pericopes/P01-Ruth-1-1-5.md` (canonical meaning map — §1 register tagging + the Level-2 figure
    note): the v.1 *wayhi* formula now reads "elevated narrative framing — COMMUNITY_MEMORY (a
    NARRATIVE_FRAMING value, not a register)," and the register is stated to stay `INFORMAL_CASUAL` throughout.
  - `concepts/CB_0029-Judges-Era.md` (Rendering concern, ×2): same reframe.
  - `figures/FIG_0050-Wayhi-Bimei-Chronicle-Formula.md` (intended-meaning, ×3): "elevated narrative
    register" → "elevated narrative framing."
- **Deliberately NOT changed:**
  - Correct uses of "elevated register" — the P01/P03/P04/P05 COMPILATION-LOGs' "…carried by figure
    flags, not by elevated register"; the Pilot-3 draft's own category-error analysis; agent
    style-guidance ("high-register filler").
  - `_examples/{meaning-map,audit,for-model}-P01-worked-example.md` are **pre-Wave-3** (pericope
    register `CONSULTATIVE`; obsolete 2-artifact AUDIT model). The phrase persists in the meaning-map
    example; a one-line patch would leave it incoherent. **Recommend a separate full examples refresh**
    (regenerate from canonical P01). This is the only remaining live occurrence.
- **Validator impact:** none.
- **Version:** unchanged — `validation-rules.json v0.5` (sha256 `a326dbdd2601089851907c2025517a7f3b076a9432d380e00487ee0ec76f1b4a`).
- **Verification:** vault sweep confirms no "elevated register bordering on COMMUNITY_MEMORY" or
  "elevated narrative register" remains in any live (non-archive, non-working, non-`_examples`) file.

---

## SC-0002 — Propagate the `COMMUNITY_MEMORY` reclassification into prompts, templates, and the worked example
- **Date:** 2026-05-29
- **Decided by:** Marcia Suzuki
- **Status:** SHIPPED (2026-05-29)
- **Type:** other (downstream consistency; **no closed-list change** — SC-0001 pin/hash still current)
- **Summary:** SC-0001 moved `COMMUNITY_MEMORY` off REGISTER onto the `NARRATIVE_FRAMING` axis and
  added `framing_override`. This entry propagates that into the human-facing locked-spec prompts,
  the FOR_MODEL template, and the P01 worked example so future runs (P07+) don't regenerate invalid
  register overrides.
- **Spec change (exact):** no change to `validation-rules.json` (closed lists untouched). Edits (all in the wiki):
  - `_spec/agent-2-system-prompt.md`: moment-level shift note now says the *wayhi* formula carries
    `COMMUNITY_MEMORY` as a `NARRATIVE_FRAMING` value (downstream `framing_override`), not a register.
  - `_spec/agent-3-system-prompt.md`: moment-level guidance now splits `override_value` (REGISTER) vs
    `framing_override` (NARRATIVE_FRAMING), with the at-least-one rule.
  - `_templates/for-model-template.md`: register value list corrected to the 7 (had listed
    `COMMUNITY_MEMORY`); `register_overrides.moment_level` field list now includes `framing_override`.
  - `_examples/for-model-P01-worked-example.md`: 1:1a `override_value: COMMUNITY_MEMORY` → `framing_override` (targeted).
- **Verified-correct, deliberately NOT changed (genre/token positions):**
  - `agent-1…:237` and `agent-2…:113` — `COMMUNITY_MEMORY` in the **GENRE** list (correct; stays the 31st genre).
  - `agent-2…` register list (L116–126) — already the correct 7.
  - `_templates/sta-vocabulary.md:837` — flat alphabetical token glossary; token still valid (genre + framing).
    `_templates/sta-vocabulary-general.md:72` — genre-position (correct).
- **Out of scope, FLAGGED for separate follow-up:**
  - Descriptive prose "register bordering on COMMUNITY_MEMORY" in `concepts/CB_0029-Judges-Era.md` (L21,44)
    and `pericopes/P01-Ruth-1-1-5.md` (L37,114) — exegetical, non-validated, non-blocking.
  - `_examples/for-model-P01-worked-example.md` and its siblings (`meaning-map-…`, `audit-…`) are
    **pre-Wave-3** (pericope register `CONSULTATIVE`, old 2-artifact AUDIT model). SC-0002 fixed only the
    `COMMUNITY_MEMORY` field; a full examples refresh is recommended as its own task.
  - `_archive/*`, `_working/P01-smoke-test/*` — historical / audit trail; untouched by policy.
- **Validator impact:** none beyond SC-0001 (no list change). Keeps agents + templates from re-introducing the drift.
- **Version:** unchanged — `validation-rules.json v0.5`
  (sha256 `a326dbdd2601089851907c2025517a7f3b076a9432d380e00487ee0ec76f1b4a`); SC-0002 touches no closed list.
- **Verification:** post-edit vault sweep confirms no remaining `COMMUNITY_MEMORY` in a register/override-value
  position in any active (non-archive, non-working) artifact, prompt, or template; genre-position and glossary
  occurrences intentionally retained.

---

## SC-0001 — REGISTER reduced 8 → 7; `COMMUNITY_MEMORY` reclassified as a framing value
- **Date:** 2026-05-29
- **Decided by:** Marcia Suzuki
- **Status:** SHIPPED (2026-05-29)
- **Type:** closed-list change + axis reclassification (+ artifact migration)
- **Summary:** `COMMUNITY_MEMORY` is not a social register. Remove it from the closed
  `REGISTER` list (8 → 7) and move it onto a separate narrative-framing / voice axis.
- **Rationale:** The 7 social registers are speaker-relationship keys (INTIMATE,
  INFORMAL_CASUAL, CONSULTATIVE, FORMAL_OFFICIAL, CEREMONIAL, ELDER_AUTHORITY,
  RELIGIOUS_WORSHIP). `COMMUNITY_MEMORY` appears in P01 only as a **moment-level override at
  Ruth 1:1a** framing the *wayhi* opening — narrator-voice/framing, not a register. The
  locked file (8) contradicted the prose docs + training paper §10 (7); ruling resolves it
  in favour of 7. This is the tool's first detected spec-vs-prose drift, now reconciled.
- **Spec change (exact, as implemented):** edited **both** representations of each closed
  list — the `closed_lists.*` arrays and the mirror `for_model_schema.$defs.*_value` enums —
  keeping them in sync.
  - `closed_lists.REGISTER` + `$defs/register_value`: **removed** `COMMUNITY_MEMORY` → 7 values:
    `INTIMATE, INFORMAL_CASUAL, CONSULTATIVE, FORMAL_OFFICIAL, CEREMONIAL, ELDER_AUTHORITY, RELIGIOUS_WORSHIP`.
  - **Added** axis **`NARRATIVE_FRAMING`** (name **confirmed**) as `closed_lists.NARRATIVE_FRAMING`
    + `$defs/narrative_framing_value`, value set `["COMMUNITY_MEMORY"]` (only framing value the
    spec currently implies; extensible).
  - `register_overrides` handling = **(b) distinct `framing_override` field** (chosen). In
    `$defs/register_override_entry`: `override_value` is no longer `required`; sibling
    `framing_override` (→ `narrative_framing_value`) added, parallel to `genre_override`;
    an `anyOf` now requires **at least one** of `override_value` / `framing_override`.
  - **GENRE left unchanged (ruling).** Discovery: `COMMUNITY_MEMORY` was *also* the 31st `GENRE`
    (in `closed_lists.GENRE` + `$defs/genre_value`). Decision: keep it as a genre — so `GENRE`
    stays 31 and `COMMUNITY_MEMORY` now legitimately lives on two axes (GENRE + NARRATIVE_FRAMING).
    Only its REGISTER membership was the error.
  - Left `PERICOPE_LEVEL_REGISTER` (narrator-voice = INFORMAL_CASUAL) unchanged.
  - Bumped `version` v0.4 → v0.5, `date_locked` → 2026-05-29, the `$id` / `for_model_schema.$id`
    version strings, and appended a `supersedes` note.
- **Artifact migration (done):**
  - **P01 (Ruth 1:1–5) FOR_MODEL:** 1:1a `moment_level` entry changed from
    `"override_value": "COMMUNITY_MEMORY"` → `"framing_override": "COMMUNITY_MEMORY"`
    (`_note` updated). P01 FOR_MODEL re-validates clean against v0.5.
  - **P02–P06 FOR_MODEL:** scanned — no `COMMUNITY_MEMORY` in any register/override position; nothing to migrate.
  - **Residual prose (NOT migrated, flagged):** P01 meaning map (`pericopes/P01-Ruth-1-1-5.md`
    ~L37 & L114) says "elevated register bordering on COMMUNITY_MEMORY"; P01 COMPILATION-LOG
    (P01-D1) mentions the override descriptively. Exegetical/descriptive prose, not validated
    structured fields — does not trip the validator. Left as-is pending Marcia's copy-edit call.
- **Validator impact:** SHIPPED — the Slice 1 validator must **BLOCK** `COMMUNITY_MEMORY` in any
  `REGISTER` / `override_value` position (closed-list violation) and **accept** it only as a
  `framing_override` (`NARRATIVE_FRAMING`) value. The closed-list **sync invariant**
  (`closed_lists.X` == `$defs/x_value.enum`) is itself a validator check.
- **Version:** `TRIPOD_STA_v2_0`, validation-rules `v0.4` → `v0.5`
  (sha256 `a326dbdd2601089851907c2025517a7f3b076a9432d380e00487ee0ec76f1b4a`).
- **Verification:** deterministic check (no LLM) passed — REGISTER==7 and ==`register_value`;
  NARRATIVE_FRAMING==`[COMMUNITY_MEMORY]` and ==`narrative_framing_value`; GENRE==31 (unchanged);
  `override_value` optional + `framing_override` + `anyOf` present; **P01 FOR_MODEL validates
  clean against the v0.5 `for_model_schema`** (python `jsonschema`); P01–P06 carry no
  `COMMUNITY_MEMORY` in a register position. Full P01–P06 re-validation through the Slice 1
  `tripod validate` CLI lands with that slice and will reconfirm against this pinned hash.
