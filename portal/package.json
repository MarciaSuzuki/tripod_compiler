{
  "name": "tripod-review-portal",
  "private": true,
  "version": "0.1.0",
  "description": "Read-only static review portal for approved Tripod artifacts. Builds ONLY from blessed fixtures on main (status complete/valid) — the approved-only rule is a failing build test, not a convention. Tier B: consumes canon, never writes it.",
  "type": "module",
  "scripts": {
    "build": "node src/build.mjs",
    "test": "node --test \"test/*.test.mjs\""
  },
  "dependencies": {
    "marked": "^16.0.0",
    "yaml": "^2.8.0"
  },
  "engines": {
    "node": ">=20"
  }
}
